# LinearEstimator 利用ガイド

対象ソースは `linear_estimator_v06.hpp`。C++20、単一スレッドで利用する、一次式の観測から未知係数を推定するライブラリである。本文とコード例は、このソースの公開APIと実装に対応している。ライブラリの変更だけでは本ガイドを更新せず、更新の明示的な指示があったときに更新する。

コード例・検証プログラム・対象ヘッダは、同梱のファイルだけで利用できる。

各ユースケースでは、同じ具体的な問題・数値・変数順を章をまたいで使う。初めに1章で共通モデルを読み、選んだU番号について **3章の問題 → 4.10の入力への対応 → 5.5の呼出しと出力 → 7章のコード** の順に読むとよい。たとえばU05なら3.5、4.10.5、5.5.5、7.5を辿る。4.1〜4.9と5.1〜5.4は、型や既定値を調べる共通リファレンスである。

本ガイドの具体例は説明用に定めた問題仕様である。観測ノイズや事前尺度は例の仮定であり、未知のAHCの生成規則を推測した推奨値ではない。実際の問題では、その仕様・単位・事前情報に置き換える。ライブラリ本体には`solve`という名前のメソッドはなく、`optimize`と`sample`を使う。`case_u...`を含む例の関数は利用側の補助コードであり、本体のAPIではない。

## 1. 何を解くライブラリか

### 1.1 合計などの観測から、個々の値を推定する

たとえば、荷物AとBの合計重量は105、BとCの合計は98と分かっていても、それだけでは各荷物の重量を一意に決められない。計測に誤差があれば、複数の観測を完全に満たす重量が存在しないこともある。

本ライブラリには、未知の値それぞれの取り得る範囲と、既知の係数で組み合わせた式に対する観測・制約・事前知識を登録する。「各観測にどの程度合うか」を合計して、整合性の高い未知値を求める。平均や分散、複数の未知値の相関を含む合計の不確かさも、標本を通じて推定できる。

式の係数と未知変数の役割を区別する。推定するのは $	heta_j$ であり、観測を表す式の係数 $a_{rj}$ は利用者が既知の値として指定する。「作業量×未知の単価」なら作業量が $a_{rj}$、単価が $	heta_j$ に当たる。

### 1.2 対象問題と目的関数

各観測・事前情報を「因子」と呼ぶ。因子には、一次式と、その値を評価する関数を一つずつ指定する。

$$z_r(\theta)=b_r+\sum_{j\in I_r}a_{rj}\theta_j$$

$$E(\theta)=\sum_{r\in\mathcal{R}}\phi_r(z_r(\theta))$$

$$\min_{\theta_0\in D_0,\ldots,\theta_{n-1}\in D_{n-1}}E(\theta)$$

| 記号 | 意味・指定内容 |
|---|---|
| $n$ | 未知変数の個数。コンストラクタに渡す値域の個数で決まる |
| $\theta=(\theta_0,\ldots,\theta_{n-1})$ | 推定する未知値の組。変数IDは0始まり |
| $D_j$ | 変数 $j$ の値域。実数区間、整数区間、有限候補、固定値のいずれか |
| $\mathcal{R}$ | 現在有効な因子の集合。無効化した因子は合計に入らない |
| $r$ | 因子のID。追加時にライブラリが返す番号 |
| $I_r$ | 因子 $r$ が参照する変数IDの集合 |
| $a_{rj}$ | 因子 $r$ における変数 $j$ の既知係数。負数や小数も指定できる |
| $b_r$ | 一次式の既知の定数項。省略時は0 |
| $z_r(\theta)$ | 未知値を代入した一次式の値。合計、差、重み付き合計など |
| $\phi_r$ | 一次式の値をコストに変換する因子の評価関数 |
| $E(\theta)$ | 全有効因子のコストの合計。小さいほど、指定したモデルに適合する |

実行可能な状態とは、全変数が値域内にあり、全有効因子のコストが有限の状態をいう。値域外やハード制約違反の評価は正の無限大になる。観測のない変数にも値を置けるが、その値をデータから特定できるとは限らない。

### 1.3 評価関数の意味

以下で $z$ は因子の一次式の値、$y$ は観測値・中心、$\sigma$ は正規ノイズの標準偏差、$s$ はその分布またはペナルティの尺度、$\nu$ はt分布の自由度、$l,u$ は区間端点を表す。$\Phi$ は標準正規分布の累積確率である。

| 指定 | コスト $\phi(z)$ | 意味 |
|---|---|---|
| `gaussian(y, sigma)` | $(z-y)^2/(2\sigma^2)$ | 正規誤差を想定。誤差を標準偏差で割ったずれの二乗 |
| `laplace(y, s)` | $\lvert z-y\rvert/s$ | 絶対誤差を評価。大きなずれを二乗しない |
| `student_t(y, s, nu)` | $(\nu+1)\log(1+(z-y)^2/(\nu s^2))/2$ | 裾の重い誤差。大きな外れ値を許すモデル |
| `gaussian_interval(l, u, sigma)` | $-\log(\Phi((u-z)/\sigma)-\Phi((l-z)/\sigma))$ | 正規ノイズを加えた値が区間に入ったという観測 |
| `hard_interval(l, u)` | 区間内は0、区間外は正の無限大 | ノイズを加える前の一次式が必ず満たす範囲 |
| `soft_interval(l, u, s)` | $d(z,[l,u])^2/(2s^2)$ | 許容範囲から離れた距離を評価。$d$ は区間までの距離 |
| `custom(id)` | 利用者が指定する関数 | 一次式の値とcustom IDに基づく任意のコスト |

正規・Laplace・tの式では、未知変数に依存しない正規化定数を省く。したがって、異なるノイズモデル間で生の `best_energy()` を比較して、モデルの優劣を判断する用途には向かない。

因子を追加するほど、その情報が目的関数に加算される。同じ観測を重複登録すると、情報を二重に数えたモデルになる。観測の信頼度を正規因子で表すなら、信頼度が高いほど小さい `sigma` を指定する。これは観測誤差のモデルであり、単にスコアをよく見せるための係数ではない。

### 1.4 最良候補と分布に基づく推定の違い

`optimize` は $E$ が小さい候補を求める。評価を負の対数尤度と負の対数事前分布の和として定義した場合、この候補はMAP推定の対象になる。目的関数を単なるペナルティとして使うこともできる。

`sample` が対象とする分布は次のとおりである。

$$\pi(\theta)=\frac{\exp(-E(\theta))}{Z}$$

$\pi$ はモデルに基づく確率密度または確率質量、$Z$ は全状態の確率を1にする正規化定数である。連続変数では積分、離散変数では和を取る。固定値は自由な次元として数えない。$Z$ が有限であることは利用者が確認する。

実数では値域内の通常の密度、整数・有限候補では各値を1個として数える。同じ個数の有限候補でも、候補間隔が広いこと自体で重みは増えない。候補ごとの事前確率が異なるなら、追加の因子で指定する。

二乗誤差を小さくする点推定には事後平均が有用だが、整数の平均は小数になり得る。制約を満たす具体的な状態が必要なら `best()` や保存された全体サンプルを使う。さらに、実際の競技スコアに応じた行動選択は利用側が行う。経路列挙・最短路・割当・日程作成などを、このライブラリが代行するわけではない。

## 2. 差分更新とターン間の再利用

### 2.1 同じインスタンスを保持して使う

ターンが進むたびに観測を追加・訂正する使い方に対応する。同じインスタンスで更新してから再度 `optimize` / `sample` を呼ぶと、保持していた現在候補・最良候補を新しいモデルで評価し、次の探索の出発点として利用する。

「前の最良候補に必ず戻る」という動作ではない。現在候補と最良候補の両方を保持し、更新後も有効な候補を選ぶ。過去の全候補を保存して新モデルで総当たりする仕組みではない。

### 2.2 後から変更できるもの

以下の $R$ は登録済み因子数、$K$ は全因子の項数の合計、$k$ は変更対象の一次式の項数を表す。計算量は評価器自体のコストを除く概算である。

| 操作 | API | 再利用と主な処理 |
|---|---|---|
| 観測・事前情報の追加 | `add_factor(form, loss)` | 因子を追加し、現在候補・最良候補についてその因子を評価。安定したIDを返す |
| 観測値・尺度・評価種類の訂正 | `set_loss(id, loss)` | 一次式の値を再利用して対象因子のコストを更新。基本は定数時間だが、候補の入替で $O(n+R)$ のコピーが起こり得る |
| 観測が参照する変数・係数・定数項の変更 | `replace_factor(id, form, loss)` | 因子IDと有効状態を維持。全因子の逆引き関係を作り直すので、局所的な時間とは限らない。概ね $O(n+R+K+k\log k)$ |
| 観測の取消・再有効化 | `set_enabled(id, enabled)` | データとIDを残したまま寄与を切替。同じ状態への指定は何もしない |
| 上下限、候補集合、型、固定値の変更 | `set_domain(j, domain)` | 両候補の変数 $j$ を新値域に射影し、関係因子だけ再評価。固定化・固定解除も可能 |
| 外部で得た初期候補を使う | `set_state(values)` | 全変数分を指定。現在候補だけでなく、保持している最良候補もこの状態に置き換える |
| 同時更新方向の変更 | `set_directions(directions)` | 方向集合全体を置換。空の集合で利用者方向を消せる |

`add_factor`の戻り値は`int`、それ以外の上表の変更APIの戻り値は`void`。`id`と`j`は`int`、`enabled`は`bool`、`form`は`LinearForm`、`loss`は`Loss`、`domain`は`Domain`であり、変更APIの各引数に省略値はない。

`set_domain` は、変更変数を含む各一次式を再評価する。そのため「関係因子の個数だけで常に定数時間」という意味ではない。両候補の優劣が変わると状態コピーが必要になり、次回実行時の準備も別に必要になる。

正規因子の観測値だけを `set_loss` で変え、一次式と `sigma` が同じなら、既存の線形値、方向の係数・曲率、利用可能な行列分解を再利用する。分解を作り直さず、平均に関係する部分を解き直せる。

因子追加・一次式置換・尺度変更・有効状態変更・値域変更では、必要な準備を無効化し、次回実行時にやり直す。行列分解を因子追加のたびに低ランク更新する実装ではない。

### 2.3 再構築が必要なもの

| 変更したいもの | 扱い |
|---|---|
| 未知変数の個数、変数IDの並び | 変更APIはない。新インスタンスを構築し、既知の値を `set_state` で渡す |
| 登録済み因子の物理削除・IDの詰め直し | 削除APIはない。無効化するか、`replace_factor` でIDを再利用。大幅な整理は再構築 |
| 新しいseedでの独立な試行 | seed変更APIはない。新インスタンスで始める |
| `CustomLoss` の型や所有する評価器そのものの差し替え | 置換APIはない。再構築するか、最初から通知可能な外部データ参照を設計する |

今後増える変数数が分かる場合、初めに必要数を確保して固定値にしておき、後で固定解除する方法もある。ただし、登録した全変数が状態・統計の次元に含まれる。何でも大量に予約する使い方が効率的とは限らない。

### 2.4 再利用される統計とリセット条件

| 操作・変更 | 採取進捗・統計の扱い |
|---|---|
| モデル・採取設定を保ったまま `sample` を再実行 | 現在値、乱数状態、warmup進捗、座標走査位置、採取間隔の進捗、統計、保存列を継続 |
| `time_limit_us` / `max_steps` だけ変更 | 同じ採取区間を継続。返る `Runtime` は今回の呼出し分 |
| 因子の追加・変更、有効状態の実変更、値域変更 | 採取統計と保存サンプルをリセット。旧モデルの標本を新モデルに混ぜない |
| `set_state` / `set_directions` | 新しい採取区間を開始 |
| 正の予算で `optimize` を実行 | 採取区間をリセット。次の `sample` ではwarmupを開始 |
| method、方向設定、step_scale、enumeration_limit、warmup_steps、sample_strideを変更 | 新しい採取区間を開始 |
| `collect_moments` だけ切替 | 平均・分散のcountをリセット。保存列や採取進捗は継続 |
| `sample_capacity` だけ変更 | 保存列を空にする。標準統計と採取進捗は継続 |
| どちらかの予算を0にした呼出し | 準備・状態・乱数・統計を進めない。終了時のdebug hookは呼び出す |

同じ内容の `set_loss` でも更新通知として働き、統計をリセットする。モデルが変わっていないのに毎ターン全因子へ再指定すると、採取の継続性を失う。`set_enabled` の同じboolへの指定は例外的に何もしない。

`set_state` を呼んでも、新しいseedの独立系列にはならない。乱数状態や学習した提案尺度まで新品に戻す操作ではない。

### 2.5 custom評価器が外部データを参照するとき

評価器は、同じID・一次式の値に対して同じコストを返す必要がある。外部の観測表を更新する場合は、その表の変更に影響される**すべての因子**へ `set_loss(factor_id, Loss::custom(custom_id))` を呼ぶ。通知が終わるまでは候補の取得・探索を行わない。

factor IDは登録因子の番号、custom IDは利用者の評価器へ渡す番号であり、別物である。同じcustom IDを複数因子で共有した場合も、各factor IDへ通知する。実行中のフック内では更新しない。具体例は7.15に示す。

## 3. ユースケース：具体的な問題からモデルへ

ここではアルゴリズムやC++の構文に入る前に、利用者が知っている情報と、推定したい値を分ける。同じ一次式でも、正確な値・二択・丸め表示・必須条件では付ける評価が違う。

| ケース | 具体的に知りたいこと | モデル化で注目する違い | 近い応用 |
|---|---|---|---|
| U01 | 2個ずつ量った3荷物の各重量 | 整数の未知値、重なる合計計測 | 品物の体積、グループの作業量 |
| U02 | 三角形の道路で経由と直行のどちらが速いか | 辺費用の相関を含む行動評価 | 通信経路、搬送経路の費用 |
| U03 | 機械で8個を処理する時間 | 既知の特徴量と未知の切片・単価 | 作業量や距離に比例するコスト |
| U04 | 3案の勝ち負けから見た相対性能 | 二択回答をノイズ付き差の区間にする | グループ比較、順位だけのフィードバック |
| U05 | 上限表示になった部品の真の長さ | 観測生成の逆像を区間にする | 丸めた合計、上限付きセンサー |
| U06 | 大遅延を含む機械の通常の中心時間 | 裾の重い誤差モデル | 一時的な外乱の混じる計測 |
| U07 | カタログのどの部品が使われたか | 有限候補と非一様な事前確率 | 既知のサイズ表・装置タイプ |
| U08 | センサーのない庭の区画の温度 | 頂点間の差を事前因子にする | 地形の難易度、隣接区画の密度 |
| U09 | 合計12球の色別個数 | 整数の厳密な総数保存 | 在庫、人数、箱別の個数 |
| U10 | バルブの希望値と許容範囲の折合い | 確率でなくsoftな違反量を最小化 | 調整値、軽い制約付き係数合わせ |
| U11 | 投入量1.5での次回成功確率 | 一次式を非線形の二値尤度へ接続 | 成功率・故障率と既知特徴の関係 |
| U12 | 真値の±10%にぶれる棒の長さ | 真値依存の支持幅と密度 | 相対一様誤差の測定器 |
| U13 | 合計10Lを分けた2槽の水量 | 実数の等式を先に変数消去する | 保存される連続資源の内訳 |

### 3.1 U01 3個の荷物を、2個ずつ量った結果から推定する

荷物A・B・Cを1個ずつ量ることはできないが、2個を一緒に量れるとする。知りたいのは各荷物の重量で、単位はg、取り得る値は整数0〜100。計測前には各荷物を50g程度と考え、その目安からのずれを尺度20gの正規形の事前重みで表す。

| 一緒に量った荷物 | 表示された重量 | 合計計測の誤差の標準偏差 |
|---|---:|---:|
| AとB | 105g | 3g |
| BとC | 98g | 3g |
| AとC | 101g | 3g |

未知変数を $\theta_0=A$、$\theta_1=B$、$\theta_2=C$ と置く。最初の計測でモデルが予測する値は $z=\theta_0+\theta_1$。105という観測値は変数でも式の係数でもなく、この予測と比較する値である。独立な正規計測誤差を仮定すると、この因子のコストは $(\theta_0+\theta_1-105)^2/18$ になる。残りの観測と事前重みも足す。

$$E(\theta)=\frac{\sum_{j=0}^{2}(\theta_j-50)^2}{800}+\frac{(\theta_0+\theta_1-105)^2+(\theta_1+\theta_2-98)^2+(\theta_0+\theta_2-101)^2}{18}$$

分母800は $2\times20^2$、18は $2\times3^2$。誤差を無視して3式を解けばA=54、B=51、C=47になるが、この問題では計測誤差と事前知識を含めた分布を求める。個々の重量の推定には平均、整数の組を提出するなら実行可能な候補を使う。3個の合計の期待値は個々の平均を足して求められる。

4.10.1でこの3行を入力配列へ移し、5.5.1で返る配列の意味、7.1で関数の組立てと具体的な呼出しを確認する。

### 3.2 U02 三角形の道路で、目的地への経路を選ぶ

地点S・U・Tがあり、道路 $e_0$ はS–U、$e_1$ はU–T、$e_2$ はS–Tを結ぶ。接続関係は既知だが、各道路の所要時間が分からない。各時間は実数0〜20秒。事前の目安は順に5・4・7秒、各尺度は3秒とする。

| 通った道路 | 観測した合計時間 | 計測誤差の標準偏差 |
|---|---:|---:|
| $e_0,e_1$ | 9秒 | 1秒 |
| $e_1,e_2$ | 11秒 | 1秒 |
| $e_0,e_2$ | 12秒 | 1秒 |

未知変数 $\theta_j$ を道路 $e_j$ の時間にする。各観測は通った道路の時間の和に対する正規因子であり、たとえば最初は $z=\theta_0+\theta_1$、観測9、標準偏差1である。目的関数は各道路の事前コストと3個の計測コストの和になる。

次にSからTへ行く候補として、「U経由」 $c_0=\theta_0+\theta_1$ と「直行」 $c_1=\theta_2$ を利用側で用意する。求めたい出力は道路時間だけでなく、どちらの期待所要時間が小さいかである。誤差なしの値は5・4・7なので、期待時間が9秒と7秒付近なら直行を選ぶ、という読み方になる。

観測は道路時間同士に相関を生む。経由時間の分散は道路ごとの分散の単純な和ではなく、同じ標本の $\theta_0+\theta_1$ を評価して求める。未知の道路を調べる価値や危険回避を加えた経路評価は利用側で拡張する。ライブラリ自身はグラフから経路を探索しない。

4.10.2で辺IDと候補経路を配列化し、5.5.2で選択結果、7.2でその比較処理まで読む。

### 3.3 U03 機械の処理時間から、起動時間と1個当たり時間を分ける

同じ機械で品物を処理したところ、1個で5秒、3個で11秒、5個で17秒かかった。計測誤差は各回独立な正規誤差で標準偏差0.5秒とする。次に8個を処理する時間を予測したい。

時間を「起動時間＋個数×1個当たり時間」とモデル化する。未知の $\theta_0$ は起動時間（秒）、$\theta_1$ は1個当たり時間（秒/個）。個数 $q$ は既知の入力なので、予測式は $z=\theta_0+q\theta_1$ となる。切片用の1は $\theta_0$ の係数であり、観測データに未知変数を追加するわけではない。

| 個数 $q$ | 時間 $y$ | 一次式 $z$ |
|---:|---:|---|
| 1 | 5秒 | $\theta_0+\theta_1$ |
| 3 | 11秒 | $\theta_0+3\theta_1$ |
| 5 | 17秒 | $\theta_0+5\theta_1$ |

事前の中心を $\mu=(2,3)$、尺度を起動時間は5秒、単価は5秒/個とする。例の係数は上限・下限を設けない実数である。各値をこれらの単位の数値で書いた目的関数は $\sum_{j=0}^{1}(\theta_j-\mu_j)^2/50+\sum_{(q,y)}(\theta_0+q\theta_1-y)^2/0.5$。ここで後半の和は上表の3行、分母0.5は $2\times0.5^2$ である。

このデータでは $(2,3)$ が事前中心と全観測を同時に満たし、最小コストは0。8個の予測は $2+8\times3=26$ 秒となる。厳密な非負係数が必要な現場では、実数全域という例の値域をそのまま使わず、非負範囲へ変更する。特徴量を増やす場合も「どの列に何の単位の係数を掛けるか」を先に決める。

4.10.3で特徴配列の形式、5.5.3で予測の返し方、7.3で8個への代入まで確認する。

### 3.4 U04 勝ち負けの回答だけから、3案の相対性能を推定する

案A・B・Cの性能を直接数値では測れず、2案を比較した勝ち負けだけが返るとする。3回の回答はA>B、B>C、A>Cだった。知りたいのは、共通基準から見た各案の相対性能である。負の値も許し、各値は実数−10〜10、事前中心0、尺度3とする。

未知変数をA・B・Cの順に $\theta_0,\theta_1,\theta_2$ とする。「A>B」は $\theta_0-\theta_1$ の正確な値ではない。判定する差に標準偏差0.5の正規ノイズ $\varepsilon$ が加わり、$\theta_0-\theta_1+\varepsilon>0$ と判定された、という観測モデルを使う。

差を $z=\theta_0-\theta_1$ とすると、この回答の確率は $\Phi(z/0.5)$、因子コストは $-\log\Phi(z/0.5)$。他の2比較も同様に加える。$\Phi$ は標準正規分布の累積確率で、差が0でも勝つ確率は1/2である。したがって「A>Bの回答」をノイズのない厳密な不等式と置き換えてはいけない。

比較だけでは全案を同じ量だけ動かしても回答確率は変わらず、勝ち負け3個から正確な差の大きさも決まらない。事前情報は共通の位置と過度な差を抑える役割を持つ。出力の平均をA・B・Cの推定性能として比較できるが、性能差が識別できるほど観測があるかは別に確認する。

4.10.4で左右のグループと回答を指定し、5.5.4で平均と勝率の違い、7.4で差を区間観測へ変換する箇所を読む。

### 3.5 U05 表示上限に張り付いた長さを、丸め規則から読み解く

部品A・Bの長さを知りたいが、計測器は「正規ノイズを加える→2mm刻みに丸める→表示を0〜10mmに制限する」という処理を行う。ノイズの標準偏差は1mm。真の長さはそれぞれ整数0〜20mmであり、表示上限10mmは真値の上限ではない。事前の目安はA=8mm、B=12mm、尺度は各5mmとする。

| 量った部品 | 表示 | 丸める前のノイズ付き計測値について分かること |
|---|---:|---|
| Aだけ | 6mm | 5以上7未満 |
| Bだけ | 10mm | 9以上。上限は分からない |
| A+B | 10mm | 9以上。上限は分からない |

通常の6という表示は幅2の丸め区間 $[5,7)$ に対応する。一方、9以上は丸めれば10以上となり、どれも表示10へ制限される。そのため表示10の逆像は $[9,\infty)$ であり、$[9,11)$ ではない。連続ノイズなので端点の包含の違いは確率に影響しない。

未知変数を $\theta_0=A$、$\theta_1=B$ とする。最初の因子は「$\theta_0$ にノイズを加えた値が $[5,7)$ に入る確率」の負の対数。残りは $\theta_1$ と $\theta_0+\theta_1$ に対する片側区間の観測になる。この3因子と事前因子が目的関数を作る。

欲しい出力は表示の再現ではなく、表示の背後にある真の長さの分布である。Aは6mm付近に絞られ、Bは10mmを超える値も十分考えられる。表示10を「真値10付近の正規観測」と扱うと、上限を超えた情報の欠落を表せない。

4.10.5で丸め幅と二種類の上限を分け、5.5.5で出力の単位、7.5で表示から区間への変換を確認する。

### 3.6 U06 一時的な大遅延が混ざった処理時間から通常の係数を求める

品物の処理時間をU03と同じ「起動時間＋個数×単価」で説明したい。ただし、この計測には大きな遅延が混じると分かっている。観測は次の6回とする。

| 個数 $q$ | 0 | 1 | 2 | 4 | 8 | 3 |
|---|---:|---:|---:|---:|---:|---:|
| 時間 $y$（秒） | 2 | 4 | 6 | 10 | 18 | 40 |

最初の5回は起動2秒・1個当たり2秒の式に合うが、3個の回は通常なら8秒程度なのに40秒かかっている。未知係数は $\theta_0$ と $\theta_1$、各実数全域、事前中心は $(2,2)$、尺度5とする。ここでは対称な裾の重い誤差の近似として、各観測に尺度1秒・自由度4のStudent tモデルを使う。

各行の予測は $z=\theta_0+q\theta_1$、誤差は $r=z-y$。観測因子のコストは $\frac{5}{2}\log(1+r^2/4)$ になる。これに2係数の事前コストを加えたものが目的関数である。尺度1はt分布の尺度であり、正規誤差の標準偏差1と同じ意味ではない。

たとえば係数 $(2,2)$ での40秒観測は誤差32秒。標準偏差1の正規コストなら512だが、このtコストは $2.5\log257$、約13.9である。大きな誤差も無視はせず、係数を大きく曲げる圧力を弱める。欲しい出力は次の5個の通常の中心時間 $\theta_0+5\theta_1$ で、このデータでは12秒付近が理解の目安になる。

このモデルは正負対称なので、遅延が必ず正側だけに出ることを厳密に表すものではない。また、40秒が別機種・別材料による本来の違いなら、外れ値扱いではなく特徴量やモデルを追加する。4.10.6で誤差モデルを指定し、5.5.6・7.6で係数から中心時間へ戻す。

### 3.7 U07 カタログ部品の組合せと、それぞれの候補確率を求める

外から見えない部品A・Bの長さを推定したい。Aは8・12・20mmのどれか、Bは5・10mmのどれかで、中間の値は存在しない。製造時の選ばれやすさは次のとおりとする。部品の選択は計測前には独立と仮定する。

| 部品 | 候補値と事前確率 |
|---|---|
| A | 8mm: 0.2、12mm: 0.6、20mm: 0.2 |
| B | 5mm: 0.75、10mm: 0.25 |

組み合わせた合計長さを量ると22mmだった。計測誤差は標準偏差2mmの正規誤差である。未知変数は $\theta_0=A$、$\theta_1=B$。値域はそれぞれ上表の有限集合とし、表の事前確率を $w_0,w_1$ と書く。

$$E(\theta)=-\log w_0(\theta_0)-\log w_1(\theta_1)+\frac{(\theta_0+\theta_1-22)^2}{8}$$

たとえば $(12,10)$ は合計22で、事前の同時確率は $0.6\times0.25=0.15$。$(20,5)$ の事前も0.15だが合計25なので、観測からの重みが $\exp(-9/8)$ 倍になる。こうして、合計が近いかだけでなく、もともとの選ばれやすさも反映する。

欲しいのは最良の組だけでなく「Aが12mmである確率」などの周辺確率である。この小例は全6状態を列挙して検算できる。候補間隔が広いこと自体は事前確率を増やさず、平均が候補の値と一致してもその候補が確定したとは限らない。

4.10.7で候補表を値域とcustom因子へ分け、5.5.7で確率配列の添字、7.7で採取中に候補を数える処理を読む。

### 3.8 U08 センサーのない中央区画の温度を、隣接関係から推定する

庭の区画がA–B–Cの順に隣接している。Aでは18℃、Cでは24℃と観測したが、Bにセンサーがない。各センサーの正規計測誤差は標準偏差1℃。近隣区画の温度は似ているという知識を使い、Bの温度を推定したい。

未知変数は区画A・B・Cの温度 $\theta_0,\theta_1,\theta_2$ で、実数全域とする。各区画の中心20℃・尺度5℃の因子を置き、さらに隣接差は中心0・尺度2℃の正規形の因子で評価する。後者は「温度差0を観測した」というデータではなく、滑らかさの事前情報である。

$$E(\theta)=\frac{\sum_{j=0}^{2}(\theta_j-20)^2}{50}+\frac{(\theta_0-\theta_1)^2+(\theta_1-\theta_2)^2}{8}+\frac{(\theta_0-18)^2+(\theta_2-24)^2}{2}$$

このように、局所観測は1変数、隣接情報は2変数の差という一次式になる。B自身の観測がなくても両端から情報が伝わる。Bは21℃付近が目安だが、事前と観測誤差も同時に考慮するので、18と24の単純な平均を返すだけの処理ではない。

差の因子だけなら全区画を同じ温度だけずらしてもコストは変わらない。絶対温度の位置を定める観測・事前因子が必要で、ここでは両方を入れている。4.10.8で既知の辺を差の式へ変換し、5.5.8・7.8で未観測のBの平均と分散を取り出す。

### 3.9 U09 合計12個の球の、色別の内訳を推定する

箱の中に赤・青・緑の球があり、全体は正確に12個と分かっている。一方、色別計測は不正確で、独立な3回の計測から「赤7個」「青と緑で5個」「青3個」という値を得た。各誤差は標準偏差1個の正規誤差とする。

未知変数 $\theta_0,\theta_1,\theta_2$ は赤・青・緑の個数で、整数0〜12。各色は4個程度という事前中心と尺度3個を置く。観測の一次式は順に $\theta_0$、$\theta_1+\theta_2$、$\theta_1$。これらの正規因子に加え、$\theta_0+\theta_1+\theta_2=12$ を必須条件とする。

総数12は誤差0の正規観測にするのではなく、違反した状態のコストを無限大にする厳密な制約である。誤差を無視した候補は赤7・青3・緑2だが、事前情報も含むので、分布全体はこの1状態だけではない。赤と残りの2色の計測は保存則を通じて関連するが、ここでは別々の独立測定である。同じ測定値から「12−赤」を計算しただけなら、別の独立観測として追加しない。

初期状態には赤4・青4・緑4を用意する。1色だけを増減すると必須条件に違反するため、ある色を増やすと同時に別の色を減らす変更を許す必要がある。このモデル上の保存則が、4.10.9の方向設定につながる。

欲しい出力が「推定平均」なら小数も読めるが、実際の内訳として使うのは総数12を満たす整数の組。5.5.9でその区別、7.9で初期状態と同時変更を確認する。

### 3.10 U10 2個のバルブを、希望値と許容範囲の折合いで調整する

2個のバルブの調整値 $\theta_0,\theta_1$ を、実数0〜10の範囲で決めたい。普段の希望値はどちらも3だが、合計は8〜9、差「1番目−2番目」は1〜2に近いことが望ましい。範囲外を全面禁止するのではなく、外れた距離も比較して折合いを付ける問題とする。

| 要望 | 対応する一次式 | ずれの評価尺度 |
|---|---|---:|
| 各バルブを3に近づける | $\theta_0$、$\theta_1$ | 各2 |
| 合計を8〜9へ | $\theta_0+\theta_1$ | 1 |
| 差を1〜2へ | $\theta_0-\theta_1$ | 1 |

区間までの距離を $d$ と書くと、目的関数は次になる。

$$E(\theta)=\frac{(\theta_0-3)^2+(\theta_1-3)^2}{8}+\frac{d(\theta_0+\theta_1,[8,9])^2+d(\theta_0-\theta_1,[1,2])^2}{2}$$

区間内では対応する距離は0、区間外では近い端点までの距離である。この例は確率推定ではなく、ペナルティの合計を小さくする調整問題として使う。小さい尺度ほど、その要望からのずれを重く評価する。

$(4.5,3.5)$ なら合計と差の範囲を両方満たすが、希望値からは離れる。この目的関数の最小点は $(13/3,31/9)$、約 $(4.333,3.444)$。合計は約7.778、差は約0.889で、両方の下限を少し下回る。これは不具合ではなく、希望値への近さと違反量を比較した結果である。必ず範囲内でなければならない仕様は別のハード制約にする。

4.10.10で一次式と範囲を直接入力し、5.5.10・7.10で最良候補の実際の違反量を確認する。

### 3.11 U11 投入量ごとの成功・失敗から、次の成功確率を予測する

ある操作は投入量 $q$ を増やすほど成功しやすいと考えている。投入量0・1・2でそれぞれ独立に10回試した結果、成功数は2・5・8だった。次に投入量1.5で実行する場合の成功確率を知りたい。

| 投入量 $q$ | 成功 | 失敗 |
|---:|---:|---:|
| 0 | 2回 | 8回 |
| 1 | 5回 | 5回 |
| 2 | 8回 | 2回 |

未知の切片 $\theta_0$ と投入量の効果 $\theta_1$ を使い、一次式 $z=\theta_0+q\theta_1$ を作る。ただし、$z$ 自体は確率ではない。成功確率へ変換するモデルを次のように定める。

$$p(q,\theta)=\frac{1}{1+\exp(-\theta_0-q\theta_1)}$$

ここで $p$ は成功確率、$q$ は既知の投入量、$\theta_0$ は基準となる対数オッズ、$\theta_1$ は投入量1当たりの対数オッズの変化量である。係数は実数全域、独立な事前中心0・尺度3とする。1回の結果 $y\in\{0,1\}$ に対して因子コストを $-y\log p-(1-y)\log(1-p)$ とし、30回分と事前コストを足す。成功なら $-\log p$、失敗なら $-\log(1-p)$ になる。

線形なのは確率そのものではなく、この対数オッズの式である。成功・失敗を0/1として正規回帰するのではなく、一次式の値を成功・失敗の尤度へ変換するcustom評価を使う。

最後は係数の分布にわたって $p(1.5,\theta)$ を平均する。「係数の平均を先に求めてから確率へ変換する値」とは一般に異なる。4.10.11で試行列とcustom IDを作り、5.5.11・7.11でこの予測平均を取り出す。

### 3.12 U12 真値の±10%に一様にぶれる長さ計測を扱う

1本の棒を独立に2回量り、10.0cmと10.5cmという結果を得た。誤差は一定のcm幅ではなく、「真の長さの±10%の範囲に一様にぶれる」という仕様である。真の長さ $z=\theta_0$ は実数1〜20cm、事前中心10cm・尺度3cmとする。

1回の生成式は $y=z(1+u)$、$u$ は区間 $[-\epsilon,\epsilon]$ の一様乱数で、ここでは $\epsilon=0.1$。$y$ が既知なら、その観測を生める真値は次の範囲に限られる。

$$\frac{y}{1+\epsilon}\le z\le\frac{y}{1-\epsilon}$$

| 観測 $y$ | その観測を生める真値の範囲（cm） |
|---:|---|
| 10.0 | $[10/1.1,10/0.9]$、約 $[9.091,11.111]$ |
| 10.5 | $[10.5/1.1,10.5/0.9]$、約 $[9.545,11.667]$ |

両方を満たす範囲は約 $[9.545,11.111]$。ただし、この区間内を単に同じ重みで扱うだけでは不十分である。真値 $z$ が大きいほど計測値の分布幅 $2\epsilon z$ が広がり、観測の密度は $1/(2\epsilon z)$ と低くなる。1回分のコストは支持内で $\log z+\log(2\epsilon)$、支持外では無限大である。

したがって2回分を含む目的関数は、共通支持内で $(z-10)^2/18+2\log z+2\log0.2$、外では無限大になる。これは連続観測の密度を使う式であり、「10.0ぴったりを観測する確率」が正という意味ではない。

出発点に10cmを選べば両方の支持内に入る。4.10.12でこの支持検査とcustom評価を準備し、5.5.12・7.12で1変数の真値を取り出す。

### 3.13 U13 合計10Lの水を、2槽へ分けた内訳として推定する

2槽A・Bの水量は非負の実数で、合計10Lは正確に分かっている。独立な2回の計測から「Aは7L」「A−Bは4L」を得ており、各正規計測誤差の標準偏差は1L。各槽の事前中心は5L、尺度は3Lとする。

そのままなら未知変数は2個で、等式 $A+B=10$ を必ず満たす必要がある。ここでは先に $x=A$、$B=10-x$ と書き換える。自由に動かす変数は $x\in[0,10]$ の1個だけになり、どの $x$ でも保存則を満たす。

| 元の情報 | 自由変数で書いた一次式 | 評価する中心 |
|---|---|---:|
| Aの事前 | $x$ | 5L |
| Bの事前 | $10-x$ | 5L |
| Aの計測 | $x$ | 7L |
| A−Bの計測 | $2x-10$ | 4L |

最後の行は $A-B=x-(10-x)=2x-10$ と展開して得られる。定数−10を落とすと、別の観測モデルになってしまう。目的関数も同じ置換で次の形になる。

$$E(x)=\frac{(x-5)^2+(10-x-5)^2}{18}+\frac{(x-7)^2+(2x-10-4)^2}{2}$$

この二次式の最小点は $x=325/47$、約6.915L。最後にA=$x$、B=$10-x$ と元の2槽の順へ戻す。平均も合計10、分散は両方で等しく、共分散はその分散の負値となる。

U09と同じ保存則だが、ここは連続量なので、細い等式制約を探索に任せるのでなく利用側で変数を消去する例である。4.10.13で係数と定数の変換、5.5.13・7.13で2変数の出力への復元を追う。

## 4. 利用準備

### 4.1 ビルドと型名

`linear_estimator_v06.hpp` を置き、C++20でincludeする。本体は標準ライブラリだけを利用するが、インクルードにGCC系の `bits/stdc++.h` を使用している。GCC/libstdc++の環境を前提に導入するとよい。コンパイル例は7章末尾にある。

通常の型は `LinearEstimator<>`。本文では `LE` と略す。独自尤度を使うときは `LinearEstimator<MyLoss>` のように評価器の型をテンプレート引数へ指定する。`Domain`、`Loss`、`SampleParam` などはこのクラスの入れ子の型であり、CustomLossが異なる推定器間では別の型になる。

### 4.2 コンストラクタ

`LinearEstimator(std::vector<Domain> domains, uint64_t seed = 0, CustomLoss custom = {})`

| 引数・型 | デフォルト | 指定方法 |
|---|---|---|
| `domains: std::vector<Domain>` | なし、必須 | 空でない値域列。要素数が変数数、位置が変数IDになる。異なる種類を混在可能 |
| `seed: uint64_t` | `0` | 乱数の初期値。再現比較は同じモデル・操作列・固定試行予算と組み合わせる |
| `custom: CustomLoss` | `{}` | 評価器を所有させる。標準Lossだけなら省略。capturing lambdaや状態付き評価器では実体を渡す |
| テンプレート引数 `CustomLoss` | `std::nullptr_t` | 標準Lossだけなら `<>`。customを使う場合はcallableの具体的な型 |

custom評価器は`double operator()(int id, double z) const`に相当する呼出しを受け付ける。標準の評価関数に加えてcustom因子を混在させることもできる。

初期状態は、各値域へ0を射影した状態になる。事前分布の平均を自動的に初期値へ使うわけではない。明示的な初期状態が有用なら、因子を登録した後で `set_state` を使う。

### 4.3 Domainの生成

| API | 引数の型・デフォルト | 値域と注意 |
|---|---|---|
| `Domain::real(lo, hi)` | `double lo=-INFINITY, hi=INFINITY` | 実数の閉区間。`lo < hi`。片側無制限も可能 |
| `Domain::integer(lo, hi)` | `int64_t`の両端、どちらも必須 | 両端を含む整数。`lo <= hi`。同じ値ならFixedに変換 |
| `Domain::values(v)` | `std::vector<double>`、必須 | 空でない有限候補。整列・重複除去後、1候補ならFixedに変換 |
| `Domain::fixed(x)` | `double`、必須 | 既知の有限値。可動変数として数えない |
| `domain.contains(x)` | `double`、必須 | 含まれるかをboolで返す。NaNや無限大は含まない |

Domainのデフォルト構築は無制限のReal。公開フィールドは `kind=Kind::Real`、`lower=-INFINITY`、`upper=INFINITY`、`candidates={}` だが、通常は上の生成関数で整合した値域を作る。Realを一点にしたい場合は `fixed` を使う。固定値の数値も内部ではdoubleである。

### 4.4 一次式と因子

| 型・API | デフォルト・設定方法 |
|---|---|
| `Term::variable` | `int`。0以上n未満の変数IDを必ず指定 |
| `Term::coefficient` | `double`。有限の既知係数を必ず指定 |
| `LinearForm::terms` | `std::vector<Term>`、初期値は空 |
| `LinearForm::offset` | `double`、初期値は0 |
| `LinearForm::sum(indices)` | `std::span<const int>`を受け取り、各IDの係数1の和を返す |
| `LinearForm::difference(left, right)` | 二つの `std::span<const int>` を受け取り、左の合計−右の合計を返す |
| `form.evaluate(values)` | `std::span<const double>`へ一次式を適用する。因子コストや値域の検査ではない |
| `add_factor(form, loss)` | 二つとも必須。新しい有効因子を追加し、`int`のfactor IDを返す |

たとえば `LE::LinearForm{{{0, 2.0}, {3, -1.0}}, 5.0}` は、変数0の2倍から変数3を引いて5を足す式である。`vector<int>` や `array<int,N>` はsum/differenceへ渡せる。波括弧だけのID列がspanへ自動変換されるとは限らないので、名前を付けたvector/arrayを使うと明確になる。

因子登録・置換時に、同じ変数の項を合算し、係数0を除去する。したがって左右グループの共通項は打ち消される。空の一次式も定数因子として使えるが、矛盾した定数ハード制約は未知値を変えても修復できない。

### 4.5 Lossの設定値

1.3の生成関数の引数はすべて必須である。`student_t` の自由度などを省略するAPIではない。

| パラメータ | 型・許容値 | 選び方 |
|---|---|---|
| 観測値 `y` | 有限の `double` | 一次式と同じ単位で指定 |
| `sigma` | 正の有限 `double` | 観測ノイズの標準偏差。既知の生成規則を優先 |
| Laplaceの `s` | 正の有限 `double` | Laplaceの尺度。標準偏差は $\sqrt{2}s$ |
| Student tの `s`, `nu` | ともに正の有限 `double` | `s`は尺度。`nu>2`なら標準偏差は $s\sqrt{\nu/(\nu-2)}$。小さい自由度は外れ値の影響を弱めるが、分布の形も変える |
| 区間 `lo`, `hi` | `double`、通常 `lo <= hi` | 片側無限は可能。GaussianIntervalだけは正の幅 `lo < hi` が必要 |
| SoftIntervalの `s` | 正の有限 `double` | 区間外への逸脱が `s` のときコスト0.5。小さいほど厳しく罰する |
| `custom_id` | `int` | 評価器が解釈する任意のID。ライブラリは表の添字範囲を管理しない |

区間は`lo < INFINITY`かつ`hi > -INFINITY`も必要であり、正負の無限大だけからなる一点区間は指定しない。

`Loss{}` は `kind=Gaussian, target=0, scale=1, lower=-INFINITY, upper=INFINITY, df=1, custom_id=0` である。不要なフィールドは選んだ種類では使わない。通常は生成関数を使い、`set_loss` で因子全体のLossを差し替える。

`loss.evaluate(z)` は標準Loss単体の評価に使える。`Loss::custom(...).evaluate(z)` は評価器を持たないため使えない。customを含むモデルの全評価には `model.evaluate(values)` を使う。

因子に汎用のweightフィールドはない。正規因子のコストを正の係数 $w$ 倍にするなら `sigma / sqrt(w)` を指定できる。Laplaceなら `s / w` で同じ倍率になる。一般の因子の重みはcustom評価で表現する。未知値に応じて尺度が変わる場合は、尺度を変数外の定数として扱わず、正規化項も含めてcustomへ実装する。

### 4.6 初期状態・利用者方向

| API | 引数・初期状態 | 指定の判断 |
|---|---|---|
| `set_state(values)` | `std::span<const double>`、全n変数分。省略すると0の値域への射影 | 各値域内でなければassert。ハード因子違反は登録でき、後で修復対象となる。既存bestを上書きする点に注意 |
| `set_directions(directions)` | `std::vector<std::vector<Term>>`、初期値は空 | 相関が強い変数や保存則を保って一緒に動かしたい変数を指定 |

方向 $d$ は、スカラーの移動量 $t$ に対して $\theta_j\leftarrow\theta_j+t d_j$ と変えるための係数列である。ここで $d_j$ は `Term::coefficient`。たとえば `{ {0,1}, {1,-1} }` は二つの合計を保つ。

各方向は空でなく、対象をすべてRealまたはすべてIntegerに揃える。Integerの方向係数は整数。Values・Fixedは利用者方向に含めない。型変更で既存方向が使えなくなると準備時に除外されるので、モデルに合う方向を再登録する。整数方向の共通の整数倍は内部で取り除かれる。

### 4.7 実行パラメータの全一覧

`OptimizeParam` と `SampleParam` に共通する設定は次のとおり。

| フィールド | 型 | デフォルト | 意味・値の選び方 |
|---|---|---|---|
| `time_limit_us` | `int64_t` | `2000` | 今回の呼出しの時間予算。単位はマイクロ秒。`-1`で時間上限なし |
| `max_steps` | `int64_t` | `-1` | 今回の更新試行数の上限。`-1`で回数上限なし。実装経路により1stepの仕事量は異なる |
| `method` | `Method` | `Automatic` | まず既定値で評価する。他の方式は比較・調整時に明示 |
| `auto_directions` | `bool` | `true` | 自動で用意する相関方向を使う。利用者方向とは別。Automaticの有限候補ペア更新にも関係 |
| `direction_probability` | `double` | `0.25` | 0〜1。準備済み同時更新方向を選ぶ確率。Automaticの有限候補ペアを試す確率にも使う |
| `step_scale` | `double` | `1` | 正の値。局所提案やsliceの幅の倍率。小さすぎると移動が遅く、大きすぎると棄却・評価が増え得る |
| `enumeration_limit` | `int` | `32` | 0以上。条件付き全列挙の候補数上限。有限候補ペアでは候補数の積に適用。大きくすると1試行が重くなる |

`OptimizeParam` 固有の設定:

| フィールド | 型 | デフォルト | 意味・値の選び方 |
|---|---|---|---|
| `temperature_start` | `double` | `1` | 0以上。探索開始時の温度。目的関数の差の尺度に対して調整 |
| `temperature_end` | `double` | `0.01` | 0以上。終了側の温度。両方が正なら幾何的に変化 |
| `greedy_probability` | `double` | `0.25` | 0〜1。温度0の改善優先更新を混ぜる確率 |

温度の一方でも0なら通常更新の温度も0になる。確実に改善優先で比較したければ、両温度を0とする。解析的な正規経路ではこれらの温度設定は使わない。正の `max_steps` を指定すると温度進行は試行数を基準にするため、時間制限が先に来た場合、終了温度まで進まないことがある。温度計画は呼出しごとに始まる。

`SampleParam` 固有の設定:

| フィールド | 型 | デフォルト | 意味・値の選び方 |
|---|---|---|---|
| `warmup_steps` | `int64_t` | `128` | 0以上。標本として数えない初期更新数。変数数や相関に対して十分かを確認する |
| `sample_stride` | `int` | `0` | 0なら通常は可動変数数ごと、独立な正規生成では毎回。正なら指定試行数ごとに採取 |
| `collect_moments` | `bool` | `true` | 全採取値の各変数の平均・分散を蓄積するか |
| `sample_capacity` | `int` | `0` | 0以上。最後の何個の全体サンプルを保存するか。`predict`を使うなら正の値が必要 |

時間・試行予算は両方とも-1にはできない。どちらかが0なら実行を進めない。固定試行で比較したい場合は `time_limit_us=-1` と正の `max_steps` を組み合わせる。実戦では外側の残り時間から少し余裕を引いて指定する。

warmup中には標本が得られない。大きい問題で初期値からかなり動く必要があるなら、可動変数を数巡する規模から検討し、十分な採取時間も残す。既定の128がすべての問題で十分という意味ではない。`sample_stride`を大きくするだけで収束するわけではなく、保存やhookの費用との兼ね合いで決める。

`step_scale`を調整するなら0.5・1・2などから、方向頻度は単独更新中心・既定値・方向多めの設定を比較すると役割を確認しやすい。整数候補が少しだけ32を超える場合には、列挙上限をその候補数へ上げる比較もできる。いずれも試行数だけで選ばず、同じ時間予算で出力品質と採取数を確認する。

### 4.8 Methodの指定

| 値 | 主な使い方 |
|---|---|
| `Automatic` | 正規モデルの専用経路、離散列挙、条件付き提案、sliceなどを適用条件に応じて選ぶ。通常の出発点 |
| `Metropolis` | 対称な局所提案を用いた比較用の基本経路。ハード因子による事前の区間絞り込みや条件付き列挙を使わない |
| `Conditional` | 小さい離散集合の列挙、正規部分の条件付き提案を優先。それが使えなければ局所提案 |
| `Slice` | 非正規の連続方向でsliceを利用。整数や有限候補まで連続sliceになる指定ではない |

Automatic以外を明示すると、正規モデル全体の行列経路やAutomatic固有の初期化を使わない。`auto_directions=true`なら、明示方式でも準備された方向の選択は行う。`step_scale`が、正規条件付き分布の分散や目的分布の温度を自由に変える設定ではない点にも注意する。

### 4.9 ユースケースによる準備の違い

U01・U05・U09は整数値域、U07は候補集合を用意する。U09は総数を満たす初期値と方向、U13は変数消去が必要になる。U02は辺IDと経路を対応付け、全体サンプルの保存を有効にする。U03・U06・U11は切片と特徴量の位置を揃える。U07・U11・U12は評価器の型とインスタンスを指定する。U08は観測だけでなく、隣接差の事前情報を因子にする。

### 4.10 具体例の情報をAPIの入力へ移す

以下の表は3章と同じ問題を扱う。各値をどの型・引数へ渡すかを確認してから7章のコードを読む。既定値のないモデル入力は表の数値を明示する。各具体例の`param`は対応する型の`{}`、`seed`は0が既定で、時間や方式の既定値は4.7に従う。

ここで使う`SumObservation`などは7章の補助型で、ライブラリには最終的に`Domain`、`LinearForm`、`Loss`を渡す。入力表に含めるノイズ尺度と、探索用の`step_scale`は別の量である。

#### 4.10.1 U01 重量と合計計測を登録する

| 3.1の情報 | 7.1の入力・登録内容 |
|---|---|
| A・B・Cの順 | `prior_mean={50,50,50}`。長さ3が変数数を決める |
| 整数0〜100g | `max_weight=100` → 各 `Domain::integer(0,100)` |
| 事前の尺度20g | `prior_sigma=20`。`add_normal_priors`が1変数ずつ因子化 |
| A+B=105g、誤差3g | `SumObservation{{0,1},105,3}` |
| 他の計測 | `{{1,2},98,3}` と `{{0,2},101,3}` |

`items={0,1}`を`LinearForm::sum`へ渡すと、項は`{0,1}`と`{1,1}`、定数項は0になる。項の第1要素は変数ID、第2要素は係数であり、入力観測の`value=105`は`Loss::gaussian(105,3)`に渡る。事前尺度20と計測尺度3を入れ替えない。

#### 4.10.2 U02 道路IDと予測したい経路を揃える

| 3.2の情報 | 7.2の入力・登録内容 |
|---|---|
| $e_0,e_1,e_2$ の事前 | `prior_edge_cost={5,4,7}`, `prior_sigma=3` |
| 各道路0〜20秒 | `max_edge_cost=20` → `Domain::real(0,20)` |
| 3本の観測経路 | `{{0,1},9,1}`, `{{1,2},11,1}`, `{{0,2},12,1}` |
| S→Tの経由・直行 | `candidate_routes={{0,1},{2}}`。外側の添字が候補番号 |
| 経路全体の不確かさ | 具体例では `param.sample_capacity=256` |

観測にも予測にも`LinearForm::sum`を使うが、観測だけに`Loss::gaussian`を付ける。候補経路は「その費用を知りたい式」であり、追加の観測ではない。候補を登録因子に混ぜると別のモデルになる。

#### 4.10.3 U03 特徴列と係数の位置を決める

| 3.3の情報 | 7.3の入力・登録内容 |
|---|---|
| 起動時間、1個当たり時間の順 | `coefficient_prior={2,3}`, `prior_sigma=5` |
| 係数2個、実数全域 | `Domain::real()`を2個 |
| 3個で11秒、誤差0.5秒 | `FeatureObservation{{3},11,0.5}` |
| 残りの計測 | `{{1},5,0.5}` と `{{5},17,0.5}` |
| 次の8個 | 結果の `best[0]+8*best[1]` |

`features`は既知の個数だけを含み、切片用の1は含めない。関数が項`{0,1}`を先に作り、特徴`features[k]`を変数ID`k+1`の係数にする。3個の行は`terms={{0,1},{1,3}}`, `offset=0`。起動時間そのものは未知変数なので、既知の定数項`offset`に入れてはいけない。

#### 4.10.4 U04 回答を「ノイズ付きの差の区間」にする

| 3.4の情報 | 7.4の入力・登録内容 |
|---|---|
| 各性能の事前 | `prior_mean={0,0,0}`, `prior_sigma=3` |
| 実数−10〜10 | `bound=10` → `Domain::real(-10,10)` |
| A>Bの回答 | `ComparisonObservation{{0},{1},true,0.5}` |
| B>C、A>C | `{{1},{2},true,0.5}`, `{{0},{2},true,0.5}` |
| A−Bに対応する因子 | `difference(left,right)` と `gaussian_interval(0,INFINITY,0.5)` |

`left_greater=false`なら同じ「左−右」の式に対して区間を`(-INFINITY,0)`にする。`sigma`は左右の個別測定ではなく、差に加わるノイズの標準偏差。同点という第3の回答がある問題は、その回答を生む区間も別途定義する。

#### 4.10.5 U05 表示規則を区間端点へ変換する

| 3.5の情報 | 7.5の入力・登録内容 |
|---|---|
| 真値の事前・整数上限 | `prior_mean={8,12}`, `prior_sigma=5`, `max_size=20` |
| 表示規則 | `step=2`, `clip_min=0`, `clip_max=10` |
| Aの表示6 | `{{0},6,1}` → `gaussian_interval(5,7,1)` |
| Bの表示10 | `{{1},10,1}` → `gaussian_interval(9,INFINITY,1)` |
| A+Bの表示10 | `{{0,1},10,1}` → 同じ片側区間を合計式に適用 |

関数内の`lo/hi`は丸め前の観測値の区間であり、`set_domain`へ渡す真値の範囲ではない。この補助関数は表示値と表示上下限が`step`の整数倍であることを前提とする。切捨てや、ノイズを丸めた後に加える規則なら、この端点計算をその規則に合わせて作り直す。

#### 4.10.6 U06 同じ一次式に重い裾の評価を付ける

| 3.6の情報 | 7.6の入力・登録内容 |
|---|---|
| 起動・単価の事前 | `coefficient_prior={2,2}`, `prior_sigma=5` |
| 1個で4秒 | `FeatureObservation{{1},4,1}`。最後の1はtの尺度 |
| 大遅延の行 | `{{3},40,1}`。他の観測と同じ形式で含める |
| 裾の重い誤差 | `noise=RobustNoise::StudentT`, `df=4` |
| 5個の中心時間 | 推定係数 `x` を使って `x[0]+5*x[1]` |

式の作り方・係数順はU03と同じで、観測因子だけが`Loss::student_t(row.value,row.scale,df)`になる。`RobustNoise::Laplace`を選ぶなら`scale`はLaplaceの尺度となり、`df`は使われない。自由度を小さくするほど裾は重くなるが、機械的な推奨値ではなく誤差の仮定に合わせて選ぶ。

#### 4.10.7 U07 候補集合と候補重みを別に登録する

| 3.7の情報 | 7.7の入力・登録内容 |
|---|---|
| Aのカタログ | `entries[0]={{8,0.2},{12,0.6},{20,0.2}}` |
| Bのカタログ | `entries[1]={{5,0.75},{10,0.25}}` |
| 取り得る値 | 各`entries`の`first`を `Domain::values`へ |
| 候補ごとの重み | `CatalogPrior`が`-log(weight)`を返す `Loss::custom(j)` |
| 合計22、誤差2 | `SumObservation{{0,1},22,2}` → 正規因子 |

`CatalogEstimator`は`LinearEstimator<CatalogPrior>`の別名。実行パラメータにも`CatalogEstimator::SampleParam`を使う。候補値は変数内で重複不可、重みは正の有限値。重みは各変数内で比が合えばよく、合計1でなくても同じ分布になる。

#### 4.10.8 U08 頂点値と隣接差を別の因子にする

| 3.8の情報 | 7.8の入力・登録内容 |
|---|---|
| A・B・Cの事前 | `prior_mean={20,20,20}`, `prior_sigma=5` |
| A–B、B–Cの接続 | `edges={{0,1},{1,2}}` |
| 隣接差の尺度 | `difference_sigma=2`。差の式に `gaussian(0,2)` |
| Aの18℃観測 | `VertexObservation{0,18,1}` |
| Cの24℃観測 | `VertexObservation{2,24,1}`。Bの観測は追加しない |

辺`{u,v}`から`terms={{u,1},{v,-1}}`, `offset=0`を作る。観測は`terms={{vertex,1}}`。グラフの頂点温度を変数にするU08と、道路費用を変数にするU02では、同じグラフという語でも変数IDの意味が異なる。

#### 4.10.9 U09 保存則を満たす状態と方向を用意する

| 3.9の情報 | 7.9の入力・登録内容 |
|---|---|
| 各色の事前・値域 | `prior_mean={4,4,4}`, `prior_sigma=3`, `max_per_item=12` |
| 正確な総数 | `total=12` → 全変数の和に `hard_interval(12,12)` |
| 実行可能な出発点 | `feasible_initial={4,4,4}` → `set_state` |
| 独立な部分計測 | `{{0},7,1}`, `{{1,2},5,1}`, `{{1},3,1}` |
| 総数を変えない方向 | `{{0,1},{1,-1}}` と `{{1,1},{2,-1}}` |

方向の項も`{変数ID,係数}`。最初の方向は赤に$t$、青に$-t$を加え、合計を変えない。補助関数は`set_directions`の後に`param.auto_directions=false`を設定する。複数変数なら`direction_probability>0`が必要で、0ではこの例の個数が移動しない。

#### 4.10.10 U10 許容したい一次式を明示する

| 3.10の情報 | 7.10の入力・登録内容 |
|---|---|
| 希望値と尺度 | `preferred={3,3}`, `preference_sigma=2` |
| 各調整値の必須範囲 | `lower_bound=0`, `upper_bound=10` → 実数値域 |
| 合計式 | `terms={{0,1},{1,1}}`, `offset=0` |
| 合計の希望範囲 | `lower=8`, `upper=9`, `scale=1` |
| 差の式と範囲 | `terms={{0,1},{1,-1}}`, `offset=0`, 範囲1〜2、尺度1 |

`RangeObservation`は`form,lower,upper,scale`を持つ。`fit_tolerance_ranges`は各行に`Loss::soft_interval`を付ける。変数値域0〜10は必須条件だが、合計8〜9はsoftな希望という違いを維持する。

#### 4.10.11 U11 二値ラベルと確率の式を対応させる

| 3.11の情報 | 7.11の入力・登録内容 |
|---|---|
| 切片と投入量の効果 | `coefficient_prior={0,0}`, `prior_sigma=3` |
| 投入量1で成功した1回 | `BinaryObservation{{1},true}`。失敗なら`false` |
| 各投入量10回、成功2・5・8 | `successes={2,5,8}`を30行の試行へ展開 |
| 各試行の式 | `terms={{0,1},{1,q}}`, `offset=0` |
| 次の投入量 | `query_features={1.5}` |

`LogisticLoss.labels[id]`にその試行の0/1を置き、対応する因子を`Loss::custom(id)`で登録する。評価器の型が違うので`LogisticEstimator::SampleParam`を使う。30回を1回分の成功率の観測へ縮めるのではなく、ここでは30個の独立な試行因子を登録する。

#### 4.10.12 U12 支持区間だけでなく密度の高さも評価する

| 3.12の情報 | 7.12の入力・登録内容 |
|---|---|
| 2回の計測と相対幅 | `RelativeUniformLoss{{{10.0,0.1},{10.5,0.1}}}` |
| 正の値域 | `lower=1`, `upper=20` → `Domain::real(1,20)` |
| 事前情報 | `prior_mean=10`, `prior_sigma=3` |
| 支持内の初期値 | `feasible_initial=10` → `set_state` |
| 各観測の式と評価 | 同じ1変数式に、別IDの `Loss::custom(id)` |

`RelativeUniformLoss`は支持外で`INFINITY`、支持内で`log(z)+log(2*epsilon)`を返す。単なる`hard_interval`では後半の項が抜ける。実行パラメータは`RelativeEstimator::SampleParam`。このcustom支持の自動修復はないので、全支持の共通部分に初期値を用意する。

#### 4.10.13 U13 元の2変数の式を1変数へ変換する

| 3.13の情報 | 7.13の入力・登録内容 |
|---|---|
| 正確な全体量 | `total=10`。内部値域は `Domain::real(0,10)` 1個 |
| 2槽の事前 | `prior_mean={5,5}`, `prior_sigma=3` |
| A=7の観測 | `SplitObservation{1,0,0,7,1}` |
| A−B=4の観測 | `SplitObservation{1,-1,0,4,1}` |
| 出力の槽の順 | `best/mean/variance`をA・Bの2要素へ復元 |

`SplitObservation`の順は「Aの係数、Bの係数、定数項、観測値、標準偏差」。元の式 $aA+bB+c$ は $(a-b)x+(c+b\times10)$ なので、コードでは`first_coefficient-second_coefficient`が係数、`offset+second_coefficient*total`が定数項になる。A−Bの行は`terms={{0,2}}`, `offset=-10`になる。

## 5. 実行方法とユースケースごとの出力利用

### 5.1 実行入口

| API | 引数 | 戻り値 |
|---|---|---|
| `optimize(param, debug_hook)` | `const OptimizeParam&`、既定 `{}`。debug hookは省略可 | `Runtime`。候補は戻り値に含まず、推定器から取得 |
| `sample(param, sample_hook, debug_hook)` | `const SampleParam&`、既定 `{}`。両hookは省略可 | `Runtime`。標本、統計、候補はhookまたは推定器から取得 |

両メソッドのパラメータは参照で受けるが、hookは値で受ける。外側の変数を更新したい場合は参照キャプチャを使う。状態を保持するhookのコピーに意図があるか確認する。

`sample_hook` の引数は `const LE::SampleView&`、`debug_hook` は `const LE::Runtime&` と書ける。SampleViewの `values` は全変数の `std::span<const double>`、`energy` はその状態の `double` の評価値。hookの戻り値を用いて停止させるAPIではない。採取されないwarmup中にsample hookは呼ばれない。

### 5.2 Runtimeを読む

| フィールド | 型・初期値 | 意味 |
|---|---|---|
| `stop_reason` | `StopReason::StepLimit` | 最終的な終了理由 |
| `elapsed_us` | `int64_t = 0` | 今回の呼出しの経過マイクロ秒。時間確認時に更新し、終了時にも更新 |
| `steps` | `int64_t = 0` | 今回の試行数。修復試行やCG反復も含む |
| `accepted` | `int64_t = 0` | 通常更新で受理・更新した回数等。修復の受理回数を同じ意味で加算する項目ではない |
| `samples` | `int64_t = 0` | 今回採取した標本数。`collect_moments=false`でも増える |
| `repair_steps` | `int64_t = 0` | 今回のハード制約修復試行数 |
| `warming_up` | `bool = false` | 採取でwarmupが残っているか。途中のdebug表示は更新時点の状態、戻り値で最終状態を確認 |

| StopReason | 解釈 |
|---|---|
| `TimeLimit` | 時間予算に達した |
| `StepLimit` | 試行上限など通常の反復終了。CGの内部上限で終わる場合もこの値になり得る |
| `NoMovableVariable` | 動かせる変数がない。全変数Fixedなら、候補が存在しても標本は採らない |
| `NoFeasibleState` | 終了時に実行可能候補がない。問題が実行不可能だという証明ではない |
| `Solved` | 正規モデルの直接計算を完了、またはCGの内部収束条件を満たした。任意の目的関数の大域最適性保証ではない |

終了時に実行可能候補がなければ、時間切れなどよりも `NoFeasibleState` が優先される。出力の可否は `has_solution()` でも確認する。

### 5.3 候補・統計・予測API

| API | 戻り値・利用条件 |
|---|---|
| `has_solution()` | `bool`。現在モデルで有限コストの候補があるか。最適化完了や推定精度の保証ではない |
| `current()` | `std::span<const double>`。現在候補。`has_solution()==true`が必要 |
| `best()` | 同型のspan。保持する最良候補。`has_solution()==true`が必要 |
| `best_energy()` | `double`。bestの評価。`has_solution()==true`が必要 |
| `moments()` | `const Moments&`。`count:int64_t=0`、`mean/variance:vector<double>`。count>0のときだけ数値を利用 |
| `stored_sample_count()` | `int`。保存中の全体サンプル数 |
| `stored_sample(index)` | 全変数の読取専用span。0以上count未満。古いものから順に並ぶ |
| `predict(form)` | `Prediction`。`count:int64_t=0`、`mean:double=0`、`variance:double=0`。保存サンプルから一次式を評価。count>0が必要 |
| `evaluate(values)` | `double`。全変数の値を現行モデルで評価。値域外・制約違反なら正の無限大 |

保存サンプルは最新の有限個を保持する列であり、全履歴から無作為に選んだ標本集合ではない。`moments`は現在の採取区間の全採取値、`predict`は保存されている末尾の値から計算する。このため、同じ変数の平均でも両者が一致するとは限らない。

両varianceは、中心からの二乗偏差を標本数で割った量である。不偏分散のように標本数−1で割るものではなく、平均推定の誤差分散でもない。

`predict`は潜在的な一次式の分散を返し、将来の観測ノイズは含まない。独立な正規ノイズの分散が $\sigma_{\rm new}^2$ なら、将来の観測値の分散にはそれを別に加える。係数が相関する場合、各係数のvarianceだけから合計の分散を再構成することはできない。

### 5.4 各ユースケースの呼出しと返り値

7章の汎用関数は、時間・方式等を利用側で変えられるよう、対応するParamを値で受け取る。原則として指定値をそのままライブラリへ渡し、U09は利用者方向を使うため`auto_directions=false`に設定する。数値入りの具体例関数`case_u02_routes`は`sample_capacity=256`も設定する。各関数の`seed`は省略時0である。

共通の `Estimate<E>` は例で定義するコピー用の結果型であり、ライブラリの型ではない。`feasible`、`best`、`best_energy`、`count`、`mean`、`variance`、そのEの `runtime` を返す。モデルを関数内で破棄しても値が残るようコピーする。標本がないときはmean/varianceを空にする。

| UC・関数 | 呼ぶAPIと主要引数 | 出力の利用と注意 |
|---|---|---|
| U01 `estimate_group_weights` | `sample(SampleParam)`。事前平均列、事前尺度、整数上限、合計観測列 | `mean`で個々の推定値、`best`で整数の具体的候補。countを確認 |
| U02 `choose_route` | `sample`の後に候補ごと `predict`。辺事前、上限、観測経路、候補経路、Paramを必須指定 | `route`が候補経路の添字。保存0件ならbestで費用を比較し、`used_samples=false`。`route=-1`なら候補なし |
| U03 `fit_feature_costs` | `optimize(OptimizeParam)`。切片込み事前列と特徴観測列 | `best[0]`が切片、以後が特徴係数。未知入力の積和へ代入 |
| U04 `infer_from_comparisons` | `sample`。事前、値の絶対上限、左右の比較列 | `mean`で値を比較。組合せが大きい確率を求めるなら全体標本上で判定する |
| U05 `estimate_quantized_sizes` | `sample`。整数上限、観測列、丸め幅、表示上下限 | 潜在サイズを返す。出力平均自体は丸め済み表示値ではない |
| U06 `fit_robust_feature_costs` | `sample`。誤差の種類、自由度、観測のscale | 外れ値モデルに基づく係数の平均・分散。自由度の例の既定値は4 |
| U07 `infer_catalog_values` | 専用型の `sample(param, hook)`。候補と正の事前重み、合計観測 | `probability[j][k]`が入力の候補順の推定確率。周辺で最頻の候補を個別選択しても、最良の同時状態とは限らない |
| U08 `estimate_smooth_field` | `sample`。頂点事前、辺、隣接差尺度、局所観測 | 頂点ごとの平均・分散。未観測点ほど事前や隣接仮定の影響が大きい |
| U09 `infer_conserved_inventory` | `set_state`、`set_directions`の後 `sample`。正確な総数と実行可能内訳を必須指定 | `best`なら整数・総数を満たす。方向確率は複数変数なら正にする |
| U10 `fit_tolerance_ranges` | `optimize`。希望値、値域、許容一次式列 | `best`と実際の各式の逸脱を確認。soft制約の違反が残ることは許容される |
| U11 `predict_success_probability` | 専用型の `sample(param, hook)`。特徴付きラベルと次の特徴 | `mean_probability`は `prediction_count>0` のとき有効。係数平均からの代用ではない |
| U12 `infer_relative_measurement` | 専用型の `set_state`→`sample`。支持内の初期値を必須指定 | 比例誤差モデルの正の真値を返す。初期値の支持検査は呼出し前提 |
| U13 `infer_real_split` | 変数消去したモデルへ `sample`。全体量、二つの事前、一次観測列 | 元の2変数の順で結果を復元。両分散は等しく、共分散はその負値 |

`collect_moments=false`なら、共通結果のcountは0でも、U07/U11のhookによる確率推定は得られる。これらの採取数は専用出力または `runtime.samples` を見る。`sample_capacity=0`でもhookは使える。

### 5.5 具体的な問題で、呼出しから結果の利用までをつなぐ

7章には、任意の入力を受け取る汎用関数と、3章の数値を渡す`case_u...`関数を対で載せる。まず具体例で出力の意味を確認し、実際の問題では汎用関数に自分のデータを渡すとよい。各関数は1回の推定用にモデルを構築する。ターン間で資源を再利用する場合は、その登録部分を外へ移して同じ推定器を保持する（2章・7.14）。

U03・U06が返す補助型`TimePrediction`には、`estimate`、`seconds`、`used_mean`がある。`seconds`は`estimate.feasible`のときだけ有効。初期値はNaNであり、出力可否の判定の代わりに用いない。

#### 5.5.1 U01 個々の重量を読む

具体例の入口は`case_u01_weights(param, seed)`、任意の荷物・計測へ使い回す入口は`estimate_group_weights(...)`で、内部では`sample`を呼ぶ。いずれも`Estimate<>`を返す。`count>0`なら`mean[0]`、`mean[1]`、`mean[2]`がA・B・Cの推定重量、単位はg。整数が必要なら`feasible`を確認して`best`を使う。平均を丸めた値が最も確からしい組になるとは限らない。

初回の短い予算では`count==0`もあり得る。この場合は平均を参照せず、`best`を暫定値にするか、追加の計算を行う。7.16の実行プログラムではこの分岐を実際に書いている。

#### 5.5.2 U02 推定結果を経路選択に使う

`case_u02_routes(param,seed)`は保存容量を256に設定して`choose_route`を呼ぶ。任意の経路を渡す`choose_route`では、呼出し側が正の`sample_capacity`を設定する必要がある。`sample`の後、候補ごとに`predict`で合計時間を評価し、平均が最小の候補を選ぶ。

返り値の`route==0`はU経由、`route==1`は直行、`-1`は推定候補なし。`predictions[k].mean`の単位は秒、`variance`は秒の二乗で、`count>0`のとき有効。保存標本0件なら`best`で比較し、`used_samples=false`となる。ここで選ぶのは期待費用の小さい経路であり、「最速である確率が最大」の経路とは定義が違う。

#### 5.5.3 U03 係数を求めて8個の時間を予測する

`case_u03_processing_time(OptimizeParam,seed)`は`fit_feature_costs`内で`optimize`を呼び、`TimePrediction`を返す。`estimate.feasible`なら`seconds`が8個の予測時間であり、このモデルの解では26秒になる。`estimate.best`には起動時間と1個当たり時間が残る。

この例は点推定なので`estimate.count==0`、`mean/variance`は空、`used_mean=false`である。「最適化できたから標本平均もある」と解釈しない。係数や予測の不確かさが必要なら、モデルを保持して`sample`を使う設計に変える。

#### 5.5.4 U04 推定性能と勝率を区別する

`case_u04_comparisons(param,seed)`は`infer_from_comparisons`を通じて`sample`し、`Estimate<>`を返す。`count>0`なら`mean[0..2]`をA・B・Cの性能として読む。3.4の回答ではA、B、Cの順を示す推定が自然だが、有限標本での並びは保証されない。

`mean[0]>mean[1]`は「Aが真にBより強い確率」の値ではない。その確率が欲しければ全体標本ごとに`values[0]>values[1]`を数える。将来のノイズ付き比較でAが勝つ確率は、さらに各標本の差を回答確率へ変換して平均する。

#### 5.5.5 U05 表示ではなく潜在長さを読む

`case_u05_quantized_sizes(param,seed)`は`estimate_quantized_sizes`で区間因子を登録して`sample`し、`Estimate<>`を返す。`count>0`なら`mean[0]`がA、`mean[1]`がBの推定長さ（mm）。Bの平均が表示上限10を超えても矛盾ではない。

`feasible`な`best`は0〜20の整数2個だが、`mean`は小数を取り得る。将来の表示を予測したい場合は、真値の平均を単に丸めるのではなく、必要な観測ノイズと丸め・制限の規則まで含めて予測する。

#### 5.5.6 U06 係数平均から通常の中心時間を読む

`case_u06_robust_time(param,seed)`は`fit_robust_feature_costs`で`sample`し、`TimePrediction`を返す。`estimate.feasible`を確認し、`seconds`を5個の予測中心時間として使う。`used_mean=true`なら係数平均、標準統計0件なら`best`による代替である。

この`seconds`は次回の観測時間を必ず当てる値ではない。係数の不確かさに加えて、次回もt誤差が加わる。将来観測の分散が必要ならその誤差も考慮し、tの尺度の二乗を無条件に観測分散とみなさない。例の回帰係数は実数全域なので、物理的な非負性が必要なら値域も調整する。

#### 5.5.7 U07 「どの候補か」の確率を読む

`case_u07_catalog(CatalogEstimator::SampleParam,seed)`は`infer_catalog_values`のsample hookで候補の出現数を数え、`CatalogResult`を返す。`probability`が空でなければ、`probability[0][1]`はA=12、`probability[1][1]`はB=10の推定確率である。入力表の順を維持するので、候補値12をそのまま添字にしない。

`estimate.best`は最良の同時状態、各確率列は周辺分布。この違いを保って使う。`collect_moments=false`でもhookが動けば確率表は得られる一方、採取0件なら表は空である。`sample_capacity`を正にする必要はない。

#### 5.5.8 U08 未観測区画の値と不確かさを読む

`case_u08_temperature(param,seed)`は`estimate_smooth_field`から`sample`し、`Estimate<>`を返す。`count>0`なら`mean[1]`がBの推定温度（℃）、`variance[1]`が潜在温度の分散（℃の二乗）。この分散は、Bに新しくセンサーを付けたときの計測ノイズを含まない。

隣接差尺度を小さくすると温度差をより強く抑えるが、それは単なる探索調整ではなく推定モデルの変更である。地域境界で温度が急変するなら、その辺の因子を弱める・外すなど、問題の情報に合わせる。

#### 5.5.9 U09 整数内訳として使える結果を選ぶ

`case_u09_inventory(param,seed)`は`infer_conserved_inventory`で保存則・初期状態・方向を登録してから`sample`し、`Estimate<>`を返す。`feasible`なら`best[0..2]`は赤・青・緑の整数個数で、合計12。個々の期待個数には`count>0`の`mean`を使う。

標本が保存則を満たすため、平均の合計も丸め誤差を除いて12になる。しかし各平均を個別に整数へ丸めると総数が変わり得る。必須の総数を提出する場面では、その丸めをしない。

#### 5.5.10 U10 最良候補と残った違反を読む

`case_u10_valve_settings(OptimizeParam,seed)`は`fit_tolerance_ranges`から`optimize`し、`Estimate<>`を返す。`feasible`なら`best[0]`、`best[1]`を調整値として読む。合計と差を計算し、許容範囲からの逸脱も確認する。

3.10の解析的な最小点は検算の目安であり、有限時間の探索結果が厳密に一致する保証はない。`feasible`はここでは値域を守り有限コストであることを意味し、すべてのsoft範囲内であるという意味ではない。`optimize`なので標本平均は返らない。

#### 5.5.11 U11 係数ではなく次回の成功確率を返す

`case_u11_success_probability(LogisticEstimator::SampleParam,seed)`は`predict_success_probability`で`sample`し、`LogisticResult`を返す。`prediction_count>0`なら`mean_probability`が投入量1.5に対する予測成功確率で、0〜1の値として使う。

`estimate.mean[0]`は切片であり確率ではない。予測平均はhook内で標本ごとに確率へ変換して集計するため、`collect_moments=false`、`sample_capacity=0`でも取得できる。`prediction_count==0`のときの初期値0を「成功確率0」と解釈しない。

#### 5.5.12 U12 正の真値の分布を読む

`case_u12_relative_length(RelativeEstimator::SampleParam,seed)`は`infer_relative_measurement`で実行可能な初期値を設定して`sample`し、`Estimate<RelativeEstimator>`を返す。`count>0`なら`mean[0]`が棒の推定長さ（cm）。`feasible`な`best[0]`は両支持内の値である。

支持区間の共通部分が空なら、この厳密な一様誤差モデルでは両観測を同時に説明できない。誤差仕様・観測・モデルを確認し、支持外の初期値のまま補助関数を呼ばない。例の初期値10は計測値から無条件に選んだものではなく、3.12で支持内であることを確認した値である。

#### 5.5.13 U13 自由変数の結果を元の水量へ戻す

`case_u13_tank_split(param,seed)`は`infer_real_split`内で1変数を`sample`し、復元済みの`Estimate<>`を返す。`feasible`なら`best[0]`がA、`best[1]`がBの水量（L）。`count>0`なら`mean`も同じ2槽順で、`variance`も2要素になる。

返り値の`runtime`は内部の1変数モデルでの実行情報である。出力が2要素でも2変数を独立に採取したわけではない。合計の分散は0なのに各槽の分散は正になり得るので、両varianceを足して合計の不確かさとしてはいけない。

## 6. 制約・注意点

### 6.1 表現できるモデルの境界

- 各因子は「既知の一次式一つの値」にしか依存できない。未知変数同士の積、未知の経路選択で式の参照先が変わるモデル、任意の全状態コールバックを直接登録するAPIはない。
- customは一次式の後段を非線形にできるが、別の係数や可変な隠れた状態を参照してよいという意味ではない。隠れた依存は差分評価の前提を壊す。
- 固定変数は指定値から動かない。Real/Integer/Valuesはモデル内で混在できるが、実数と整数が混在する**同じ利用者方向**は指定できない。
- Valuesの利用者方向は未対応。Automaticの単独更新と、ハード区間で結ばれた小さい候補ペアを使う。3変数以上の同時変更が必要な支持集合などで、移動可能性は保証しない。
- ハード区間は閉区間であり、厳密な実数の `<` を一般に直接表すものではない。整数差が正なら、値が整数と保証できる場合に限り下限1で表せる。

### 6.2 実行可能性と初期値

- 一般の制約充足solverではない。標準ハード区間の違反は修復を試すが、実行可能解の発見や不可能性判定を保証しない。
- customが正の無限大を返す支持集合には専用の初期化がない。できる限り利用側で支持内の初期値を与える。
- `set_state`は全値域内の値が必要。値域外の入力を自動射影するAPIではない。値域の変更による既存状態の射影は `set_domain` の動作である。
- 実数の自由変数に幅0の等式因子を付けて連続分布を採取しない。自由変数を消去するか、既知値ならFixedへ変える。細い区間への置換は別のモデルになる。
- 全変数がFixedなら `has_solution()` が真でもサンプル数は0のままになり得る。既知の状態が必要ならbestを使う。

### 6.3 確率モデルと推定品質

- 分布が正規化可能であることを確認する。無制限の変数に十分な観測・事前情報がない場合、有限の目的関数値が得られてもサンプリング対象として不適切なことがある。
- 平均・分散を使うなら、それらが有限であることも必要。裾の重い分布では、正規化できても平均や分散が存在しない場合がある。
- 同じ情報の二重登録、相関した観測を独立と仮定した重複計上、真の計測規則と違う丸め区間などは、ライブラリの計算が正常でも過信した推定を生む。
- 機械的にacceptedの割合や標本数が大きければよいわけではない。棄却時に同じ値を採取するのは通常の動作。受理した状態だけを集めると別の分布になる。
- 標本間には相関があり得る。ライブラリはESS、自己相関、複数系列の収束診断を返さない。厳しい判断に使うときはseedを変えた複数実行や採取時間を変えた安定性確認を利用側で行う。
- `best`、事後平均、候補ごとの最頻値、実際の競技スコアが最大となる行動は異なる。目的に合う出力を選ぶ。

### 6.4 数値・時間・メモリ

- 内部の数値計算はdouble。整数および厳密に扱う整数の積和は絶対値 $2^{50}$ 未満で、途中の演算も正確に表現できる範囲を前提にする。任意精度整数やlong doubleへ自動昇格しない。
- 一次式、二乗、損失の加算などが有限の範囲に収まるよう、特徴量と単位を整える。極端に小さいsigmaや非常に大きい係数は、数値条件を悪化させる。
- 時間制限は協調的停止。マイクロ秒は指定・計測の単位であり、超過ゼロの保証ではない。custom評価器、hook、再評価や大きい列挙の途中では任意に停止できない。
- `add_factor`や各更新API、呼出し側の結果コピーなどの時間は `optimize` / `sample` の時間予算とは別。競技全体の時計で残り時間を管理する。
- 一つのstepの仕事量は更新方式で違う。固定stepsは再現性確認に向くが、実戦性能は同じ時間制限で比較する。
- 全体サンプル保存は概ね `8 * n * sample_capacity` バイト。標準統計は概ね $O(n)$、因子・状態・逆引きは $O(n+R+K)$、小さい正規モデルの行列は自由変数数の二乗分を使う。

### 6.5 API利用時の失敗を避ける

- spanとconst参照は所有物ではない。変更・再実行を越えて保持する結果はvector等へコピーする。sample hookの `values` も後で使うならその場でコピーする。
- `moments().count==0`や `predict(...).count==0`では、そのmean/varianceを使わない。内部配列に以前の値が残っていても有効な統計ではない。
- 同じ推定器を実行中のhookから変更・再実行しない。参照や `evaluate` は利用できるが、終了前にモデルを変更してはいけない。
- customはNaN・負の無限大を返さない。有限の負コストは可能だが、モデルの正規化や比較の意味は利用側が担う。
- 入力契約の検査はassertが中心で、明示的な例外throwは行わない。`NDEBUG`でassertを消しても契約は変わらない。
- 本体はGCC系の単一提出ファイルへincludeする想定。ヘッダを単体でコンパイルすると `#if __INCLUDE_LEVEL__ == 0` 内のテストmainが有効になり、そのテストはLinuxのforkを使う。通常のinclude利用時にそのテストは入らない。

## 7. ユースケースごとのコード例

### 7.0 共通部とコピー方法

各ケースは、問題仕様の説明（3章）、入力への対応（4.10）、出力の読み方（5.5）と同じU番号である。前半の関数は任意の入力を受け取り、後半の`case_u...`関数が3章の数値を実際に渡す。数値入りの関数は試運転用であり、実際の入力に対応させるときは前半の関数へ引数を渡す。

ガイドからコピーする場合は、以下の共通部と必要なU番号の2個のコードブロックを同じファイルへ置く。共通部に続くコードだけで依存を満たす。推定器から得たspanを返すのではなくvectorへコピーするため、関数から戻った後も結果を使える。コードは単一翻訳単位のC++20プログラムを想定する。

同梱ファイルなら`guide_cases_v07.hpp`をincludeし、`linear_estimator_examples::`名前空間を使う。これにより汎用関数の`guide_examples_v07.hpp`も読み込まれる。更新・hookの補足例は`guide_api_examples_v07.hpp`にある。ライブラリ本体は`linear_estimator_v06.hpp`である。

```cpp
#include "linear_estimator_v06.hpp"

using LE = LinearEstimator<>;

template <class Estimator = LE>
struct Estimate {
    // feasible=falseならbestは空。count=0ならmean/varianceは空。
    bool feasible = false;
    std::vector<double> best, mean, variance;
    double best_energy = INFINITY;
    int64_t count = 0;
    typename Estimator::Runtime runtime;
};

template <class Estimator>
Estimate<Estimator> read_result(const Estimator& model, typename Estimator::Runtime runtime) {
    Estimate<Estimator> result;
    result.runtime = runtime;
    result.feasible = model.has_solution();
    if (result.feasible) {
        const auto best = model.best();
        result.best.assign(best.begin(), best.end());
        result.best_energy = model.best_energy();
    }
    const auto& moments = model.moments();
    result.count = moments.count;
    if (moments.count > 0) {
        result.mean = moments.mean;
        result.variance = moments.variance;
    }
    return result;
}

template <class Estimator>
void add_normal_priors(Estimator& model, std::span<const double> mean, double sigma) {
    assert(std::isfinite(sigma) && sigma > 0);
    for (int j = 0; j < static_cast<int>(mean.size()); ++j) {
        model.add_factor({{{j, 1}}, 0}, Estimator::Loss::gaussian(mean[j], sigma));
    }
}

struct SumObservation {
    std::vector<int> items; // 合計する変数ID。経路なら辺ID。重複はその回数だけ加算。
    double value;           // 観測された合計値。
    double sigma;           // この合計観測に加わる正規ノイズの標準偏差。
};

struct FeatureObservation {
    std::vector<double> features; // 既知の特徴量。切片用の1は含めない。
    double value;                 // 観測値。
    double scale;                 // 正規なら標準偏差。頑健推定では選んだ分布の尺度。
};

struct TimePrediction {
    Estimate<> estimate;
    double seconds = std::numeric_limits<double>::quiet_NaN(); // feasible=falseなら無効。
    bool used_mean = false;
};
```

### 7.1 U01 3個の荷物を、2個ずつ量った結果から推定する

3.1の具体的な問題を実装する。入力の位置は4.10.1、戻り値の利用条件は5.5.1に対応する。まず、同じ種類の問題を任意の入力で解く関数を示す。

```cpp
Estimate<> estimate_group_weights(const std::vector<double>& prior_mean,
                                  double prior_sigma,
                                  int64_t max_weight,
                                  const std::vector<SumObservation>& observations,
                                  LE::SampleParam param = {},
                                  uint64_t seed = 0) {
    // 入力: n個の重量は整数0..max_weight。prior_meanの長さがn。
    assert(!prior_mean.empty() && max_weight >= 0);
    LE model(std::vector<LE::Domain>(prior_mean.size(), LE::Domain::integer(0, max_weight)), seed);
    add_normal_priors(model, prior_mean, prior_sigma);
    for (const auto& row : observations) {
        model.add_factor(LE::LinearForm::sum(row.items), LE::Loss::gaussian(row.value, row.sigma));
    }
    // 出力: bestは実行可能な整数候補。meanは整数とは限らない。
    return read_result(model, model.sample(param));
}
```

`estimate_group_weights`は、この具体例では重量の値域を3個作り、事前因子3個、計測因子3個を登録する。`row.items`から合計式を作るループが3.1の表と1対1に対応する。最後の`read_result`は、モデルを破棄しても出力が残るよう候補・統計をコピーする。

次の具体例関数では3.1の数値をそのまま渡す。A・B・CのIDを変える場合は、事前配列・全観測のID・出力の解釈を同時に揃える。

```cpp
Estimate<> case_u01_weights(LE::SampleParam param = {}, uint64_t seed = 0) {
    // A=0, B=1, C=2。単位g。1個ずつは量れず、3組の合計だけを観測した。
    const std::vector<SumObservation> observations{
        {{0, 1}, 105, 3},
        {{1, 2}, 98, 3},
        {{0, 2}, 101, 3},
    };
    // 各重量は整数0..100g。独立な事前の目安は50g、尺度20g。
    return estimate_group_weights({50, 50, 50}, 20, 100, observations, param, seed);
}
```

### 7.2 U02 三角形の道路で、目的地への経路を選ぶ

3.2の具体的な問題を実装する。入力の位置は4.10.2、戻り値の利用条件は5.5.2に対応する。まず、同じ種類の問題を任意の入力で解く関数を示す。

```cpp
struct RouteDecision {
    Estimate<> estimate;
    int route = -1; // 候補経路の添字。推定候補がなければ-1。
    bool used_samples = false;
    std::vector<LE::Prediction> predictions; // count=0ならそのmean/varianceは無効。
};

RouteDecision choose_route(const std::vector<double>& prior_edge_cost,
                           double prior_sigma,
                           double max_edge_cost,
                           const std::vector<SumObservation>& observed_routes,
                           const std::vector<std::vector<int>>& candidate_routes,
                           LE::SampleParam param,
                           uint64_t seed = 0) {
    // 入力: 辺IDは0..n-1。経路探索は呼出し側が行い、比較したい経路を渡す。
    // predictを使うため、呼出し側でparam.sample_capacityを正にする。
    assert(!prior_edge_cost.empty() && !candidate_routes.empty() && max_edge_cost > 0);
    assert(param.sample_capacity > 0);
    LE model(std::vector<LE::Domain>(prior_edge_cost.size(), LE::Domain::real(0, max_edge_cost)), seed);
    add_normal_priors(model, prior_edge_cost, prior_sigma);
    for (const auto& row : observed_routes) {
        model.add_factor(LE::LinearForm::sum(row.items), LE::Loss::gaussian(row.value, row.sigma));
    }
    RouteDecision result;
    result.estimate = read_result(model, model.sample(param));
    if (!result.estimate.feasible) return result;
    double minimum = INFINITY;
    for (int k = 0; k < static_cast<int>(candidate_routes.size()); ++k) {
        const auto form = LE::LinearForm::sum(candidate_routes[k]);
        const auto prediction = model.predict(form);
        result.predictions.push_back(prediction);
        result.used_samples = prediction.count > 0;
        const double score = prediction.count > 0 ? prediction.mean : form.evaluate(model.best());
        if (score < minimum) {
            minimum = score;
            result.route = k;
        }
    }
    // 保存標本が0ならbestによる経路費用へ切替。分散を0とみなしてはいけない。
    return result;
}
```

`choose_route`の前半はU01と同じ合計観測だが、値域が実数になる。後半の`candidate_routes`ループが行動選択である。`form.evaluate(model.best())`は標本がないときの代替であり、分散を0と決め付ける処理ではない。

次の具体例では、道路順を固定した上で3.2の3観測と2候補を別々の配列にする。`route`の添字は辺IDではなく、この2候補の添字として読む。

```cpp
RouteDecision case_u02_routes(LE::SampleParam param = {}, uint64_t seed = 0) {
    // 道路e0,e1,e2が三角形を作る。S->Tは[e0,e1]経由か[e2]直行の二択。
    // 単位秒。経路観測のsigmaは、経路全体に加わる計測ノイズの標準偏差。
    const std::vector<SumObservation> observations{
        {{0, 1}, 9, 1},
        {{1, 2}, 11, 1},
        {{0, 2}, 12, 1},
    };
    const std::vector<std::vector<int>> routes{{0, 1}, {2}};
    param.sample_capacity = 256; // この具体例はpredict用に直近256標本を保持する。
    return choose_route({5, 4, 7}, 3, 20, observations, routes, param, seed);
    // 戻り値route=0なら経由、1なら直行。-1は推定候補なし。
}
```

### 7.3 U03 機械の処理時間から、起動時間と1個当たり時間を分ける

3.3の具体的な問題を実装する。入力の位置は4.10.3、戻り値の利用条件は5.5.3に対応する。まず、同じ種類の問題を任意の入力で解く関数を示す。

```cpp
Estimate<> fit_feature_costs(const std::vector<double>& coefficient_prior,
                             double prior_sigma,
                             const std::vector<FeatureObservation>& observations,
                             LE::OptimizeParam param = {},
                             uint64_t seed = 0) {
    // 入力/出力の係数順: [切片, 特徴0の係数, 特徴1の係数, ...]。
    assert(!coefficient_prior.empty());
    LE model(std::vector<LE::Domain>(coefficient_prior.size(), LE::Domain::real()), seed);
    add_normal_priors(model, coefficient_prior, prior_sigma);
    for (const auto& row : observations) {
        assert(row.features.size() + 1 == coefficient_prior.size());
        LE::LinearForm form{{{0, 1}}, 0};
        for (int k = 0; k < static_cast<int>(row.features.size()); ++k) {
            form.terms.push_back({k + 1, row.features[k]});
        }
        model.add_factor(std::move(form), LE::Loss::gaussian(row.value, row.scale));
    }
    // bestで未知タスクの費用を予測できる。optimizeなのでmean/varianceは返さない。
    return read_result(model, model.optimize(param));
}
```

`fit_feature_costs`の`features.size()+1`という検査は、特徴数に切片の1変数を足したものが係数数になることを確認する。式のループでは既知の個数を係数として置き、観測時間と計測尺度は正規因子へ渡す。

具体例関数は推定した2係数を使って8個の時間を計算する。`TimePrediction.seconds`は実行可能性を確認した場合だけ有効であり、未計算値を時間として利用しない。

```cpp
TimePrediction case_u03_processing_time(LE::OptimizeParam param = {}, uint64_t seed = 0) {
    // q個の品物を処理する時間 = 起動時間theta[0] + q * 1個当たり時間theta[1]。
    const std::vector<FeatureObservation> observations{
        {{1}, 5, 0.5},
        {{3}, 11, 0.5},
        {{5}, 17, 0.5},
    };
    TimePrediction result;
    result.estimate = fit_feature_costs({2, 3}, 5, observations, param, seed);
    if (result.estimate.feasible) {
        const auto& x = result.estimate.best;
        result.seconds = x[0] + 8 * x[1]; // 次の8個を処理する時間を予測。
    }
    return result;
}
```

### 7.4 U04 勝ち負けの回答だけから、3案の相対性能を推定する

3.4の具体的な問題を実装する。入力の位置は4.10.4、戻り値の利用条件は5.5.4に対応する。まず、同じ種類の問題を任意の入力で解く関数を示す。

```cpp
struct ComparisonObservation {
    std::vector<int> left, right;
    bool left_greater;
    double sigma; // 左右の「差」に加わるノイズの標準偏差。正の有限値。
};

Estimate<> infer_from_comparisons(const std::vector<double>& prior_mean,
                                  double prior_sigma,
                                  double bound,
                                  const std::vector<ComparisonObservation>& observations,
                                  LE::SampleParam param = {},
                                  uint64_t seed = 0) {
    // 入力: 二択回答で、同点という回答はないモデル。未知値は実数[-bound,bound]。
    assert(!prior_mean.empty() && bound > 0);
    LE model(std::vector<LE::Domain>(prior_mean.size(), LE::Domain::real(-bound, bound)), seed);
    add_normal_priors(model, prior_mean, prior_sigma);
    for (const auto& row : observations) {
        const auto loss = row.left_greater ? LE::Loss::gaussian_interval(0, INFINITY, row.sigma)
                                           : LE::Loss::gaussian_interval(-INFINITY, 0, row.sigma);
        model.add_factor(LE::LinearForm::difference(row.left, row.right), loss);
    }
    return read_result(model, model.sample(param));
}
```

`ComparisonObservation`の`left`と`right`は複数IDも許すため、案1個同士からチームの合計比較へ自然に拡張できる。`difference`が左を正係数、右を負係数にする。回答の向きは式を作り直すのではなく、`loss`の区間で表している。

具体例の各行の`true`は「左が大きい」という回答。推定結果が正確な勝ち幅を直接観測したものではない点を、3.4のモデルに戻って確認する。

```cpp
Estimate<> case_u04_comparisons(LE::SampleParam param = {}, uint64_t seed = 0) {
    // A=0, B=1, C=2の相対的な性能。負の値も許す共通基準からの偏差。
    const std::vector<ComparisonObservation> observations{
        {{0}, {1}, true, 0.5}, // A > Bという回答。
        {{1}, {2}, true, 0.5}, // B > Cという回答。
        {{0}, {2}, true, 0.5}, // A > Cという回答。
    };
    return infer_from_comparisons({0, 0, 0}, 3, 10, observations, param, seed);
    // count>0ならmean[0..2]をA,B,Cの推定性能として読む。
}
```

### 7.5 U05 表示上限に張り付いた長さを、丸め規則から読み解く

3.5の具体的な問題を実装する。入力の位置は4.10.5、戻り値の利用条件は5.5.5に対応する。まず、同じ種類の問題を任意の入力で解く関数を示す。

```cpp
Estimate<> estimate_quantized_sizes(const std::vector<double>& prior_mean,
                                    double prior_sigma,
                                    int64_t max_size,
                                    const std::vector<SumObservation>& observations,
                                    double step,
                                    double clip_min,
                                    double clip_max,
                                    LE::SampleParam param = {},
                                    uint64_t seed = 0) {
    // 観測生成: 合計+正規ノイズ -> step単位で四捨五入 -> [clip_min,clip_max]へ制限。
    // clip_min/clip_max/valueはstepの整数倍。潜在サイズは整数0..max_size。
    assert(!prior_mean.empty() && max_size >= 0 && step > 0 && clip_min < clip_max);
    const auto on_grid = [step](double value) {
        return std::abs(value / step - std::round(value / step)) < 1e-8;
    };
    assert(on_grid(clip_min) && on_grid(clip_max));
    LE model(std::vector<LE::Domain>(prior_mean.size(), LE::Domain::integer(0, max_size)), seed);
    add_normal_priors(model, prior_mean, prior_sigma);
    for (const auto& row : observations) {
        assert(row.value >= clip_min && row.value <= clip_max && on_grid(row.value));
        const double lo = row.value == clip_min ? -INFINITY : row.value - step / 2;
        const double hi = row.value == clip_max ? INFINITY : row.value + step / 2;
        model.add_factor(LE::LinearForm::sum(row.items), LE::Loss::gaussian_interval(lo, hi, row.sigma));
    }
    return read_result(model, model.sample(param));
}
```

`row.value==clip_max`のとき`hi=INFINITY`になる1行が、表示10を上限なしの情報として扱う要所である。表示下限では対称に`lo=-INFINITY`となる。通常の表示には`value±step/2`を使う。

具体例の`max_size=20`と`clip_max=10`は意図的に異なる。前者は未知部品の値域、後者は計測器の表示仕様として別々に読む。

```cpp
Estimate<> case_u05_quantized_sizes(LE::SampleParam param = {}, uint64_t seed = 0) {
    // 部品A=0,B=1の長さ。単位mm。表示器は2mm刻みに丸め、0..10mmに制限する。
    const std::vector<SumObservation> observations{
        {{0}, 6, 1},  // ノイズを加えた値は[5,7)だった。
        {{1}, 10, 1}, // ノイズを加えた値は9以上だった。
        {{0, 1}, 10, 1},
    };
    // 真の長さは整数0..20mm。表示上限10mmとは別の上限。
    return estimate_quantized_sizes({8, 12}, 5, 20, observations, 2, 0, 10, param, seed);
}
```

### 7.6 U06 一時的な大遅延が混ざった処理時間から通常の係数を求める

3.6の具体的な問題を実装する。入力の位置は4.10.6、戻り値の利用条件は5.5.6に対応する。まず、同じ種類の問題を任意の入力で解く関数を示す。

```cpp
enum class RobustNoise { Laplace, StudentT };

Estimate<> fit_robust_feature_costs(const std::vector<double>& coefficient_prior,
                                    double prior_sigma,
                                    const std::vector<FeatureObservation>& observations,
                                    RobustNoise noise,
                                    double df = 4,
                                    LE::SampleParam param = {},
                                    uint64_t seed = 0) {
    // 係数順は[切片, 特徴0, ...]。row.scaleは分布の尺度で、一般には標準偏差ではない。
    // dfはStudentTだけで使用し、正にする。小さいほど裾が重い。
    assert(!coefficient_prior.empty());
    LE model(std::vector<LE::Domain>(coefficient_prior.size(), LE::Domain::real()), seed);
    add_normal_priors(model, coefficient_prior, prior_sigma);
    for (const auto& row : observations) {
        assert(row.features.size() + 1 == coefficient_prior.size());
        LE::LinearForm form{{{0, 1}}, 0};
        for (int k = 0; k < static_cast<int>(row.features.size()); ++k) {
            form.terms.push_back({k + 1, row.features[k]});
        }
        const auto loss = noise == RobustNoise::Laplace ? LE::Loss::laplace(row.value, row.scale)
                                                        : LE::Loss::student_t(row.value, row.scale, df);
        model.add_factor(std::move(form), loss);
    }
    return read_result(model, model.sample(param));
}
```

汎用関数の`noise`分岐は、同じ一次式にどの誤差の負の対数密度を付けるかを決める。40秒の行を削除・補正する処理はない。具体例は6行すべてを登録し、標本が得られたかを見て平均と最良候補を使い分ける。

`features={3}`は3個の既知入力、`value=40`は観測、`scale=1`は誤差モデルの尺度。この3つを分けて読めば、U03のコードからのモデル上の違いが分かる。

```cpp
TimePrediction case_u06_robust_time(LE::SampleParam param = {}, uint64_t seed = 0) {
    // 起動時間+個数*単価で処理時間を表す。q=3の40秒は大きく外れた観測。
    const std::vector<FeatureObservation> observations{
        {{0}, 2, 1},
        {{1}, 4, 1},
        {{2}, 6, 1},
        {{4}, 10, 1},
        {{8}, 18, 1},
        {{3}, 40, 1},
    };
    TimePrediction result;
    result.estimate =
        fit_robust_feature_costs({2, 2}, 5, observations, RobustNoise::StudentT, 4, param, seed);
    if (result.estimate.feasible) {
        result.used_mean = result.estimate.count > 0;
        const auto& x = result.used_mean ? result.estimate.mean : result.estimate.best;
        result.seconds = x[0] + 5 * x[1]; // 次の5個に対する観測中心の予測。
    }
    return result;
}
```

### 7.7 U07 カタログ部品の組合せと、それぞれの候補確率を求める

3.7の具体的な問題を実装する。入力の位置は4.10.7、戻り値の利用条件は5.5.7に対応する。まず、同じ種類の問題を任意の入力で解く関数を示す。

```cpp
struct CatalogPrior {
    // 変数ごとの(value, 事前重み)。各変数でvalueは重複不可、重みは正の有限値。
    std::vector<std::vector<std::pair<double, double>>> entries;

    double operator()(int id, double z) const {
        for (const auto& [value, weight] : entries[id]) {
            if (z == value) return -std::log(weight);
        }
        return INFINITY;
    }
};

using CatalogEstimator = LinearEstimator<CatalogPrior>;

struct CatalogResult {
    Estimate<CatalogEstimator> estimate;
    // 入力entriesと同じ順。採取0件なら外側vectorも空。
    std::vector<std::vector<double>> probability;
};

CatalogResult infer_catalog_values(CatalogPrior prior,
                                   const std::vector<SumObservation>& observations,
                                   CatalogEstimator::SampleParam param = {},
                                   uint64_t seed = 0) {
    using E = CatalogEstimator;
    assert(!prior.entries.empty());
    std::vector<E::Domain> domains;
    for (const auto& entries : prior.entries) {
        std::vector<double> values;
        for (const auto& [value, weight] : entries) {
            assert(std::isfinite(weight) && weight > 0);
            values.push_back(value);
        }
        auto sorted = values;
        std::sort(sorted.begin(), sorted.end());
        assert(std::adjacent_find(sorted.begin(), sorted.end()) == sorted.end());
        domains.push_back(E::Domain::values(std::move(values)));
    }
    E model(std::move(domains), seed, prior);
    for (int j = 0; j < static_cast<int>(prior.entries.size()); ++j) {
        model.add_factor({{{j, 1}}, 0}, E::Loss::custom(j));
    }
    for (const auto& row : observations) {
        model.add_factor(E::LinearForm::sum(row.items), E::Loss::gaussian(row.value, row.sigma));
    }
    std::vector<std::vector<double>> counts;
    for (const auto& entries : prior.entries)
        counts.emplace_back(entries.size(), 0);
    int64_t count = 0;
    const auto runtime = model.sample(param, [&](const E::SampleView& sample) {
        ++count;
        for (size_t j = 0; j < prior.entries.size(); ++j) {
            for (size_t k = 0; k < prior.entries[j].size(); ++k) {
                if (sample.values[j] == prior.entries[j][k].first) ++counts[j][k];
            }
        }
    });
    CatalogResult result;
    result.estimate = read_result(model, runtime);
    if (count > 0) {
        for (auto& row : counts)
            for (double& value : row)
                value /= static_cast<double>(count);
        result.probability = std::move(counts);
    }
    return result;
}
```

汎用関数は各表から候補値だけを抜き出して値域を作り、候補の重みは`custom(j)`で別に加える。ここでのcustom IDは変数の表番号であり、登録因子IDとは役割が違う。

hook内で`sample.values[j]`と入力候補値を照合し、最後に総採取数で割る部分が周辺確率を作る。以下の具体例を呼ぶと、3.7のA・Bの表と22mm観測がそのままこの処理へ入る。

```cpp
CatalogResult case_u07_catalog(CatalogEstimator::SampleParam param = {}, uint64_t seed = 0) {
    // Aの候補は8,12,20mm、Bは5,10mm。各組のsecondは事前確率。
    CatalogPrior prior{{{{8, 0.2}, {12, 0.6}, {20, 0.2}}, {{5, 0.75}, {10, 0.25}}}};
    return infer_catalog_values(std::move(prior), {{{0, 1}, 22, 2}}, param, seed);
    // probability[0][1]はA=12の確率、probability[1][1]はB=10の確率。
}
```

### 7.8 U08 センサーのない中央区画の温度を、隣接関係から推定する

3.8の具体的な問題を実装する。入力の位置は4.10.8、戻り値の利用条件は5.5.8に対応する。まず、同じ種類の問題を任意の入力で解く関数を示す。

```cpp
struct VertexObservation {
    int vertex;
    double value, sigma;
};

Estimate<> estimate_smooth_field(const std::vector<double>& prior_mean,
                                 double prior_sigma,
                                 const std::vector<std::pair<int, int>>& edges,
                                 double difference_sigma,
                                 const std::vector<VertexObservation>& observations,
                                 LE::SampleParam param = {},
                                 uint64_t seed = 0) {
    // 入力: n頂点の既知グラフ。difference_sigmaは隣接値の差に対する事前標準偏差。
    // 各頂点の有限な事前分散が、未観測の連結成分の位置も定める。
    assert(!prior_mean.empty());
    LE model(std::vector<LE::Domain>(prior_mean.size(), LE::Domain::real()), seed);
    add_normal_priors(model, prior_mean, prior_sigma);
    for (const auto& [u, v] : edges) {
        model.add_factor({{{u, 1}, {v, -1}}, 0}, LE::Loss::gaussian(0, difference_sigma));
    }
    for (const auto& row : observations) {
        model.add_factor({{{row.vertex, 1}}, 0}, LE::Loss::gaussian(row.value, row.sigma));
    }
    return read_result(model, model.sample(param));
}
```

コードの3つの登録部分は、区画ごとの事前、辺ごとの差、センサー観測に対応する。Bに観測値を仮置きする必要はなく、具体例の観測配列はAとCの2行だけである。

`mean[1]`を読むとき、1は「センサー観測の2行目」ではなく頂点BのID。未観測でも状態配列・統計配列には全頂点分の要素がある。

```cpp
Estimate<> case_u08_temperature(LE::SampleParam param = {}, uint64_t seed = 0) {
    // A=0--B=1--C=2の3区画。温度の単位は摂氏。Bにはセンサーがない。
    const std::vector<std::pair<int, int>> edges{{0, 1}, {1, 2}};
    const std::vector<VertexObservation> observations{{0, 18, 1}, {2, 24, 1}};
    return estimate_smooth_field({20, 20, 20}, 5, edges, 2, observations, param, seed);
    // count>0ならmean[1]がBの温度、variance[1]が潜在温度の分散。
}
```

### 7.9 U09 合計12個の球の、色別の内訳を推定する

3.9の具体的な問題を実装する。入力の位置は4.10.9、戻り値の利用条件は5.5.9に対応する。まず、同じ種類の問題を任意の入力で解く関数を示す。

```cpp
Estimate<> infer_conserved_inventory(const std::vector<double>& prior_mean,
                                     double prior_sigma,
                                     int64_t max_per_item,
                                     int64_t total,
                                     const std::vector<double>& feasible_initial,
                                     const std::vector<SumObservation>& observations,
                                     LE::SampleParam param = {},
                                     uint64_t seed = 0) {
    // 入力: initial[j]は整数0..max_per_itemで、その合計がtotal。
    // 出力bestも同じ総数を保つ。meanを個別に丸めると総数を壊すことがある。
    assert(!prior_mean.empty() && feasible_initial.size() == prior_mean.size() && max_per_item > 0);
    const double total_value = static_cast<double>(total);
    assert(std::accumulate(feasible_initial.begin(), feasible_initial.end(), 0.0) == total_value);
    LE model(std::vector<LE::Domain>(prior_mean.size(), LE::Domain::integer(0, max_per_item)), seed);
    add_normal_priors(model, prior_mean, prior_sigma);
    std::vector<int> all(prior_mean.size());
    std::iota(all.begin(), all.end(), 0);
    model.add_factor(LE::LinearForm::sum(all), LE::Loss::hard_interval(total_value, total_value));
    model.set_state(feasible_initial);
    std::vector<std::vector<LE::Term>> directions;
    for (int j = 1; j < static_cast<int>(prior_mean.size()); ++j) {
        directions.push_back({{j - 1, 1}, {j, -1}});
    }
    model.set_directions(std::move(directions));
    param.auto_directions = false; // この例は明示した隣接移送方向を使う。
    assert(prior_mean.size() == 1 || param.direction_probability > 0);
    for (const auto& row : observations) {
        model.add_factor(LE::LinearForm::sum(row.items), LE::Loss::gaussian(row.value, row.sigma));
    }
    return read_result(model, model.sample(param));
}
```

`set_state`の直前に総数因子を登録し、利用者が用意した実行可能な内訳を渡す。次のループは隣接する色ID間の移送方向を作る。色が多くてもこの鎖を通じて個数を移せるので、全色ペアを入力する必要はない。

具体例で2回現れる12は、`max_per_item`と`total`という別の引数。前者は各変数の上限、後者は全変数の和であり、一般には異なってよい。

```cpp
Estimate<> case_u09_inventory(LE::SampleParam param = {}, uint64_t seed = 0) {
    // 赤=0,青=1,緑=2の球が合計12個。下記はそれぞれ独立に行った計測。
    const std::vector<SumObservation> observations{
        {{0}, 7, 1},
        {{1, 2}, 5, 1},
        {{1}, 3, 1},
    };
    return infer_conserved_inventory({4, 4, 4}, 3, 12, 12, {4, 4, 4}, observations, param, seed);
    // bestは必ず整数内訳。実行可能ならbest[0]+best[1]+best[2]==12。
}
```

### 7.10 U10 2個のバルブを、希望値と許容範囲の折合いで調整する

3.10の具体的な問題を実装する。入力の位置は4.10.10、戻り値の利用条件は5.5.10に対応する。まず、同じ種類の問題を任意の入力で解く関数を示す。

```cpp
struct RangeObservation {
    LE::LinearForm form;
    double lower, upper, scale; // 許容範囲と、範囲外のずれを評価する尺度。
};

Estimate<> fit_tolerance_ranges(const std::vector<double>& preferred,
                                double preference_sigma,
                                double lower_bound,
                                double upper_bound,
                                const std::vector<RangeObservation>& ranges,
                                LE::OptimizeParam param = {},
                                uint64_t seed = 0) {
    // 入力: 各係数の希望値preferredと、一次式ごとの許容範囲。
    // 範囲はsoft制約なので、互いに矛盾していても違反量を比較できる。
    assert(!preferred.empty());
    LE model(std::vector<LE::Domain>(preferred.size(), LE::Domain::real(lower_bound, upper_bound)), seed);
    add_normal_priors(model, preferred, preference_sigma);
    for (const auto& row : ranges) {
        model.add_factor(row.form, LE::Loss::soft_interval(row.lower, row.upper, row.scale));
    }
    return read_result(model, model.optimize(param));
}
```

`preferred`には正規形の二乗コスト、`ranges`には区間からの距離コストを付けている。どちらも同じ目的関数に加算されるので、一方の登録が他方を自動的に優先することはない。

具体例の波括弧は、まず`LinearForm{terms,offset}`、その外側が`RangeObservation{form,lower,upper,scale}`という入れ子。4.10.10の表に戻ると、それぞれの数値の役割を確認できる。

```cpp
Estimate<> case_u10_valve_settings(LE::OptimizeParam param = {}, uint64_t seed = 0) {
    // 2個のバルブの調整値x0,x1は0..10。希望値はともに3。
    const std::vector<RangeObservation> ranges{
        {{{{0, 1}, {1, 1}}, 0}, 8, 9, 1},  // 合計を8..9へ近づける。
        {{{{0, 1}, {1, -1}}, 0}, 1, 2, 1}, // 差を1..2へ近づける。
    };
    return fit_tolerance_ranges({3, 3}, 2, 0, 10, ranges, param, seed);
    // bestの合計や差が範囲外でも、soft制約としての失敗ではない。
}
```

### 7.11 U11 投入量ごとの成功・失敗から、次の成功確率を予測する

3.11の具体的な問題を実装する。入力の位置は4.10.11、戻り値の利用条件は5.5.11に対応する。まず、同じ種類の問題を任意の入力で解く関数を示す。

```cpp
struct BinaryObservation {
    std::vector<double> features; // 切片用の1を除く既知特徴。
    bool success;
};

struct LogisticLoss {
    std::vector<int> labels;

    double operator()(int id, double z) const {
        // -log P(label | logit=z)。大きい正負のzでもexpのオーバーフローを避ける。
        return std::max(z, 0.0) - labels[id] * z + std::log1p(std::exp(-std::abs(z)));
    }
};

using LogisticEstimator = LinearEstimator<LogisticLoss>;

struct LogisticResult {
    Estimate<LogisticEstimator> estimate;
    int64_t prediction_count = 0;
    double mean_probability = 0; // prediction_count=0なら無効。
};

LogisticResult predict_success_probability(const std::vector<double>& coefficient_prior,
                                           double prior_sigma,
                                           const std::vector<BinaryObservation>& observations,
                                           const std::vector<double>& query_features,
                                           LogisticEstimator::SampleParam param = {},
                                           uint64_t seed = 0) {
    using E = LogisticEstimator;
    assert(!coefficient_prior.empty() && query_features.size() + 1 == coefficient_prior.size());
    LogisticLoss loss;
    for (const auto& row : observations)
        loss.labels.push_back(row.success ? 1 : 0);
    E model(std::vector<E::Domain>(coefficient_prior.size(), E::Domain::real()), seed, std::move(loss));
    add_normal_priors(model, coefficient_prior, prior_sigma);
    for (int id = 0; id < static_cast<int>(observations.size()); ++id) {
        const auto& row = observations[id];
        assert(row.features.size() + 1 == coefficient_prior.size());
        E::LinearForm form{{{0, 1}}, 0};
        for (int k = 0; k < static_cast<int>(row.features.size()); ++k) {
            form.terms.push_back({k + 1, row.features[k]});
        }
        model.add_factor(std::move(form), E::Loss::custom(id));
    }
    LogisticResult result;
    const auto runtime = model.sample(param, [&](const E::SampleView& sample) {
        double z = sample.values[0];
        for (size_t k = 0; k < query_features.size(); ++k)
            z += query_features[k] * sample.values[k + 1];
        const double probability = z >= 0 ? 1 / (1 + std::exp(-z)) : std::exp(z) / (1 + std::exp(z));
        ++result.prediction_count;
        result.mean_probability +=
            (probability - result.mean_probability) / static_cast<double>(result.prediction_count);
    });
    result.estimate = read_result(model, runtime);
    return result;
}
```

`LogisticLoss::operator()`は、3.11の二値尤度の負の対数を、指数関数のオーバーフローを避ける式で計算する。観測IDと同じ順で`labels`を作っているので、`custom(id)`が正しいラベルを参照する。

sample hookでは観測式ではなく、次の投入量1.5の式を評価して確率へ変換する。具体例の`trial<successes[q]`は与えた成功数を30行へ展開するだけで、ここで成功・失敗の乱数を新たに発生させているわけではない。

```cpp
LogisticResult case_u11_success_probability(LogisticEstimator::SampleParam param = {}, uint64_t seed = 0) {
    // 投入量q=0,1,2の各条件で独立に10回試し、成功数が2,5,8だった。
    const std::array<int, 3> successes{2, 5, 8};
    std::vector<BinaryObservation> observations;
    for (int q = 0; q < 3; ++q) {
        for (int trial = 0; trial < 10; ++trial) {
            observations.push_back({{static_cast<double>(q)}, trial < successes[q]});
        }
    }
    return predict_success_probability({0, 0}, 3, observations, {1.5}, param, seed);
    // prediction_count>0ならmean_probabilityはq=1.5で次に成功する予測確率。
}
```

### 7.12 U12 真値の±10%に一様にぶれる長さ計測を扱う

3.12の具体的な問題を実装する。入力の位置は4.10.12、戻り値の利用条件は5.5.12に対応する。まず、同じ種類の問題を任意の入力で解く関数を示す。

```cpp
struct RelativeUniformLoss {
    std::vector<std::pair<double, double>> observations; // (観測値y, 相対誤差幅epsilon)。

    double operator()(int id, double z) const {
        const auto [y, epsilon] = observations[id];
        if (!(z > 0) || y < (1 - epsilon) * z || y > (1 + epsilon) * z) return INFINITY;
        return std::log(z) + std::log(2 * epsilon);
    }
};

using RelativeEstimator = LinearEstimator<RelativeUniformLoss>;

Estimate<RelativeEstimator> infer_relative_measurement(RelativeUniformLoss loss,
                                                       double lower,
                                                       double upper,
                                                       double feasible_initial,
                                                       double prior_mean,
                                                       double prior_sigma,
                                                       RelativeEstimator::SampleParam param = {},
                                                       uint64_t seed = 0) {
    // 生成モデル: y=z*(1+u)、uは[-epsilon,epsilon]の一様乱数。
    // 入力initialは全観測の支持区間を満たす正の実数。custom支持の自動修復はない。
    using E = RelativeEstimator;
    assert(lower > 0 && lower < upper);
    for (const auto& [y, epsilon] : loss.observations) {
        assert(y > 0 && epsilon > 0 && epsilon < 1);
    }
    const int count = static_cast<int>(loss.observations.size());
    E model({E::Domain::real(lower, upper)}, seed, std::move(loss));
    model.add_factor({{{0, 1}}, 0}, E::Loss::gaussian(prior_mean, prior_sigma));
    for (int id = 0; id < count; ++id)
        model.add_factor({{{0, 1}}, 0}, E::Loss::custom(id));
    const std::array<double, 1> initial{feasible_initial};
    assert(std::isfinite(model.evaluate(initial)));
    model.set_state(initial);
    return read_result(model, model.sample(param));
}
```

評価器の最初の条件が支持判定、最後の対数項が分布幅に依存する密度の評価である。`model.evaluate(initial)`のassertは、準備した初期値が事前も全custom因子も含めて有限コストであることを確認する。

具体例では同じ棒の2回の観測に別のcustom IDが付く。状態は1変数でも因子は2個となり、3.12の`2*log(z)`に対応する。

```cpp
Estimate<RelativeEstimator> case_u12_relative_length(RelativeEstimator::SampleParam param = {},
                                                     uint64_t seed = 0) {
    // 1本の棒を別々に計測し10.0cmと10.5cm。各回の誤差は真値の±10%の一様分布。
    RelativeUniformLoss loss{{{10.0, 0.1}, {10.5, 0.1}}};
    // 支持区間の共通部分は約[9.545,11.111]。初期値10cmはこの中にある。
    return infer_relative_measurement(std::move(loss), 1, 20, 10, 10, 3, param, seed);
}
```

### 7.13 U13 合計10Lの水を、2槽へ分けた内訳として推定する

3.13の具体的な問題を実装する。入力の位置は4.10.13、戻り値の利用条件は5.5.13に対応する。まず、同じ種類の問題を任意の入力で解く関数を示す。

```cpp
struct SplitObservation {
    double first_coefficient, second_coefficient, offset, value, sigma;
};

Estimate<> infer_real_split(double total,
                            const std::array<double, 2>& prior_mean,
                            double prior_sigma,
                            const std::vector<SplitObservation>& observations,
                            LE::SampleParam param = {},
                            uint64_t seed = 0) {
    // 入力: 実数x0,x1>=0かつx0+x1=total。x1をtotal-x0へ置換し、自由変数1個で解く。
    assert(total > 0);
    LE model({LE::Domain::real(0, total)}, seed);
    model.add_factor({{{0, 1}}, 0}, LE::Loss::gaussian(prior_mean[0], prior_sigma));
    model.add_factor({{{0, -1}}, total}, LE::Loss::gaussian(prior_mean[1], prior_sigma));
    for (const auto& row : observations) {
        LE::LinearForm form{{{0, row.first_coefficient - row.second_coefficient}},
                            row.offset + row.second_coefficient * total};
        model.add_factor(std::move(form), LE::Loss::gaussian(row.value, row.sigma));
    }
    auto result = read_result(model, model.sample(param));
    // 出力は元の2変数の順へ復元する。2変数の共分散は-variance[0]。
    if (result.feasible) result.best.push_back(total - result.best[0]);
    if (result.count > 0) {
        result.mean.push_back(total - result.mean[0]);
        result.variance.push_back(result.variance[0]);
    }
    return result;
}
```

汎用関数の2個目の事前因子は`10-x`に付けるため、`{0,-1}`と定数`total`になる。観測ループでは同じ置換を一般の係数に適用する。これが3.13の表をコードで書いた部分である。

採取後に`total-result.best[0]`などを追加して、元の2槽の配列へ戻す。平均は同じ線形変換、分散は係数−1の二乗により同じ値になる。具体例を呼ぶ側は内部表現を意識せず、A・Bの順で水量を使える。

```cpp
Estimate<> case_u13_tank_split(LE::SampleParam param = {}, uint64_t seed = 0) {
    // 2槽の水量A+B=10Lは正確。Aの計測は7L、A-Bの独立な計測は4L。
    const std::vector<SplitObservation> observations{
        {1, 0, 0, 7, 1},
        {1, -1, 0, 4, 1},
    };
    return infer_real_split(10, {5, 5}, 3, observations, param, seed);
    // 元のA,B順で返す。内部で採取する自由変数はAだけ。
}
```

### 7.14 ターンをまたぐ更新とhook

次は、同じインスタンスを保持しながら3ターン分の結果をコピーして返す例。実際の対話問題では、モデルをループの外で構築し、入出力の間に対応する更新・実行部分を置く。

```cpp
struct TurnResults {
    Estimate<> first, second, third;
};

TurnResults run_three_turns(LE::SampleParam param = {}) {
    LE model({LE::Domain::integer(0, 100), LE::Domain::integer(0, 100)}, 0);
    add_normal_priors(model, std::array<double, 2>{50, 50}, 20);
    const int total_id = model.add_factor({{{0, 1}, {1, 1}}, 0}, LE::Loss::gaussian(105, 3));
    TurnResults result;
    result.first = read_result(model, model.sample(param));

    // 次のターン: 合計観測を訂正し、単独観測を追加する。以前の候補を引き継ぐ。
    model.set_loss(total_id, LE::Loss::gaussian(107, 3));
    const int single_id = model.add_factor({{{0, 1}}, 0}, LE::Loss::gaussian(52, 2));
    // モデル変更後は採取統計がリセットされる。
    assert(model.moments().count == 0 && model.stored_sample_count() == 0);
    result.second = read_result(model, model.sample(param));

    // さらに次のターン: 単独観測を取り消し、係数0が54と判明。
    model.set_enabled(single_id, false);
    model.set_domain(0, LE::Domain::fixed(54));
    // total_idを再利用して、一次式そのものを差の観測へ置換する。
    model.replace_factor(total_id, {{{0, 1}, {1, -1}}, 0}, LE::Loss::gaussian(2, 1));
    result.third = read_result(model, model.sample(param));
    return result; // コピー済みなので、modelの寿命や以後の変更に依存しない。
}
```

sample hookとdebug hookの両方を渡す形も示す。hookの時間も実行時間に入るため、全標本の大量出力などは避ける。

```cpp
struct HookResult {
    LE::Runtime runtime;
    int64_t sample_calls = 0, debug_calls = 0;
    double mean_energy = 0; // sample_calls=0なら無効。
};

HookResult collect_with_hooks(LE::SampleParam param = {}) {
    LE model({LE::Domain::real()}, 0);
    model.add_factor({{{0, 1}}, 0}, LE::Loss::gaussian(10, 2));
    HookResult result;
    result.runtime = model.sample(
        param,
        [&](const LE::SampleView& sample) {
            ++result.sample_calls;
            result.mean_energy +=
                (sample.energy - result.mean_energy) / static_cast<double>(result.sample_calls);
            // sample.valuesをこの呼出し後にも使うならvector<double>へコピーする。
        },
        [&](const LE::Runtime& now) {
            ++result.debug_calls;
            (void)now;
            // 必要ならstderrなどへ記録する。ここからmodelを変更・再実行しない。
        });
    return result;
}
```

### 7.15 外部データを使うcustom因子の更新通知

同じcustom IDに依存する二つの因子を、どちらも更新通知する例。評価器が共有データを所有するため、参照の寿命も明確になる。

```cpp
struct SharedTargets {
    std::shared_ptr<std::vector<double>> values;

    double operator()(int id, double z) const {
        const double d = z - (*values)[id];
        return 0.5 * d * d;
    }
};

using SharedEstimator = LinearEstimator<SharedTargets>;

Estimate<SharedEstimator> retarget_custom_observations(SharedEstimator::SampleParam param = {}) {
    using E = SharedEstimator;
    auto targets = std::make_shared<std::vector<double>>(std::initializer_list<double>{1});
    E model({E::Domain::real()}, 0, SharedTargets{targets});
    model.add_factor({{{0, 1}}, 0}, E::Loss::gaussian(0, 10));
    // custom_id=0を使う異なる2因子。factor IDとcustom IDは別の番号。
    const int first_id = model.add_factor({{{0, 1}}, 0}, E::Loss::custom(0));
    const int second_id = model.add_factor({{{0, 2}}, 0}, E::Loss::custom(0));
    model.sample(param);

    (*targets)[0] = 3;
    // 変更の影響を受ける全因子へ通知する。通知途中で結果を利用しない。
    model.set_loss(first_id, E::Loss::custom(0));
    model.set_loss(second_id, E::Loss::custom(0));
    return read_result(model, model.sample(param));
}
```

### 7.16 呼出しとビルド

`main_example_v07.cpp`は、U01の具体例を2,000マイクロ秒の予算で呼ぶ。出力順は荷物A・B・Cで、先頭の文字列によって平均か最良候補かを区別する。時間予算によって標本数・数値は変わる。

```cpp
#include "guide_cases_v07.hpp"

int main() {
    using namespace linear_estimator_examples;
    LE::SampleParam param;
    param.time_limit_us = 2000; // この実行に使う時間予算。単位はマイクロ秒。
    param.max_steps = -1;       // 試行数では制限しない。
    const auto result = case_u01_weights(param, 0);
    if (!result.feasible) {
        std::cout << "no feasible candidate\n";
        return 0;
    }
    // 入力は3.1のA+B=105、B+C=98、A+C=101g。出力順はA,B,C。
    // 標本がなければ実行可能な候補を表示し、平均と区別する。
    const auto& values = result.count > 0 ? result.mean : result.best;
    std::cout << (result.count > 0 ? "posterior_mean" : "best_candidate");
    for (double value : values)
        std::cout << ' ' << value;
    std::cout << '\n';
}
```

```bash
g++ -std=c++20 -O2 -Wall -Wextra main_example_v07.cpp -o example
./example
g++ -std=c++20 -O2 -Wall -Wextra -Wshadow -Wconversion test_examples_v07.cpp -o test_examples
./test_examples
g++ -std=c++20 -O2 -Wall -Wextra -Wshadow -Wconversion test_cases_v07.cpp -o test_cases
./test_cases
python3 verify_guide_v07.py
```

別の具体例を実行する場合は、`case_u...`とParam型、結果の読み方を該当節のものへ変更する。たとえばU11なら`LogisticEstimator::SampleParam`で呼び、`prediction_count>0`を確認して`mean_probability`を読む。出力型が異なるので、U01の`mean`参照だけを無条件に使い回さない。

### 7.17 コード例の検証範囲

対象ヘッダのSHA-256は `f832843fe0df487e22ed4e3c35bf7522f9df11930dfc5a4749ec40fbab3b41fd`。GCC 13.3.0、C++20で次を確認した。

| 検証 | 結果 |
|---|---|
| 汎用関数13種類と更新・hook・custom通知例 | 149チェック合格 |
| 本文と同じ具体的な13問題・出力の利用条件 | 255チェック合格 |
| 上記2組のASan/UBSan実行 | 合計404チェック合格。実行環境の制約によりLeakSanitizerは無効 |
| `-Wall -Wextra -Wshadow -Wconversion -Werror` | 通常ビルドで警告なし |
| 掲載C++コード31ブロックと検証済みファイルの照合 | 全ブロック一致 |
| 共通部と各ケースを個別にコピーした形 | 13ケースすべてコンパイル成功 |
| `main_example_v07.cpp` | コンパイル・実行成功 |

詳細と再実行コマンドは `verification_v07.txt`。`verify_guide_v07.py`は掲載コードとの一致・個別コンパイル・章の対応・数式の書式を検証する読取用スクリプトであり、ガイドを更新しない。

検証は、小さい離散問題の全列挙、正規モデルの解析解、一次元の数値積分、総数保存、更新通知、hook回数などと照合している。本文の具体的な13問題も別のテストからすべて呼び出し、seed 0と101で出力・制約を確認する。採取0件の代替処理、確率出力と標準統計の独立性も確認する。

有限標本の比較には誤差許容を置き、再現性のある検証では時間制限を外して試行数を固定している。これはコード例とモデル対応の検証であり、あらゆる入力で2,000マイクロ秒以内に十分な推定精度へ達する保証ではない。解析解を記したケースでも、時間制限下の一般探索が厳密に一致するとは限らない。

## 8. 実装

### 8.1 全体の流れ

1. 値域と因子を所有し、一次式の重複項を整理する。変数から関係因子への逆引きも保持する。
2. モデルや設定に応じて計算経路を決める。小さい無制限の正規モデルでは行列分解、それ以外では座標・方向ごとの準備を行う。
3. 実行可能な状態がない場合、標準ハード区間の違反を減らす修復を試す。
4. `optimize`では評価値の小さい候補を保持しながら探索する。`sample`では初期化・warmup後に温度1の分布から標本を採る。
5. 状態の差分、最良候補、統計、保存列を必要に応じて更新する。予算到達時に整合した状態で戻る。

一般の採取経路はMCMCで、現在状態から候補を提案し、目的分布に対応する受理規則で状態を更新する。一つの状態から次へつながるため、標本が互いに独立とは限らない。

以下では、実装経路の条件と役割を順に説明する。

### 8.2 因子と状態のキャッシュ

現在候補と最良候補には、変数値の列に加え、因子ごとの一次式の値・コストを保持する。有限コストの合計と、無限大コストの因子数を分けて持つため、無限大同士の差を通常の合計更新に使わない。

一つの座標や疎な方向を変更すると、その方向が影響する因子だけを再評価する。複数変数の寄与が相殺される因子は、方向準備時に除外する。非正規の候補を評価した値は、同じ候補を受理したときに再利用する。bestの更新も、使える場合は変更した変数・因子だけをコピーする。

有限候補や実数の境界では、最終的な代入値に合わせて再評価し、単純な差分の丸めで候補集合や境界から外れないよう扱う。一般の実数同時更新では、方向を崩して各変数を勝手に境界へ押し戻さず、値域外なら棄却する。

### 8.3 小さい正規モデルの行列経路

次を満たす場合、AutomaticはCholesky分解を試す。

- 可動変数がすべて上下無制限のRealで、自由変数数が1〜256。
- 有効因子がすべてGaussian。
- 準備に使える時間があり、正の対角を持つ分解が得られる。

固定値を定数項へ移した後、自由変数列を $x$、各因子の係数ベクトルを $a_r$、固定値分を調整した目標を $\tilde y_r$、重みを $w_r=1/\sigma_r^2$ とする。

$$Q=\sum_r w_r a_r a_r^{\mathsf T},\qquad h=\sum_r w_r a_r\tilde y_r$$

$$Qm=h,\qquad Q=LL^{\mathsf T}$$

$Q$ は精度行列、$h$ は線形項、$m$ は最小点であり事後平均、$L$ はCholeskyの下三角行列である。`optimize`は三角行列の解法でmを求める。これは浮動小数点計算での解であり、記号的な厳密計算ではない。

採取では、独立な標準正規乱数ベクトル $\epsilon$ を使う。

$$x=m+L^{-\mathsf T}\epsilon$$

各標本を独立に生成できるため、この経路ではwarmupを行わない。`sample_stride=0`なら毎回採取し、正の指定は尊重する。modeにおける因子値を基準に、標本のenergyを正規乱数の二乗和から計算し、呼出し終了前に因子キャッシュを再評価する。

観測の目標だけの訂正ではQが変わらず、分解を再利用できる。特異なモデルや分解失敗では、この経路を使わない。サンプリング可能な分布かどうかを代わりに証明する動作ではない。

### 8.4 大きい正規モデルのCG

全変数数が256を超え、固定値以外は上下無制限のReal、有効因子がGaussianだけのとき、Automaticは小さい行列経路を使えなかった後で対角前処理付き共役勾配法を試す。

精度行列全体を作らず、各一次式を走査して行列とベクトルの積を求める。`optimize`では今回の試行予算と最大128反復の範囲で実行する。必要なら次の呼出しで現在候補から続けられる。

`sample`ではwarmupの一部として初期位置を改善するために使い、その後は通常の採取経路へ進む。CGを使ったからといって、以後の全標本が独立な多変量正規になるわけではない。時間予算があるとき、初期化CGには残り時間の一部だけを割り当てる。

### 8.5 座標更新と同時方向

各可動変数の単独更新に加え、利用者方向と自動方向を用意する。方向ごとに変数係数、影響する因子、正規部分の曲率などを集約する。

自動方向は全変数対を生成しない。正規因子やハード等式で隣接する候補対を調べ、相対的な結合が強いものなどに絞る。整数では整数として動かせる方向だけを使う。一般の制約集合の自由度や到達可能性を自動解析する機構ではない。

Automaticでは単独座標を順に走査し、指定確率で同時方向へ切り替える。ほかのMethodでは単独座標をランダムに選ぶ。採取の分割呼出しでも走査位置は保持する。

### 8.6 離散列挙と有限候補ペア

許容候補数が `enumeration_limit` 以下なら、方向上の候補を全評価する。採取ではコストに対応したBoltzmann重み、温度0では最小の候補を選ぶ。

Valuesの2変数がハード区間で結ばれている場合、Automaticは準備時に選んだペアの全組合せ更新も試す。候補数の積が上限以下のときに有効になる。ペアを現在値に応じて都合よく変更することはせず、固定した対応を使う。単独では移動できない支持集合への対策になるが、任意の多変数制約を解決するものではない。

### 8.7 正規部分の条件付き提案

方向上の正規コストを二次式で表し、条件付き平均・分散を計算する。Realなら許容区間内の切断正規、Integerなら整数上の正規重みから候補を生成する。小さい範囲は列挙も使う。

正規以外の因子が残るときは、提案に使った正規部分と残りのコストを分け、Metropolis–Hastingsの補正を行う。有限幅のGaussianIntervalには、区間中心と「ノイズ分散＋区間幅の二乗/12」から作った正規参照分布を提案に使う。その近似を最終的な尤度として置き換えるわけではなく、元の区間尤度へ補正する。

正規曲率も非正規コストもない有限の方向区間では、一様な条件付き分布を利用する。切断正規や整数正規の生成では、区間外の生成値を単に端点へ押し戻す方法を採らない。

### 8.8 非正規の連続方向と局所提案

Automaticの採取で非正規の連続方向を扱うときは、利用できる正規参照と曲率の強さに応じて条件付き提案またはsliceを選ぶ。sliceは現在値から左右へ区間を伸ばし、採択領域が見つかるまで区間を縮める。左右への拡張回数をランダムに配分する。

Student tやcustomなど非凸の可能性がある方向では、幅を広げた対称Metropolis提案を少数混ぜる。実装上の確率は5%、幅の倍率は8である。これでも離れたモードを十分に往来できる保証はない。

通常のMetropolis提案は、Realなら正規の移動量、Integerなら整数幅、Valuesなら候補集合からの選択を使う。

### 8.9 最適化・warmup・尺度調整

`optimize`の一般経路では、開始温度から終了温度まで変える探索に、温度0の更新を混ぜる。温度0のAutomaticでは、非正規方向に対し現在位置の両側の値から局所的な二次近似も試し、実際のコストが改善する候補を採る。

`sample`のAutomaticではwarmup前半に改善優先の初期化を使い、後半では必要な局所提案の尺度を受理状況から調整する。warmup後にはこの尺度調整を止め、温度1の規則で採取する。棄却した試行も間隔に数え、その時点の状態を採取する。

温度、提案幅、列挙上限、方向頻度は役割が異なる。特にサンプリングの分布を変えたい場合は、それに対応する評価モデルを定義する。単に最適化温度を上げた結果を、そのまま温度1の標本として使うことはしない。

### 8.10 ハード制約の修復

違反している標準ハード区間を選び、その因子の変数を変更して違反距離を減らす。関係するハード因子の違反距離の二乗を比較し、確率的に変更を受け入れる。Integerでは大きく往復しすぎないよう移動を調整する。

修復中も時間とstepsを消費するが、実行不可能な途中状態を標本として採らない。汎用custom支持集合の形をこの修復器が推測することはない。

### 8.11 統計・予測・停止

平均と中心二乗偏差は逐次更新する。全体サンプルは固定容量のリングバッファへ保存し、古い標本から上書きする。容量や標準統計の取得有無を変えても、それだけで乱数を消費する仕組みではない。

`predict`は保存標本ごとに一次式を計算して平均・分散を取るため、係数間の相関が反映される。非線形な行動評価はsample hookか保存標本への利用者処理で集計する。

時計はsteady clockを使用する。通常経路では数試行〜数十試行ごと、独立な正規生成では毎回確認する。棄却法の長いループにも確認箇所を設けるが、評価器などの単一処理を途中で打ち切る仕組みではない。戻る際には必要なキャッシュを整え、最後のdebug hookの後も経過時間を更新する。

### 8.12 区間尤度の数値評価と乱数

GaussianIntervalでは、中心付近はerf/erfcによる確率の差を計算する。負の遠い裾では対数累積確率とexpm1を使い、非常に狭い区間では中点密度の積分展開を使う。近い確率をそのまま引いたときの桁落ちを抑えるための処理であり、すべてdoubleで計算する。

乱数生成器は64bit状態を加算・混合する方式を内蔵する。一様実数は0と1を含まない値として作り、候補番号の生成では剰余の偏りを棄却で除く。正規乱数は一様乱数から二つずつ生成し、片方を次回用に保存する。外部の乱数生成器を差し込むAPIはない。
