# particle_filter 利用ガイド

対象は `particle_filter_v09.hpp`。C++20、GCC 12.2、単一スレッドでの利用を前提とする。本ガイドはこのソースの仕様を説明する。ライブラリが変更されても本ガイドは自動更新せず、更新の明示的な指示があったときに更新する。

通常は `ParticleFilter<Model>` を使う。新しい観測を `observe()` に渡し、`estimate()` で推定を読む。ライブラリに `solve()` というメソッドはない。第8章の `solve` は、各問題の入出力をまとめた利用者側の関数である。

第4章・第6章・第8章では、同じ問題に同じ番号U01〜U16を付ける。「具体的な問題 → 未知量と観測の対応 → 呼び出し方 → 完全なコード例」の順に読める。

初めて使う場合はU01、動く状態を追う場合はU12から読む。大きな粒子のコピーを避けたい場合はU16、推定した仮説を行動評価に使う場合はU02・U15が入口になる。

目次：

1. [何を解くライブラリか？](#chapter-1)
2. [ユーザー定義関数](#chapter-2)
3. [差分更新](#chapter-3)
4. [ユースケース](#chapter-4)
5. [利用準備](#chapter-5)
6. [ユースケースごとの使い方](#chapter-6)
7. [制約・注意点](#chapter-7)
8. [ユースケースごとのコード例](#chapter-8)
9. [実装](#chapter-9)

<a id="chapter-1"></a>

## 1. 何を解くライブラリか？

### 1.1 見えない値を、観測から推定する

例えば、ロボットに操作量3を与えたところ、移動距離が4.1と測定されたとする。操作量と真の移動距離の関係を「倍率a × 操作量 + 固定ずれb」と考えても、測定誤差があれば、この1回だけでaとbは決まらない。別の操作量を試し、観測を追加するにつれて、どのaとbが説明しやすいかを絞りたい。

本ライブラリが扱うのは、このような**未知のパラメータ、隠れた種類、時々刻々変わる状態に関する不確実性**である。実数だけでなく、整数、カテゴリ、配列、構造体などを仮説として使える。

ユーザーが基本的に指定するのは、仮説の初期分布と、「その仮説が正しいとしたら、今回の観測がどれほど起こりやすいか」である。後者を尤度と呼ぶ。ユーザー関数は「その粒子が正しい確率」を直接返す関数ではない。

| 項目 | ロボットの例 |
|---|---|
| 未知量 | 操作の倍率a、固定ずれb |
| 既知の入力条件 | 自分が与えた操作量x |
| 観測 | 測定された移動距離y |
| 観測モデル | 真の値はax+b、そこに測定誤差が加わる |
| 求めるもの | 観測を見た後のa、bの分布、その平均、不確実性を考慮した行動評価 |

このライブラリ自体は最短経路や最適な行動列を求めない。例えば経路のコストを推定する部分に使い、その推定を経路探索や行動選択へ渡す。

### 1.2 固定パラメータの場合の対象分布

観測中ずっと変わらない未知量を、まとめてベクトル$\theta$とする。各観測の既知条件を$u_s$、得られた値を$y_s$とする。目標は次の事後分布を近似することである。

$$\pi_t(\theta)=\frac{p_0(\theta)\prod_{s=1}^{t}L(y_s\mid\theta,u_s)}{Z_t}$$

$$Z_t=\int p_0(\theta)\prod_{s=1}^{t}L(y_s\mid\theta,u_s)d\theta$$

| 記号 | 意味 |
|---|---|
| $\theta$ | 未知の値をひとまとめにした仮説。例では$(a,b)$ |
| $t$ | 取り込んだ観測の件数 |
| $s$ | 何件目の観測かを表す添字 |
| $u_s$ | その観測を得たときの既知の条件。操作量、測定位置、経路など |
| $y_s$ | そのとき得られた観測値。数値、成否、記号など |
| $p_0(\theta)$ | 観測前に考える仮説の確率分布。事前分布 |
| $L(y_s\mid\theta,u_s)$ | 仮説と条件を固定したとき、その観測が得られる確率または確率密度 |
| $\pi_t(\theta)$ | t件の観測を取り込んだ後の仮説の確率分布。事後分布 |
| $Z_t$ | 全体の確率が1になるようにする正規化定数 |
| $\prod$ | 観測1件目からt件目までの尤度を掛けること |
| $\int\cdots d\theta$ | 実数の全仮説を足し合わせる操作。離散仮説なら通常の和に置き換える |

この積の形は、仮説と既知条件を与えると各観測が独立になる場合のもの。同じセンサー誤差を共有する複数の測定など、条件を与えても依存が残る場合は、依存を含む同時確率を1件の観測の尤度として表すか、依存を説明する隠れた変数を仮説に含める。

自分が過去の結果を見て操作量を選んだ場合も、既知の操作量を条件として扱う。その操作量を選んだ確率を、測定の尤度に重ねて掛ける必要はない。ただし、観測の選別に未観測の値が影響する問題では、その選別過程もモデル化する必要がある。

### 1.3 「目的関数」は何か

本ライブラリの中心的な目的は、上の分布を近似することであり、単一の関数値を最大化することではない。代表値が欲しいときには、用途に応じた目的関数が別に生じる。

実数パラメータの二乗誤差を小さくしたい場合、目的関数とその最適な代表値は次のようになる。

$$\hat\theta_t=\operatorname*{argmin}_{z}\mathbb{E}_{\theta\sim\pi_t}[\|z-\theta\|^2]=\mathbb{E}_{\theta\sim\pi_t}[\theta]$$

ここで$z$は回答候補、$\hat\theta_t$は選んだ回答、$\|z-\theta\|^2$は成分ごとの差の二乗の合計、$\mathbb{E}_{\theta\sim\pi_t}$は事後分布に従う仮説についての加重平均である。`estimate()` は、この意味での平均を取り出す入口になる。

行動を選ぶ場合は、例えば次の目的関数を使う。

$$a_t^*=\operatorname*{argmin}_{a\in\mathcal{A}_t}\mathbb{E}_{\theta\sim\pi_t}[C(a,\theta)]$$

ここで$a$は行動候補、$\mathcal{A}_t$は今選べる行動の集合、$C(a,\theta)$は仮説が$\theta$だったときの行動aの費用、$a_t^*$は期待費用を最小にする行動である。報酬を最大化する場合は費用の代わりに報酬を使って最大化する。U02とU15で、実際に各仮説の費用を平均して経路を選ぶ。

パラメータの平均を1個作ってから費用へ代入する方法と、各仮説で費用を計算して平均する方法は、一般には一致しない。締切超過や通路の開閉のような条件分岐を含む費用では、後者が重要になる。

### 1.4 時間とともに状態が変わる場合

移動中のロボットの位置など、未知量自体が変化する場合は、その時刻の状態を$x_t$とする。目標は過去の観測から現在の状態を推定することである。

$$\pi_t(x_t)=\frac{L(y_t\mid x_t,u_t)\int f(x_t\mid x_{t-1},u_t)\pi_{t-1}(x_{t-1})dx_{t-1}}{Z_t}$$

追加の記号$x_{t-1}$は1時刻前の状態、$f(x_t\mid x_{t-1},u_t)$は制御$u_t$を与えたときの実際の状態変化の確率分布、$Z_t$はこの式全体を正規化する定数である。離散状態のときは積分を和に置き換える。U12は位置と速度、U13は加熱後の温度を状態とする。

この式では「過去の必要な情報が現在の状態に含まれている」ことを前提とする。速度が将来位置を左右するなら、位置だけでなく速度も状態に持たせる。現在状態の推定であり、過去の全状態を後から推定し直す機能ではない。

<a id="chapter-2"></a>

## 2. ユーザー定義関数

### 2.1 まず四つを決める

1. **何が未知か。** その値一式を`Particle`にする。aとbを別々に推定すると相関を失うため、同じ粒子に持つ。
2. **何を観測したか。** 測定値だけでなく、そのときの操作量や測定位置も`Observation`にする。
3. **最初に何を知っているか。** 事前分布から1個の仮説を生成する`sample(rng)`を用意する。
4. **その仮説なら観測がどう生じるか。** 今回1件の確率を`likelihood(p,o)`、またはその対数を`log_likelihood(p,o)`で返す。

実数パラメータが独立な一様事前分布に従うなら、`UniformBox<D>`を継承すると3の生成処理などを任せられる。基本例U01では、利用者が書く中心部分は観測の式である。

### 2.2 「仮説が正しい確率」と向きが違う

例えば鉱石の種類がAなら赤い表示が出る確率が0.8、Bなら0.2と分かっているとする。赤い表示を観測したとき、Aの粒子のユーザー関数は0.8、Bの粒子のユーザー関数は0.2を返す。

この0.8は「赤を見たのでAである確率」ではない。観測前のAとBの割合も含めて、後者を求める役割はライブラリ側にある。事前にAが0.1、Bが0.9なら、赤を見た後のAの確率は次になる。

$$\frac{0.1\times0.8}{0.1\times0.8+0.9\times0.2}=\frac{4}{13}$$

ユーザー関数が粒子群全体を見て正規化する必要はない。事前確率を毎回掛けたり、前回までの尤度を毎回掛け直したりもしない。

### 2.3 確率と確率密度

成否や記号のように値が離散的なら、その値が得られる**確率**を返す。真の成功確率がpなら、成功の観測ではp、失敗の観測では1-pを返す。両方の和は1になる。

測定値が連続的なら、ちょうど1.234…という一点になる確率は通常0である。そこで**確率密度**を使う。密度は、値の近くの狭い区間へ入る確率を、区間幅で割ったものと考えればよい。密度は1を超えてよく、本ライブラリも許可する。

測定誤差を平均0、標準偏差$\sigma$の正規分布とすると、予測値$\mu(\theta,u)$に対する測定yの密度は次になる。

$$L(y\mid\theta,u)=\frac{1}{\sqrt{2\pi}\sigma}\exp\left(-\frac{(y-\mu(\theta,u))^2}{2\sigma^2}\right)$$

ここで$\mu$は仮説から計算する測定値の中心、$\sigma$は測定誤差の標準偏差、$\pi$は円周率、$\exp$は指数関数である。U01では$\mu=ax+b$、U03では測定位置と発信源の距離になる。

対数版は次の式をそのまま返せばよい。

$$\log L=-\frac{(y-\mu)^2}{2\sigma^2}-\log\sigma-\frac{1}{2}\log(2\pi)$$

標準偏差と分散は区別する。標準偏差が2なら分散は4であり、式の分母には$2\sigma^2=8$が入る。

### 2.4 誤差の小ささを適当なスコアへ変えない

誤差が小さい粒子を好むだけなら、いろいろな式を作れる。しかし、その式が今回の観測の確率を表していなければ、推定する分布が変わる。正規誤差なら二乗誤差、外れ値を含む混合分布ならその混合密度、丸めなら対応区間の確率を使う。

特に、$\sigma$自体を未知量にするU08では、上式の$-\log\sigma$を落としてはいけない。大きな$\sigma$は広い範囲を説明できる代わりに、個々の観測点への密度が薄くなる。この項を落とすと、その代償がなくなる。

全粒子共通の正の倍率を省いても正規化後の重みだけは変わらない。しかし`log_predictive`の意味が変わり、履歴の高速化関数との整合性も必要になる。本ガイドの例は、観測の確率・密度を正規化定数込みで定義する。異なる単位や異なる観測表現の`log_predictive`を、そのままモデルの優劣として比較しない。

### 2.5 観測の形式ごとの作り方

| 観測形式 | 返すもの | 具体例 |
|---|---|---|
| 実数値 | その測定値での密度 | 操作倍率U01、距離U03 |
| 真偽 | 実際に得られた真偽の確率 | 成功数U05、比較U10 |
| カテゴリ | 仮説の種類から、その記号が出る確率 | 鉱石U06 |
| 整数への丸め | 丸め前の値が対応区間に入る確率 | 寸法U04 |
| 閾値以上・以下 | 閾値を超える側の全確率 | 上限・下限付き計測U14 |
| 同時に得た複数値 | その同時確率・密度 | 位置センサーU12 |
| 条件付き独立な複数値 | 各確率の積、対数なら和 | 方位の2成分U09 |
| 欠測 | 情報を加えない1、対数なら0 | 追跡U12、温度U13 |

丸められた整数mは、丸め前の値がm-0.5からm+0.5までに入ったという情報である。mの一点での密度とは違う。上限100への張り付きは100以上だったという情報であり、100付近だったという情報とは違う。この差をU04とU14でコードにする。

ベクトル観測の誤差が相関している場合、独立と仮定して各成分の密度を掛けてはいけない。相関を含む同時密度を1回のユーザー関数で返す。

### 2.6 最小モデルと追加機能の契約

以下の`P`は`Model::Particle`、`O`は`Model::Observation`、`R`は`ParticleFilter`に渡す乱数器の型。例では`R=std::mt19937_64`である。

| 定義 | 必須か | 内容 |
|---|---|---|
| `using Particle = ...;` | 必須 | 未知量の組。コピー・代入できる値型 |
| `using Observation = ...;` | 必須 | 今回の条件と測定結果 |
| `P sample(R& rng)` | 必須 | 宣言した事前分布から1個生成 |
| `double likelihood(const P&, const O&) const` | 対数版とどちらか一方 | 今回の観測の確率または密度。有限かつ0以上 |
| `double log_likelihood(const P&, const O&) const` | 通常版とどちらか一方 | 同じものの自然対数。有限値または負の無限大 |
| `void transition(P&, const O&, R&)` | 動的状態で任意 | 制御と過程ノイズに従う真の状態変化 |
| `double propose_transition(P&, const P&, const O&, R&)` | 動的状態の発展機能 | 観測に合わせて次状態を提案し、遷移密度と提案密度の比を返す |
| `void propose(P&, R&)` | 固定パラメータで任意 | 対称な再探索候補を作る |
| `double propose(P&, R&)` | 非対称な再探索候補で任意 | 候補を作り、逆向きと順向きの提案確率の対数比を返す |
| `double prior(const P&) const` | 固定モデルに`propose`または`propose_in_place`があれば対数版と一方が必須 | 事前確率・密度 |
| `double log_prior(const P&) const` | 固定モデルに`propose`または`propose_in_place`があれば通常版と一方が必須 | 対数事前確率・密度 |
| `auto propose_in_place(P&, R&)` | 固定モデルのコピー削減に任意 | 粒子を直接変更し、`ParticleCloud<P>::MoveProposal<Token>`を返す |
| `void undo(P&, const Token&)` | `propose_in_place`を定義した場合に必須 | 棄却されたときに、粒子と利用者キャッシュを変更前へ戻す |
| `auto project(const P&) const` | 任意 | `estimate()`で平均したい数値・数値列へ変換。`estimate_into`では数値列にする |
| `double log_likelihood_history(const P&, std::span<const O>) const` | 固定モデルの高速化に任意 | 渡された履歴の対数尤度の合計。共通定数も含めて逐次計算と一致させる |

`likelihood`と`log_likelihood`を両方定義することはできない。`UniformBox`を継承すると、`Particle`、`sample`、`log_prior`、`propose`を引き継ぐ。そこへ別の`prior`を追加すると、事前分布の通常版・対数版を両方持つことになるので注意する。

`sample`だけを用意し、`propose`も`propose_in_place`も省略しても、固定パラメータの再探索は利用できる。その場合、ライブラリは`sample`から別の仮説を生成する。利用者が事前密度や提案比を追加で書く必要はない。ただし、観測後の狭い領域に事前から入りにくい問題では効率が落ちる。

`propose`と`propose_in_place`は、固定された未知量の別候補を作るための任意機能である。両方あれば`propose_in_place`を使い、`propose`は呼ばない。基本的な観測尤度の契約はどちらでも同じで、粒子と今回の観測を受け取って確率・密度を求める。

### 2.7 事前分布と粒子の座標を揃える

`sample`が実際に生成する分布と、`prior`または`log_prior`が表す分布は一致させる。事前分布の固定された正規化定数は、再探索の比で相殺するため省略できる。`UniformBox`の範囲内の`log_prior`が0なのは、このためである。

U08では粒子に$\log\sigma$を持ち、その座標に一様分布を置く。これは$\sigma$自身の一様分布ではない。座標変換をすれば事前分布の意味も変わる。一様にしたい対象を先に決める。

カテゴリ番号は平均しても意味を持たないことが多い。U06では各種類の確率を集計する。U09では角度をそのまま平均せず、cosとsinへ写す。U11では合計1の制約を満たす粒子を初期化と提案の両方で保つ。

### 2.8 追加関数の責任を混同しない

`propose`は固定された真のパラメータについて、別の説明候補を探す関数。`transition`は現実の状態が時間とともに変わる様子を表す関数。固定パラメータへ探索用のノイズを加える目的で`transition`を定義すると、「未知量は実際に変動する」という別のモデルになる。

非対称な`propose`が返す補正は次である。

$$\log q(\theta_{\rm old}\mid\theta_{\rm new})-\log q(\theta_{\rm new}\mid\theta_{\rm old})$$

ここで$q$は候補を作る確率・密度、oldは元の仮説、newは候補。往復が同じなら差は0なので`void`でよい。受理・棄却を利用者側で重ねて行わない。

一方、`propose_transition`が返す補正は次である。

$$\log f(x_{\rm new}\mid x_{\rm old},u)-\log q(x_{\rm new}\mid x_{\rm old},o)$$

ここで$f$は真の遷移分布、$q$は観測oを利用して作る提案分布。`o`には制御uと測定値が入る。**この戻り値に今回の観測尤度を含めない。** 尤度はライブラリが`likelihood`または`log_likelihood`を呼んで別途加える。この発展機能はU13だけで使い、通常の動的問題はU12の`transition`から始められる。

### 2.9 コードに出てくる確率の式を読む

**成功数の観測。** U05で、成功確率pの試行をn回行い、k回成功したとする。ある特定の成功・失敗の並びの確率はpをk回、1-pをn-k回掛けたものになる。成功数だけを観測しているなら、成功位置の選び方の数も掛ける。

$$L(k\mid p,n)=\binom{n}{k}p^k(1-p)^{n-k}$$

ここでnは試行数、kは成功数、$\binom{n}{k}$はn箇所から成功するk箇所を選ぶ組合せ数である。コードの`lgamma(n+1)`は、非負整数nに対して`log(n!)`を計算するために使っている。大きな階乗を直接作らず、組合せ数の対数を引き算で求める。U11の種類別個数も同じ考え方で、各種類への割り振り方の数と、その並びの確率を掛ける。

U05のBeta事前分布の平均はalpha/(alpha+beta)。alpha+betaを大きくすると、その平均付近を強く信じる分布になる。初期生成には「二つの独立なGamma乱数の比を取るとBeta分布になる」という既知の生成法を使っている。Gamma乱数は正の値を生成する標準ライブラリの分布であり、観測尤度を評価するための乱数ではない。

U11では三つの独立な指数乱数を作り、合計で割る。これは合計1の三角形上で一様な比率を生成する方法で、どれか一つの比率を特別扱いしない。以後の再探索も、合計1の領域を保つように行う。

**二者比較の観測。** U10では、特徴差と未知重みの内積をtemperatureで割った値をzとする。Aが選ばれる確率は次の形にする。

$$P(A\text{が選ばれる})=\frac{1}{1+\exp(-z)}$$

z=0なら両者は半々、大きな正ならAが選ばれやすく、大きな負ならBが選ばれやすい。Bが選ばれた観測には1からこの確率を引いた値を使う。コードでは成否に応じてzの符号を替え、同じ式の対数を安定に計算する。`log1p(v)`は小さなvに対しても精度を保ちやすい`log(1+v)`の関数である。

**丸めと打ち切り。** $\Phi(z)$を「平均0・標準偏差1の正規乱数がz以下になる確率」とする。U04の表示m、予測中心mu、標準偏差sigmaに対し、丸め表示の確率は次になる。

$$L(m\mid\mu,\sigma)=\Phi\left(\frac{m+0.5-\mu}{\sigma}\right)-\Phi\left(\frac{m-0.5-\mu}{\sigma}\right)$$

上端までの確率から下端までの確率を引けば、その間に入る確率になる。U14で閾値c以上と分かった場合は、$1-\Phi((c-\mu)/\sigma)$を使う。c以下なら$\Phi((c-\mu)/\sigma)$を使う。

標準C++の`erfc`は、この裾確率を計算するのに使える関数で、$\Phi(z)=\operatorname{erfc}(-z/\sqrt{2})/2$という関係がある。コードでは小さい裾の計算で1からほぼ1を引くことをなるべく避けている。未知の圧力が大きいほど「閾値以上」は起こりやすくなる、という向きも確認できる。

### 2.10 大きな粒子は「変更して、必要なときだけ戻す」

例えばU16で、1粒子が千地点の温度を持つとする。候補を作るたびに千個をコピーする代わりに、1地点だけ書き換え、変更前の値を小さな記録へ保存できる。

`propose_in_place`は粒子を直接変更し、`MoveProposal<Token>`を返す。`Token`は利用者が決める「元へ戻すための記録」の型である。候補を採用するかどうかはライブラリが判定する。採用しないときだけ`undo`が呼ばれる。採用時の完了通知はないので、候補側のキャッシュは提案関数を返すまでに整えておく。

| `MoveProposal<Token>`のメンバー | 型・既定値 | 入れるもの |
|---|---|---|
| `undo` | `Token`、提案時に用意 | 変更した添字、変更前の値、変更前のキャッシュなど |
| `log_correction` | `double`、`0` | 逆向きの提案密度の対数−順向きの提案密度の対数。第2.8節の`propose`と同じ補正 |

**この補正へ、事前分布や観測尤度の差を含めない。** ライブラリが別に計算する。候補が対称なら既定値0のままでよい。通常の`propose`と違い、戻り値を`void`にはできない。

U16では温度1個と、対数事前密度のキャッシュを一緒に更新し、棄却時には両方を戻す。Undoへ粒子全体を保存すれば実質的に全コピーへ戻る。何を保存すれば正確に元へ戻せるか分からない場合は、通常の`propose`の方が扱いやすい。

省けるのは候補作成のための粒子コピーである。過去の全観測の評価まで自動で差分化されるわけではなく、粒子のコピー可能性も引き続き必要になる。再サンプリングや推定器の分岐では粒子をコピーする。

<a id="chapter-3"></a>

## 3. 差分更新

### 3.1 同じ推定器へ観測を追加する

ターンごとに`ParticleFilter`を構築し直す必要はない。最初に1回構築し、観測を得たら同じインスタンスの`observe(o)`を呼ぶ。それまでの分布、固定モデルの履歴、確保済みの配列容量などを再利用する。

固定モデルでは、今回の条件を含めて`Observation`に保存する。例えば測距位置が変わっても、過去の観測には過去の測距位置を残す。全履歴の評価時に現在の測距位置を使ってはいけない。

### 3.2 後からできること・再構築が必要なこと

| 変更したいもの | 同じ推定器でできるか | 方法と意味 |
|---|---|---|
| 新しい測定・結果の追加 | できる | `observe(o)`。新しい情報を1回だけ反映 |
| 次の測定位置、経路、操作量 | できる | 新しい`Observation`にその条件を持たせる |
| 観測が欠けた動的ターン | できる | 欠測フラグ付き観測を渡す。尤度1または対数0。状態は進む |
| 推定後に比較する候補経路や費用関数 | できる | `for_each_weighted`や`estimate(project)`で再集計。観測更新は不要 |
| 同じ分布から別の行動評価用標本を取る | できる | `sample_indices`。外部RNGだけを進め、粒子・重み・履歴は維持 |
| 平均の出力先を使い回す | できる | `estimate_into`。射影と同じ要素数のdouble領域を先に用意 |
| 粒子数・再探索回数・頻度・ESS閾値 | できる | `set_param`。粒子数変更だけは即時再サンプリング |
| 固定モデルで、同じ観測まで使って追加計算 | できる | `refine(steps)`。観測を二重に追加しない |
| 仮想的な次観測で分岐する | できる | 推定器をコピーして、コピー側へ観測を追加 |
| 同じモデルで最初から推定し直す | できる | `reset()`。履歴と成功観測数を消し、事前分布から再生成 |
| 過去の観測の削除・訂正・順序変更 | 通常APIではできない | 正しい観測列でリセット後に再投入、または新規構築 |
| 事前範囲や既知ノイズの意味を変える | モデルの直接更新APIはない | 新しいModelで再構築。必要なら観測を再投入 |
| パラメータ次元、粒子の意味、未知の辺の集合を変える | 一般には再構築 | 既存の粒子・履歴の解釈が変わるため |
| 固定モデルと動的モデルを切り替える | できない | Modelの関数定義からコンパイル時に決まる。別の型・インスタンスを使う |
| 過去の動的状態を後から平滑化する | 専用機能はない | 現在状態の推定を扱う。過去の軌跡全体の再推定は別設計 |

粒子数を増やす操作は、まず既存の仮説を複製する操作である。増やした瞬間に未知の領域が新たに探索されるわけではない。固定モデルなら必要に応じて`refine`で追加探索する。

### 3.3 グラフや外部状態が変わる場合

U02では「辺そのもの」をライブラリへ渡さず、測定経路が各地域を何単位通ったかを観測に持たせる。次ターンに別の経路を使ったり、候補経路を入れ替えたりしても、同じ地域別コストを推定し続けられる。

U15では未知の通路の番号と開閉状態が固定である。既知の接続を変えて候補経路を再列挙するだけなら、同じ粒子から新候補を評価できる。ただし、未知の通路を追加してビットの意味を変えることや、真の通路状態が途中で変わることは、そのままでは表せない。後者は状態変化を持つ動的モデルとして作る。

Modelが共有ポインタなどで外部グラフを参照している場合、推定器をコピーしても参照先は共有される。参照先を書き換えると、過去の履歴の意味まで変わり得る。推定中に意味を変えない共有データは読み取り専用にし、観測時点の条件は値として記録する。

### 3.4 余った時間を追加計算に使う

固定モデルでは、既に観測した内容に対して`refine(steps)`を追加実行できる。重みと履歴は維持され、仮説の位置を探索する。同じ観測を再び`observe`へ渡すと、追加の独立観測として数えることになるので、追加計算の代用にしない。

組込みの時間制限引数はない。`observe`と1回の`refine`の途中に、期限切れで安全に中断するコールバックもない。AHCの時間管理は呼び出し側が行い、残り時間に応じて粒子数や探索回数を減らす。`refine(1)`の実行時間も、粒子数と履歴の評価費用に依存する。

### 3.5 コピー、失敗、リセットの意味

- 推定器のコピーはモデル・乱数器・現在分布・固定モデルの履歴を引き継ぐ。標準コンテナを使う限り、分岐側の更新は元へ影響しない。外部参照や自作の浅いコピーは別途注意が必要。
- コピー構築はModel・Param・RNG・現在粒子・重み・固定履歴・成功観測数を複製する。準備用の粒子群や一時配列は複製しない。次の更新で必要になれば、その分岐側で作業領域を確保する。
- コピー代入`branch = filter`では、代入先の作業領域を保持して再利用する。複数の仮観測を同じ基点から試す場合は、1個のbranchへ繰り返し代入すると使いやすい。ただし、Model・現在粒子・履歴そのもののコピー費用は残る。
- コピー構築にはModelとRNGのコピー構築、コピー代入には両者のコピー代入が必要。コピー不可のModelで推定器を使う場合は、この分岐方法を使えない。
- `observe`が`ok=false`なら、公に見える粒子・重み・固定履歴・成功観測数は維持される。ただし乱数器やModel内の可変状態、外部副作用は巻き戻さない。
- `reset()`は乱数器を先頭へ戻さず、その続きから初期粒子を生成する。同じ結果を再現したければ、同じModelとseedで新しく構築する。
- `reset()`はModelを作り直さない。独自キャッシュがあるなら、そのライフサイクルを自分で合わせる。第8章の履歴高速化例は、共有可変キャッシュを使わず、この問題を避ける。

### 3.6 長い履歴の評価を速くする

固定パラメータの追加観測は1件ずつ取り込めるが、新しい仮説を再探索するときには、その仮説が過去の観測も説明できるか確認する。この履歴評価が高価になることがある。

同じバイアスmuを共通の既知ノイズで測るなら、全測定値の和、二乗和、件数があれば全履歴の尤度を計算できる。任意の`log_likelihood_history`でこの式を提供できる。**毎回の`likelihood`は依然として今回1件の尤度**であり、両者の役割は違う。

この高速化で履歴自体が保存されなくなるわけではない。ライブラリは固定モデルの全`Observation`を保持する。関数へ渡された履歴だけを評価し、まだ確定していない次の観測を含めない。第8.17節で実装する。

### 3.7 再利用できる計算と、再利用できない計算

上位kの集計は、同じ重みと同じkの間では選択した粒子番号を再利用する。射影結果は保存しないので、別の費用関数や`refine`後の粒子の値は毎回読み直される。別のkへ切り替えてから戻す場合は、選択をやり直す。全粒子集計はこの選択キャッシュを使わない。

`sample_indices`は重みの累積配列を再利用する。`estimate_into`は呼び出し側が確保した出力領域を再利用する。いずれも利用者がキャッシュの破棄を指示する必要はない。重み更新や再サンプリングなど、必要な箇所でライブラリが無効化する。

一方、グラフ探索・最短路・シミュレーションの結果を自動保存する機能はない。粒子や観測に紐づく前計算を自分で持つ場合は、何を変えたときに無効になるかもモデル側で管理する。

<a id="chapter-4"></a>

## 4. ユースケース

### 4.0 一覧と読み進め方

| 番号 | 具体的な問題 | 未知量 | 観測の特徴 | コード |
|---|---|---|---|---|
| U01 | ロボットの操作倍率と固定ずれの校正 | 実数2個 | 入力付きの連続測定 | 8.1 |
| U02 | 地域別走行コストの推定と経路選択 | 入力で個数が決まる実数列 | 経路全体の測定値 | 8.2 |
| U03 | 測距から発信源の位置を探す | 実数座標2個 | 非線形、候補が分離し得る | 8.3 |
| U04 | 整数に丸められた測定から寸法を推定 | 実数の幅・高さ | 連続量を区間確率として観測 | 8.4 |
| U05 | 試行の成否から成功率を推定 | 0〜1の実数 | 成功数・試行数 | 8.5 |
| U06 | ノイズを含むスキャナーで鉱石を分類 | 離散的な種類 | カテゴリ観測 | 8.6 |
| U07 | センサーの反転故障と倍率を同時推定 | 真偽と実数 | 混合型の仮説 | 8.7 |
| U08 | 真の値と測定ノイズの幅を同時推定 | 真値、対数標準偏差 | ノイズ自体も未知 | 8.8 |
| U09 | 方位センサーから方向を推定 | 周期的な角度 | 円周上の平均 | 8.9 |
| U10 | 二者比較から評価の重みを推定 | 実数3個 | 順位しか観測できない | 8.10 |
| U11 | 抽出標本から混合材料の構成比を推定 | 合計1の実数列 | 制約付き仮説、種類別個数 | 8.11 |
| U12 | 制御と欠測を含むロボット追跡 | 位置・速度 | 時間変化する状態 | 8.12 |
| U13 | 高精度測定を使った温度追跡 | 温度 | 観測に合わせた動的提案 | 8.13 |
| U14 | 測定上限・下限に張り付くセンサー | 真の圧力 | 打ち切り観測 | 8.14 |
| U15 | 通路配置の推定と失敗費用を含む経路選択 | 開閉のビット列 | 構造的な仮説、相関を残した評価 | 8.15 |
| U16 | 領域内の最高温度から多地点の温度を推定 | 大きな実数列と評価キャッシュ | 非線形の集約観測、差分再探索 | 8.16 |

### 4.1 U01：操作倍率と固定ずれを校正する

ロボットへ与える移動命令と実際の移動距離が少しずれる。例えば、命令1でも距離1になるとは限らず、命令の倍率aと毎回の固定ずれbを知りたい。各回の真の距離をax+bとする。

未知量はaとb。利用者はそれぞれの初期範囲、各回の命令x、測定距離y、測定誤差の標準偏差を指定する。標準偏差は観測ごとに違っていてもよい。xが毎回同じではaとbを分けにくいため、複数の大きさや符号を試す。

得られた平均は、次の移動命令を補正する材料になる。上位k平均も比較できるが、二乗誤差を目的とするなら全粒子平均が基本である。

### 4.2 U02：地域別の走行時間を学び、締切を考えて経路を選ぶ

地図は分かっているが、舗装路、砂地、坂道など各地域の単位距離あたり時間が未知である。ある経路を走ったとき、地域別に何単位通ったかと、全体の所要時間だけが分かる。

粒子は地域ごとの単位時間の列。地域数は問題入力から決まる。観測の予測値は「各地域の距離 × その地域の単位時間」の合計に、既知の固定時間を加えたものになる。利用者は地域数分の事前範囲と、観測ごとの距離内訳を指定する。全ての経路が同じ内訳比だと地域差を識別できない。

利用例は、候補経路の期待時間と締切超過確率を計算し、「期待時間＋超過ペナルティ×超過確率」を最小にする。ペナルティは利用者が問題の費用に合わせて決める。例中の測定誤差は時間の読み取り誤差であり、将来の実走行時間にも独立な揺らぎがある場合は、その分布を行動評価へ追加する。

### 4.3 U03：複数地点からの測距で発信源を探す

平面上のどこかに発信源があり、任意の測定位置からそこまでの距離を、誤差付きで測れるとする。1地点だけで測ると、同じ距離を持つ位置が多数残る。2地点でも左右対称の位置が候補になり得る。

粒子は発信源のx、y座標。利用者は存在範囲と、各回の測定位置、距離、測定誤差を指定する。予測する距離は座標から計算したユークリッド距離であり、a×入力のような線形式ではない。

測定位置を変えることで候補を区別する。ただし、その位置選びはライブラリが自動で行うものではない。分離した候補の中央に平均が来る場合は、平均だけを確定位置として使わず、粒子ごとの探索費用や検出確率で次の行動を評価する。

### 4.4 U04：丸められた測定から幅と高さを推定する

箱の幅wと高さhが未知で、測定器は「a倍の幅＋b倍の高さ＋誤差」を整数に丸めた値を返す。例えば幅だけを測る場合はa=1、b=0、周囲に沿った測定なら両方の係数を使える。

未知量は実数のw、h。利用者はその範囲、測定係数、丸められた整数、丸め前の誤差の標準偏差を指定する。表示が7なら、丸め前の値が6.5から7.5へ入った確率を使う。

この例の「整数」はセンサー表示のことであり、寸法そのものの整数制約ではない。真の寸法が整数に限定される問題では、粒子の寸法を整数にし、その事前と提案も離散値に対応させる。

### 4.5 U05：成否から成功率を推定する

ある機械や作業の成功率pが未知で、何回試して何回成功したかが分かる。pが0.8という仮説なら、10回中8回成功する場合と、10回中1回しか成功しない場合の確率は違う。

粒子はpそのもの。入力は成功数・試行数と、事前分布の形を決めるalpha、beta。alpha=beta=1は0〜1の一様分布で、両方を大きくすると事前の情報が強くなる。観測の試行はpを条件として独立である必要がある。

結果としてpの平均と分散を返す。平均を成功報酬の期待値へ使い、分散を追加試行の必要性を考える材料にできる。この単純形は厳密更新が容易だが、成功率が状態や操作条件に依存するモデルへ発展させる基礎にもなる。

### 4.6 U06：表示が間違うスキャナーで鉱石を分類する

隠れた鉱石の種類をスキャンする。種類Aでも必ず赤が出るわけではなく、各種類に対して赤・青などの表示確率が決まっている。同じ鉱石へ複数回の独立なスキャンを行い、種類を絞る。

粒子は種類の整数番号。利用者は種類ごとの事前の割合と、「真の種類から表示への確率表」を指定する。表の各行は和が1になる。観測は表示された記号番号だけでよい。

出力は種類ごとの確率と、最も確率が高い種類の番号。種類番号の平均を出しても、存在しない中間の種類になるため使わない。種類が少ないときには全種類の厳密更新が自然である。

### 4.7 U07：反転故障と倍率を同時に推定する

センサーの出力が「入力×倍率」に従うはずだが、配線の故障で符号が反転しているかもしれない。倍率も不明なので、故障の有無だけ、倍率だけを別々に推定すると説明を取り違えやすい。

粒子に反転の真偽と正の倍率を同時に持たせる。利用者は倍率の範囲、故障している事前確率、各回の入力・出力・測定誤差を指定する。倍率を正に限定することで、「負の倍率」と「反転故障」の重複表現を避ける。

出力は反転している確率と倍率の平均。この例では故障状態は観測期間中ずっと固定である。途中で故障・復旧するなら、それは状態遷移のある問題になる。

### 4.8 U08：真の値とノイズの幅を同時に推定する

同じ温度や寸法を繰り返し測ったところ、測定値が散らばっている。中心がどこかだけでなく、センサーがどの程度不正確なのかも知りたい。

粒子に中心muとlog(sigma)を持たせる。利用者はmuの範囲と、正のsigmaの下限・上限を指定する。例の事前はmuとlog(sigma)に一様である。小数でもsigmaが0にならないため、対数密度を安定して評価しやすい。

結果はmuの平均とsigmaの平均。sigmaの平均が欲しいので、各粒子をsigmaへ戻してから平均する。log(sigma)の平均を指数関数へ入れると別の代表値になる。十分な反復測定がないと、中心とノイズ幅を分けて推定しにくい。

### 4.9 U09：方位を境界で壊さず平均する

方位センサーが方向のx成分、y成分を返し、各成分に正規誤差が加わる。真の方位角は未知である。179度と-179度はほぼ同じ方向だが、数値をそのまま平均すると0度になってしまう。

粒子は角度。利用者は方向成分の測定値と、その成分誤差の標準偏差を指定する。例の事前は円周上で一様で、候補角度は円周へ折り返す。

各粒子をcosとsinへ写して平均し、最後に角度へ戻す。平均方向ベクトルの長さも返し、方向が揃っているかを読む。長さが0付近なら、平均方向は安定した回答ではない。観測された角度そのものに誤差が加わるセンサーなら、この成分誤差モデルとは別の尤度が必要になる。

### 4.10 U10：二者比較から評価の重みを学ぶ

相手の評価点を直接見られず、「候補AとBのどちらを好んだか」だけ分かる。各候補には品質、速さ、資源量など3つの既知特徴があり、相手は未知の重みで特徴を評価すると考える。

粒子は3つの重み。観測にはAとBの特徴差、選ばれた側、比較の曖昧さを表す既知のtemperatureを入れる。重み付き特徴差が大きいほどAが選ばれやすくなる、滑らかな二値確率を使う。

得られた重みを次の候補づくりへ使える。temperatureも自由な倍率として同時に動かすと重みのスケールと区別しにくいため、この例では固定して既知とする。情報を持たない同じ特徴の比較ばかりでは重みは分からない。

### 4.11 U11：合計1の構成比を推定する

3種類の材料が混ざった大きな集団から標本を取り出し、種類ごとの個数が観測される。知りたいのは全体の構成比であり、各比率は非負で合計1でなければならない。

粒子は3つの比率。利用者は独立に採った標本の個数列を渡す。例は、合計1の三角形上で一様な事前分布を使う。各成分を独立に0〜1へ置く方法では、合計1を保証できない。

初期生成も再探索も制約を保ち、返す平均の比率も合計1になる。母集団が小さく、非復元抽出の影響が大きい場合は、この例の多項分布とは違う確率が必要になる。

### 4.12 U12：制御と欠測のある移動ロボットを追跡する

ロボットへ加速度の指示を出し、ときどき誤差付きの位置が届く。途中で位置測定が届かなくても、ロボットは移動している。

粒子は位置x、yと速度vx、vy。利用者は開始時点の平均・標準偏差、動作の揺らぎ、各ターンの経過時間と加速度、測定の有無を指定する。例の動作モデルでは位置と速度へ独立な正規ノイズを加える。

観測を渡すたびに先に移動し、その移動後の位置で測定の尤度を計算する。欠測も1ターンとして渡す。返すのは各ターンの現在状態の平均であり、後から過去の全軌跡を修正した結果ではない。

### 4.13 U13：高精度の温度測定を使って状態を追跡する

装置を加熱し、温度を測る。加熱量は既知でも、環境の揺らぎで真の温度変化には幅がある。一方、温度計は非常に高精度なので、観測と整合する次温度の範囲は狭い。

粒子は温度。開始分布、加熱量、過程ノイズの分散、測定の標準偏差を指定する。ここでは観測を利用して次温度を提案する追加関数を使い、提案方法の偏りを密度比で補正する。

この関数は一般モデルで必須ではない。真の遷移と観測を組み合わせた提案分布を自分で導ける場合の発展例である。例の正規分布同士の組合せについては第9.8節で式を説明する。

### 4.14 U14：測定上限・下限による情報を正しく使う

圧力センサーには測定範囲があり、強い圧力では「上限以上」、弱い圧力では「下限以下」としか分からない。表示が上限値に張り付いても、真の値がその近くとは限らない。

粒子は真の圧力。利用者はその事前範囲、通常値なのか閾値以下なのか閾値以上なのか、対応する値・閾値、測定誤差を渡す。通常値なら密度、打ち切りなら裾全体の確率を返す。

数値測定と打ち切り記号が混在した観測を、その出力形式に合わせて表している。上限を超えた観測しかなければ下側を絞れても、上側は事前範囲への依存が大きく残る。

### 4.15 U15：隠れた通路配置を推定して経路の失敗費用を評価する

いくつかの通路が開いているか分からず、経路を試すと通れたかどうかの表示だけが得られる。経路は、その経路に含まれる全通路が開いているときに通れる。表示自体にも少し誤りがある。

粒子は通路ごとの開閉をまとめたビット列。利用者は通路数、独立な事前の開通率、表示の反転確率、各経路が使う通路集合を指定する。コード例は20通路程度までを想定し、1粒子を32ビット整数で表す。

複数通路を含む経路の観測は、通路間の依存関係を生む。例えば2通路を使う経路が失敗しても、どちらが閉じていたのかは分からない。各通路の開通確率を別々に推定して掛け合わせると、この依存を失う。

結果として個々の開通確率を返しつつ、経路の期待費用は**配置全体を表す各粒子**で通行可能性を判定して計算する。探索で確かめる価値まで含む長期計画は自動では行わず、例は今回の期待費用で選ぶ。

### 4.16 U16：領域内の最高温度から、多地点の温度を推定する

工場の多数地点の温度を知りたいが、計測装置は指定した領域の最高温度しか返さないとする。地点0と地点1を含む領域で最高温度22度が観測されても、どちらが高かったかは分からない。重なり方の違う領域を測り、各地点の温度を絞る。

粒子には全地点の温度を持たせる。温度は観測期間中一定であり、各地点の事前分布は同じ既知の平均・標準偏差を持つ独立な正規分布とする。観測は地点番号の集合、領域内の最高温度の測定値、測定誤差の標準偏差である。この例では**真の最高温度を求めた後に測定誤差が加わる**。各地点を別々に誤差付きで測ってから最大表示を選ぶ装置なら、観測の分布が異なる。

地点数が大きい場合は、1回の再探索で1地点だけ動かし、棄却時に元へ戻す。全地点の事前評価を毎回計算し直さずに済むよう、対数事前密度も粒子へ保存し、変更した地点の寄与だけ更新する。ライブラリはそれを読み、全観測との整合性と候補の受理を判定する。

利用者は地点数、事前の平均・標準偏差、領域と測定値、候補の移動幅を指定する。出力は地点ごとの温度の平均。ただし、平均温度列で領域の最大を取っても「最高温度の期待値」には一般に一致しない。後者は各粒子で最大を取ってから平均する。最高温度に現れない地点は十分に絞れないこともあり、コピーの削減だけで高次元推定の難しさが解消するわけではない。

### 4.17 同じ問題での応用例

問題設定を重複させず、次の機能は対応する問題の応用として扱う。

| 元の問題 | 追加でやりたいこと | 指定するもの・判断 | コード |
|---|---|---|---|
| U01の固定ずれの推定 | 長い履歴の候補評価を速くする | 共通の既知sigma、測定値。合計・二乗和・件数から同じ尤度を計算 | 8.17 |
| U01の校正 | 余った時間で再探索し、仮の次観測を試す | 追加探索回数、分岐先の計算量、実測と区別した仮観測 | 8.18 |
| U15の経路選択 | 多数の経路の費用を少数の仮説で概算する | 復元抽出の標本数、行動評価専用RNG。小さい費用差や稀な失敗には誤差が出る | 8.19 |
| U02の地域コスト | ターンごとに経路候補を変えて期待時間を計算する | 観測後の候補経路、使い回す平均用領域。締切の非線形な費用は別に集計 | 8.20 |
| U06の鉱石分類 | 少数の全種類を、外部の事前重みとともに登録する | 全種類の事前重みと表示確率表。下位APIで再サンプリングせず直接更新 | 8.21 |

U15の標本評価は全粒子を使う評価の代わりに必ず選ぶものではない。同じ標本で全候補を比較し、必要なら有望な候補を全粒子で評価し直す。観測を増やす行動の長期的な価値や、最適な標本数はライブラリが決めるものではない。

<a id="chapter-5"></a>

## 5. 利用準備

### 5.1 ファイルとビルド

`particle_filter_v09.hpp`をソースの隣へ置き、`#include "particle_filter_v09.hpp"`とする。標準ライブラリ以外の依存はないが、ヘッダはGCC系の`<bits/stdc++.h>`を使用する。C++20でコンパイルし、`-O2`または`-O3`を使える。`-ffast-math`と`-Ofast`は使わない。

ヘッダをincludeすると通常のライブラリ部分だけが有効になる。ヘッダ自身を直接コンパイルすると、`#if __INCLUDE_LEVEL__ == 0`内の自己テストが有効になる。AHCの1ファイル提出では、通常部分を提出ソースへ貼り付け、ヘッダの自己テスト部分は含めない。

### 5.2 `ParticleFilter`のコンストラクタ

型は`ParticleFilter<Model, Rng>`。第2テンプレート引数の既定値は`std::mt19937_64`。コンストラクタは`ParticleFilter(Model model, Param param = {}, Rng rng = Rng(0))`である。

| 引数 | 型 | 既定値 | 指定する内容 |
|---|---|---|---|
| `model` | 利用者定義の`Model` | なし | 粒子・観測の型、事前生成、尤度、必要な追加関数。値として保持される |
| `param` | `ParticleFilter<Model,Rng>::Param` | 下表 | 粒子数と探索の計算量・閾値 |
| `rng` | `Rng` | `Rng(0)` | 乱数器。標準型ならseed 0で初期化した器 |

構築時点で`sample`を粒子数分呼んで初期化する。未初期化状態で待つコンストラクタはない。同じ型を再現可能に使いたければ、Modelの内容、Param、seedを同じにして新しく構築する。コンパイラや標準ライブラリが異なる環境まで同一の乱数列を保証するものではない。

第8.1節のModelを使う準備の例は次のとおり。

```cpp
using PF = ParticleFilter<case01::Model>;
PF::Param parameters;
parameters.count = 2048;
parameters.move_steps = 2;
parameters.move_interval = 8;
PF filter(case01::Model({-3, -3}, {3, 3}), parameters, std::mt19937_64(123));
```

第8章で`count`と`seed`引数を持つ例では、省略時は1024粒子・seed 0になる。例のModelに明記した乱数引数は`std::mt19937_64&`なので、乱数器の型を変える場合はその関数の引数型も合わせる。

### 5.3 `Param`の全項目

| 名前 | 型 | 既定値 | 有効範囲 | 役割・値の選び方 |
|---|---|---|---|---|
| `count` | `int` | `1024` | 1以上 | 粒子数。まず256〜2048程度で時間と精度を比較する。高次元・多峰性では増やす価値があるが、計算量はほぼ粒子数に比例 |
| `ess_ratio` | `double` | `0.5` | 0〜1 | 次の観測に入る前に、現在のESSが`count * ess_ratio`未満なら再サンプリング。0でこの判定だけ無効。大きいほど早く再サンプリング |
| `move_steps` | `int` | `2` | 0以上 | 固定モデルの再探索回数。0は自動再探索とテンパリングを無効にする。履歴評価が重いときは減らす。動的モデルでは使わない |
| `move_interval` | `int` | `8` | 0以上 | 固定モデルで何件成功観測を処理したら定期再探索するか。8なら8件後、次の9件目に入る前が最初。0で定期分だけ無効 |
| `tempering_ess_ratio` | `double` | `0.5` | 0〜1 | 固定モデルで今回の尤度を段階的に取り込む際のESS目標。0で無効。`move_steps=0`でも無効。動的モデルでは使わない |

ここでESSは重みの集中度を表す指標であり、粒子の種類数ではない。詳細は第9章で説明する。

値の候補は導入時の出発点であり、全問題に最適な設定ではない。まず既定値で動作させ、`observe`の時間、ESS、再探索の受理回数、実際の推定誤差や行動費用を測る。尤度が非常に重いなら少ない粒子から、高精度測定で重みが急に偏る固定問題なら段階更新を残すところから検討する。

重要な組合せは次のとおり。

- `ess_ratio=0`だけでは、定期再探索やテンパリングが行う再サンプリングは止まらない。
- `move_interval=0`でも、ESSによる準備やテンパリングに伴う再探索は残る。
- 固定モデルで観測処理中の自動再サンプリング・再探索を全て止めるには、`ess_ratio=0`かつ`move_steps=0`とする。明示的な`refine`や粒子数変更は別である。
- 動的モデルでは`ess_ratio`と`count`が主要な設定で、上の再探索用3項目を変えても動的再探索は有効にならない。
- テンパリングのESS目標は絶対保証ではない。段階数に上限があり、最後に残りを全て取り込むため、更新後に目標未満になることがある。

### 5.4 `UniformBox`の準備

| 指定 | 型・既定値 | 意味 |
|---|---|---|
| テンプレート引数`D` | `std::size_t`、既定は`std::dynamic_extent` | 固定次元Dなら`std::array<double,D>`。省略なら`std::vector<double>` |
| `lower` | `Particle`、必須 | 各成分の有限な下限 |
| `upper` | `Particle`、必須 | 同じ長さの有限な上限。各成分で`lower < upper` |
| `move_scale` | `double`、既定`0.2` | 正の有限値。通常の局所提案の幅を事前区間幅に対する割合で指定 |

`UniformBox<2>({-3,-3},{3,3})`は実数2個の独立な一様事前分布を表す。`UniformBox<>(lower_vector,upper_vector)`なら入力で次元を決められる。0次元や空の範囲は使えない。

`move_scale`は内部で`sqrt(min(4,D))`により調整される。局所的な1回の座標変化の標準偏差は概ね「区間幅×move_scale÷sqrt(min(4,D))」。加えて一定割合で区間中心に対する全座標の反転提案があるため、この値は全ての提案の最大移動幅を意味しない。

区間内の事後分布がとても狭いとき、既定の提案幅では受理が少なくなることがある。必要ならModelのコンストラクタで小さい値を渡すか、問題に合った`propose`を作る。`ParticleFilter::set_param`で`UniformBox`の範囲や`move_scale`を変更することはできない。

### 5.5 問題別に用意する値

| 問題 | 必ず用意するもの | 事前・状態の準備 |
|---|---|---|
| U01、U03、U04 | 各測定の既知条件と標準偏差 | 実数2個の範囲 |
| U02 | 地域ごとの距離列、固定時間、測定時間、標準偏差 | 地域数分の範囲。全距離列の次元を統一 |
| U05 | 成功数と試行数 | 正のalpha、beta。省略時1、1 |
| U06 | 種類の事前重み、表示確率表 | 重みは総和が正、確率表の各行は和1 |
| U07 | 入力、出力、標準偏差 | 正の倍率範囲、0より大きく1より小さい故障事前確率 |
| U08 | 反復測定値 | muの範囲、正のsigmaの範囲 |
| U09 | 方向の2成分測定と標準偏差 | 円周上の一様事前を例が実装 |
| U10 | 特徴差、比較結果、既知temperature | 重み3個の範囲 |
| U11 | 3種類の個数 | 合計1の一様事前を例が実装 |
| U12 | 時間、加速度、測定の有無・値 | 初期状態の平均と標準偏差、位置・速度の過程ノイズ |
| U13 | 加熱量、測定の有無・値と標準偏差 | 初期温度分布、正の過程分散Q |
| U14 | 通常・閾値以下・閾値以上の区別、値、標準偏差 | 真の値の範囲 |
| U15 | 経路ビット集合、表示成否 | 通路数、開通率の事前、表示反転率 |
| U16 | 非空の領域内地点番号、最高温度の測定値とsigma | 地点数、正規事前の平均と正の標準偏差。差分提案とundo |

### 5.6 `ParticleCloud`を直接使う場合

`ParticleCloud<Particle>`は、モデルを保存せず粒子と重みを扱う下位APIである。通常は`ParticleFilter`で十分であり、独自の更新手順や外部で作った粒子を直接扱う場合だけ選ぶ。

コンストラクタは`ParticleCloud(double ess_ratio = 0.5)`。この直後は粒子数0である。`initialize(count, init, rng)`または`assign(particles, log_weights)`で初期化してから更新する。主な引数の意味は次のとおり。

| 準備API | 入力・既定値 | 結果 |
|---|---|---|
| `initialize(count, init, rng)` | 正のcount、`init(rng)`が1粒子を返す関数、乱数器 | 全粒子を等重みで生成。戻り値なし |
| `assign(particles, log_weights={})` | 非空の`std::vector<Particle>`、省略または同数の`std::span<const double>` | 対数重みを正規化。省略時は等重み。全て無効ならfalseで元を維持 |

通常版と異なり、観測履歴、モデルの確率計算、更新と再探索の組合せを自動管理しない。第6.5節に残りのAPIをまとめる。

### 5.7 任意機能と応用例で設定するもの

以下は公開Paramを増やす設定ではなく、メソッドの引数や、第8章の利用者側コードで指定する値である。

| 機能・例 | 型・既定値 | 設定と選び方 |
|---|---|---|
| 上位kの集計 | `int top_k=0` | 0は全粒子、1〜現在粒子数は上位k。まず全粒子で評価し、計算量を減らす際にkと選択質量を比較 |
| `sample_indices`の出力先 | `std::span<int>`、必須 | 事前にM要素確保。vectorやarrayから渡せる。Mが標本数で、粒子数以下という制約はない |
| `sample_indices`の乱数器 | `SamplingRng&`、必須 | 標準の一様乱数器の要件を満たす型。推定用RNGとは別に呼び出し側で保持 |
| `estimate_into`の出力先 | 書き込み可能な連続double列、必須 | `vector<double>`、`array<double,D>`、C配列、`span<double>`。要素数を射影結果の長さと一致させる |
| U16の`step_scale` | `double`、例の既定`0.4` | 1回の移動の標準偏差を事前の標準偏差に対する比で指定。観測後の分布が狭い場合は小さくして比較 |
| U16の`extra_moves` | `int`、例の既定`4` | 全観測後の`refine`回数。0以上。多数地点を1回ずつ変える回数ではなく、各粒子でこの回数だけ1地点を選ぶ |
| `sampled_routes::solve`の`samples` | `int`、例の既定`64` | 正の標本数。64は例の出発点であり、精度保証ではない。候補数・費用の分散・誤選択時の損失に合わせる |
| 同関数の`planning_seed` | `std::uint64_t`、例の既定`100003` | 行動評価専用の乱数器のseed。推定側の`seed=0`とは独立に設定 |
| `streaming_routes::solve`の`top_k` | `int`、例の既定`0` | 全分布の期待時間が目的なら0。上位kは選択集合の中での条件付き集計になる |

出力先を指定する方法はメモリ確保を避けるための選択肢であり、常に高速とは限らない。単一の数値を読む場合や、固定長の小さなarrayを返すだけの場合は`estimate`で十分なことが多い。射影関数自身が毎回vectorを作ると、その確保は`estimate_into`では省けない。既存の列へのconst参照や有効なspanを返せる場合に効果を得やすい。

<a id="chapter-6"></a>

## 6. ユースケースごとの使い方

### 6.1 通常の1ターン

第5.2節の`filter`を作った後は、観測を作り、成功を確認して結果を読む。

```cpp
case01::Observation observation{2.0, 2.2, 0.3};
const auto update = filter.observe(observation);
if (update.ok) {
    const auto all = filter.estimate();
    const auto top = filter.estimate(64);
    const double a = all.mean[0];
    const double b = all.mean[1];
    // a、bを次の操作の計算へ利用する。top.selected_massで上位64個の割合も読める。
    (void)a; (void)b; (void)top;
}
```

`observe(const Observation&)`の戻り値は`ParticleFilter::ObserveResult`。

| メンバー | 型 | 意味 |
|---|---|---|
| `ok` | `bool` | 観測を取り込めたか。falseなら今回の観測は確定していない |
| `log_predictive` | `double` | 観測前の分布に基づく、今回の観測の予測確率・密度の対数の推定値。段階更新では各段階の対数正規化量の合計 |
| `resampled` | `bool` | この成功した呼び出しの処理で再サンプリングが行われたか |
| `moves.attempted` | `int` | この呼び出しで行った固定パラメータの再探索候補数 |
| `moves.accepted` | `int` | そのうち受理した候補数 |

失敗時の戻り値は`ok=false`、`log_predictive=-inf`、`resampled=false`、移動数0。内部で準備の試行が一切なかったという意味ではなく、確定しなかった処理の情報はこの戻り値へ残さない。

`log_predictive`は「得られた点推定が正しい確率」ではない。連続密度では対数が正になることもある。比較するなら同じ観測表現・単位・正規化で用いる。利用者が累積値を保存する場合は、成功したときだけ加算する。

### 6.2 問題ごとの呼び出しと結果の対応

第8章では全例を`caseXX::solve`という関数で包む。観測をまとめて渡しているが、内部は1件ずつ同じ推定器へ渡す処理である。インタラクティブ問題では、その推定器をターンループの外に置き、観測を受信するたびに同じ処理を行う。

| 番号 | `observe`へ渡すもの | 平均・集計の取り方 | `solve`の成功時の出力と使い方 |
|---|---|---|---|
| U01 | `Observation{x,y,sigma}` | `estimate()`と`estimate(top_k)` | a、bの全平均・上位平均・選択質量。操作補正に使う |
| U02 | 地域別距離、固定時間、測定時間、sigma | `estimate()`で地域コスト、`for_each_weighted`で経路費用 | 地域コスト列、候補の期待時間と超過確率、選択経路番号 |
| U03 | 測定位置、測定距離、sigma | 座標列の`estimate()` | [x,y]。候補が複数残る場合は平均だけに頼らない |
| U04 | a、b、sigma、丸め表示 | 寸法列の`estimate()` | [幅,高さ]の実数平均。整数解への丸めを自動では行わない |
| U05 | `Observation{successes,trials}` | `estimate()`と二乗への`estimate(project)` | pの平均・分散。試行回数の意思決定などに使う |
| U06 | 表示記号番号 | 各種類の重みを`for_each_weighted`で合計 | 種類別確率と最多の種類番号 |
| U07 | 入力、測定出力、sigma | Modelの`project`で真偽と倍率へ写して`estimate()` | 反転確率と倍率の平均 |
| U08 | 測定値1個 | Modelの`project`でlog(sigma)をsigmaへ戻す | [muの平均,sigmaの平均] |
| U09 | 方向のx、y成分、sigma | cos、sinの平均を`atan2`へ渡す | ラジアン角と平均方向ベクトルの長さ |
| U10 | 3特徴の差、Aを選んだか、temperature | 重み列の`estimate()` | 3つの重みの平均。候補の比較予測に使う |
| U11 | 3種類の個数 | 比率列の`estimate()` | 合計1の3比率 |
| U12 | 経過時間・制御・測定・欠測フラグ | 各更新後の状態列の`estimate()` | ターンごとの[x,y,vx,vy]。現在位置・速度として使う |
| U13 | 加熱量・温度測定・sigma・欠測フラグ | 各更新後の数値の`estimate()` | ターンごとの温度平均 |
| U14 | 測定形式、値または閾値、sigma | 1成分の`estimate().mean[0]` | 真の圧力の平均 |
| U15 | 通路集合ビットと表示成否 | 通路確率と経路費用を`for_each_weighted`で集計 | 開通確率、候補期待費用、選択経路番号 |
| U16 | `Observation{region,measured,sigma}` | `refine(extra_moves)`後に`estimate_into(output)` | 各地点の温度平均。最高温度の期待値はmaxへの別の射影で計算 |

全`solve`例は`std::optional`を返す。応用例の`sampled_routes::evaluate`は新たな観測を処理せず、`Result`を直接返す。観測更新が失敗すると`std::nullopt`になり、その後の観測を処理しない。これは関数例の方針であり、ライブラリがプログラムを終了させるわけではない。

不正な入力値の検出には`assert`を使う。`nullopt`は「sigmaが負」「カテゴリ番号が範囲外」といったプログラム上の誤りまで回復する仕組みではない。空の観測列を渡すと、静的な例では事前分布の近似から結果を返し、時系列を返すU12・U13では空の列を返す。

### 6.3 代表値と粒子ごとの集計

| 呼び出し | 使いどころ |
|---|---|
| `estimate()` | `Model.project`があればそれを平均。なければ粒子自体を平均 |
| `estimate(k)` | 上位k粒子の中で重みを再正規化して平均。k=0は全粒子 |
| `estimate(project, k=0)` | その場で射影を指定。Modelの`project`より、この引数を使用 |
| `for_each_weighted(consume, k=0)` | `(const Particle&, double weight)`で各粒子を受け取り、任意の集計 |
| `estimate_into(buffer, k=0)` | Modelの射影または粒子自体の数値列を、確保済みの出力へ平均 |
| `estimate_into(buffer, project, k=0)` | 射影もその場で指定し、出力領域を再利用。戻り値は選択質量 |

`estimate`の戻り値には`mean`と`selected_mass`がある。`selected_mass`は選択前の全重みに対する選択集合の質量。全粒子の場合は1。上位kなら、例えば0.75は「選んだ粒子が元の全体の重みの75%を持っていた」という意味になる。

射影結果が数値なら`mean`は`double`、`std::array<T,D>`なら`std::array<double,D>`、`std::vector<T>`や数値のspan等なら`std::vector<double>`になる。列の要素は算術型で、全粒子で同じ長さが必要。独自構造体はそのまま平均できないため、U07のように射影する。射影が参照やspanを返す場合、その参照先は呼び出し中に有効でなければならない。

`for_each_weighted`へ渡される重みも、選択集合内で合計1になる。戻り値が`selected_mass`に相当する。選択前の質量で集計したければ、得た結果にこの質量を掛ける。全体のコールバックでは重み0の粒子も呼ばれるので、粒子が数値的に不正でも0を掛ければ安全になる、とは考えない。

上位kは**現在の重み順**であり、事後密度が高い順や最大事後確率解の探索ではない。再サンプリング直後は等重みなので、上位kに特別な意味がない場合がある。同点では元の粒子インデックスを優先する。kが1より大きいときのコールバック順は、重みの降順とは限らない。

`k=1`は最大重みの粒子を取り出す手段にもなるが、「MAP解が得られる」と解釈しない。全平均から上位平均へ変えると推定量自体も変わるので、見た目の鋭さだけで選ばず、実際の目的関数で比較する。

同じ重みと同じkの間は、`estimate`、`estimate_into`、`for_each_weighted`で選択した粒子番号を共有して再利用する。**数値の集計結果は毎回計算する。** 特に`refine`後は重みが同じでも粒子の値が変わるので、値までキャッシュされていると考えない。

### 6.4 継続処理・観察用API

| API | 戻り値 | 補足 |
|---|---|---|
| `param()` | `const Param&` | 現在の設定。変更するなら値へコピーして`set_param`へ渡す |
| `set_param(Param)` | `void` | 計算量の設定を変更。粒子数が変わると即時再サンプリング |
| `reset()` | `void` | 事前から再生成。乱数器は継続 |
| `refine(int steps)` | `MoveResult` | 固定モデルのみ。stepsは0以上。0なら何もしない |
| `size()` | `int` | 現在の粒子数 |
| `ess()` | `double` | 現在の重みに対するESS |
| `observation_count()` | `int` | 最後のreset以降に成功した観測数 |
| `sample_indices(output,rng)` | `void` | 現在の重みで独立に復元抽出。outputを書き換え、渡したRNGだけを進める |
| `weights()` | `std::span<const double>` | 正規化済みの重み |
| `log_weights()` | `std::span<const double>` | 正規化済みの対数重み |
| `particles()` | 読み取り専用のrange/view | 利用者のParticleを順に参照。vectorのコピーを返すものではない |
| `history()` | `std::span<const Observation>` | 固定モデルのみ。成功観測が入る |

返されたview、span、参照は、推定器を変更する前に使い終える。長く保持したければ必要な値をコピーする。取得した重みや粒子を書き換えるAPIはない。`sample_indices`と集計だけでは粒子・重みのviewは無効化されない。ただし、抽出したインデックスの先の値は、その後の`observe`・`refine`・粒子数変更などで変わり得る。更新をまたぐ仮説を固定したいなら、対象粒子の値をコピーする。

### 6.5 `ParticleCloud`の下位API一覧

通常版と同じ代表値機能に加えて、手順を自分で組み合わせられる。以下のPはParticle、Rは乱数器である。

| API | コールバック・引数 | 戻り値と注意 |
|---|---|---|
| `update_log(f)` | `f(const P&) -> double`、今回の増分対数重み | `UpdateResult{ok,log_normalizer}`。自動の再サンプリングはない |
| `predict(f,rng)` | `f(P&,R&)`、真の遷移 | `void`。その場で変更。後の更新失敗では巻き戻らない |
| `propose_update_log(f,rng)` | `f(const P& old,P& candidate,R&) -> double` | 候補コピーと返した増分を一括確定。全無効なら元を維持 |
| `resample_if_needed(rng)` | 構築時のESS比率を使う | 実行時true。未実行なら乱数器も変えない |
| `resample(rng,new_count=0)` | 0なら同数、正なら新粒子数 | 選んだ異なる親インデックス数。粒子値の種類数ではない |
| `resample_from(source,rng,new_count=0)` | 別のCloudまたは自分を元にする | 元を保つ別Cloudへの再サンプリングも可能 |
| `rejuvenate(steps,target,propose,rng)` | `target(const P&)->double`、`propose(P&,R&)->double` | `MoveResult`。targetは全履歴を含む対数対象密度。proposeは対数提案比を返す |
| `rejuvenate(steps,target,propose,undo,rng)` | `propose(P&,R&)->MoveProposal<Token>`、`undo(P&,const Token&)` | `MoveResult`。差分候補を作り、棄却時だけundo。targetは全履歴を含む対数対象密度 |
| `rejuvenate(steps,kernel,rng)` | `kernel(P&,R&)` | `void`。対象分布を保つ更新を利用者が完成させて渡す。受理・棄却も利用者側 |
| `estimate(project,k=0)` | 数値・数値列への射影 | 通常版と同じ`mean`、`selected_mass`。下位APIには射影省略版がない |
| `for_each_weighted(consume,k=0)` | `(const P&,double)`で集計 | 選択前の質量を返す |
| `estimate_into(buffer,project,k=0)` | 既存のdouble列と数値列への射影 | 選択質量。下位APIには射影省略版がない |
| `sample_indices(output,rng)` | `span<int>`と外部RNG | `void`。粒子群を変えない独立な復元抽出 |
| `size / ess / particles / weights / log_weights` | 引数なし | 状態観察。ここでの`particles()`は`std::span<const P>` |

`ParticleCloud::propose_update_log`が受け取る戻り値は、利用者が組み立てた**重みの増分全体**である。通常版のModelに書く`propose_transition`は遷移と提案の補正だけを返す。この二つを取り違えない。

下位APIのコピー方式の`rejuvenate`では対称提案でも`0.0`を返す。差分方式は補正0の`MoveProposal<Token>`を返す。通常版のModelに書く`propose`だけは、対称なら`void`が許される。通常利用では、このような手順の組合せを意識する必要の少ない`ParticleFilter`を選ぶ。

### 6.6 推定用の粒子を保ったまま、行動評価用の標本を取る

`sample_indices(std::span<int> output, SamplingRng& rng) const`は、現在の重みに従って粒子番号を独立に復元抽出する。戻り値は`void`。抽出数はoutputの要素数で指定し、同じ番号が何回出てもよい。

第5.2節のfilterに対して、例えば倍率aの平均を少数の標本で近似する呼び出しは次になる。

```cpp
std::mt19937_64 planning_rng(42);
std::vector<int> indices(64);
filter.sample_indices(indices, planning_rng);
const auto sampled_particles = filter.particles();
double sampled_a = 0;
for (int index : indices) sampled_a += sampled_particles[index][0] / double(indices.size());
// sampled_aは現在の粒子分布での倍率aの平均の近似。元の重みを再び掛けない。
(void)sampled_a;
```

これは`resample`と異なり、元の粒子・重み・履歴・推定用の乱数器を変えない。空のoutputなら何もしない。`samples=64`でも最大64種類というだけで、64種類の異なる仮説を保証しない。抽出後は各標本を等重み1/Mで評価する。Mは標本数である。

各候補の費用を標本平均で近似するのがU15の応用例である。固定した候補の費用平均は現在の粒子分布の平均を狙えるが、その推定値で最小の候補を選ぶと、抽出誤差で順位が変わる場合がある。低確率・高損失の失敗が少数標本へ一度も現れないこともある。候補間の費用差が小さいときや失敗が高価なときは、標本を増やすか、全粒子で確認する。

`planning_rng`を継続して使えば毎回別の標本になる。同じseedで毎回作り直すかどうかは利用者の選択であり、ライブラリがseedを決めることはない。同じ標本を全候補で共有する例を第8.19節に示す。

### 6.7 平均の出力領域を指定する

`estimate_into`は、指定した領域を最初に0で埋めてから平均を上書きし、`double`の選択質量を返す。値を追記する関数ではない。出力先は自動リサイズされないため、`reserve`だけでなく`resize`または要素数付きの構築が必要になる。

```cpp
std::array<double, 2> mean_buffer{};
const double all_mass = filter.estimate_into(mean_buffer);
std::vector<double> moments(2);
const double top_mass = filter.estimate_into(
    std::span<double>(moments),
    [](const case01::Model::Particle& p) { return std::array<double, 2>{p[0], p[0] * p[0]}; },
    64);
// mean_buffer=[a,b]の全平均。moments=[a,a*a]の上位64粒子平均。
(void)all_mass; (void)top_mass;
```

bufferは数値列の射影結果と同じ長さのdouble列でなければならない。`float`列やconst領域へは出力できない。スカラーの平均は`estimate`を使う。出力先と粒子・射影の参照先は重ねない。先頭の0クリアで入力まで消してしまうためである。空の出力列は、全粒子の射影結果も長さ0の場合に限って使える。

U02のターン処理では、地域数Gのbufferを一度確保してから各観測後に上書きし、毎回違う候補経路を評価できる。出力領域を使い回せても、前ターンの推定値を自動保存するものではない。必要な結果は別に保存する。第8.20節がそのコードである。

### 6.8 差分再探索の呼び出し方

U16のようにModelへ`propose_in_place`と`undo`を定義しても、利用側の呼び出しは`observe(o)`と`refine(steps)`のままである。候補を採用する判定や、失敗した提案の巻き戻しをsolve関数へ追加する必要はない。

固定モデルの自動再探索にも明示的な`refine`にも、この差分提案が使われる。`refine`の戻り値は`MoveResult{attempted,accepted}`で、重みや観測履歴は変わらない。`steps=0`は何もしない。試行数は各粒子に対する試行の合計であり、「変更された座標数」ではない。対数重みが負の無限大の粒子は試行対象から除外される。

受理率は`accepted / attempted`で観察できるが、attemptedが0なら割らない。受理率が高いだけで探索が十分とは限らず、移動が小さすぎればほとんど同じ仮説に留まる。幅を変える判断は推定誤差や実際の費用も見て行う。

<a id="chapter-7"></a>

## 7. 制約・注意点

1. **尤度は今回の観測だけ。** 事前や累積尤度を毎回含めると二重計上する。全履歴用の任意関数と、通常の1件用関数を分ける。
2. **通常尤度は有限で0以上、対数尤度は有限または負の無限大。** NaNや正の無限大は契約違反。確率密度は1を超えてよい。任意の負のスコアを通常尤度として返さない。
3. **全粒子で今回の確率が0なら更新失敗。** 自動で一様重みに戻したり、ノイズを広げたりしない。真の解が事前範囲外、モデルが違う、桁落ち・アンダーフローがある、粒子が足りない、などを調べる。
4. **小さな正の確率を早期に0へ丸めない。** 連続測定の指数関数がアンダーフローするなら対数版を直接書く。`log(likelihood(...))`へ変えるだけでは、元の0を回復できない。任意の下限値で尤度を底上げすると別のモデルになる。
5. **確率0を多用する制約は導入を難しくすることがある。** ただし、それを避けるために実在しない観測ノイズを加えることも、モデルの変更である。既知の制約なら初期生成と提案で有効状態を作る。
6. **固定モデルでは過去の条件を値として保存。** 観測にspanや外部配列へのポインタを入れると、保存したつもりでも後から内容が変わる場合がある。vectorやarrayなど、所有する値を使うのが基本。
7. **動的モデルでは最初の`observe`も遷移する。** 初期粒子は最初の移動より前の状態。初期時刻に測った値だけを先に反映したいなら、初回だけゼロ時間・無移動になる条件を明示する。
8. **欠測で関数呼び出し自体を省略しない。** 時間が進んだならその遷移を表す。欠測は尤度1、対数0で追加情報を与えない形にする。
9. **固定量への探索ノイズを`transition`にしない。** 関数の存在だけで動的モデルになり、履歴保存・`refine`・固定モデル向け段階更新が使われなくなる。
10. **`transition`と`propose_transition`を両方書くと後者が優先。** 両方が順に実行されるわけではない。動的モデルの`propose`・`propose_in_place`・`prior`・`log_prior`は、固定パラメータの再探索には使われない。
11. **Modelの`propose`または`propose_in_place`を定義した固定モデルは事前評価も必要。** `move_steps=0`でも初期化時の対象評価があるため、この要件は消えない。`prior`と`log_prior`は一方だけにする。
12. **提案関数には有効な粒子型・形を保つ責任がある。** 補正に負の無限大を返す場合も、内部データの長さを壊したり、後の射影が不正になる値を残したりしない。固定再探索では棄却される候補も対象評価され得る。
13. **確率の定義は同じ入力なら同じ値にする。** 尤度の中で毎回乱数を引いて評価を揺らす使い方は想定しない。シミュレーションから確率を近似する場合も、その近似が推定全体へどう影響するかを別に検討する。
14. **上位kは0〜現在粒子数。** 大きいkを自動で粒子数へ丸める機能はない。粒子数を減らした後に、古いkをそのまま使わない。
15. **平均が実行可能な仮説とは限らない。** カテゴリや開閉ビットの平均は仮説ではない。円周、制約集合、複数の離れた候補も用途に合う射影・集計が必要。U06、U09、U15を参照。
16. **再サンプリング後のESSが大きくても、多様性は保証しない。** 同じ値を複製した粒子が多数あっても等重みならESSは粒子数に等しい。動的モデルに自動の再探索はない。
17. **観測数に比例して固定履歴のメモリが増える。** `log_likelihood_history`を用意しても履歴の保持はなくならない。長期間運用で自動的に古い観測を忘れる機能はない。
18. **時間制限・最適粒子数・観測行動の自動選択はない。** 選ぶ行動によって得られる情報まで考える計画は、利用者側で組み立てる。分岐のためのコピーは使える。
19. **コピーの共有参照と副作用に注意。** ModelやParticleが外部の変更可能なオブジェクトを参照すると、値のコピーだけで分岐を独立にはできない。コールバックから同じ推定器へ再入したり、構造を変更したりしない。
20. **スケールと次元に限界がある。** 粒子数・総試行回数はintの範囲、有限の対数値の演算はdoubleの範囲を想定。極端な数値を扱う特別な型は使わない。高次元や強く相関した分布では、粒子を増やすだけでは足りず、モデルの簡約や良い提案が必要になる。
21. **`assert`は開発時の契約確認。** 例外をthrowして回復する設計ではない。`NDEBUG`でチェックが消えても、利用者が守るべき前提は変わらない。
22. **外部ライブラリや標準ライブラリの性質は別。** ライブラリ自身は明示的に例外をthrowしないが、メモリ確保失敗等まで無例外を保証するものではない。
23. **固定量と動的状態を同じ粒子に入れても、固定量の再探索は自動には増えない。** 例えば位置と未知の摩擦係数を同時に持ち、位置だけ遷移させることはできる。ただし推定器全体が動的モデルになり、摩擦係数に対する固定履歴の再探索は行われない。固定成分の多様性が失われる問題では、別の推定手順や問題に合う提案設計が必要になる。

24. **差分提案はundoと一組。** `propose_in_place`だけを定義してundoを省略しない。undoは変更した全フィールドと利用者キャッシュを復元する。候補の評価は補正が負の無限大でも行われ得るため、途中で型や配列の形を壊さない。
25. **差分の提案補正へ対象密度の差を入れない。** `MoveProposal::log_correction`は候補生成の逆向き・順向きの比だけ。ライブラリによる事前・履歴・今回分の評価と二重計上しない。
26. **標本には元の重みを再び掛けない。** `sample_indices`は復元抽出。重複を削除したり、重みによる再加重をしたりすると、元と異なる集計になる。
27. **標本の数と粒子数は別。** M標本で失敗が0回でも、その事象の確率が0だとは言えない。ESS、selected_mass、標本数も、それだけで推定精度の保証にはならない。
28. **出力領域は独立したdouble列を正しい長さで用意。** `estimate_into`は出力を先にクリアする。単なるcapacity確保、入力との重複、スカラー用射影との組合せは使えない。
29. **constの集計も同時実行向けではない。** 上位kの選択と抽出用の累積重みは内部で遅延計算される。同一インスタンスを複数スレッドから同時に使うことは想定しない。

### 7.1 専用の方法で十分な場合

仮説が数個だけなら全仮説の確率を厳密に更新できる。Beta分布と成功数の組合せ、Dirichlet分布と種類別個数の組合せにも厳密な更新式がある。線形の状態変化と正規誤差だけで成立する追跡には、平均と共分散だけを更新する専用の方法がある。

U05・U06・U11・U12・U13の単純な形には、こうした専用解法が使える。本ガイドでは確率の作り方や型の使い方を理解するためにも例を示す。観測の式が非線形になったり、離散条件や複雑な制約が混ざったりすると、この汎用ライブラリを使う意義が大きくなる。

<a id="chapter-8"></a>

## 8. ユースケースごとのコード例

U01〜U16のコードは独立した名前空間`caseXX`に入り、応用例は用途ごとの名前空間に入る。各例の`solve`を呼び出せる。第8.18節は第8.1節、8.19節は8.15節、8.20節は8.2節のModelを再利用する。それ以外は単独で必要な定義を含む。入出力の単位・意味は各構造体と関数のコメントに記した。

全例をまとめたファイルは `particle_filter_usage_examples_v09.cpp`。各ブロックはそのファイルから直接抽出しており、説明用に省略した仮実装ではない。通常は必要な節だけコピーして使う。

### 8.1 U01：操作倍率・固定ずれ、全粒子平均と上位k平均

`case01::solve(observations, lower, upper, top_k, count, seed)`を呼ぶ。`mean[0]`がa、`mean[1]`がb。上位平均を使うなら`top_mean`と`top_mass`を組で確認する。

```cpp
#include "particle_filter_v09.hpp"
namespace case01 {
// 入力: 既知の操作量x、測定値y、測定誤差の標準偏差sigma。
struct Observation { double x, y, sigma; };
struct Model : UniformBox<2> {
    using Observation = case01::Observation;
    Model(std::array<double, 2> lower, std::array<double, 2> upper)
        : UniformBox(lower, upper) {}
    double log_likelihood(const Particle& p, const Observation& o) const {
        const double z = (o.y - (p[0] * o.x + p[1])) / o.sigma;
        return -0.5 * z * z - std::log(o.sigma) - 0.5 * std::log(2 * std::numbers::pi);
    }
};
struct Result {
    std::array<double, 2> mean;      // [操作量の倍率a, 固定ずれb]の全粒子平均
    std::array<double, 2> top_mean;  // 上位top_k粒子内で再正規化した平均
    double top_mass;                // 選んだ粒子が元の全重みに占める割合
};
// lower/upperは[a,b]の事前範囲。top_k=0は全粒子、1..countは上位k。
// nulloptは、観測を取り込めなかったことを示す。
std::optional<Result> solve(const std::vector<Observation>& observations,
                           std::array<double, 2> lower, std::array<double, 2> upper,
                           int top_k = 0, int count = 1024, std::uint64_t seed = 0) {
    assert(0 <= top_k && top_k <= count);
    ParticleFilter<Model> filter(Model(lower, upper), {.count = count}, std::mt19937_64(seed));
    for (const auto& o : observations) {
        assert(o.sigma > 0);
        if (!filter.observe(o).ok) return std::nullopt;
    }
    const auto top = filter.estimate(top_k);
    return Result{filter.estimate().mean, top.mean, top.selected_mass};
}
}
```

### 8.2 U02：地域コストと締切を考慮した経路選択

`lower`、`upper`、各`length`の長さを地域数に揃える。`selected_route`は渡した`routes`内の0始まりの番号。候補経路の生成はこの関数の外で行う。

```cpp
#include "particle_filter_v09.hpp"
namespace case02 {
// length[g]は経路中の地域gの距離。fixedは既知の固定時間。
struct Observation { std::vector<double> length; double fixed, time, sigma; };
struct Route { std::vector<double> length; double fixed = 0; };
struct Model : UniformBox<> {
    using Observation = case02::Observation;
    Model(std::vector<double> lower, std::vector<double> upper)
        : UniformBox(std::move(lower), std::move(upper)) {}
    double log_likelihood(const Particle& p, const Observation& o) const {
        const double predicted = o.fixed + std::inner_product(p.begin(), p.end(), o.length.begin(), 0.0);
        const double z = (o.time - predicted) / o.sigma;
        return -0.5 * z * z - std::log(o.sigma) - 0.5 * std::log(2 * std::numbers::pi);
    }
};
struct Result {
    std::vector<double> unit_time;        // 地域ごとの単位距離あたり時間の推定
    std::vector<double> expected_time;    // 候補経路の真の所要時間の期待値
    std::vector<double> late_probability; // 真の所要時間がdeadlineを超える確率
    int selected_route;                  // 期待時間 + penalty * 超過確率が最小の経路
};
// lower/upperの長さが地域数G。全lengthも長さG。
// 測定ノイズと走行時間自体の変動は区別する。この例の遅延確率はパラメータ不確実性のみ。
std::optional<Result> solve(const std::vector<Observation>& observations,
                           std::vector<double> lower, std::vector<double> upper,
                           const std::vector<Route>& routes, double deadline, double penalty,
                           int count = 1024, std::uint64_t seed = 0) {
    assert(!routes.empty() && penalty >= 0);
    const int groups = int(lower.size());
    ParticleFilter<Model> filter(Model(std::move(lower), std::move(upper)),
                                 {.count = count}, std::mt19937_64(seed));
    for (const auto& o : observations) {
        assert(int(o.length.size()) == groups && o.sigma > 0);
        if (!filter.observe(o).ok) return std::nullopt;
    }
    for (const auto& route : routes) assert(int(route.length.size()) == groups);
    Result result{filter.estimate().mean, std::vector<double>(routes.size()),
                  std::vector<double>(routes.size()), 0};
    filter.for_each_weighted([&](const Model::Particle& p, double weight) {
        for (int r = 0; r < int(routes.size()); ++r) {
            const double time = routes[r].fixed + std::inner_product(p.begin(), p.end(), routes[r].length.begin(), 0.0);
            result.expected_time[r] += weight * time;
            result.late_probability[r] += weight * (time > deadline);
        }
    });
    auto loss = [&](int r) { return result.expected_time[r] + penalty * result.late_probability[r]; };
    for (int r = 1; r < int(routes.size()); ++r)
        if (loss(r) < loss(result.selected_route)) result.selected_route = r;
    return result;
}
}
```

### 8.3 U03：非線形の測距モデル

測定ごとに`sx`、`sy`を保存する。返る座標は平均であり、複数候補を解消できているかは測定配置にも依存する。

```cpp
#include "particle_filter_v09.hpp"
namespace case03 {
// (sx,sy)は測距位置、rangeは発信源までの距離の測定値。
struct Observation { double sx, sy, range, sigma; };
struct Model : UniformBox<2> {
    using Observation = case03::Observation;
    Model(std::array<double, 2> lower, std::array<double, 2> upper) : UniformBox(lower, upper) {}
    double log_likelihood(const Particle& p, const Observation& o) const {
        const double z = (o.range - std::hypot(p[0] - o.sx, p[1] - o.sy)) / o.sigma;
        return -0.5 * z * z - std::log(o.sigma) - 0.5 * std::log(2 * std::numbers::pi);
    }
};
// 入力: 発信源の存在範囲と測距履歴。出力: [x,y]の事後平均。
// 対称な候補が残ると平均は候補間に来る。行動評価には粒子ごとの評価も検討する。
std::optional<std::array<double, 2>> solve(const std::vector<Observation>& observations,
                                        std::array<double, 2> lower, std::array<double, 2> upper,
                                        int count = 1024, std::uint64_t seed = 0) {
    ParticleFilter<Model> filter(Model(lower, upper), {.count = count}, std::mt19937_64(seed));
    for (const auto& o : observations) {
        assert(o.sigma > 0);
        if (!filter.observe(o).ok) return std::nullopt;
    }
    return filter.estimate().mean;
}
}
```

### 8.4 U04：丸め区間の確率

正規分布の区間確率を`erfc`から計算する。浮動小数点で表せないほど小さい裾確率は0になり得るため、極端な入力へ任意の下限確率を足す処理は入れていない。

```cpp
#include "particle_filter_v09.hpp"
namespace case04 {
// センサーは a*幅+b*高さ+正規ノイズ を最も近い整数に丸めて返す。
struct Observation { double a, b, sigma; int rounded; };
struct Model : UniformBox<2> {
    using Observation = case04::Observation;
    Model(std::array<double, 2> lower, std::array<double, 2> upper) : UniformBox(lower, upper) {}
    double likelihood(const Particle& p, const Observation& o) const {
        const double mean = o.a * p[0] + o.b * p[1];
        const double lo = (o.rounded - 0.5 - mean) / o.sigma;
        const double hi = (o.rounded + 0.5 - mean) / o.sigma;
        const double s = std::sqrt(2.0);
        // 同じ側の裾では、小さい裾確率どうしを引き、1からの差し引きを避ける。
        if (lo >= 0) return 0.5 * (std::erfc(lo / s) - std::erfc(hi / s));
        if (hi <= 0) return 0.5 * (std::erfc(-hi / s) - std::erfc(-lo / s));
        return 1 - 0.5 * std::erfc(hi / s) - 0.5 * std::erfc(-lo / s);
    }
};
// lower/upper=[幅,高さ]の実数範囲。出力も実数。真に整数の寸法を扱う場合は離散粒子が必要。
std::optional<std::array<double, 2>> solve(const std::vector<Observation>& observations,
                                        std::array<double, 2> lower, std::array<double, 2> upper,
                                        int count = 1024, std::uint64_t seed = 0) {
    ParticleFilter<Model> filter(Model(lower, upper), {.count = count}, std::mt19937_64(seed));
    for (const auto& o : observations) {
        assert(o.sigma > 0);
        if (!filter.observe(o).ok) return std::nullopt;
    }
    return filter.estimate().mean;
}
}
```

### 8.5 U05：成功率の平均と分散

この例は`propose`を省略し、事前生成と1件尤度だけをユーザーが定義する。平均に加え、二乗への射影で分散を計算する。

```cpp
#include "particle_filter_v09.hpp"
namespace case05 {
struct Observation { int successes, trials; };
struct Model {
    using Particle = double; // 未知の成功確率p
    using Observation = case05::Observation;
    double alpha, beta;      // Beta事前分布。どちらも正。1,1なら一様。
    Particle sample(std::mt19937_64& rng) {
        const double a = std::gamma_distribution<double>(alpha, 1)(rng);
        const double b = std::gamma_distribution<double>(beta, 1)(rng);
        assert(a + b > 0);
        return a / (a + b);
    }
    double log_likelihood(Particle p, const Observation& o) const {
        double value = std::lgamma(o.trials + 1.0) - std::lgamma(o.successes + 1.0)
                     - std::lgamma(o.trials - o.successes + 1.0);
        if (o.successes > 0) value += o.successes * std::log(p);
        if (o.trials > o.successes) value += (o.trials - o.successes) * std::log1p(-p);
        return value;
    }
    // proposeを省略。再探索の際はsampleからの提案をライブラリが処理する。
};
struct Result { double probability, variance; };
// 入力: 独立試行の成功数と試行数。出力: 成功確率の平均と分散。
// この単純なBeta-Binomial問題は厳密更新も可能。複雑な観測へ拡張する際の基本形として示す。
std::optional<Result> solve(const std::vector<Observation>& observations, double alpha = 1, double beta = 1,
                           int count = 1024, std::uint64_t seed = 0) {
    assert(alpha > 0 && beta > 0);
    ParticleFilter<Model> filter(Model{alpha, beta}, {.count = count}, std::mt19937_64(seed));
    for (const auto& o : observations) {
        assert(0 <= o.successes && o.successes <= o.trials);
        if (!filter.observe(o).ok) return std::nullopt;
    }
    const double mean = filter.estimate().mean;
    const double second = filter.estimate([](double p) { return p * p; }).mean;
    return Result{mean, std::max(0.0, second - mean * mean)};
}
}
```

### 8.6 U06：種類の確率

`prior_weight`は合計1でなくてもよい。表示確率`emission`の各行は合計1でなければならない。観測番号は全行共通の記号集合の番号である。

```cpp
#include "particle_filter_v09.hpp"
namespace case06 {
struct Model {
    using Particle = int;     // 鉱石の種類の番号
    using Observation = int;  // スキャナーが返した記号の番号
    std::vector<double> prior_weight;
    std::vector<std::vector<double>> emission; // [種類][記号]の確率、各行の和は1
    Particle sample(std::mt19937_64& rng) {
        return std::discrete_distribution<int>(prior_weight.begin(), prior_weight.end())(rng);
    }
    double likelihood(Particle type, Observation symbol) const { return emission[type][symbol]; }
};
struct Result { std::vector<double> probability; int selected_type; };
// prior_weightは非負で総和が正。emissionの行数=種類数、全行の列数=記号数。
// 出力のprobability[k]は「種類kである」確率。番号自体の平均は取らない。
std::optional<Result> solve(const std::vector<int>& symbols, std::vector<double> prior_weight,
                           std::vector<std::vector<double>> emission,
                           int count = 1024, std::uint64_t seed = 0) {
    const int types = int(prior_weight.size());
    assert(types > 0 && int(emission.size()) == types && !emission[0].empty());
    const int alphabet = int(emission[0].size());
    assert(std::accumulate(prior_weight.begin(), prior_weight.end(), 0.0) > 0);
    for (double p : prior_weight) assert(p >= 0);
    for (const auto& row : emission) {
        assert(int(row.size()) == alphabet);
        assert(std::abs(std::accumulate(row.begin(), row.end(), 0.0) - 1) < 1e-9);
        for (double p : row) assert(p >= 0);
    }
    ParticleFilter<Model> filter(Model{std::move(prior_weight), std::move(emission)},
                                 {.count = count}, std::mt19937_64(seed));
    for (int symbol : symbols) {
        assert(0 <= symbol && symbol < alphabet);
        if (!filter.observe(symbol).ok) return std::nullopt;
    }
    Result result{std::vector<double>(types), 0};
    filter.for_each_weighted([&](int type, double weight) { result.probability[type] += weight; });
    result.selected_type = int(std::max_element(result.probability.begin(), result.probability.end()) - result.probability.begin());
    return result;
}
}
```

### 8.7 U07：真偽と実数を持つ粒子

構造体をそのまま平均せず、`project`で「反転しているかの0/1」と倍率へ変換する。返る反転成分の平均が故障確率になる。

```cpp
#include "particle_filter_v09.hpp"
namespace case07 {
struct Observation { double input, measured, sigma; };
struct Model {
    struct Particle { bool reversed; double gain; };
    using Observation = case07::Observation;
    UniformBox<1> gain_range;
    double fault_prior;
    Model(double lower, double upper, double prior)
        : gain_range({lower}, {upper}), fault_prior(prior) {}
    Particle sample(std::mt19937_64& rng) {
        return {std::bernoulli_distribution(fault_prior)(rng), gain_range.sample(rng)[0]};
    }
    double log_prior(const Particle& p) const {
        return gain_range.log_prior({p.gain}) + std::log(p.reversed ? fault_prior : 1 - fault_prior);
    }
    void propose(Particle& p, std::mt19937_64& rng) {
        if (std::bernoulli_distribution(0.2)(rng)) p.reversed = !p.reversed;
        else {
            std::array<double, 1> gain{p.gain};
            gain_range.propose(gain, rng);
            p.gain = gain[0];
        }
    }
    double log_likelihood(const Particle& p, const Observation& o) const {
        const double predicted = (p.reversed ? -1 : 1) * p.gain * o.input;
        const double z = (o.measured - predicted) / o.sigma;
        return -0.5 * z * z - std::log(o.sigma) - 0.5 * std::log(2 * std::numbers::pi);
    }
    std::array<double, 2> project(const Particle& p) const { return {double(p.reversed), p.gain}; }
};
struct Result { double reversed_probability, mean_gain; };
// 正のgain範囲と、反転故障の事前確率0<fault_prior<1を指定する。
// 故障状態は全観測中一定。このコードは途中で故障が発生・回復するモデルではない。
std::optional<Result> solve(const std::vector<Observation>& observations, double lower, double upper,
                           double fault_prior, int count = 1024, std::uint64_t seed = 0) {
    assert(lower > 0 && 0 < fault_prior && fault_prior < 1);
    ParticleFilter<Model> filter(Model(lower, upper, fault_prior), {.count = count}, std::mt19937_64(seed));
    for (const auto& o : observations) {
        assert(o.sigma > 0);
        if (!filter.observe(o).ok) return std::nullopt;
    }
    const auto mean = filter.estimate().mean;
    return Result{mean[0], mean[1]};
}
}
```

### 8.8 U08：未知の標準偏差

尤度の`-p[1]`は`-log(sigma)`に対応し、省略できない。`project`によって、戻り値はlog(sigma)ではなくsigma自身の平均になる。

```cpp
#include "particle_filter_v09.hpp"
namespace case08 {
struct Model : UniformBox<2> {
    using Observation = double;
    // p[0]=真の値mu、p[1]=log(sigma)。sigma自身への一様事前ではない。
    Model(double mu_lower, double mu_upper, double sigma_lower, double sigma_upper)
        : UniformBox({mu_lower, std::log(sigma_lower)}, {mu_upper, std::log(sigma_upper)}) {}
    double log_likelihood(const Particle& p, double measured) const {
        const double z = (measured - p[0]) / std::exp(p[1]);
        return -0.5 * z * z - p[1] - 0.5 * std::log(2 * std::numbers::pi);
    }
    std::array<double, 2> project(const Particle& p) const { return {p[0], std::exp(p[1])}; }
};
// 入力: 同じ真の値の反復測定と事前範囲。出力: [muの平均, sigmaの平均]。
std::optional<std::array<double, 2>> solve(const std::vector<double>& measurements,
                                        double mu_lower, double mu_upper, double sigma_lower, double sigma_upper,
                                        int count = 1024, std::uint64_t seed = 0) {
    assert(0 < sigma_lower && sigma_lower < sigma_upper);
    ParticleFilter<Model> filter(Model(mu_lower, mu_upper, sigma_lower, sigma_upper),
                                 {.count = count}, std::mt19937_64(seed));
    for (double y : measurements) if (!filter.observe(y).ok) return std::nullopt;
    return filter.estimate().mean;
}
}
```

### 8.9 U09：円周上の平均

`angle`だけでなく`resultant`も読む。測定がなく一様なままなら、得られた小さな平均方向ベクトルの向きに意味を持たせない。

```cpp
#include "particle_filter_v09.hpp"
namespace case09 {
// センサーは真の方向(cos(theta),sin(theta))の両成分に独立な正規誤差を加える。
struct Observation { double x, y, sigma; };
struct Model {
    using Particle = double; // ラジアン角
    using Observation = case09::Observation;
    Particle sample(std::mt19937_64& rng) {
        return (2 * std::generate_canonical<double, 53>(rng) - 1) * std::numbers::pi;
    }
    double log_prior(Particle) const { return 0; } // 円周上の一様事前
    void propose(Particle& angle, std::mt19937_64& rng) {
        angle = std::remainder(angle + std::normal_distribution<double>(0, 0.3)(rng), 2 * std::numbers::pi);
    }
    double log_likelihood(Particle angle, const Observation& o) const {
        const double dx = o.x - std::cos(angle), dy = o.y - std::sin(angle);
        return -0.5 * (dx * dx + dy * dy) / (o.sigma * o.sigma)
             - std::log(2 * std::numbers::pi * o.sigma * o.sigma);
    }
    std::array<double, 2> project(Particle angle) const { return {std::cos(angle), std::sin(angle)}; }
};
struct Result { double angle, resultant; };
// 出力angleは[-pi,pi]の円周平均、resultantは0..1。0付近なら方向の平均が不安定。
std::optional<Result> solve(const std::vector<Observation>& observations,
                           int count = 1024, std::uint64_t seed = 0) {
    ParticleFilter<Model> filter(Model{}, {.count = count}, std::mt19937_64(seed));
    for (const auto& o : observations) {
        assert(o.sigma > 0);
        if (!filter.observe(o).ok) return std::nullopt;
    }
    const auto mean = filter.estimate().mean;
    return Result{std::atan2(mean[1], mean[0]), std::hypot(mean[0], mean[1])};
}
}
```

### 8.10 U10：二者比較の確率

特徴差を作る向きと`a_won`を揃える。特徴差をB-Aにするなら、成否の解釈も変わる。対数確率は指数関数が大きくなりすぎない形で計算している。

```cpp
#include "particle_filter_v09.hpp"
namespace case10 {
// difference=候補Aの特徴-候補Bの特徴。a_wonは比較結果。
// temperature>0は比較の曖昧さで、問題から既知とする。
struct Observation { std::array<double, 3> difference; bool a_won; double temperature; };
struct Model : UniformBox<3> {
    using Observation = case10::Observation;
    Model(std::array<double, 3> lower, std::array<double, 3> upper) : UniformBox(lower, upper) {}
    double log_likelihood(const Particle& p, const Observation& o) const {
        double z = std::inner_product(p.begin(), p.end(), o.difference.begin(), 0.0) / o.temperature;
        if (!o.a_won) z = -z;
        return z >= 0 ? -std::log1p(std::exp(-z)) : z - std::log1p(std::exp(z));
    }
};
// 出力: 3特徴の未知重みの平均。全特徴に差が生じる比較を集めないと識別できない。
std::optional<std::array<double, 3>> solve(const std::vector<Observation>& observations,
                                        std::array<double, 3> lower, std::array<double, 3> upper,
                                        int count = 1024, std::uint64_t seed = 0) {
    ParticleFilter<Model> filter(Model(lower, upper), {.count = count}, std::mt19937_64(seed));
    for (const auto& o : observations) {
        assert(o.temperature > 0);
        if (!filter.observe(o).ok) return std::nullopt;
    }
    return filter.estimate().mean;
}
}
```

### 8.11 U11：合計1を保つ構成比

初期生成と提案の双方で合計1を保つ。2成分間で割合を配り直す提案は、この例では往復の密度が同じなので`void`で返せる。各標本の個数合計はintに収まる範囲とする。

```cpp
#include "particle_filter_v09.hpp"
namespace case11 {
struct Model {
    using Particle = std::array<double, 3>; // 非負、合計1の材料構成比
    using Observation = std::array<int, 3>; // 標本中の種類別個数
    Particle sample(std::mt19937_64& rng) {
        Particle p;
        for (double& x : p) x = std::exponential_distribution<double>(1)(rng);
        const double sum = std::accumulate(p.begin(), p.end(), 0.0);
        assert(sum > 0);
        for (double& x : p) x /= sum;
        return p; // Dirichlet(1,1,1)、三角形上の一様事前
    }
    double log_prior(const Particle& p) const {
        if (std::abs(p[0] + p[1] + p[2] - 1) > 1e-9 || *std::min_element(p.begin(), p.end()) < 0)
            return -std::numeric_limits<double>::infinity();
        return 0;
    }
    void propose(Particle& p, std::mt19937_64& rng) {
        const int i = std::uniform_int_distribution<int>(0, 2)(rng);
        const int j = (i + 1 + std::uniform_int_distribution<int>(0, 1)(rng)) % 3;
        const double total = p[i] + p[j];
        p[i] = total * std::generate_canonical<double, 53>(rng);
        p[j] = total - p[i]; // 2成分の合計を保つ対称提案
    }
    double log_likelihood(const Particle& p, const Observation& counts) const {
        const int n = std::accumulate(counts.begin(), counts.end(), 0);
        double result = std::lgamma(n + 1.0);
        for (int j = 0; j < 3; ++j) {
            result -= std::lgamma(counts[j] + 1.0);
            if (counts[j] > 0) result += counts[j] * std::log(p[j]);
        }
        return result;
    }
};
// 入力: 独立に抽出した標本の種類別個数。出力: 3種類の比率の平均。
// 単純なDirichlet-Multinomial問題は厳密更新も可能。制約付き粒子のモデル化例。
std::optional<std::array<double, 3>> solve(const std::vector<std::array<int, 3>>& batches,
                                        int count = 1024, std::uint64_t seed = 0) {
    ParticleFilter<Model> filter(Model{}, {.count = count}, std::mt19937_64(seed));
    for (const auto& counts : batches) {
        for (int x : counts) assert(x >= 0);
        if (!filter.observe(counts).ok) return std::nullopt;
    }
    return filter.estimate().mean;
}
}
```

### 8.12 U12：制御入力・欠測のある動的状態

`initial_mean`と`initial_sd`は第1観測に含まれる移動より前の状態。`measured=false`ならx、yは使わないが、dt、ax、ayは使う。返す配列の並びは全て[x,y,vx,vy]である。

```cpp
#include "particle_filter_v09.hpp"
namespace case12 {
// 1行=dt時間の移動と、その移動後の位置測定。欠測でも1行を渡す。
struct Observation { double dt, ax, ay, x, y, sigma; bool measured; };
struct Model {
    using Particle = std::array<double, 4>; // [x,y,vx,vy]
    using Observation = case12::Observation;
    Particle initial_mean, initial_sd;
    double position_noise, velocity_noise; // 時間1あたりの遷移ノイズ標準偏差
    Particle sample(std::mt19937_64& rng) {
        Particle p = initial_mean;
        std::normal_distribution<double> normal;
        for (int j = 0; j < 4; ++j) p[j] += initial_sd[j] * normal(rng);
        return p;
    }
    void transition(Particle& p, const Observation& o, std::mt19937_64& rng) {
        std::normal_distribution<double> normal;
        const double dt = o.dt, root_dt = std::sqrt(dt);
        p[0] += p[2] * dt + 0.5 * o.ax * dt * dt + position_noise * root_dt * normal(rng);
        p[1] += p[3] * dt + 0.5 * o.ay * dt * dt + position_noise * root_dt * normal(rng);
        p[2] += o.ax * dt + velocity_noise * root_dt * normal(rng);
        p[3] += o.ay * dt + velocity_noise * root_dt * normal(rng);
    }
    double log_likelihood(const Particle& p, const Observation& o) const {
        if (!o.measured) return 0; // 欠測は追加情報なし。ただし遷移は既に実行済み。
        const double dx = o.x - p[0], dy = o.y - p[1];
        return -0.5 * (dx * dx + dy * dy) / (o.sigma * o.sigma)
             - std::log(2 * std::numbers::pi * o.sigma * o.sigma);
    }
};
// 出力: 各ターンの移動・観測後の[x,y,vx,vy]推定。initialは最初の移動より前。
std::optional<std::vector<std::array<double, 4>>> solve(
    const std::vector<Observation>& observations, std::array<double, 4> initial_mean,
    std::array<double, 4> initial_sd, double position_noise, double velocity_noise,
    int count = 1024, std::uint64_t seed = 0) {
    assert(position_noise >= 0 && velocity_noise >= 0);
    for (double s : initial_sd) assert(s >= 0);
    ParticleFilter<Model> filter(Model{initial_mean, initial_sd, position_noise, velocity_noise},
                                 {.count = count}, std::mt19937_64(seed));
    std::vector<std::array<double, 4>> trajectory;
    for (const auto& o : observations) {
        assert(o.dt >= 0 && (!o.measured || o.sigma > 0));
        if (!filter.observe(o).ok) return std::nullopt;
        trajectory.push_back(filter.estimate().mean);
    }
    return trajectory;
}
}
```

### 8.13 U13：観測に合わせた遷移提案

`process_variance`は分散、`sigma`は測定の標準偏差。提案補正と通常の観測尤度を分ける。提案分布の導出は第9.8節を参照する。

```cpp
#include "particle_filter_v09.hpp"
namespace case13 {
// 真の温度は「前温度+加熱量+正規過程ノイズ」。測定は移動後の温度に対するもの。
struct Observation { double heating, measured_temperature, sigma; bool measured; };
struct Model {
    using Particle = double;
    using Observation = case13::Observation;
    double initial_mean, initial_sd, process_variance;
    static double log_normal(double x, double mean, double variance) {
        return -0.5 * ((x - mean) * (x - mean) / variance + std::log(2 * std::numbers::pi * variance));
    }
    Particle sample(std::mt19937_64& rng) {
        return initial_mean + initial_sd * std::normal_distribution<double>{}(rng);
    }
    double propose_transition(Particle& candidate, const Particle& previous,
                              const Observation& o, std::mt19937_64& rng) {
        const double mean = previous + o.heating, q = process_variance;
        if (!o.measured) {
            candidate = mean + std::sqrt(q) * std::normal_distribution<double>{}(rng);
            return 0; // 真の遷移から生成したのでp/q=1
        }
        const double r = o.sigma * o.sigma;
        const double proposed_mean = mean + q / (q + r) * (o.measured_temperature - mean);
        const double proposed_variance = q * r / (q + r);
        candidate = proposed_mean + std::sqrt(proposed_variance) * std::normal_distribution<double>{}(rng);
        // 観測尤度をここへ足さない。ライブラリが次のlog_likelihoodを別途加える。
        return log_normal(candidate, mean, q) - log_normal(candidate, proposed_mean, proposed_variance);
    }
    double log_likelihood(Particle temperature, const Observation& o) const {
        return o.measured ? log_normal(o.measured_temperature, temperature, o.sigma * o.sigma) : 0;
    }
};
// 入力process_varianceは標準偏差ではなく分散Q>0。initial_sdは標準偏差。
// 出力: 各ターンの温度平均。狭い観測に合わせた提案を利用する発展例。
std::optional<std::vector<double>> solve(const std::vector<Observation>& observations,
                                       double initial_mean, double initial_sd, double process_variance,
                                       int count = 1024, std::uint64_t seed = 0) {
    assert(initial_sd >= 0 && process_variance > 0);
    ParticleFilter<Model> filter(Model{initial_mean, initial_sd, process_variance},
                                 {.count = count}, std::mt19937_64(seed));
    std::vector<double> estimates;
    for (const auto& o : observations) {
        assert(!o.measured || o.sigma > 0);
        if (!filter.observe(o).ok) return std::nullopt;
        estimates.push_back(filter.estimate().mean);
    }
    return estimates;
}
}
```

### 8.14 U14：打ち切り観測

`Kind::exact`は値の密度、`Kind::below`は値以下の確率、`Kind::above`は値以上の確率。連続誤差なので閾値ちょうどの確率は0であり、以上・超過の差はない。

```cpp
#include "particle_filter_v09.hpp"
namespace case14 {
struct Observation {
    enum class Kind { exact, below, above };
    Kind kind;
    double value; // exactは測定値、below/aboveは打ち切り閾値
    double sigma;
};
struct Model : UniformBox<1> {
    using Observation = case14::Observation;
    Model(double lower, double upper) : UniformBox({lower}, {upper}) {}
    double likelihood(const Particle& p, const Observation& o) const {
        const double z = (o.value - p[0]) / o.sigma;
        if (o.kind == Observation::Kind::below) return 0.5 * std::erfc(-z / std::sqrt(2.0));
        if (o.kind == Observation::Kind::above) return 0.5 * std::erfc(z / std::sqrt(2.0));
        return std::exp(-0.5 * z * z) / (o.sigma * std::sqrt(2 * std::numbers::pi));
    }
};
// 入力: 真の圧力muの事前範囲、各測定の値または「閾値以下/以上」という情報。
// 出力: muの平均。上限100への張り付きをexact=100に変換してはいけない。
std::optional<double> solve(const std::vector<Observation>& observations, double lower, double upper,
                            int count = 1024, std::uint64_t seed = 0) {
    ParticleFilter<Model> filter(Model(lower, upper), {.count = count}, std::mt19937_64(seed));
    for (const auto& o : observations) {
        assert(o.sigma > 0);
        if (!filter.observe(o).ok) return std::nullopt;
    }
    return filter.estimate().mean[0];
}
}
```

### 8.15 U15：通路配置を保った行動評価

`route_mask`を通路番号から作る。個別の開通確率を掛けるのではなく、同じ配置粒子で経路全体の通行可否を計算する。`failure_cost`には迂回や失敗のペナルティを含めて利用者が指定する。

```cpp
#include "particle_filter_v09.hpp"
namespace case15 {
// route_maskのbit j=1なら通路jを通る。全通路が開いていると経路は通行可能。
// successの表示が確率sensor_errorで反転する。通路状態は全期間一定。
struct Observation { std::uint32_t route_mask; bool success; };
struct Route { std::uint32_t route_mask; double success_cost, failure_cost; };
struct Model {
    using Particle = std::uint32_t; // bit j=1なら通路jは開いている
    using Observation = case15::Observation;
    int passages;
    double open_prior, sensor_error;
    Particle sample(std::mt19937_64& rng) {
        Particle p = 0;
        for (int j = 0; j < passages; ++j)
            if (std::bernoulli_distribution(open_prior)(rng)) p |= std::uint32_t{1} << j;
        return p;
    }
    double log_prior(Particle p) const {
        const int opened = std::popcount(p);
        return opened * std::log(open_prior) + (passages - opened) * std::log1p(-open_prior);
    }
    void propose(Particle& p, std::mt19937_64& rng) {
        p ^= std::uint32_t{1} << std::uniform_int_distribution<int>(0, passages - 1)(rng);
    }
    double likelihood(Particle p, const Observation& o) const {
        const bool passable = (p & o.route_mask) == o.route_mask;
        return passable == o.success ? 1 - sensor_error : sensor_error;
    }
};
struct Result {
    std::vector<double> open_probability; // 通路ごとの周辺確率
    std::vector<double> expected_cost;    // 通路間の相関も使って計算した候補経路の期待費用
    int selected_route;
};
// passagesは1..20程度を想定。先頭passagesビットだけを使う。
// 観測の反転率0<sensor_error<0.5、独立事前の開通率0<open_prior<1を指定。
std::optional<Result> solve(const std::vector<Observation>& observations, int passages,
                           double open_prior, double sensor_error, const std::vector<Route>& routes,
                           int count = 1024, std::uint64_t seed = 0) {
    assert(1 <= passages && passages <= 20 && !routes.empty());
    assert(0 < open_prior && open_prior < 1 && 0 < sensor_error && sensor_error < 0.5);
    const std::uint32_t allowed = (std::uint32_t{1} << passages) - 1;
    ParticleFilter<Model> filter(Model{passages, open_prior, sensor_error},
                                 {.count = count}, std::mt19937_64(seed));
    for (const auto& o : observations) {
        assert((o.route_mask & ~allowed) == 0);
        if (!filter.observe(o).ok) return std::nullopt;
    }
    for (const auto& r : routes) assert((r.route_mask & ~allowed) == 0);
    Result result{std::vector<double>(passages), std::vector<double>(routes.size()), 0};
    filter.for_each_weighted([&](std::uint32_t p, double weight) {
        for (int j = 0; j < passages; ++j) result.open_probability[j] += weight * ((p >> j) & 1);
        for (int r = 0; r < int(routes.size()); ++r) {
            const bool passable = (p & routes[r].route_mask) == routes[r].route_mask;
            result.expected_cost[r] += weight * (passable ? routes[r].success_cost : routes[r].failure_cost);
        }
    });
    result.selected_route = int(std::min_element(result.expected_cost.begin(), result.expected_cost.end()) - result.expected_cost.begin());
    return result;
}
}
```

### 8.16 U16：大きな温度分布の差分再探索

第4.16節の最高温度測定を扱う。`Particle::temperature`が仮説、`log_prior_cache`がその仮説の評価を速くする補助値である。キャッシュ自体を独立な未知量として推定するものではない。

各地点の正規事前に対する対数密度は、共通定数を除くと「各地点の標準化したずれの二乗和×-0.5」。1地点の変更では、その地点の変更前の寄与を除き、変更後の寄与を加えればよい。Undoには変更前の温度とキャッシュを両方残す。棄却時は計算を逆算せず、保存した値を直接戻す。

```cpp
#include "particle_filter_v09.hpp"
namespace case16 {
// regionは測定対象の地点番号。measuredは「領域内の最高温度」の測定値、sigmaは測定誤差の標準偏差。
// 各地点の温度は観測期間中一定。測定誤差は最高温度を求めた後に加わるとする。
struct Observation { std::vector<int> region; double measured, sigma; };
struct Model {
    struct Particle {
        std::vector<double> temperature;
        double log_prior_cache; // 温度に対応する対数事前密度。共通の定数だけ省く。
    };
    struct Undo { int cell; double temperature, log_prior_cache; };
    using Observation = case16::Observation;
    int cells;
    double prior_mean, prior_sd, step_scale;
    std::normal_distribution<double> normal;

    Model(int cells, double prior_mean, double prior_sd, double step_scale)
        : cells(cells), prior_mean(prior_mean), prior_sd(prior_sd), step_scale(step_scale) {
        assert(cells > 0 && prior_sd > 0 && step_scale > 0);
    }
    Particle sample(std::mt19937_64& rng) {
        Particle p{std::vector<double>(cells), 0};
        for (double& x : p.temperature) {
            x = prior_mean + prior_sd * normal(rng);
            const double z = (x - prior_mean) / prior_sd;
            p.log_prior_cache -= 0.5 * z * z;
        }
        return p;
    }
    double log_prior(const Particle& p) const { return p.log_prior_cache; }
    double log_likelihood(const Particle& p, const Observation& o) const {
        double highest = -std::numeric_limits<double>::infinity();
        for (int cell : o.region) highest = std::max(highest, p.temperature[cell]);
        const double z = (o.measured - highest) / o.sigma;
        return -0.5 * z * z - std::log(o.sigma) - 0.5 * std::log(2 * std::numbers::pi);
    }
    auto propose_in_place(Particle& p, std::mt19937_64& rng) {
        const int cell = std::uniform_int_distribution<int>(0, cells - 1)(rng);
        const Undo old{cell, p.temperature[cell], p.log_prior_cache};
        p.temperature[cell] += prior_sd * step_scale * normal(rng);
        const double before = (old.temperature - prior_mean) / prior_sd;
        const double after = (p.temperature[cell] - prior_mean) / prior_sd;
        p.log_prior_cache += 0.5 * (before * before - after * after);
        // 対称な候補生成なのでlog_correctionは既定値0。尤度や事前の差をここへ返さない。
        return ParticleCloud<Particle>::MoveProposal<Undo>{old};
    }
    void undo(Particle& p, const Undo& old) {
        p.temperature[old.cell] = old.temperature;
        p.log_prior_cache = old.log_prior_cache; // 棄却時にはキャッシュも元の値へ戻す。
    }
    const std::vector<double>& project(const Particle& p) const { return p.temperature; }
};
// 入力: 地点数、各領域の観測、各地点で独立な正規事前の平均と標準偏差。
// step_scaleは候補の移動幅/事前標準偏差。extra_movesは全観測後の追加再探索回数。
// 出力: 地点ごとの温度の事後平均。最高温度の平均が欲しい場合は、各粒子でmaxを取ってから平均する。
std::optional<std::vector<double>> solve(
    int cells, const std::vector<Observation>& observations, double prior_mean, double prior_sd,
    double step_scale = 0.4, int extra_moves = 4, int count = 1024, std::uint64_t seed = 0) {
    assert(extra_moves >= 0);
    ParticleFilter<Model> filter(Model(cells, prior_mean, prior_sd, step_scale),
                                 {.count = count}, std::mt19937_64(seed));
    for (const auto& o : observations) {
        assert(!o.region.empty() && o.sigma > 0);
        for (int cell : o.region) assert(0 <= cell && cell < cells);
        if (!filter.observe(o).ok) return std::nullopt;
    }
    filter.refine(extra_moves);
    std::vector<double> output(cells);
    filter.estimate_into(output);
    return output;
}
}
```

この例の`step_scale`は温度を動かす幅だけを決め、観測誤差sigmaとは無関係である。温度の尤度は毎回regionの最大を計算するため、最大値の計算や過去の全領域の評価まで自動で差分化されることはない。

### 8.17 固定履歴の評価を十分統計量で高速化する

これは新しい観測形式ではなく、固定パラメータ問題の履歴評価を速くする追加例である。一定のバイアスmuを共通の既知sigmaで測定する場合に使う。muの候補ごとに履歴を走査する代わりに、末尾の観測へ保存した和・二乗和・件数から評価する。

観測の確定前に外部の累積値を進めず、成功後にだけ確定する。新しい推定器の最初の観測から件数と累積値を作り直す。コピーした分岐に追加する場合は、その分岐の履歴末尾の累積値から続きを作る。

```cpp
#include "particle_filter_v09.hpp"
namespace history_example {
// 一定バイアスmuを、既知で共通の標準偏差sigmaの正規測定から推定する。
// 観測には、今回までのsum(y),sum(y*y),件数を含める。1件尤度はyだけを使う。
struct Observation { double y, sum, square_sum; int count; };
struct Model : UniformBox<1> {
    using Observation = history_example::Observation;
    double sigma;
    Model(double lower, double upper, double sd) : UniformBox({lower}, {upper}), sigma(sd) {}
    double log_likelihood(const Particle& p, const Observation& o) const {
        const double z = (o.y - p[0]) / sigma;
        return -0.5 * z * z - std::log(sigma) - 0.5 * std::log(2 * std::numbers::pi);
    }
    double log_likelihood_history(const Particle& p, std::span<const Observation> history) const {
        if (history.empty()) return 0;
        const auto& last = history.back();
        const double sse = last.square_sum - 2 * p[0] * last.sum + last.count * p[0] * p[0];
        return -sse / (2 * sigma * sigma) - last.count * (std::log(sigma) + 0.5 * std::log(2 * std::numbers::pi));
    }
};
// 出力はmuの平均。値のスケールは通常のAHC程度を想定。大きい共通オフセットは先に引く。
std::optional<double> solve(const std::vector<double>& measurements, double lower, double upper,
                            double sigma, int count = 1024, std::uint64_t seed = 0) {
    assert(sigma > 0);
    ParticleFilter<Model> filter(Model(lower, upper, sigma), {.count = count}, std::mt19937_64(seed));
    double sum = 0, squares = 0;
    int accepted = 0;
    for (double y : measurements) {
        const Observation next{y, sum + y, squares + y * y, accepted + 1};
        if (!filter.observe(next).ok) return std::nullopt;
        sum = next.sum; squares = next.square_sum; ++accepted;
    }
    return filter.estimate().mean[0];
}
}
```

### 8.18 追加探索・計算量変更・仮想観測の分岐

この例はU01のModelをそのまま使う。仮想観測を追加した分岐は元の推定器を変更しない。仮想観測を生成する方法や、その結果から測定行動を選ぶ計画処理までは含めていない。

```cpp
// 8.1のcase01も一緒に使う例。モデルの式は同じで、継続・分岐・計算量調整を示す。
namespace workflow_example {
struct Result { std::array<double, 2> actual, hypothetical; bool branch_ok; };
std::optional<Result> solve(const std::vector<case01::Observation>& actual_observations,
                           const case01::Observation& hypothetical_observation) {
    ParticleFilter<case01::Model> filter(case01::Model({-3, -3}, {3, 3}));
    for (const auto& o : actual_observations) {
        assert(o.sigma > 0);
        if (!filter.observe(o).ok) return std::nullopt;
    }
    auto parameters = filter.param();
    parameters.move_steps = 1; // 今後の計算量を減らす。これだけなら粒子は変わらない。
    filter.set_param(parameters);
    filter.refine(2); // 同じ観測を再投入せず、今までの観測で追加探索する。
    const auto actual = filter.estimate().mean;
    auto branch = filter; // この先の仮観測は元のfilterへ反映しない。
    parameters.count = 512; // 分岐側だけ粒子数を減らす。即時再サンプリングされる。
    branch.set_param(parameters);
    assert(hypothetical_observation.sigma > 0);
    const bool ok = branch.observe(hypothetical_observation).ok;
    return Result{actual, branch.estimate().mean, ok};
}
}
```

### 8.19 U15の応用：重み付き標本で候補経路を比較する

第8.15節の`case15`も一緒にコピーする。粒子が表す通路配置と観測の尤度はU15と同じである。違うのは、観測後の経路費用を、現在の分布から取ったM個の標本で概算する点である。

`evaluate`は既に観測を取り込んだfilterを受け取り、粒子群を保ったまま候補経路を評価する。`solve`は初期化からまとめて使う場合の関数である。実際のターン制問題では、filterとplanning_rngをターンの外で保持して`evaluate`を使う。

```cpp
#include "particle_filter_v09.hpp"
// case15のModel、Observation、Routeも一緒に使う。
namespace sampled_routes {
struct Result { std::vector<double> expected_cost; int selected_route; };
// 入力: 観測済みのfilter、同じ通路番号を使う非空の候補経路、正の標本数、行動評価専用RNG。
// 出力: 現在の粒子分布から復元抽出した標本による平均費用と、最小費用の候補番号。
// filterは変更しない。返った後の次ターンも同じfilterへobserveできる。
Result evaluate(const ParticleFilter<case15::Model>& filter, const std::vector<case15::Route>& routes,
                int samples, std::mt19937_64& planning_rng) {
    assert(!routes.empty() && samples > 0);
    std::vector<int> indices(samples);
    filter.sample_indices(indices, planning_rng);
    const auto particles = filter.particles();
    Result result{std::vector<double>(routes.size()), 0};
    // 全候補を同じ標本で比較する。重複インデックスもそのまま使う。
    for (int index : indices) {
        const std::uint32_t map = particles[index];
        for (int r = 0; r < int(routes.size()); ++r) {
            const bool passable = (map & routes[r].route_mask) == routes[r].route_mask;
            result.expected_cost[r] += (passable ? routes[r].success_cost : routes[r].failure_cost) / samples;
        }
    }
    // 元のweights()[index]を再び掛けない。抽出頻度に重みが既に反映されている。
    result.selected_route = int(std::min_element(result.expected_cost.begin(), result.expected_cost.end())
                                - result.expected_cost.begin());
    return result;
}
// U15と同じ観測・事前・経路を指定する。samplesは粒子数countと独立に設定する。
// planning_seedは行動評価用のseedで、推定用のseedと役割を分ける。
std::optional<Result> solve(
    const std::vector<case15::Observation>& observations, int passages, double open_prior, double sensor_error,
    const std::vector<case15::Route>& routes, int samples = 64, int count = 1024,
    std::uint64_t seed = 0, std::uint64_t planning_seed = 100003) {
    assert(1 <= passages && passages <= 20 && !routes.empty() && samples > 0);
    assert(0 < open_prior && open_prior < 1 && 0 < sensor_error && sensor_error < 0.5);
    const std::uint32_t allowed = (std::uint32_t{1} << passages) - 1;
    ParticleFilter<case15::Model> filter(case15::Model{passages, open_prior, sensor_error},
                                         {.count = count}, std::mt19937_64(seed));
    for (const auto& o : observations) {
        assert((o.route_mask & ~allowed) == 0);
        if (!filter.observe(o).ok) return std::nullopt;
    }
    for (const auto& route : routes) assert((route.route_mask & ~allowed) == 0);
    std::mt19937_64 planning_rng(planning_seed);
    return evaluate(filter, routes, samples, planning_rng);
}
}
```

この例の通路判定は軽いので、常に全粒子評価より速いとは限らない。1粒子に対する行動評価が高価な場合に、標本数を減らす意味が大きくなる。実際の経路選択を変えれば将来に得る観測も変わるため、推定用RNGが保たれることと、最終的な行動列が不変であることは別である。

### 8.20 U02の応用：出力領域を再利用し、毎ターン違う経路を選ぶ

第8.2節の`case02`も一緒にコピーする。単位時間が同じ意味を持つ限り、毎ターンの観測経路と次の候補経路を変えてよい。既知の地図から地域別の距離へ変換するところは利用者側で行う。

目的が期待時間だけなら、所要時間は地域コストの線形式なので、コストの平均と距離の内積から同じ期待時間を計算できる。締切超過確率のような非線形な量にはこの置き換えを使わず、第8.2節のように粒子ごとに判定する。

```cpp
#include "particle_filter_v09.hpp"
// case02のModel、Observation、Routeも一緒に使う。
namespace streaming_routes {
struct Turn {
    case02::Observation observation; // 今回走った経路の測定。各turnは新しい観測を1件含む。
    std::vector<case02::Route> candidates; // 観測後に比較する次の経路。毎ターン変更してよい。
};
struct Decision {
    std::vector<double> expected_time; // このターンの各候補の期待所要時間
    int selected_route;
    double selected_mass; // top_kを指定した場合の元の重みの合計。全粒子なら1。
};
// 入力: U02と同じ地域別時間の事前範囲。目的は期待時間だけの最小化。
// top_k=0が全粒子。上位kを使った結果は全分布の期待値とは違う。
// 出力: ターンごとの選択。地域コストの平均用バッファはturnをまたいで使い回す。
std::optional<std::vector<Decision>> solve(
    const std::vector<Turn>& turns, std::vector<double> lower, std::vector<double> upper,
    int top_k = 0, int count = 1024, std::uint64_t seed = 0) {
    assert(0 <= top_k && top_k <= count);
    const std::size_t groups = lower.size();
    ParticleFilter<case02::Model> filter(case02::Model(std::move(lower), std::move(upper)),
                                         {.count = count}, std::mt19937_64(seed));
    std::vector<double> mean(groups); // reserveだけでは不足。出力先の要素数を先に決める。
    std::vector<Decision> result;
    for (const auto& turn : turns) {
        assert(turn.observation.length.size() == groups && turn.observation.sigma > 0);
        assert(!turn.candidates.empty());
        if (!filter.observe(turn.observation).ok) return std::nullopt;
        const double mass = filter.estimate_into(mean, top_k); // ゼロクリアも含めて上書きされる。
        Decision decision{std::vector<double>(turn.candidates.size()), 0, mass};
        for (int r = 0; r < int(turn.candidates.size()); ++r) {
            const auto& route = turn.candidates[r];
            assert(route.length.size() == groups);
            decision.expected_time[r] = route.fixed
                + std::inner_product(mean.begin(), mean.end(), route.length.begin(), 0.0);
        }
        decision.selected_route = int(std::min_element(decision.expected_time.begin(), decision.expected_time.end())
                                       - decision.expected_time.begin());
        result.push_back(std::move(decision));
    }
    return result;
}
}
```

上位kの選択番号が再利用されるのは重みとkが変わらない区間であり、毎ターンの観測後まで同じ選択を維持するわけではない。この例でターンをまたいで使い回しているのは、平均を書き込むdouble列の領域である。

### 8.21 U06の応用：全種類を外部重み付きで直接登録する

鉱石の種類が少なく、全種類を保持できる場合の例である。`ParticleCloud`へ種類番号を1個ずつ登録し、事前重みの対数を渡す。観測後も種類を間引く必要はないため、再サンプリングや再探索を呼ばず、各観測で重みだけ更新する。

この有限分布に対しては粒子の初期抽出による誤差がなく、浮動小数点の範囲で直接ベイズ更新を行う。通常の`ParticleFilter`には外部粒子を代入するAPIがないので、下位APIを直接使う具体例でもある。

```cpp
#include "particle_filter_v09.hpp"
namespace finite_types {
struct Result { std::vector<double> probability; int selected_type; double log_predictive_sum; };
// U06と同じ問題。全種類を1個ずつ登録し、再サンプリングせず有限分布を直接更新する。
// prior_weightは非負、総和が正。emission[type][symbol]は表示確率で、各行の和は1。
// 出力は種類別確率、最多の種類、全観測列の確率の対数。更新失敗ならnullopt。
std::optional<Result> solve(const std::vector<int>& symbols, const std::vector<double>& prior_weight,
                           const std::vector<std::vector<double>>& emission) {
    const int types = int(prior_weight.size());
    assert(types > 0 && int(emission.size()) == types && !emission[0].empty());
    assert(std::accumulate(prior_weight.begin(), prior_weight.end(), 0.0) > 0);
    const int alphabet = int(emission[0].size());
    std::vector<int> particles(types);
    std::iota(particles.begin(), particles.end(), 0);
    std::vector<double> log_weights(types);
    for (int i = 0; i < types; ++i) {
        assert(prior_weight[i] >= 0 && int(emission[i].size()) == alphabet);
        assert(std::abs(std::accumulate(emission[i].begin(), emission[i].end(), 0.0) - 1) < 1e-9);
        for (double p : emission[i]) assert(p >= 0);
        log_weights[i] = prior_weight[i] > 0 ? std::log(prior_weight[i])
                                           : -std::numeric_limits<double>::infinity();
    }
    ParticleCloud<int> cloud(0);
    const bool initialized = cloud.assign(std::move(particles), log_weights);
    assert(initialized); // assignの呼び出し自体をassert内へ入れない。
    (void)initialized;
    double evidence = 0;
    for (int symbol : symbols) {
        assert(0 <= symbol && symbol < alphabet);
        const auto update = cloud.update_log([&](const int& type) {
            const double p = emission[type][symbol];
            return p > 0 ? std::log(p) : -std::numeric_limits<double>::infinity();
        });
        if (!update.ok) return std::nullopt;
        evidence += update.log_normalizer;
    }
    const auto weights = cloud.weights();
    std::vector<double> probability(weights.begin(), weights.end());
    const int selected = int(std::max_element(probability.begin(), probability.end()) - probability.begin());
    return Result{std::move(probability), selected, evidence};
}
}
```

ここでは種類を途中で増減させない。`ParticleCloud::assign`で粒子を置き換える場合は、その重みがどの観測まで含んでいるかも利用者側で管理する。`ParticleCloud`自体は履歴を保持しない。

### 8.22 コードの確認方法

```bash
g++ -std=c++20 -O2 -Wall -Wextra -Wpedantic -DPARTICLE_FILTER_USAGE_SELF_TEST particle_filter_usage_examples_v09.cpp -o usage_check
./usage_check
```

検証では、通常の推定シナリオに加え、離散仮説の全列挙、Beta・Dirichletの厳密な事後平均、線形正規追跡の解析的な平均、丸め確率の総和、提案補正の恒等式、履歴の直接評価との一致を確認している。差分提案とコピー提案の全粒子・重みの一致、Undoと事前キャッシュの整合、標本の集計と全粒子集計、抽出後の推定用乱数列、出力バッファでの経路費用、0の事前確率と更新失敗も確認する。乱数seed 0、1、17の計3,618項目。数値推定のチェックには粒子近似と生成データのばらつきを踏まえた許容幅を使う。

Markdown内の25個のC++ブロックは、依存するModelを補った22個の翻訳単位として抽出・コンパイルする。冒頭の準備・短いAPI例も実行し、関数形式の21例は上の自己テストで呼び出す。コードを変更して使う場合も、掲載例の前提に合う入力から確認するとよい。

```bash
python3 particle_filter_usage_work_v09/verify_snippets.py
g++ -std=c++20 -O1 -g -fsanitize=address,undefined -fno-omit-frame-pointer -DPARTICLE_FILTER_USAGE_SELF_TEST particle_filter_usage_examples_v09.cpp -o usage_sanitized
ASAN_OPTIONS=detect_leaks=0 ./usage_sanitized
```

通常の実行とASan・UBSanで同じチェックを行う。提供時の実行環境ではLeakSanitizerを無効にしており、リーク検査は含めない。通常の環境でリークも確認できる場合は、その環境の設定に合わせて実行する。

動作確認は、あらゆるAHCの入力に対する精度を保証するものではない。新しい問題に使う際は、既知の真値や小さな厳密解がある入力で、自分の尤度と入出力の意味を最初に確認する。

<a id="chapter-9"></a>

## 9. 実装

### 9.1 全体像

本ライブラリは、有限個の仮説とその重みで事後分布を近似する粒子法を使う。高水準の`ParticleFilter`が観測処理と履歴を管理し、その中で`ParticleCloud`が粒子列と正規化済み重みを管理する。`UniformBox`は実数の一様事前分布と候補生成を提供する。

初期化時には事前分布からN粒子を生成し、それぞれの重みを1/Nにする。Nは`Param::count`である。

固定パラメータの1回の`observe`は、概ね次の順になる。

1. **前回までの重みを見る。** ESSが閾値未満か、定期再探索の時期なら、準備用の粒子群を再サンプリングする。
2. **準備した仮説を再探索する。** 過去の観測を使うMetropolis–Hastings法を指定回数行う。
3. **今回の観測を取り込む。** 通常の重み更新、または段階的な更新を行う。段階更新の途中でも再サンプリングと再探索を行い得る。
4. **成功時だけ確定する。** 新しい粒子群と重みを採用し、今回の観測を履歴へ追加し、成功観測数を増やす。

動的モデルでは、前回の重みによる準備の後、各粒子を今回の次状態へ遷移させ、その状態の観測尤度で重みを更新する。固定モデルの履歴再探索やテンパリングは使わない。遷移も候補上で行い、更新に成功したときだけ現在状態として採用する。

### 9.2 重みの更新と対数正規化

前の正規化済み重みを$w_i$、今回の尤度を$L_i$とする。iは0からN-1までの粒子番号。通常の重み更新は次である。

$$w_i'=\frac{w_iL_i}{\sum_{j=0}^{N-1}w_jL_j}$$

分子は「それまでの重み×今回の観測の起こりやすさ」、分母はその全粒子合計である。ユーザー関数に必要なのが今回の尤度だけなのは、この掛け算をライブラリ側が行うからである。

小さい確率の積を直接持つと0へ丸まりやすいため、内部では対数重みも保持する。$\ell_i=\log w_i+\log L_i$、$m=\max_i\ell_i$と置き、次の量を計算する。

$$S=\sum_{i=0}^{N-1}\exp(\ell_i-m)$$

$$\log w_i'=\ell_i-m-\log S$$

ここでmは指数関数の入力を大きくしすぎないために引く最大値、Sは引いた後の指数値の和。通常の重みは同じ指数計算結果を使って作る。対数正規化量は$m+\log S$となり、通常の観測更新で`log_predictive`へ使われる。

全ての候補の対数重みが負の無限大なら正規化できないので失敗する。正の無限大やNaNは失敗結果として扱わず、`assert`で契約違反を確認する。

通常重みが浮動小数点で0へ丸まっても、対数重みが有限なら、その粒子は以後の相対重み更新や再探索で扱われる余地が残る。再サンプリングで選ばれず実際に粒子が消えた場合は別である。

### 9.3 ESSとSystematic再サンプリング

正規化済み重みに対して、ESSは次の式で計算する。

$$\mathrm{ESS}=\frac{1}{\sum_{i=0}^{N-1}w_i^2}$$

全粒子の重みが1/NならESSはN、1粒子だけに集中すれば1になる。ただし、同じ値の粒子がN個あっても、等重みならESSはNになる。分布の広がりや仮説の種類数を直接数える指標ではない。

再サンプリングは、重みの大きい粒子を多く残し、小さい粒子を減らす操作である。Systematic法では、重みを累積した0〜1の区間に、等間隔のN個の点を並べ、最初のずれだけ乱数で決める。各点が入る区間の粒子を親にする。

新粒子数を$N'$、一様乱数を$r\in[0,1)$とすると、参照する点は次である。

$$u_j=\frac{j+r}{N'},(0\le j<N')$$

元粒子を累積重みの順に1回走査するため、親選択はO(N+N')。選んだ粒子のコピー費用がこれに加わる。新しい重みは全て1/N'になる。元粒子群を保ったまま準備用粒子群へ再サンプリングする経路もある。

### 9.4 固定パラメータのMetropolis–Hastings再探索

再サンプリングは既存の仮説の複製なので、それだけでは新しい仮説を作れない。固定モデルでは、全履歴を説明する別の仮説を提案する再探索を組み合わせる。

現在の仮説を$\theta$、候補を$\theta'$、正規化前の事後密度を$T(\theta)$、提案密度を$q(\theta'\mid\theta)$とする。候補を受理する確率は次である。

$$\alpha=\min\left(1,\frac{T(\theta')q(\theta\mid\theta')}{T(\theta)q(\theta'\mid\theta)}\right)$$

Tには事前と過去の全尤度が含まれる。分布の正規化定数は比で相殺するため不要。実装はこの比の対数と、一様乱数の対数を比較する。候補が棄却されたら元の粒子を維持する。

`propose`も`propose_in_place`もないModelでは、事前分布から独立に候補を生成する。提案密度が事前密度と一致するので、その比が相殺し、累積尤度だけを使った比較でよい。これにより、`sample`と観測尤度だけでも固定モデルを使える。

各粒子には現在の対象値をキャッシュする。観測追加では今回分を加算し、新しい候補を作るときにその候補の全履歴を評価する。`refine`は現在の重みを変えず、同じ対象分布を保つこの更新を粒子へ適用する。

コピー方式の再探索は、元粒子を候補へコピーし、受理時に入れ替える。差分方式は現在粒子に直接提案を適用し、棄却時にUndoを呼ぶ。どちらも同じ対数受理比を評価する。差分方式でも、提案後の全履歴の評価は必要である。

高水準の`ParticleFilter`は、粒子に対する利用者のUndoに加え、ライブラリ内部の変更前の対象値も保存する。棄却時には双方を復元する。テンパリング中は、過去の全観測と今回尤度のbeta乗を合わせた対象で候補を評価する。この途中段階も利用者のUndoで扱えるよう、粒子とキャッシュの復元を完結させる。

### 9.5 `UniformBox`の提案

`UniformBox`の事前は、各成分の指定区間で独立な一様分布である。局所提案では、ランダムに選んだ座標を正規乱数でずらす操作を`min(4,D)`回行う。同じ座標を複数回選ぶ場合もある。

区間をはみ出した成分は境界で反射させる。単純なclampで端へ押しつける方法と違い、この反射は提案の対称性を保つ。したがって利用者が対数提案比を書かなくてもよい。

さらに確率0.1で、全座標をそれぞれの区間中心に対して反転する。成分jの下限を$l_j$、上限を$h_j$とすると、次の候補になる。

$$\theta_j'=l_j+h_j-\theta_j$$

この操作は2回行うと元に戻る対称な提案で、体積も保つ。離れた候補へ移る機会を作るが、反転先を常に採用するわけではない。実際の事前と全観測を使って受理・棄却する。全ての複数ピークの問題で効率が良いとは限らない。

### 9.6 尤度を段階的に取り込むテンパリング

非常に情報量の強い観測を一度に掛けると、一部の粒子だけが残り、重要な領域を探索する前に候補が減ることがある。固定モデルでは、今回の尤度を0乗から1乗まで少しずつ強くする。

前回までの対象分布を$\pi_{t-1}$、今回の尤度を$L_t$とすると、中間の分布は次の形である。

$$\pi_{t,\beta}(\theta)\propto\pi_{t-1}(\theta)L_t(\theta)^\beta,(0\le\beta\le1)$$

betaは今回の情報をどこまで反映したかを示す割合。0ならまだ反映せず、1なら全部反映した状態。重みを強めた後に再サンプリングと再探索を行い、観測に合う候補を増やす。

実装では、残りを全部取り込んだ場合のESSを評価する。低すぎる場合、ESS目標に近づく増分を16回の二分探索で求める。進行量には最小値0.0001を設け、最大12段階で最後の残りを全て取り込む。いずれも内部の固定値であり、公開Paramではない。

各段階で使う尤度の指数の合計は必ず1になる。観測を弱くしたまま終えたり、同じ観測を何回も余分に掛けたりする機能ではない。目標ESSの維持より、有限回で今回の観測を取り込み終えることを優先する最後の段階がある。

元の粒子群への退避コピーは、最初の部分更新が必要になるまで遅延する。1回で全部取り込める観測や、最初に全無効と分かる観測では、そのためのコピーを避ける。

### 9.7 動的モデルの遷移と観測更新

通常の`transition`では、元の状態を候補へコピーし、利用者が宣言した真の状態変化を適用する。この真の遷移から候補を生成するので、遷移密度と提案密度の比は1であり、追加の補正を利用者が計算する必要はない。

その後、候補状態の今回の尤度を計算し、元の重みへ掛ける。全粒子の候補を作ってから、正規化に成功した場合だけ候補群を現在状態へ採用する。これにより、失敗した観測で位置だけが進んでしまうことを避ける。

固定モデルにおける「同じパラメータで過去の観測を評価し直す」方法は、変動する現在状態へそのまま適用できない。そのため、通常APIでは動的モデルの`refine`や固定履歴を提供しない。必要な過去情報は状態に含め、現在状態の遷移として表す。

### 9.8 観測誘導提案とU13の式

真の遷移と違う分布qから次状態を生成する場合は、重要度の補正を加える。元の重みを$w_i$、次状態を$x_i'$とすると、正規化前の重みは次になる。

$$w_i'\propto w_i\frac{f(x_i'\mid x_i,u_t)}{q(x_i'\mid x_i,o_t)}L(y_t\mid x_i',u_t)$$

ユーザーの`propose_transition`は、候補を作り、この式のf/qの対数だけを返す。観測のLは通常の尤度関数が返す。候補は呼び出し前には元粒子のコピーであり、元粒子は別のconst参照として渡される。

U13では、前の温度に加熱量を足した値をm、過程ノイズの分散をQ、測定の分散をR、測定温度をyとする。真の次温度xに対し、遷移は平均m・分散Qの正規分布、測定yは平均x・分散Rの正規分布である。

この二つを掛け合わせ、同じ親粒子を固定したときのxの条件付き分布を作ると、次の平均と分散になる。

$$m_q=m+\frac{Q}{Q+R}(y-m)$$

$$V_q=\frac{QR}{Q+R}$$

ここで$m_q$は提案する温度の中心、$V_q$は提案分布の分散。測定が高精度でRが小さいほど中心はyへ寄り、分散も狭くなる。コードはこの分布から候補を作り、「平均m・分散Qでの候補密度」と「平均$m_q$・分散$V_q$での候補密度」の対数差を返す。

提案が観測に寄っていても補正があるため、観測と合う位置へ無条件に粒子を貼り付ける操作にはならない。また、親粒子が違えば今回の観測の説明しやすさも違うため、全粒子の最終重みが必ず等しくなるわけではない。

この式を使うにはQ>0、R>0が必要。欠測では真の遷移から生成し、補正0、対数尤度0とする。任意の非線形モデルに同じ式を適用できるわけではなく、その場合は別の提案と正しい密度比が必要である。

### 9.9 代表値・上位kの実装

全粒子平均は、各粒子を射影して重み付きで足す。上位1を初めて求めるときは対数重みの最大を1回の走査で探す。その他の上位kの初回はインデックス配列へ`nth_element`を使い、全粒子を完全にソートする費用を避ける。

直近のkと選択番号を記録し、重みとkが同じなら選択処理を省く。複数のkの結果を全て保存する方式ではない。重みや粒子数・順序を更新する操作では必要に応じて無効化し、粒子の値だけを変える再探索では選択番号を再利用できる。射影と加算は各呼び出しで実行する。

選んだ集合の元の重みを合計して`selected_mass`を求め、その質量で割った重みを使って平均する。重みの比較は通常の重みではなく対数重みで行い、極小値が0へ丸まったときの順序を残す。同点なら元インデックス順を優先する。

数値列の射影結果を毎回コピーせず、その呼び出し中の参照を使う。平均はdoubleで蓄積する。任意の構造体の加算規則をライブラリへ組み込まず、数値への射影とコールバックを入口にすることで、カテゴリ確率、方向、行動費用などを表現できる。

`estimate`と`estimate_into`は共通の集計処理を使う。前者は戻り値の平均を作り、後者は受け取った連続double領域をspanとして0クリアしてから集計する。バッファの再利用は出力のメモリ確保を減らす機能であり、尤度評価や数値列の全成分への加算が省けるわけではない。

### 9.10 履歴評価の高速化とメモリ

一定のmuを標準偏差sigmaで測る例では、測定値の和を$S_1=\sum_s y_s$、二乗和を$S_2=\sum_s y_s^2$、件数をnと置くと、次の式が成り立つ。

$$\sum_{s=1}^{n}(y_s-\mu)^2=S_2-2\mu S_1+n\mu^2$$

したがって全履歴の対数尤度は次で計算できる。

$$\log L_{1:n}(\mu)=-\frac{S_2-2\mu S_1+n\mu^2}{2\sigma^2}-n\log\sigma-\frac{n}{2}\log(2\pi)$$

$L_{1:n}$は1〜n件目の尤度の積。履歴を走査せずに候補muを評価できる。これは、その観測モデルで全履歴の情報をこの3値へまとめられるから成立する。任意の非線形観測へ同じ式を使うことはできない。

`ParticleCloud`は粒子列、コピー先、対数重み、通常重み、計算用配列、上位k用のインデックス配列を再利用する。コピーでは実際の粒子と重みを複製し、作業用配列は複製しない。代入先が既に持つ容量は再利用する。

`ParticleFilter`はこれに加えて準備用粒子群、今回の尤度増分、固定モデルの観測履歴などを持つ。高水準のコピー構築は準備用粒子群と尤度増分を複製せず、モデル・設定・乱数器・現在分布・固定履歴・成功観測数を複製する。コピー代入は代入先の準備用粒子群と尤度増分の領域を保持する。上位kの選択番号と抽出用の累積重みは、コピー先で必要になったときに作り直す。従って、全体が粒子列1本分のメモリで動くわけではない。粒子自身に大きなvectorを持つ場合、そのコピー・確保費用も考える。

### 9.11 粒子を保ったままの重み付き抽出

`sample_indices`は、現在の正規化済み重みから累積重みの列を作る。各標本ごとに独立な一様乱数を1個生成し、累積重みのどの区間に入るかを二分探索で調べる。Systematic再サンプリングのような等間隔の点列ではなく、各標本は独立した復元抽出である。

0重みの粒子を選ばないよう`upper_bound`で区間を探す。浮動小数点の丸めに備えて累積値の実際の総和を使い、探索位置が終端へ到達しないよう調整する。返すのは粒子のコピーではなくインデックスである。

累積重みは必要なときだけ作り、同じ重みの間は再利用する。重み更新用の一時配列と領域を共用するため、更新失敗で元の分布が残る場合でも、更新で一時配列が上書きされたら累積重みを作り直す。これはライブラリ内部の管理であり、利用者が無効化を呼ぶ必要はない。

### 9.12 おおよその計算量

Nを粒子数、Tを保存した観測数、Mを再探索の指定回数、Cを1粒子のコピー費用、Eを1件の尤度評価費用、Hを候補1個の全履歴評価費用、Pを提案生成費用、Aを集計の射影・加算費用、Kを行動評価用の標本数、Uを差分の巻き戻し費用、Dを射影した数値列の長さとする。

| 処理 | おおよその費用 |
|---|---|
| 初期化 | O(N × 事前生成費用) |
| 単純な固定観測の更新 | O(N × E) |
| 動的観測の更新 | O(N × (C + 遷移費用 + E)) |
| 同数への再サンプリング | O(N × (1 + C)) |
| 固定再探索・コピー方式 | M>0ならO(N × M × (C + P + H))。ライブラリ内の現在値はキャッシュされる |
| 固定再探索・差分方式 | M>0ならO(N × M × (P + H + U))。Uは棄却時だけ必要。M=0は両方式ともO(1) |
| 直接の全履歴評価 | H = O(T × E) |
| 十分統計量の例 | H = O(1)。統計量の維持費用は別途必要 |
| 段階更新 | 各段階の尤度評価・重み計算・ESS探索・必要な再探索の合計。最大12段階 |
| 全粒子平均 | O(N × A) |
| 上位kの初回平均 | 平均O(N + k × A)。上位1の初回はO(N + A) |
| 同じ重み・kでの上位k平均 | O(k × A)。選択質量はその都度合計。選択番号を再利用 |
| `estimate_into` | 選択・集計の費用は上記と同じ。先頭の出力クリアがO(D)、出力の新規確保は不要 |
| 重み付き抽出の初回 | O(N + K log N)。累積重みの構築とK回の二分探索 |
| 同じ重みからの再抽出 | O(K log N)。K=0はO(1) |
| 推定器の分岐コピー | 現在粒子・重み・固定履歴・Model・RNG・設定のコピー費用。準備用の粒子群や一時配列は複製しない |

定数倍は、尤度の中でグラフ探索や重いシミュレーションをするかどうかで大きく変わる。AHCでは、推定器の内部設定だけでなく、観測時点の既知条件を先に計算することや、候補評価のための十分統計量を作ることが実用上の改善になり得る。こうした前計算でも、過去と今回の観測を二重に取り込まないこと、尤度の意味を変えないことを保つ。
