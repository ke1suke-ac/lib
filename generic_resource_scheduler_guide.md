# Generic Resource Scheduler v07 ユーザーガイド

対象ヘッダ：`generic_resource_scheduler_v07.hpp`。C++20・GCC向けの、資源制約付きスケジューリングを扱う1ヘッダライブラリである。

第1〜4章で適用できる問題を判断し、第5章で入力を準備する。第6章の呼出し例と第8章の関数定義は、同じユースケース番号で対応している。第7章に適用限界、第9章に実装の説明をまとめる。コード例の関数は、コンテスト固有の入力をライブラリへ変換するための例であり、ヘッダに収録された公開関数とは区別する。

## 1. 何を解くライブラリか？

### 1.1 「何を、いつ、どの方法で実行するか」を決める

例えば、複数の製品を作る工場を考える。各製品には加工・検査・梱包があり、加工が終わるまで検査を始められない。一方、検査員や加工機は複数の製品で共有するため、同時に使える数にも上限がある。さらに、ある工程には「時間は短いが人手と費用が多い方法」と「時間は長いが省人化できる方法」があるかもしれない。

本ライブラリは、このような問題を次の要素で受け取る。

| 要素 | 意味 | 具体例 |
|---|---|---|
| タスク | 必ず1回実行する、中断できない仕事 | 1回の加工、1つの検査、1つの計算ジョブ |
| 資源 | 同時利用量に上限があり、仕事の終了後に再利用できるもの | 機械、作業員、計算枠、実行中だけ占有するメモリ |
| 先行制約 | 前のタスクが完了するまで次を開始できない関係 | 加工→検査、データ生成→データ利用 |
| モード | そのタスクを実行する方法の選択肢 | 機械Aで5分、機械Bで3分、2人で4分、4人で2分 |
| 開始可能時刻 | それ以前には開始できない時刻 | 材料到着時刻、ジョブ投入時刻 |
| 納期と重要度 | 早く終えたい仕事、遅れると困る仕事の優先度 | 出荷期限、遅延1単位あたりの損失 |

タスクの処理時間と資源使用量は、選んだモードによって決まる。実行途中でモードを切り替えたり、使用量を変えたりはしない。資源容量も計画の全期間で一定である。「電力を同時に何kW使えるか」は資源容量にできるが、「燃料を合計何Lまで使えるか」は使い切る量の制約なので別の問題になる。

返される主な結果は、各タスクの開始時刻と選択モードである。全タスクを実行することが前提であり、仕事を受注するかどうか、途中で仕事を破棄するかどうかは選ばない。

### 1.2 満たす制約

タスク数を$N$、資源数を$R$とする。タスク$i$のモード集合を$M_i$、選択モードを$m_i$、開始時刻を$S_i$とする。各タスクは$M_i$からちょうど1つのモードを選ぶ。

$$C_i=S_i+p_{i,m_i}$$

$p_{i,m_i}$は選んだモードの処理時間、$C_i$は完了時刻である。開始可能時刻を$r_i$、先行制約の集合を$E$とすると、次を満たす。

$$S_i\ge r_i,\qquad C_i\le S_j\quad ((i,j)\in E)$$

$E$の$(i,j)$は「タスク$i$の完了後にタスク$j$を開始できる」という意味で、閉路のない有向グラフでなければならない。

資源$r$の容量を$B_r$、タスク$i$がモード$m$で実行中に使う量を$a_{i,m,r}$とすると、全ての時刻$t$について次を満たす。

$$\sum_{i:S_i\le t<C_i}a_{i,m_i,r}\le B_r\quad (0\le r<R)$$

使用区間は半開区間$[S_i,C_i)$である。あるタスクが時刻5に終われば、別のタスクは時刻5から同じ資源を使える。処理時間0のタスクは資源を占有する時間がない。

開始時刻・処理時間・資源量は整数で扱う。タスクごとの処理時間や開始可能時刻は入力では`int`、返却される開始時刻は`long long`である。小数時間を扱う場合は、例えば「1単位＝0.1秒」のように単位を決めて整数化する。

### 1.3 最小化する目的関数

組み込みの目的関数は、全体の終了時刻、各タスクの完了時刻、納期遅延、モード費用の重み付き和である。

$$F=w_M C_{\max}+\sum_{i=0}^{N-1}w_i^C C_i+\sum_{i:d_i\ge0}w_i^T\max(0,C_i-d_i)+w_K\sum_{i=0}^{N-1}k_{i,m_i}$$

| 記号 | 意味 | 対応するデータ |
|---|---|---|
| $F$ | 小さいほど良い目的値 | 返却値の`objective` |
| $N$ | タスク数 | `problem.tasks.size()` |
| $i$ | タスクID。0から$N-1$ | `problem.tasks[i]`の添字 |
| $m_i$ | タスク$i$で選んだモードの添字 | `schedule.mode_of_task[i]` |
| $S_i$ | タスク$i$の開始時刻 | `schedule.start_time[i]` |
| $p_{i,m_i}$ | 選択モードの処理時間 | `problem.tasks[i].modes[m_i].duration` |
| $C_i$ | タスク$i$の完了時刻 | $S_i+p_{i,m_i}$で計算 |
| $C_{\max}$ | 全タスクの完了時刻の最大値。タスク0個なら0 | 各$C_i$の最大値 |
| $w_M$ | 全体の終了時刻を1単位遅らせる費用 | `problem.makespan_weight` |
| $w_i^C$ | タスク$i$の完了時刻を1単位遅らせる費用 | `problem.tasks[i].completion_weight` |
| $d_i$ | タスク$i$の納期。負値の指定は納期なしを表す`-1`だけ | `problem.tasks[i].due_time` |
| $w_i^T$ | タスク$i$の納期超過1単位あたりの費用 | `problem.tasks[i].tardiness_weight` |
| $k_{i,m_i}$ | 選択モードを1回使う費用 | `problem.tasks[i].modes[m_i].fixed_cost` |
| $w_K$ | モード費用を目的値へ換算する係数 | `problem.mode_cost_weight` |

第1項は「最後の仕事が終わるまでの時間」を短くする。第2項は、全体の終了時刻が同じでも重要な仕事を先に終えることを評価する。第3項は納期を超えた部分だけを評価し、納期前に終えた仕事の遅延費用は0になる。第4項は時間と実行費用の交換条件を決める。

例えば、2つのタスクの完了時刻が3と5、後者の納期が4、後者だけに完了重み2・遅延重み3を付けるとする。全体終了時刻の重みは1、選択モードの費用合計は2、その換算係数は4とする。このとき目的値は、全体終了の5、後者の完了費用の10、遅延費用の3、モード費用の8を合わせて26になる。

重みは有限の非負値で、使わない項は重みを0にする。モードの`fixed_cost`自体には負値も使える。全体終了時刻だけを最小化したい場合は`makespan_weight=1`、各タスクの2つの重みを0、`mode_cost_weight=0`とする。

`due_time`は超過を禁止する時刻ではない。納期を必ず守る必要がある場合、遅延重みを大きくするだけでは制約の保証にならない。

### 1.4 独自の目的関数を使う場合

組み込み式では表せないときは、問題とスケジュールを受け取り、最小化する有限値を返す`Evaluator`を指定できる。例えば「各作業の終了後に残る配送時間まで含めた全体終了時刻」を評価できる。

対象は、モードを固定したとき、タスクを余分に遅らせるだけで良くなることのない目的である。早すぎる完成への罰則、安い時刻帯まで意図的に待つ利益などが重要なら、第7章の適用限界を先に確認する。独自Evaluatorは評価方法を変える仕組みであり、資源容量以外の任意の制約を自動的に追加する仕組みではない。

## 2. 厳密解アルゴリズム

本ライブラリは、返却解の最適性や最適値との差を証明するAPIを持たない。ただし、同じ問題でも自由度を限定すると厳密に解きやすくなる。ここで述べる専用解法は、本ヘッダが提供する追加APIではなく、使う手法を選ぶための判断材料である。

### 2.1 モードが固定され、資源競合がない場合

全タスクのモードが決まっていて、資源を使わない、または全てを重ねても容量内であれば、残るのは開始可能時刻と先行制約だけである。先行タスクから順に、次で開始時刻を決められる。

$$S_i=\max(r_i,\max_{j:(j,i)\in E}C_j)$$

先行タスクがない場合は$S_i=r_i$とする。先行関係をたどる順序で計算すれば、全てのタスクをそれぞれ最も早く終えられる。組み込み目的の時間項は遅くなるほど増えるため、これが最適である。モード費用はモード固定後には定数になる。計算量はタスク数$N$と辺数$|E|$に対して$O(N+|E|)$である。

モードが複数残っている場合は、この結論だけではモードの最適選択まで決まらない。例えば「資源がないから最短モードだけを選ぶ」とすると、モード費用との交換条件を無視することになる。

### 2.2 Job Shopで、機械ごとの実行順も固定した場合

各工程の機械とモードが固定され、各機械上の実行順も決まっているなら、機械上で隣り合う工程の間に「前の工程が終わってから次へ」という辺を追加する。追加後のグラフがDAGなら、2.1と同じ最早時刻計算で、その固定順序の中の最適解を得られる。

これは機械順序そのものの最適性を示すものではない。問題全体を厳密に解くには、機械順序や代替機械の選択も探索する必要がある。正の処理時間を持つ工程だけなら、追加した辺の閉路は固定順序の実行不能を意味する。処理時間0を含む場合でも、本ライブラリへ渡す先行グラフはDAGにする必要がある。

また、容量が2以上の累積資源に対して全タスクを一本の順序へ固定すると、本来可能な同時実行を禁止してしまう。この変換は、機械ごとの排他を表す容量1資源についての説明である。

### 2.3 単一機械で、全仕事が時刻0から実行できる場合

モード固定、機械1台、各タスクの使用量1、全ての開始可能時刻0、先行制約なしという条件を考える。

| 最小化したいもの | 厳密に使える方法 | 適用条件の注意 |
|---|---|---|
| 全体終了時刻だけ | どの順序でも、隙間なく実行すれば処理時間の合計 | 順序では値が変わらない。モード費用との交換条件は別問題 |
| 完了時刻の合計 | 処理時間の短い順 | 重みが全て同じ場合 |
| 重み付き完了時刻の合計 | 処理時間÷完了重みの小さい順 | 納期遅延項を含めない。正の重みを対象とし、重み0は後ろへ置ける |

固定モードの処理時間を$p_i$と書く。最後の規則は、隣り合う仕事$i,j$を交換して確かめられる。$i$を先に置く方が良い条件は$p_i w_j^C\le p_j w_i^C$なので、この条件を満たす順に並べればよい。重みが全て1なら短い順になる。全体終了項と固定済みモードの費用は順序によらないため、併用してもこの結論は変わらない。

開始可能時刻、先行制約、納期遅延、複数機械を加えると、この単純な並べ替えの最適性はそのまま使えない。本ライブラリ内部の初期順序規則も、この限定問題の厳密アルゴリズムを提供するAPIではない。

### 2.4 タスク数が小さい単一機械：部分集合DP

モード固定、全タスクの開始可能時刻0、機械1台、各タスクの機械使用量1なら、処理済みタスクの集合を状態にするDPが候補になる。先行制約がある場合も、全ての先行タスクを処理済みのタスクだけを次へ追加する。

処理済み集合を$A$、その処理時間合計を$T(A)$とすると、次に置くタスク$i$の完了時刻は$T(A)+p_i$である。そのタスクの重み付き完了費用と納期遅延費用をDP値へ加えれば、組み込み目的を厳密に最小化できる。全体終了時刻とモード費用は全タスク処理後の定数として加える。開始可能時刻が全て0で時間の遅延が利益にならないので、意図的な空き時間は不要である。

状態数は$2^N$、素直な遷移数は$O(N2^N)$で、前提となる先行集合の検査も効率化する必要がある。大きな$N$では時間・メモリとも現実的でなくなる。開始可能時刻がばらばらの場合は、同じ集合でも経過時間が変わるため、この1値のDPでは不足する。

### 2.5 小規模の一般問題：制約プログラミング・整数最適化

一般のJob Shopや複数モードの資源制約問題では、開始時刻・モード選択を変数として、先行制約、同時使用量、目的関数を直接モデル化する厳密ソルバーが候補になる。例えばCP-SATでは、時間区間と重複禁止制約でJob Shopを表現できる。[OR-ToolsのJob Shop例](https://developers.google.com/optimization/scheduling/job_shop)

ただし「厳密なモデルへ渡すこと」と「時間内に最適性を証明できること」は異なる。CP-SATの`OPTIMAL`は最適性の確認済み、`FEASIBLE`は実行可能解が見つかっただけである。整数制約へ変換するときの単位もそろえる必要がある。[OR-ToolsのCP-SAT説明](https://developers.google.com/optimization/cp/cp_solver)

このヘッダにはそのような外部ソルバーへの接続や、任意Evaluatorの式を整数最適化モデルへ自動変換する機能はない。コンテストで外部ソルバーを使えるか、構築・起動・証明までの時間があるかを確認する。小さい問題の厳密解を、ライブラリのモデル変換やスコアの検査用に使う方法も有用である。

### 2.6 選択の目安

まず資源競合、機械順序、モード選択のうち、何が未決定かを確認する。ほぼ決まっていれば最早時刻計算、小さい単一機械ならDP、厳密な制約や最適性の証明が必要なら対応する厳密ソルバーを検討する。一般的な資源競合とモード選択が残り、短い時間で実行可能解を得て改善したい場合が本ライブラリの主な用途になる。

## 3. 差分更新

### 3.1 再利用できるのは「解」であり、永続的な探索状態ではない

前ターンの`result.schedule`を、次ターンの問題に対する`resource_scheduling_improve`へ渡せる。ただし、そのスケジュールは次ターンの問題でも実行可能でなければならない。グラフや入力データを変更できることと、古い解がそのまま使えることは別である。

| 更新内容 | 問題データの更新 | 前の解をそのまま渡せる条件 |
|---|---|---|
| 完了・遅延・全体終了・モード費用の重み | 各重みを書き換える | 有限・非負などの条件を守れば実行可能性は変わらない。目的値は再評価される |
| 納期 | `due_time`を書き換える | 納期はsoft penaltyなので、正しい範囲の変更なら実行可能性は変わらない |
| モードの固定費用 | `fixed_cost`を書き換える | 有限値なら実行可能性は変わらない。費用は新しい値で評価される |
| 開始可能時刻 | `release_time`を書き換える | 引き上げた時刻以降に前の開始時刻があること。引下げだけなら通常そのまま使える |
| 選択モードの処理時間・使用量 | モードを書き換える | 先行関係・資源競合を新しい値で再検査する。増加時は特に注意 |
| 資源容量 | `capacity`を書き換える | 正の容量で、前の全実行区間が新容量内に収まること |
| 先行辺 | `precedences`を書き換える | 新しいDAGに対して、前の順序と開始時刻の両方が有効であること |
| 未選択モードの追加・変更 | `modes`を書き換える | 全モードが有効で、選択中のモード添字の意味を変えないこと |
| タスク追加・削除・IDの並べ替え | `tasks`と関連IDを組み直す | 3つの解配列と先行辺を新IDへ変換し、追加タスクも含む実行可能解を作る必要がある |
| モード削除・並べ替え、資源ID変更 | 使用する全IDを変換する | 添字が範囲内でも別の意味になることがある。意味の対応を利用側で保証する |
| 独自Evaluatorの変更 | 新しいEvaluatorを渡す | 解が実行可能で、新Evaluatorが有限で一貫した値を返すこと。保証は新Evaluatorでの値についてだけ |

例えば、検査員が2人から3人へ増えるなら、2人で実行できた前の解は通常そのまま有効である。逆に3人から2人へ減ると、重なっていた仕事の使用量が容量を超えるかもしれない。その場合、前の解を直接`improve`へ渡すと例外になる。ライブラリは自動で競合箇所だけを修復しない。

### 3.2 次ターンでの手順

1. 現在の問題を完成させる。現在時刻を含め、全ての時刻を同じ時間軸で表す。
2. タスクID・モード添字・資源IDの意味が前ターンと対応するか確認する。変わる場合は利用側で解を変換する。
3. `resource_scheduling_is_feasible(current_problem, previous_schedule)`で、新しい問題に対する実行可能性を確認する。
4. 有効なら`resource_scheduling_improve`、無効なら外側で修復した解を渡すか、`resource_scheduling_solve`から構築する。
5. 同じ問題・同じEvaluatorで目的値を読み直す。前ターンの`objective`との単純比較では、入力変更の影響と改善効果を区別できない。

第6・8章のケース14は、このうち「IDの意味が対応する前の解を検査し、有効ならimprove、無効ならsolve」を関数にしたものだ。意味が対応しない解は、検査を通る可能性があっても渡さない。

### 3.3 どこまで効率的か

`improve`は前のタスク順序とモードから探索を始められるため、毎回完全に新しい順序を選ぶ手間を避け、良い解を出発点にできる。同じ決定的Evaluatorで評価する限り、返却目的値は渡した実行可能な解以下になる。

一方で、呼出しごとに問題検証、先行・後続リスト、トポロジカル順序、時刻範囲、資源calendarなどを準備する。前の開始時刻も探索中の固定値にはせず、順序・モードから配置し直す。元の開始時刻を持つスケジュール自体は別に評価し、良ければ返却候補として保持する。

したがって、変更した辺だけを処理するような計算量の差分更新ではない。小さな変更なら必ず速くなる、解の変更が局所だけに収まる、とも保証しない。事前の`is_feasible`検査と`improve`内部検査は重複するが、検証済みの準備データを渡して省略する公開APIはない。

### 3.4 実行済み・実行中の仕事がある場合

このライブラリは、指定タスクをこれから計画する問題を扱う。過去の実行結果をロックしたり、実行中のタスクを途中から続ける状態を保持したりはしない。

完了済みタスクを除くときは、残るタスクへの先行辺を整理し、完了済みの先行タスクの実績完了時刻を残り側の開始可能時刻へ反映する。実行中の仕事が資源を占有している場合、その占有を無視した計画は実行できない。外部の固定予約をそのまま入力するAPIはないので、資源競合しない部分だけを切り出す、全ての占有が終わる保守的な時刻から再計画する、固定予約対応の別手法を使うなど、外側のモデル化が必要になる。

保守的に再計画の開始を遅らせる場合は、残る全タスクの開始可能時刻へその下限を反映する。早く空いた資源を利用できなくなるため、これは一般に最適性を保つ変換ではない。

共通の絶対終了時刻`deadline`は複数ターン・複数呼出しで共有できる。ただし期限切れでも初期構築を行うため、残り時間が少なければ呼出し自体を行うかを外側で判断する。

## 4. ユースケース

ここでは問題をどう捉えるかを説明する。具体的な型・引数・呼出しは第5・6章、コピー用の関数定義は第8章に置く。同じ資源モデルを業種名だけ変えて繰り返さず、モデル化や利用手順が異なる15ケースに分ける。例えば製造・建設・計算ジョブはいずれも、必要に応じてケース1〜3のどれかへ対応させられる。

| 番号 | ユースケース | 主に決めること | 特に必要な入力 |
|---|---|---|---|
| 1 | 1種類の累積資源を使う工程計画 | どの仕事を先に進めるか | 容量、各仕事の処理時間・使用量、先行関係 |
| 2 | 複数種類の資源を同時に使う計画 | 全資源が足りる時間への配置 | 各資源の容量、仕事ごとの使用量ベクトル |
| 3 | 複数の実行方法を選ぶ計画 | 順序と、速度・費用・資源量の組合せ | 各仕事のモード一覧、目的の重み |
| 4 | 同一並列機械 | 同じ性能の機械群への仕事の時間配置 | 機械数、各仕事の処理時間・時刻条件 |
| 5 | Job Shop | 固定機械を使う各工程の開始時刻 | 製品ごとの工程列と各工程の機械 |
| 6 | Flexible Job Shop | 工程ごとの機械選択と開始時刻 | 各工程で利用可能な機械・処理時間・費用 |
| 7 | 開始可能時刻・納期・重要度を考慮する計画 | 誰を早く終え、誰の遅延を避けるか | 時刻条件とタスクごとの2種類の重み |
| 8 | 複数プロジェクトの共有資源計画 | プロジェクトをまたぐ資源配分 | プロジェクト内の仕事・先行関係、共通資源 |
| 9 | 残り工程時間まで含めた部分問題 | 局所完了後の全体への影響 | 各仕事の後段時間と独自の評価尺度 |
| 10 | 外部で作った実行可能解の改善 | 良い初期解をどこまで改善するか | 現在の問題に対する完全な実行可能解 |
| 11 | 資源競合しない部分集合の切出し | 選んだ仕事だけの再配置 | 対象ID、外部先行仕事の完了時刻、境界条件 |
| 12 | 共通期限内の複数試行 | 複数の探索結果から最良を選ぶ | 総時間、試行数、最初の乱数seed |
| 13 | 固定反復による比較・検査 | 入力やseedに対する探索の比較 | 提案回数上限、seed |
| 14 | ターン間の再計画 | 前の解を再利用するか、構築し直すか | 更新した問題、対応する前の解、共通期限 |
| 15 | 資源使用が段階ごとに変わる工程 | 段階をまたいだ時間配置 | 段階ごとの時間・使用量、段階順序 |

### 4.1 1種類の累積資源を使う工程計画

例えば作業員が6人いて、ある仕事は2人で4時間、別の仕事は3人で2時間必要とする。5人分の2仕事は重ねられるが、合計7人分は重ねられない。これが「累積資源」であり、1人ずつに名前を付ける必要はない。

各仕事を1タスク、作業員全体を容量6の1資源にする。仕事ごとに処理時間と必要人数を指定し、実行方法は1つに固定する。加工終了後の検査など、仕事上必要な前後関係だけを先行制約にする。人数が足りない2仕事の順序を先行制約で固定する必要はない。

全仕事が終わる時刻を短くする計画を得られる。必要人数0の仕事も表現できるが、個々の作業員の技能・相性・休憩は、この単一容量では表せない。

### 4.2 複数種類の資源を同時に使う計画

例えば計算ジョブがCPU枠2、GPU1、実行中メモリ4単位を同時に必要とする場合である。CPUだけ空いていてもGPUが足りなければ開始できず、全資源の必要量が実行期間を通じて確保される必要がある。

資源の種類ごとに容量を用意し、各タスクには同じ並びの使用量ベクトルを与える。使用量0はその資源を使わない意味になる。処理時間、先行関係、資源の並びは利用者が指定する。

発電量の範囲で同時電力を抑える問題にも使える。ただし総消費エネルギーの上限とは異なる。メモリをタスク終了後も保持し続ける場合や、CPUとGPUを工程の前半・後半で別々に使う場合は、この「実行中ずっと一定量を占有する」モデルにはそのまま入らない。後者はケース15を検討する。

### 4.3 複数の実行方法を選ぶ計画

同じ仕事を「2人で8時間・費用1」と「4人で3時間・費用6」のどちらでも実行できる場合を考える。速い方法ばかり選ぶと人員不足や費用増加が起き、安い方法ばかり選ぶと全体終了が遅れる。順序だけでなく方法の選択も必要になる。

仕事ごとに複数のモードを列挙し、各モードに処理時間、全資源の使用量、固定費用を設定する。全体終了・タスク完了・納期遅延・費用のどれを重視するかも指定できる。モードの一覧が選べる範囲そのものであり、人数や速度の連続的な中間値を自動生成することはない。

各仕事は必ず1モードだけで実行される。「実行しない」という選択肢や、途中から別の方法へ切り替える選択は含まれない。また、あるタスクのモードによって別タスクの処理時間が変わる関係は、自動では表現されない。

### 4.4 同一並列機械

同じ性能の機械が3台あり、どの仕事もそのうち1台で実行できる場合である。機械ごとの差がないため、「3台のうちどの番号か」より「同時に3仕事まで」という条件が本質になる。

機械群全体を容量3の1資源とし、各仕事の使用量を1にする。仕事ごとに処理時間、開始可能時刻、納期、重要度を指定できる。処理時間が違う仕事を重ねて、全体終了や完了費用を小さくする。

返却されるのは仕事の開始時刻で、機械番号は直接含まれない。正の処理時間の仕事を開始時刻順に見て、その時刻までに空いている機械へ割り当てれば機械番号を付けられる。時刻が同じなら終了を先に扱い、処理時間0の仕事は占有なしとして扱う。機械ごとに速度や利用可否が違う場合は、容量だけでまとめずケース6のようなモード表現を使う。

### 4.5 Job Shop

製品Aは「機械0で加工→機械1で仕上げ」、製品Bは「機械1で加工→機械0で仕上げ」という工程列を持つ。製品内の工程順は決まっているが、同じ機械でAとBのどちらを先に加工するかは決まっていない、という問題である。

1工程を1タスク、各機械を容量1資源にする。工程には使う機械と処理時間を指定し、同じ製品の隣接工程を先行制約でつなぐ。機械上の製品間順序を入力側で固定すると探索の選択肢を減らすので、物理的に必要な場合以外は追加しない。

返却結果は製品番号・工程番号に対応する開始時刻へ戻せる。工程間の待機は許される。「前の工程が終わった瞬間に次を始めなければならない」「機械を離れられず後工程が空くまで塞ぐ」といった無待機・blocking制約には対応しない。

### 4.6 Flexible Job Shop

ケース5に対し、各工程を複数の機械のいずれかで実行できる問題である。例えば工程Aは機械0なら5時間、機械1なら3時間かかり、機械1の利用費用が高い、といった違いを持たせられる。

各工程の代替案をモードにする。代替案ごとに機械番号、処理時間、固定費用を指定する。製品内の工程順は先行制約、機械の排他は容量1資源で表す。全ての工程に1つ以上の代替案が必要である。

出力から、開始時刻だけでなく選ばれた代替案と機械番号を読み戻す。機械番号と代替案の添字は別であり、同じ機械で速度や費用だけ異なる複数案も作れる。機械切替の移動時間や段取りが直前の製品・機械に依存する場合、その依存関係をこの例だけで表すことはできない。

### 4.7 開始可能時刻・納期・重要度を考慮する計画

同じ機械を使う注文でも、材料が時刻10に到着する注文、時刻20までに出荷したい注文、遅れてもよいが早く終える価値が高い注文は異なる。ケース1〜6などの資源モデルへ、これらの時刻条件と評価を重ねる使い方である。

仕事を開始してよい最初の時刻、納期、完了時刻の重み、納期超過の重みを仕事ごとに指定する。完了重みは納期前でも早い完了を評価し、遅延重みは納期超過部分だけを評価する。全体終了時刻も重視するかは別に指定する。

ある製品の出荷完了だけを評価したい場合は、その製品の最終工程へ重みを付ける。全工程に同じ重みを付けると、工程途中の完了時刻まで合計する別の目的になる。どの重みも「必ずその順に処理する」という命令ではなく、資源競合と合わせて評価される。

### 4.8 複数プロジェクトの共有資源計画

各プロジェクトに別々の工程図があり、同じ作業員や設備を共有する場合である。プロジェクトごとに独立に解くと、それぞれの計画は正しくても、合計すると同時使用量が容量を超えることがある。

タスクを全プロジェクトから集め、1つの問題として解く。先行関係はプロジェクト内のIDで入力し、内部で全体IDへ変換する。一方、資源IDは全プロジェクトで共通のものを使う。資源をプロジェクトごとに複製してしまうと共有の競合が失われる。

例ではプロジェクト間の先行制約はない。必要なら全体問題へ統合した後、その関係も追加する。プロジェクトの完了に複数の終端工程がある場合は、全終端を先行タスクに持つ処理時間0の完了タスクを用意し、そこへ重みや納期を付けると「プロジェクト全体の完了」を表せる。

### 4.9 残り工程時間まで含めた部分問題

AHCなどで、今回決めるのは加工工程だけだが、その後に配送や仕上げが残ることがある。加工の終了が同じ時刻でも、その後に10分かかる仕事と1分しかかからない仕事では、全体への影響が異なる。

各タスクに、局所工程の完了後に必要な後段時間を与える。「完了時刻＋後段時間」の最大値を主に評価し、必要に応じて重み付き完了時刻とモード費用を加える独自Evaluatorを使う。後段時間は固定の見積値で、選んだモードや他タスクの順序で変わらないものを想定する。

後段が別の共有資源で待たされるなら、一定時間を加えるだけでは正確にならない。その場合は後段もタスクに含めるか、外側で近似評価として扱う必要がある。小さな完了重みを補助項に付けても、厳密な辞書式最適化になるわけではない。

### 4.10 外部で作った実行可能解の改善

問題固有の貪欲法や別の探索器で良い解を作れる場合、さらに改善する段階として利用できる。必要なのは、全タスクの順序、選択モード、実行可能な開始時刻を持つ完全な解である。

タスク順序は先行制約を満たす順列にする。開始時刻が正しくても、順序に重複や欠落があれば受け付けられない。開始時刻は最早でなくても構わない。

出力は、入力した解より目的値が悪くない解になる。ただし、開始時刻や特定タスクを固定する機能ではない。入力した順序・モードから別の配置も試し、その後も全タスクを動かせる。特定部分を変更禁止にしたい場合は、その条件を表現できる外側の手法が必要になる。

### 4.11 資源競合しない部分集合の切出し

大きい問題の一部だけを局所的に解き直したい場合である。例えば、独立した設備区画で行う未処理仕事だけを取り出す、他の仕事の資源使用が終わった後の残作業だけを計画する、といった場面を想定する。

対象タスクIDの一覧と、外部にある先行タスクの固定完了時刻を指定する。対象同士の先行関係は残し、外部から入ってくる先行関係は対象側の開始可能時刻へ変換する。結果にはローカルIDと全体IDの対応を付ける。

この切出しが妥当なのは、外部タスクとの資源競合がなく、対象から外部へ出る辺が外部の固定開始時刻を制約しない場合である。外部の予約量を自動で容量から控除したり、外部の固定開始時刻に合わせて対象の終了上限を設定したりはしない。対象外の値は結果中で未設定とし、全体へ戻すときは利用側の完全な解に反映して全体を再検査する。

### 4.12 共通期限内の複数試行

同じ問題を異なる乱数seedで複数回解き、最も良い結果を選ぶ使い方である。短い試行を複数回行う方がよいか、1回へ長い時間を使う方がよいかを比較できる。

総時間、最大試行数、最初のseedを指定する。各試行へ残り時間の一部を配り、全体で同じ絶対期限を使う。試行は直列で、途中の探索状態は共有しない。

各試行に初期構築が必要なので、試行数を増やすほど改善に使える割合が減ることがある。総時間0でも、解を返すために初回の構築は行う。出力の診断カウンタは最良結果を出した1試行分であり、全試行の合計ではない。

### 4.13 固定反復による比較・検査

入力のモデル化が正しいか、seedでどれほど結果が変わるか、時間枠による反復数の揺れを抑えて比較したい場合に使う。指定するのは探索の反復上限とseedである。

反復は「追加の初期順序の評価回数＋近傍の提案回数」であり、スケジュールを評価した回数とは一致しない。無効な提案も反復数へ含まれる。初期解だけを確認するなら反復上限0を使う。

時間条件が完全になくなるわけではなく、内部の冷却は経過時間も参照する。固定反復・同じseedを指定しても、全環境で同じ結果になる保証はない。この例は開発用で、競技本番へ長い時間上限のまま持ち込まない。

### 4.14 ターン間の再計画

作業員の増減、新しい納期、更新された処理時間などに応じ、前ターンの良い解を利用したい場合である。ケース10は外部解が実行可能であることを前提に改善するのに対し、このケースでは問題変更によってその前提が崩れることも扱う。

更新した問題と、意味の対応が確認できる前の解を渡す。実行可能性を満たせば改善し、満たさなければ初期構築から解き直す。タスクやモードの添字を変更した場合、対応の変換は利用側で行う。変換できなければ前の解を渡さない。

結果には前の解を利用したかどうかを含める。同じID・同じモードの意味を検査関数が自動認識するわけではない点と、実行済み・実行中タスクの固定は別途必要な点が重要になる。第3章の更新条件を満たす残作業の計画に使う。

### 4.15 資源使用が段階ごとに変わる工程

例えば「人員と加工機を使う準備→人員なしで乾燥機を使う乾燥→人員で取り出す」という工程である。全期間に最大量の資源を予約すると過剰に制約し、全期間の平均量を予約すると実際には足りない時間が生じる。

使用量が一定の段階ごとにタスクを分け、同じ工程の段階を先行制約でつなぐ。段階ごとの処理時間・資源使用量を指定し、プロジェクトの開始可能時刻は最初の段階へ設定する。別プロジェクトの段階は、資源容量が許す限り重ねられる。

この例が表すのは「各段階は中断しないが、段階間では待ってよい」工程である。乾燥機の中で待つ間もその機械を占有し続ける、全段階を無待機で連続実行する、といった条件には使えない。単一タスクの途中で自由に中断・再開できるモデルとも異なる。

## 5. 利用準備

### 5.1 導入と、最初に作るもの

ヘッダを自分のソースと同じディレクトリへ置き、次のように読み込む。

```cpp
#include "generic_resource_scheduler_v07.hpp"
```

GCC向けのC++20コードで、`bits/stdc++.h`、`__uint128_t`、`__INCLUDE_LEVEL__`を使う。ACLなどの外部ライブラリやリンク対象のバイナリは不要である。公開型・公開関数はグローバル名前空間にある。

```bash
g++ -std=c++20 -O2 -Wall -Wextra -Wshadow -Wconversion -Wno-expansion-to-defined main.cpp -o main
```

**専用solverオブジェクトのコンストラクタはない。** `resource_scheduling_problem`と`resource_scheduling_options`を作り、自由関数を呼ぶ。公開データ型は集成体として初期化できる。準備の順序は次のとおりである。

1. 時間と資源量の整数単位を決める。
2. 資源を並べ、その添字を資源IDにする。
3. タスクを並べ、その添字をタスクIDにする。
4. 各タスクへ1個以上のモードを登録する。使用しない資源はモードの使用一覧に登録しない。
5. タスク間の先行辺、開始可能時刻、納期、目的の重みを設定する。
6. 時間枠・seedなどを設定する。初期解だけの確認なら反復上限を0にする。

次は「2人の作業員を使う、2工程の順次作業」の準備である。ここには`solve`の呼出しまで含めず、何を入力として完成させるかを示す。

```cpp
resource_scheduling_problem problem;
problem.resources = {{2}}; // 資源0: 同時に2人まで。
problem.tasks.resize(2);   // タスクIDは0, 1。
problem.tasks[0].modes = {{3, {{0, 1}}, 0}}; // 3時間、資源0を1人分、費用0。
problem.tasks[1].modes = {{2, {{0, 2}}, 0}}; // 2時間、資源0を2人分、費用0。
problem.precedences = {{0, 1}};             // タスク0の完了後にタスク1。
problem.makespan_weight = 1;
problem.mode_cost_weight = 0;

resource_scheduling_options options;
options.time_limit_ms = 50.0; // 初期構築後に使う改善時間。
options.seed = 1;
options.max_iterations = 0;  // まず初期解だけでモデルを確認する。
```

`tasks.resize(2)`だけでは、各タスクのモードが空なので問題は未完成である。次の一覧の「既定値」は、そのまま有効な入力を意味するとは限らない。

### 5.2 資源と使用量

`scheduling_resource`は資源1種類を表す。

| フィールド | 型 | 既定値 | 設定方法・範囲 |
|---|---|---|---|
| `capacity` | `int` | `1` | 正の容量。排他機械なら1、同一機械群なら機械数、作業員なら人数 |

`scheduling_resource_use`は、あるモードが使う資源1種類とその量を表す。

| フィールド | 型 | 既定値 | 設定方法・範囲 |
|---|---|---|---|
| `resource` | `int` | `0` | `problem.resources`の範囲内の添字 |
| `amount` | `int` | `0` | 登録する場合は1以上、その資源の容量以下。0なら要素を省略する |

同じモードに同じ資源IDを2回書くことはできない。2人分必要なら`{資源ID, 2}`とし、`{資源ID, 1}`を2つ登録しない。要求が容量を超えるモードは「選ばれなければよい」とは扱われず、問題全体が不正になる。

### 5.3 モードとタスク

`scheduling_mode`は実行方法1つを表す。

| フィールド | 型 | 既定値 | 設定方法・範囲 |
|---|---|---|---|
| `duration` | `int` | `1` | 0以上の処理時間。0は完了の合流点などのダミータスクにも使える |
| `renewable_uses` | `std::vector<scheduling_resource_use>` | 空 | 実行中ずっと使う資源だけを列挙。空なら資源を使わない |
| `fixed_cost` | `long double` | `0` | 有限の費用。負の報酬も可。選んだモードの費用を1回加える |

`scheduling_task`は仕事1つを表す。

| フィールド | 型 | 既定値 | 設定方法・範囲 |
|---|---|---|---|
| `release_time` | `int` | `0` | 0以上。仕事を開始できる時刻の下限 |
| `due_time` | `int` | `-1` | 納期なしは-1、納期を使うなら0以上 |
| `modes` | `std::vector<scheduling_mode>` | 空 | 必ず1個以上登録。選択肢がなければ1個だけ入れる |
| `completion_weight` | `long double` | `0` | 有限・非負。納期前も含め、完了を早める価値を表す |
| `tardiness_weight` | `long double` | `0` | 有限・非負。納期超過1単位の費用を表す |

納期を指定しても`tardiness_weight=0`なら遅延の目的項は0である。逆に、`due_time=-1`なら`tardiness_weight>0`でも遅延項を加えない。ただし、納期や重みは初期解の優先度にも参照されるため、「目的項が0」と「探索に全く影響しない」は同じではない。納期を使わない意図なら、`due_time=-1`と`tardiness_weight=0`を組にすると明確になる。

### 5.4 先行制約と問題全体

`scheduling_precedence`は1本の先行辺を表す。

| フィールド | 型 | 既定値 | 設定方法・範囲 |
|---|---|---|---|
| `before` | `int` | `0` | 先に完了させるタスクID |
| `after` | `int` | `0` | 後から開始できるタスクID。`before`とは異なる |

初期値の`{0,0}`は自己辺になるため、実際に登録すると無効である。全辺を合わせたグラフをDAGにする。重複辺は拒否されないが、意味は増えず処理量が増えるので入力側で除くとよい。

`resource_scheduling_problem`は問題全体をまとめる。

| フィールド | 型 | 既定値 | 設定方法・範囲 |
|---|---|---|---|
| `resources` | `std::vector<scheduling_resource>` | 空 | 0個以上。全タスクで共有する資源一覧 |
| `tasks` | `std::vector<scheduling_task>` | 空 | 0個以上。全タスクが実行対象 |
| `precedences` | `std::vector<scheduling_precedence>` | 空 | タスクIDを用いた先行制約 |
| `makespan_weight` | `long double` | `1` | 有限・非負。全体終了時刻の重み |
| `mode_cost_weight` | `long double` | `1` | 有限・非負。モード費用合計の換算係数 |

内部に非公開の実装型があるが、利用者が構築・設定する対象ではない。公開フィールドを準備して公開関数へ渡せばよい。

### 5.5 目的の重みの決め方

| 目的 | 代表的な設定 |
|---|---|
| とにかく全仕事を早く終える | `makespan_weight=1`、各タスクの2重み0、`mode_cost_weight=0` |
| 平均完了を早める | 全タスクの`completion_weight=1`。全体終了を評価しなければ`makespan_weight=0` |
| 重要仕事を早く終える | 重要度に応じた`completion_weight`。完了1単位の遅れの損失で考える |
| 納期超過を減らす | `due_time>=0`と正の`tardiness_weight`を設定。使わない完了項は0 |
| 時間とモード費用を両方考える | 時間1単位と費用1単位の交換条件で`mode_cost_weight`を決める |
| プロジェクトの完了を評価する | 最終タスクか、終端を合流させた処理時間0タスクに重み・納期を付ける |
| 独自の目的 | Evaluatorで全式を返す。組み込みの重みは初期解の目安として別途設定できる |

重みはアルゴリズムの探索率ではなく、解の良し悪しの定義である。例えばモード費用1を完了時刻5単位と同程度に扱いたいなら、他の時間項との関係を確認して係数を決める。巨大な係数で優先順位を擬似的に固定すると、数値範囲や探索の温度尺度へ影響する。厳密な辞書式優先やhard制約の代用品にはしない。

### 5.6 探索オプションと時間枠

`resource_scheduling_options`が公開する探索設定は次の4つである。

| フィールド | 型 | 既定値 | 設定方法・選び方 |
|---|---|---|---|
| `time_limit_ms` | `double` | `1950.0` | 有限の相対改善時間。0以下なら改善を行わない。初期構築・外側の検査・出力の時間を別に確保する |
| `deadline` | `std::optional<std::chrono::steady_clock::time_point>` | `std::nullopt` | 共通の絶対終了時刻。複数回呼ぶときに使う。`system_clock`時刻は渡さない |
| `seed` | `std::uint64_t` | `1` | 乱数seed。入力比較では固定し、複数試行では変える。0も有効 |
| `max_iterations` | `long long` | `-1` | -1は回数上限なし、0は初期構築まで、正値は追加順序評価と近傍提案の合計上限 |

相対時間は、最初の解の構築・評価が済んだ後から測る。`improve`では元の解との評価比較もこの前に行う。終了条件は、相対時間から決めた終了時刻と`deadline`の早い方、および反復上限である。`max_iterations`を設定しても時間条件は残る。

`solve`の反復上限0は最初の配置を返す。`improve`の反復上限0は、入力した順序・モードで配置し直した解と、入力した開始時刻を持つ元の解を比較した結果を返す。空問題、または探索状態が一意の問題では、時間や反復の上限まで使い切るとは限らない。

温度、近傍確率、calendarの方式を選ぶ公開ハイパーパラメータはない。内部定数の意味は第9章で説明する。

共通期限の準備は次のように行う。1900msは例であり、問題規模や出力処理に応じて余裕を調整する。

```cpp
const auto program_begin = std::chrono::steady_clock::now();
const auto solver_deadline = program_begin + std::chrono::milliseconds(1900);
resource_scheduling_options options;
options.time_limit_ms = 100.0;
options.deadline = solver_deadline;
options.seed = 0;
```

過ぎた期限でも初期構築を行う。SAでは8提案ごとの時刻確認で、1回の配置中には停止しないため、厳密な時刻上限ではない。また、有限の`double`ならどんな巨大値でも安全に時計へ加算できるわけではない。時計の表現範囲に十分収まる実用的なmsを指定し、無限大を時間上限として渡さない。

### 5.7 解と戻り値の全フィールド

`resource_schedule`は、外部解を渡すときにも、結果を受け取るときにも使う。

| フィールド | 型 | 既定値 | 内容・利用方法 |
|---|---|---|---|
| `task_order` | `std::vector<int>` | 空 | 全タスクを1回ずつ含むトポロジカルな優先順列。時刻順の出力一覧とは限らない |
| `mode_of_task` | `std::vector<int>` | 空 | `mode_of_task[i]`がタスク$i$のモード添字 |
| `start_time` | `std::vector<long long>` | 空 | `start_time[i]`がタスク$i$の開始時刻 |

全配列の長さはタスク数と一致させる。`mode_of_task`と`start_time`は**順序位置ではなくタスクIDで引く**。`task_order[k]`に入ったタスクの開始時刻は`start_time[task_order[k]]`で読む。

`resource_scheduling_result`は解と評価値・診断値をまとめる。

| フィールド | 型 | 既定値 | 内容・数え方 |
|---|---|---|---|
| `schedule` | `resource_schedule` | 3配列とも空 | 返却する最良実行可能解 |
| `objective` | `long double` | `0` | 返却解を、呼出しに使ったEvaluatorで評価した値。最小化 |
| `iterations` | `long long` | `0` | 追加順序の評価数＋SAの近傍提案数。初回配置は含めず、無効な提案は含める |
| `decoded_schedules` | `long long` | `0` | 実際に配置・評価した回数。非空問題なら初回の1を含む |
| `accepted_moves` | `long long` | `0` | SAで受理した有効な近傍数。同値・悪化受理を含み、追加初期順序の採用は含めない |

空問題では解の配列と3カウンタは空・0で、Evaluatorを1回呼んだ値を返す。非空で探索状態が一意なら、`iterations=0`、`decoded_schedules=1`、`accepted_moves=0`になる。無効な交換、移動先がない提案、別モードがない提案はdecodeされない。`improve`の元解の評価もdecode数には含めない。

### 5.8 Evaluatorの準備

呼出し形式は`evaluator(const resource_scheduling_problem&, const resource_schedule&)`で、戻り値を`long double`へ変換できる必要がある。通常は、これを`operator()`として持つ小さな構造体かlambdaを渡す。

| 条件 | 理由 |
|---|---|
| 小さいほど良い有限値を返す | 最小化するため。最大化スコアなら符号を反転するなど、全式を統一する |
| 同じ問題・解に同じ値を返す | 候補比較、最良解保持、元解以下の比較を一貫させるため |
| 必要な評価項を自分で全て加える | 組み込みのmakespan・完了・遅延・費用項は自動加算されない |
| 入力問題と解を書き換えない | 探索中のデータと評価の整合性を保つため |
| 重い問題検証を毎回しない | 有効な探索候補に対して何度も呼ばれるため |
| 参照先を呼出し完了まで有効に保つ | 参照captureや`std::ref`を使う場合に必要 |

Evaluatorは値渡しで受け取り、その後moveされる。move-onlyのEvaluatorも使える。コピーしたくない既存オブジェクトなら`std::ref`を使えるが、そのオブジェクトの寿命を利用側で管理する。

組み込み式を追加したいとき、Evaluator内から公開の`resource_scheduling_evaluate(problem, schedule)`を呼ぶと、問題と解の検証まで毎回行われる。探索のたびにその費用を払いたくなければ、必要な目的項だけを自分のEvaluatorで計算する。ケース9がその例になる。

### 5.9 ユースケースで異なる準備

ケース1・2は全タスク1モード、ケース3・6は複数モードを準備する。ケース4は機械群を1資源へまとめ、ケース5・6は機械ごとに容量1資源を作る。ケース8・15は二次元の入力IDを全体タスクIDへ変換する。ケース9は追加の評価データ、ケース10・14は実行可能性を確認する前の解、ケース11は部分集合の境界情報を必要とする。ケース12は共通期限、ケース13は反復上限を主に設定する。

第8章の変換関数は、この準備の違いを各関数内にまとめている。ただし、資源を使い切る制約や外部の固定予約など、モデル外の条件を変換関数が補ってくれるわけではない。

## 6. ユースケースごとの使い方

### 6.0 公開関数と共通の読戻し

本体の公開関数は次の5系統・8呼出し形式である。`problem`は`const resource_scheduling_problem&`、`schedule`と`initial`は`const resource_schedule&`、`options`は`const resource_scheduling_options&`で受け取る。Evaluatorはテンプレートの値引数で受け取る。

| 呼出し形式 | 戻り値 | 目的・前提 |
|---|---|---|
| `resource_scheduling_validate_problem(problem)` | `void` | 問題だけを検証。不正なら`std::invalid_argument` |
| `resource_scheduling_is_feasible(problem, schedule)` | `bool` | 問題を検証した上で解を検査。解だけが不正ならfalse、問題自体が不正なら例外 |
| `resource_scheduling_evaluate(problem, schedule)` | `long double` | 問題・解を検証して組み込み式を計算 |
| `resource_scheduling_evaluate(problem, schedule, evaluator)` | `long double` | 問題・解を検証して独自の式を計算。有限値かも検査 |
| `resource_scheduling_solve(problem, options)` | `resource_scheduling_result` | 初期構築して改善。`options`は省略可能 |
| `resource_scheduling_solve(problem, evaluator, options)` | `resource_scheduling_result` | 独自評価で初期構築・改善。末尾の`options`は省略可能 |
| `resource_scheduling_improve(problem, initial, options)` | `resource_scheduling_result` | 実行可能な元解を利用して改善。`options`は省略可能 |
| `resource_scheduling_improve(problem, initial, evaluator, options)` | `resource_scheduling_result` | 独自評価で元解を改善。末尾の`options`は省略可能 |

`solve`へ初期解を渡す形式はない。元解があるときは`improve`を使う。Evaluatorを渡した場合、組み込み式も必要ならEvaluator自身で加える。

第5.1節の準備コードを実行した後なら、次のように呼び出せる。

```cpp
resource_scheduling_validate_problem(problem); // デバッグ時の事前確認。必須ではない。
const auto result = resource_scheduling_solve(problem, options);
assert(resource_scheduling_is_feasible(problem, result.schedule));
const long double value = resource_scheduling_evaluate(problem, result.schedule);
assert(value == result.objective);
for (int task = 0; task < static_cast<int>(problem.tasks.size()); ++task) {
    const int mode = result.schedule.mode_of_task[task];
    const long long start = result.schedule.start_time[task];
    const long long finish = start + problem.tasks[task].modes[mode].duration;
    std::cout << task << ' ' << mode << ' ' << start << ' ' << finish << '\n';
}
```

以降の各コードは、**対応する第8章の関数定義の後に置く、`main`などの関数本体用の呼出し例**である。入力構造体・変換関数は第8章に全て定義している。時間指定の`20.0`は初期構築後の改善ms、seedの`1`は再現用の例で、全ケース共通の推奨値ではない。

### 6.1 1種類の累積資源

`solve_basic_rcpsp`へ作業員数、タスク一覧、先行辺、改善時間、seedを渡す。タスク入力は処理時間と必要人数だけで、目的は全体終了時刻である。返却型は`resource_scheduling_result`、IDは入力タスクの添字のままになる。

```cpp
const std::vector<basic_rcpsp_task_input> tasks = {
    {3, 2}, // タスク0: 3時間、2人。
    {2, 1}, // タスク1: 2時間、1人。0と同時に実行できる。
    {4, 2}  // タスク2: 4時間、2人。0と1の完了後に開始。
};
const std::vector<std::pair<int, int>> edges = {{0, 2}, {1, 2}};
const auto result = solve_basic_rcpsp(3, tasks, edges, 20.0, 1);
for (int task = 0; task < static_cast<int>(tasks.size()); ++task) {
    const auto start = result.schedule.start_time[task];
    std::cout << task << ' ' << start << ' ' << start + tasks[task].duration << '\n';
}
```

必要人数が容量を超えるタスクは拒否される。必要人数0は資源を使わないタスクへ変換される。

### 6.2 複数の累積資源

`capacities[r]`と全タスクの`use[r]`は同じ資源順序にする。使用量ベクトルの長さは容量数と一致させ、使わない資源には0を書く。この変換関数が、正の使用量だけをライブラリの疎な使用一覧へ変換する。

```cpp
const std::vector<int> capacities = {4, 1}; // 資源0: CPU4枠、資源1: GPU1台。
const std::vector<cumulative_task_input> tasks = {
    {5, {2, 1}}, // CPU2枠とGPU1台を5時間同時使用。
    {3, {2, 0}}, // CPU2枠だけを3時間使用。
    {2, {1, 1}}
};
const auto result = solve_cumulative_resources(
    capacities, tasks, {{0, 2}}, 20.0, 1);
std::cout << result.objective << '\n'; // 全タスクの最大完了時刻。
for (long long start : result.schedule.start_time) std::cout << start << '\n';
```

タスク0と1は容量上同時実行できるが、0と2はGPUを共有できない。先行辺によっても0の後に2を置く。

### 6.3 複数モード

`solve_mrcpsp`は、全ての資源容量、各タスクの時刻条件・重み・モード、先行辺、全体終了と費用の重みを受け取る。返却モードは各タスクの`modes`内の添字である。費用や処理時間を読むときも、その選択添字で入力へ戻る。

```cpp
mrcpsp_task_input task;
task.release_time = 0;
task.due_time = 6;
task.completion_weight = 0;
task.tardiness_weight = 2;
task.modes = {{8, {2}, 1}, {3, {4}, 6}}; // duration, use, fixed_cost。
const std::vector<mrcpsp_task_input> tasks = {task, task};
const auto result = solve_mrcpsp({4}, tasks, {}, 1, 0.5L, 20.0, 1);
for (int id = 0; id < static_cast<int>(tasks.size()); ++id) {
    const int chosen = result.schedule.mode_of_task[id];
    const auto& mode = tasks[id].modes[chosen];
    std::cout << id << ' ' << chosen << ' ' << result.schedule.start_time[id]
              << ' ' << mode.duration << ' ' << mode.fixed_cost << '\n';
}
```

費用係数0.5は、この問題で費用1を目的値0.5へ換算するという意味である。納期を必ず守る設定ではない。

### 6.4 同一並列機械

`solve_identical_parallel_machines`へ機械数、仕事一覧、全体終了重み、時間、seedを渡す。仕事一覧の各要素は処理時間、開始可能時刻、納期、完了重み、遅延重みの順である。目的に使わない重みは0、納期なしは-1とする。

```cpp
const std::vector<parallel_job_input> jobs = {
    {5, 0, -1, 0, 0},
    {3, 0, 4, 0, 2},
    {2, 1, -1, 1, 0}
};
const auto result = solve_identical_parallel_machines(2, jobs, 1, 20.0, 1);
for (int job = 0; job < static_cast<int>(jobs.size()); ++job) {
    std::cout << job << ' ' << result.schedule.start_time[job] << '\n';
}
```

この例の返却モードは全て0である。特定の機械番号への割当は返さない。機械番号が必要な場合の読戻しは第4.4節を参照する。

### 6.5 Job Shop

入力は`jobs[job][operation]`で、各要素に機械番号と処理時間を持たせる。返却する`job_shop_solution`は同じ二次元添字の`start_time`、全体終了時刻`makespan`、ライブラリの元の戻り値`raw_result`を持つ。

```cpp
const std::vector<std::vector<job_shop_operation_input>> jobs = {
    {{0, 3}, {1, 2}}, // 製品0: 機械0→機械1。
    {{1, 4}, {0, 1}}  // 製品1: 機械1→機械0。
};
const auto solution = solve_job_shop(2, jobs, 20.0, 1);
for (int job = 0; job < static_cast<int>(jobs.size()); ++job) {
    for (int op = 0; op < static_cast<int>(jobs[job].size()); ++op) {
        const auto start = solution.start_time[job][op];
        std::cout << job << ' ' << op << ' ' << jobs[job][op].machine
                  << ' ' << start << ' ' << start + jobs[job][op].duration << '\n';
    }
}
```

`raw_result.schedule`のタスクIDは、製品0の全工程、製品1の全工程、という順に平坦化した添字になる。通常の出力には二次元の`start_time`を使うとIDの取り違えを避けやすい。

### 6.6 Flexible Job Shop

入力は`jobs[job][operation][alternative]`で、代替案は機械・時間・費用を持つ。返却する`machine`と`mode`は二次元であり、`mode[job][operation]`が選択した代替案の添字になる。

```cpp
std::vector<std::vector<flexible_operation_input>> jobs(2);
jobs[0] = {
    {{0, 4, 0}, {1, 2, 3}}, // 工程0の代替案。
    {{1, 3, 0}}             // 工程1は機械1だけ。
};
jobs[1] = {{{0, 2, 0}, {1, 5, 0}}};
const auto solution = solve_flexible_job_shop(2, jobs, 0.5L, 20.0, 1);
for (int job = 0; job < static_cast<int>(jobs.size()); ++job) {
    for (int op = 0; op < static_cast<int>(jobs[job].size()); ++op) {
        const int mode = solution.mode[job][op];
        const auto& chosen = jobs[job][op][mode];
        std::cout << job << ' ' << op << ' ' << solution.machine[job][op]
                  << ' ' << solution.start_time[job][op]
                  << ' ' << chosen.duration << '\n';
    }
}
```

機械番号が1でも、選ばれた代替案の添字が0である場合がある。結果の`machine`を`jobs`の代替案添字として使わない。

### 6.7 時刻条件と重要度

資源・モード・先行関係を準備した`problem`と、タスク数と同じ長さの`priorities`を渡す。関数は問題をコピーし、時刻条件とタスク重み、全体終了重みを上書きする。モード費用の重みは、渡した問題の設定を使用する。

```cpp
resource_scheduling_problem problem;
problem.resources = {{1}};
problem.tasks.resize(2);
for (auto& task : problem.tasks) task.modes = {{3, {{0, 1}}, 0}};
problem.mode_cost_weight = 0;
const std::vector<task_timing_priority> priorities = {
    {0, 4, 1, 3}, // release, due, completion_weight, tardiness_weight。
    {2, -1, 2, 0}
};
const auto result = solve_with_timing_priorities(problem, priorities, 1, 20.0, 1);
for (int task = 0; task < 2; ++task) {
    const auto finish = result.schedule.start_time[task] + 3;
    const auto delay = priorities[task].due_time < 0 ? 0LL :
        std::max(0LL, finish - priorities[task].due_time);
    std::cout << task << ' ' << finish << ' ' << delay << '\n';
}
```

元の`problem`は書き換わらない。`result.objective`を公開評価関数で再計算するなら、同じ優先度の上書きを反映した問題を使う必要がある。

### 6.8 複数プロジェクト

資源容量は全プロジェクト共通である。各プロジェクトはライブラリの`scheduling_task`一覧と、プロジェクト内IDの先行辺を持つ。モード内の資源IDは、最初から共通の容量配列の添字にする。

```cpp
scheduling_task first;
first.modes = {{2, {{0, 1}}, 0}};
scheduling_task last = first;
last.completion_weight = 2; // プロジェクトの最終工程だけを重視。
shared_project_input project;
project.tasks = {first, last};
project.precedences = {{0, 1}};
const std::vector<shared_project_input> projects = {project, project};
const auto solution = solve_shared_projects({2}, projects, 1, 0, 20.0, 1);
for (int p = 0; p < static_cast<int>(projects.size()); ++p) {
    std::cout << p << ' ' << solution.start_time[p][1] + 2 << '\n';
}
```

返却値の`start_time[p][i]`と`mode[p][i]`は元のプロジェクト内添字で読める。`raw_result`は全タスクを統合した添字を使う。

### 6.9 後段時間を持つ独自Evaluator

準備済みの問題に、タスクごとの後段時間`tail_time`と、補助的な完了重み`tie_weight`を渡す。両配列はタスク数と同じ長さにする。`tie_scale`は補助完了項の係数であり、0ならその項を使わない。

```cpp
resource_scheduling_problem problem;
problem.resources = {{1}};
problem.tasks.resize(2);
for (auto& task : problem.tasks) task.modes = {{3, {{0, 1}}, 0}};
problem.mode_cost_weight = 0;
const std::vector<int> tail_time = {10, 1};
const std::vector<long double> tie_weight = {1, 1};
const long double tie_scale = 0.01L;
const auto result = solve_tail_aware_subproblem(
    problem, tail_time, tie_weight, tie_scale, 20.0, 1);
const auto value = resource_scheduling_evaluate(
    problem, result.schedule, tail_aware_evaluator{tail_time, tie_weight, tie_scale});
assert(value == result.objective);
std::cout << value << '\n';
```

このEvaluatorの目的式は次である。$h_i$は`tail_time[i]`、$q_i$は`tie_weight[i]`、$\lambda$は`tie_scale`で、全て非負にする。空問題では最大値の項を0とする。

$$F_{\mathrm{tail}}=\max_i(C_i+h_i)+\lambda\sum_i q_i C_i+w_K\sum_i k_{i,m_i}$$

この例では組み込みの納期遅延項は加えない。関数内で設定するタスク重みは初期構築の目安であり、返却目的値そのものはこのEvaluatorで計算する。

### 6.10 外部解の改善

例では説明用に初期解を本体から作るが、同じ3配列を満たせば別の手法で作った解を渡せる。`schedule_improvement`は新しい問題で評価した`initial_objective`と、改善結果`improved`を返す。

```cpp
resource_scheduling_problem problem;
problem.resources = {{1}};
problem.tasks.resize(3);
for (auto& task : problem.tasks) task.modes = {{2, {{0, 1}}, 0}};
problem.tasks[2].completion_weight = 5;
resource_scheduling_options initial_options;
initial_options.max_iterations = 0;
const auto initial = resource_scheduling_solve(problem, initial_options);
const auto output = improve_external_schedule(problem, initial.schedule, 20.0, 1);
assert(output.improved.objective <= output.initial_objective);
std::cout << output.initial_objective << ' ' << output.improved.objective << '\n';
```

この変換関数は元値も返すため、事前検査・元値評価・`improve`内検査の費用がかかる。実行可能な解と評価値を既に管理している場合は、公開の`resource_scheduling_improve`を直接呼ぶ方が準備費用を減らせる。

### 6.11 独立部分集合

`selected_global_tasks`は解き直す全体ID、`fixed_completion`は全体タスク数と同じ長さの配列で、外部先行タスクの確定完了時刻を入れる。読まれない要素には-1を入れてよい。外部先行から入る完了時刻は`int`の開始可能時刻へ変換できる範囲にする。

```cpp
resource_scheduling_problem whole;
whole.resources = {{1}};
whole.tasks.resize(3);
for (auto& task : whole.tasks) task.modes = {{2, {{0, 1}}, 0}};
whole.precedences = {{0, 1}, {1, 2}};
const std::vector<int> selected = {2, 1}; // ローカルID0→全体2、1→全体1。
const std::vector<long long> fixed_completion = {5, -1, -1};
const auto solution = solve_independent_task_subset(
    whole, selected, fixed_completion, 20.0, 1);
assert(solution.start_time_by_global_task[0] == -1); // 対象外は未設定。
assert(solution.start_time_by_global_task[1] >= 5);
for (int global : solution.global_task_order) {
    std::cout << global << ' ' << solution.start_time_by_global_task[global]
              << ' ' << solution.mode_by_global_task[global] << '\n';
}
```

`global_task_order`は選んだタスクだけの順序であり、全体問題の完全な順列ではない。`local_result.objective`も部分問題の値である。全体のmakespanが外部の固定完了時刻にも依存する場合、部分問題のmakespanとの重み付き和が全体目的の改善と一致するとは限らない。元の全体解へ戻して、全体目的と実行可能性を再検査する。

### 6.12 共通期限での複数試行

`restart_count`は実行する試行数の上限、`total_time_ms`は呼出し開始からの総時間の目安、`first_seed`は最初のseedである。seedは各試行で1ずつ増やす。各回の目的は組み込み式で統一される。

```cpp
resource_scheduling_problem problem;
problem.resources = {{2}};
problem.tasks.resize(4);
for (auto& task : problem.tasks) task.modes = {{3, {{0, 1}}, 0}};
const auto result = solve_with_multistart(problem, 3, 60.0, 100);
assert(resource_scheduling_is_feasible(problem, result.schedule));
std::cout << result.objective << '\n';
```

各回のカウンタを合計したい場合は、この例の関数内で別途加算する。返却カウンタは最良の1試行分である。最良値が同じなら先に得た結果を保持する。

### 6.13 固定反復

`solve_with_fixed_iterations`は、時間条件の影響を小さくするため相対上限を1,000,000msに設定し、指定反復数を上限にする。無制限時間を実現するAPIではなく、固定仕事量の検証用の例である。

```cpp
resource_scheduling_problem problem;
problem.resources = {{1}};
problem.tasks.resize(3);
for (auto& task : problem.tasks) task.modes = {{2, {{0, 1}}, 0}};
problem.tasks[0].completion_weight = 1;
const auto result = solve_with_fixed_iterations(problem, 200, 0);
std::cout << result.objective << ' ' << result.iterations
          << ' ' << result.decoded_schedules << ' ' << result.accepted_moves << '\n';
```

回数上限0、一意状態、空問題、時間条件による停止では、指定回数まで進まないことがある。`iterations`と`decoded_schedules`が一致するとは仮定しない。

### 6.14 ターン間の再計画

`replan_turn`は現在の問題と前の解へのポインタを受け取る。前の解がない、またはIDの意味を対応させられない場合は`nullptr`を渡す。返却値の`reused_previous`で改善開始に使ったか確認し、`result`から解を読む。

```cpp
resource_scheduling_problem current;
current.resources = {{2}};
current.tasks.resize(2);
for (auto& task : current.tasks) task.modes = {{3, {{0, 1}}, 0}};
resource_scheduling_options initial_options;
initial_options.max_iterations = 0;
const auto previous = resource_scheduling_solve(current, initial_options);

current.resources[0].capacity = 1; // 前の同時実行解は新容量で使えない可能性がある。
current.tasks[1].completion_weight = 2;
const auto deadline = std::chrono::steady_clock::now() + std::chrono::milliseconds(100);
const auto output = replan_turn(current, &previous.schedule, 20.0, 1, deadline);
assert(resource_scheduling_is_feasible(current, output.result.schedule));
std::cout << output.reused_previous << ' ' << output.result.objective << '\n';
```

不正な問題を自動修正する関数ではない。例えば容量0、容量を超えるモード、非DAGは例外になる。前の解を使えなかったときは、新規構築した解と前ターンの目的値の間に大小保証はない。

### 6.15 段階ごとに資源使用を変える工程

`solve_phased_projects`へ共通資源容量とプロジェクト一覧を渡す。各プロジェクトは開始可能時刻と段階列を持ち、各段階に処理時間と資源使用量ベクトルを指定する。全体終了時刻を最小化し、`start_time[project][phase]`で結果を返す。

```cpp
phased_project_input project;
project.release_time = 0;
project.phases = {
    {2, {1, 0}}, // 準備: 人員1、乾燥機なし。
    {5, {0, 1}}, // 乾燥: 人員なし、乾燥機1。
    {1, {1, 0}}  // 取出し: 人員1。
};
const std::vector<phased_project_input> projects = {project, project};
const auto solution = solve_phased_projects({2, 1}, projects, 20.0, 1);
for (int p = 0; p < static_cast<int>(projects.size()); ++p) {
    for (int phase = 0; phase < static_cast<int>(projects[p].phases.size()); ++phase) {
        std::cout << p << ' ' << phase << ' ' << solution.start_time[p][phase] << '\n';
    }
}
```

前段完了より後に次段が始まれば有効で、時刻がぴったり連続する保証はない。乾燥後の待機中にも乾燥機を占有するような設備では、この例の資源予約だけでは不足する。

## 7. 制約・注意点

### 7.1 表現できない制約

| 条件 | 何が不足するか・どう判断するか |
|---|---|
| タスクの途中中断・再開 | 1タスクは中断しない。段階分割できても、途中で自由に止める一般的な処理とは異なる |
| 時刻によって変わる資源容量 | 容量は全期間で一定。休憩・夜間停止・設備停止のcalendarを入力できない |
| 外部に固定した資源予約 | 既存予約を直接登録する公開APIがない。部分集合だけを解くときに外部競合を無視しない |
| hardな完了期限 | `due_time`はsoftな評価項。重みを増やしても期限以内の完了を保証しない |
| 直前の仕事に依存する段取り・移動 | モードの時間は固定。機械上の直前タスクや直前モードに応じて増減しない |
| 非再生可能資源 | 合計燃料、材料消費、総予算の上限は、同時使用量の容量とは異なる |
| 無待機・blocking | 先行辺が表すのは完了後に開始可能という下限だけ。連続開始や待機中の占有は保証しない |
| タスク選択・省略 | 全タスクを必ず実行する。省略によって先行関係も変わる条件は扱わない |
| タスク間のモード連動 | タスクAのモード選択に応じてBのモードを禁止するような対応関係は指定できない |
| グループ単位の開始時刻固定 | 開始時刻は固定条件として探索へ渡せない。`improve`の入力も固定を意味しない |

後段で検査して違反解を捨てることはできるが、その検査を追加しただけで、制約を満たす解を必ず見つけるsolverにはならない。特に、モデル外の制約を大きいpenaltyだけで表現した場合、その制約の実行可能性保証はない。

### 7.2 意図的な待機が有利な目的

モードを固定したとき、タスクの完了を遅らせるだけで評価が良くなる目的は、本ライブラリの探索対象と合わない。例えば早すぎる完了への罰則、時刻別の安い料金帯まで待つ利益、複数仕事を同時刻に終えたい費用が該当する。

Evaluatorの型として渡せても、必要な待機スケジュールを探索できる保証はない。`improve`へ与えた待機付きの元解を保持できることと、待機を変化させて探索できることも別である。独自式が対象に合うかは、単に「有限値を返すか」だけで判断しない。

### 7.3 入力で特に間違えやすい組合せ

| 間違い・思い込み | 実際の扱い |
|---|---|
| `modes`が空でも既定モードを補う | 各タスク1個以上が必須。空なら例外 |
| 使用量0を使用一覧へ入れる | `amount`は正。未使用資源は要素を省略する |
| 容量0の資源を登録する | 容量は正。利用停止を表すための0容量は使えない |
| 容量を超えるモードは選ばれなければよい | 全モードが検証されるため、未選択でも不正 |
| 納期を設定すればそれ以前に必ず終わる | softな評価基準。遅延重み0なら目的項は0 |
| `due_time=-1`なら遅延重みも完全に無影響 | 目的項には入らないが初期優先度には参照される |
| `task_order`は開始時刻順 | 優先順列であり、開始時刻順とは限らない |
| `mode_of_task[k]`が順序のk番目のモード | 添字はタスクID。順序位置と混同しない |
| 全体重み0ならタスク重みも無効 | タスクの完了・遅延重みは別に評価される |
| 全ての組み込み重み0なら探索が勝手に省かれる | 一意状態などを除き、同値moveも受理しながら探索し得る。不要なら反復上限0を使う |
| 独自Evaluatorなら問題の重み検証を省ける | 独自評価でもproblem内の重みは有限・非負でなければならない |
| 一度`validate_problem`すれば次回は検証不要 | 検証結果を保持するAPIはなく、各公開呼出しで再検証する |

同値の最良解が複数ある場合、元の解の配列形状や開始時刻まで保持する保証はない。最良結果は厳密に目的値が下がったときに更新されるため、全重み0の`solve`では基本的に最初の解が返る。

### 7.4 `improve`と部分問題

`initial`は、呼出し時の問題の全タスクを含む実行可能解でなければならない。開始時刻だけでなく、タスク順序、モード、配列長も検査される。`improve`は実行不能解のrepair APIではない。

独自Evaluatorが決定的なら、同じ新問題で測った元解より目的値が悪くない結果を返す。組み込み評価で作った解を独自Evaluatorで改善することもできるが、保証されるのは独自Evaluatorでの値であり、組み込み目的値まで改善するとは限らない。

部分集合を解いたときは、外部との資源競合、外向きの先行関係、全体目的への影響を確認する。特に「外部は固定なのだから考えなくてよい」とは限らない。外部の仕事が対象の実行時間帯に資源を使っていれば、その容量は既に使われている。ケース11は、この問題がない条件に限定した変換である。

### 7.5 時間制限と反復数

タスクの納期`due_time`と、計算の終了期限`options.deadline`は別物である。前者はスケジュール中の時刻、後者はCPU上の処理をいつ切り上げるかの`steady_clock`時刻を表す。

初期構築は時間制限前に必ず行う。過ぎた絶対期限や相対時間0を渡しても、即座に何もしないで戻るとは限らない。追加順序の評価は各回の開始前、SAでは8提案ごとに停止を確認し、1decodeの途中では中断しない。超過が一定ms以内になる保証はない。

相対時間と絶対期限と反復上限を併用すると、先に達した条件で改善が終わる。反復上限が正でも温度計算は経過時間の進捗を参照する。固定反復の結果を比較する場合も、コンパイラ、設定、入力、seed、時間枠を記録し、完全な再現性を前提にしない。

### 7.6 数値範囲・例外・メモリ

| 項目 | 条件・注意 |
|---|---|
| 入力の時刻・処理時間 | `int`。開始可能時刻と処理時間は0以上、納期は-1または0以上 |
| 出力の時刻 | `long long`。入力処理時間が`int`でも、工程をつないだ完了時刻は32bitを超え得る |
| 件数・ID | タスク、資源、各モード数、入出次数、内部区間添字は`int`で扱う。極端な件数を全て安全検査するAPIではない |
| 時刻加算 | 入力から作る時刻範囲と、外部解の開始時刻＋処理時間のオーバーフローを検査する |
| 重み・固定費用 | 個々の入力は有限値。重みは非負、固定費用は負値も可。積・和など中間値も実用的な範囲に収める |
| 独自評価の値 | solverと独自`evaluate`は有限値を要求し、非有限なら`std::invalid_argument` |
| 組み込み`evaluate` | 問題・解は検証するが、目的値の最終有限性を別途検査する分岐はない。極端な重みの積和で溢れないようにし、必要なら利用側でも`std::isfinite`を確認する |
| 時間オプション | `NaN`・無限大は不正。有限でも時計の変換・加算の範囲を超える巨大なmsを指定しない |
| dense calendar | 自動選択時は最大4,000,000セル、セル配列だけで32,000,000byte。問題・解・その他配列は別に必要 |
| interval calendar | 時刻範囲の数値そのものではなく、主に資源使用の区間境界数に依存する。非常に多いタスク・資源ではこちらも大きくなる |

不正な問題・オプション・入力スケジュールは主に`std::invalid_argument`になる。ただし`is_feasible`は「問題は有効だが解が不正」の場合だけfalseを返す。Evaluatorが自分で送出した例外はそのまま伝播し、メモリ確保失敗などの標準例外も起こり得る。第8章のadapterには、配列長不一致やID変換の範囲検査もある。

単一スレッドで動作する。Evaluatorが外部の可変状態へアクセスする場合、その整合性は利用側で管理する。

### 7.7 実行前後の短い確認手順

問題の組立て直後は、反復上限0で呼び、返却時刻・ID変換・独自の競技スコアを確認する。その後で探索時間を増やす。公開checkerは本モデルの実行可能性を調べるので、コンテスト固有の制約をモデル化し忘れていないかは、別の問題固有checkerでも確認する。

付属のテストは次で実行できる。`assert`を有効にするため、検証時に`-DNDEBUG`を付けない。

```bash
g++ -std=c++20 -O2 test_generic_resource_scheduler_v07.cpp -o test_scheduler
./test_scheduler
g++ -std=c++20 -O2 test_generic_resource_scheduler_guide_v07.cpp -o test_guide
./test_guide
```

## 8. ユースケースごとのコード例

各節は、ヘッダの読込み、入力・出力型、変換、solver呼出し、読戻しを含む独立した例である。必要な節をそのまま自分のソースへコピーし、第6章の同番号の呼出し例を`main`などへ置いて使える。複数の例を一緒に使っても、型名・関数名は衝突しない。コード例内の関数名は、このガイドが定義するadapterである。

配列の順序、時刻単位、0・-1の意味、返却値のID対応はコード内にもコメントしている。特に断りのない件数とIDは`int`で表せる範囲とする。ライブラリのモード使用一覧と異なり、例の`use`ベクトルでは未使用を0で指定し、変換時に省略する。モデル外の制約については第7章も確認する。

時間とseedを受け取る例では、値を省略せず呼出し側で決める。例の入力構造体に既定値がある場合も、実際の処理時間、資源使用、目的重みを明示すると意図を確認しやすい。戻り値のスケジュールは必ず実行可能性を確認した問題と同じID体系で読む。

### 8.1 1種類の累積資源を使う工程計画

```cpp
#include "generic_resource_scheduler_v07.hpp"

// 共通入力: time_limit_msは初期構築後の改善時間(ms)、seedはuint64_tの乱数seed。
// 時刻・durationは同じ整数単位。要素数とIDはintで表せる範囲に収める。
// 入力: worker_capacityは正の人数上限。tasks[i]はタスクiの時間と必要人数。
// precedencesの{a,b}はa完了後にbを開始する辺。全体をDAGにする。
// 出力: 入力タスクIDのままの開始時刻・mode(常に0)、目的値は最大完了時刻。

struct basic_rcpsp_task_input {
    int duration = 1; // 処理時間。0以上。
    int worker_use = 1; // 必要人数。0は資源を使わない。
};

resource_scheduling_result solve_basic_rcpsp(
    int worker_capacity,
    const std::vector<basic_rcpsp_task_input>& tasks,
    const std::vector<std::pair<int, int>>& precedences,
    double time_limit_ms,
    std::uint64_t seed) {
    resource_scheduling_problem problem;
    problem.resources = {{worker_capacity}};
    problem.tasks.resize(tasks.size());
    problem.makespan_weight = 1;
    problem.mode_cost_weight = 0;

    for (int task = 0; task < static_cast<int>(tasks.size()); ++task) {
        if (tasks[task].worker_use < 0) {
            throw std::invalid_argument("worker_useは0以上でなければならない");
        }
        scheduling_mode mode;
        mode.duration = tasks[task].duration;
        if (tasks[task].worker_use > 0) {
            mode.renewable_uses.push_back({0, tasks[task].worker_use});
        }
        problem.tasks[task].modes.push_back(std::move(mode));
    }
    for (const auto& [before, after] : precedences) {
        problem.precedences.push_back({before, after});
    }

    resource_scheduling_options options;
    options.time_limit_ms = time_limit_ms;
    options.seed = seed;
    return resource_scheduling_solve(problem, options);
}
```

### 8.2 複数種類の資源を同時に使う計画

```cpp
#include "generic_resource_scheduler_v07.hpp"

// 共通入力: time_limit_msは初期構築後の改善時間(ms)、seedはuint64_tの乱数seed。
// 時刻・durationは同じ整数単位。要素数とIDはintで表せる範囲に収める。
// 入力: capacities[r]は正の資源容量。tasks[i].useは同じ資源順序・同じ長さ。
// precedencesの{a,b}はタスクa→b。use=0は未使用として省略する。
// 出力: 入力タスクIDの開始時刻・mode(常に0)、目的値は最大完了時刻。

struct cumulative_task_input {
    int duration = 1; // 処理時間。0以上。
    std::vector<int> use; // 容量配列と同じ長さ・同じ資源順序。未使用は0。
};

resource_scheduling_result solve_cumulative_resources(
    const std::vector<int>& capacities,
    const std::vector<cumulative_task_input>& tasks,
    const std::vector<std::pair<int, int>>& precedences,
    double time_limit_ms,
    std::uint64_t seed) {
    resource_scheduling_problem problem;
    for (int capacity : capacities) problem.resources.push_back({capacity});
    problem.tasks.resize(tasks.size());
    problem.makespan_weight = 1;
    problem.mode_cost_weight = 0;

    for (int task = 0; task < static_cast<int>(tasks.size()); ++task) {
        if (tasks[task].use.size() != capacities.size()) {
            throw std::invalid_argument("useの長さが資源数と一致しない");
        }
        scheduling_mode mode;
        mode.duration = tasks[task].duration;
        for (int resource = 0;
             resource < static_cast<int>(capacities.size()); ++resource) {
            const int amount = tasks[task].use[resource];
            if (amount < 0) {
                throw std::invalid_argument("資源使用量は0以上でなければならない");
            }
            if (amount > 0) mode.renewable_uses.push_back({resource, amount});
        }
        problem.tasks[task].modes.push_back(std::move(mode));
    }
    for (const auto& [before, after] : precedences) {
        problem.precedences.push_back({before, after});
    }

    resource_scheduling_options options;
    options.time_limit_ms = time_limit_ms;
    options.seed = seed;
    return resource_scheduling_solve(problem, options);
}
```

### 8.3 複数の実行方法を選ぶ計画

```cpp
#include "generic_resource_scheduler_v07.hpp"

// 共通入力: time_limit_msは初期構築後の改善時間(ms)、seedはuint64_tの乱数seed。
// 時刻・durationは同じ整数単位。要素数とIDはintで表せる範囲に収める。
// 入力: capacitiesは全タスク共通の正の容量。各taskに1個以上のmodeを入れる。
// 各mode.useはcapacitiesと同じ長さ・順序。precedencesはタスクIDのDAG。
// makespan_weightとmode_cost_weightは有限・非負の目的係数。
// 出力: タスクIDごとの開始時刻・選択mode添字、全指定項を合計した目的値。

struct mrcpsp_mode_input {
    int duration = 1; // 処理時間。0以上。
    std::vector<int> use; // 容量配列と同じ長さ・同じ資源順序。未使用は0。
    long double fixed_cost = 0; // モード1回の有限費用。負値も可。
};

struct mrcpsp_task_input {
    int release_time = 0; // 開始可能時刻。0以上。
    int due_time = -1; // 納期なしは-1。設定するなら0以上。
    long double completion_weight = 0; // 完了時刻の有限・非負重み。
    long double tardiness_weight = 0; // 納期超過の有限・非負重み。
    std::vector<mrcpsp_mode_input> modes; // 必ず1個以上の実行方法。
};

resource_scheduling_result solve_mrcpsp(
    const std::vector<int>& capacities,
    const std::vector<mrcpsp_task_input>& tasks,
    const std::vector<std::pair<int, int>>& precedences,
    long double makespan_weight,
    long double mode_cost_weight,
    double time_limit_ms,
    std::uint64_t seed) {
    resource_scheduling_problem problem;
    for (int capacity : capacities) problem.resources.push_back({capacity});
    problem.tasks.resize(tasks.size());
    problem.makespan_weight = makespan_weight;
    problem.mode_cost_weight = mode_cost_weight;

    for (int task = 0; task < static_cast<int>(tasks.size()); ++task) {
        const auto& input = tasks[task];
        auto& output = problem.tasks[task];
        output.release_time = input.release_time;
        output.due_time = input.due_time;
        output.completion_weight = input.completion_weight;
        output.tardiness_weight = input.tardiness_weight;

        for (const mrcpsp_mode_input& input_mode : input.modes) {
            if (input_mode.use.size() != capacities.size()) {
                throw std::invalid_argument("useの長さが資源数と一致しない");
            }
            scheduling_mode mode;
            mode.duration = input_mode.duration;
            mode.fixed_cost = input_mode.fixed_cost;
            for (int resource = 0;
                 resource < static_cast<int>(capacities.size()); ++resource) {
                const int amount = input_mode.use[resource];
                if (amount < 0) {
                    throw std::invalid_argument("資源使用量は0以上でなければならない");
                }
                if (amount > 0) {
                    mode.renewable_uses.push_back({resource, amount});
                }
            }
            output.modes.push_back(std::move(mode));
        }
    }
    for (const auto& [before, after] : precedences) {
        problem.precedences.push_back({before, after});
    }

    resource_scheduling_options options;
    options.time_limit_ms = time_limit_ms;
    options.seed = seed;
    return resource_scheduling_solve(problem, options);
}
```

### 8.4 同一並列機械

```cpp
#include "generic_resource_scheduler_v07.hpp"

// 共通入力: time_limit_msは初期構築後の改善時間(ms)、seedはuint64_tの乱数seed。
// 時刻・durationは同じ整数単位。要素数とIDはintで表せる範囲に収める。
// 入力: machine_countは同じ性能の機械の正の台数。全仕事の使用量は1。
// jobs[i]に時間・開始可能時刻・納期・完了重み・遅延重みを指定する。
// makespan_weightは有限・非負。先行制約なし、mode費用なしの例。
// 出力: 仕事IDごとの開始時刻と目的値。機械番号は返さず、modeは常に0。

struct parallel_job_input {
    int duration = 1; // 処理時間。0以上。
    int release_time = 0; // 開始可能時刻。0以上。
    int due_time = -1; // 納期なしは-1。設定するなら0以上。
    long double completion_weight = 0; // 完了時刻の有限・非負重み。
    long double tardiness_weight = 0; // 納期超過の有限・非負重み。
};

resource_scheduling_result solve_identical_parallel_machines(
    int machine_count,
    const std::vector<parallel_job_input>& jobs,
    long double makespan_weight,
    double time_limit_ms,
    std::uint64_t seed) {
    resource_scheduling_problem problem;
    problem.resources = {{machine_count}};
    problem.tasks.resize(jobs.size());
    problem.makespan_weight = makespan_weight;
    problem.mode_cost_weight = 0;

    for (int job = 0; job < static_cast<int>(jobs.size()); ++job) {
        const parallel_job_input& input = jobs[job];
        scheduling_task& task = problem.tasks[job];
        task.release_time = input.release_time;
        task.due_time = input.due_time;
        task.completion_weight = input.completion_weight;
        task.tardiness_weight = input.tardiness_weight;
        task.modes = {{input.duration, {{0, 1}}, 0}};
    }

    resource_scheduling_options options;
    options.time_limit_ms = time_limit_ms;
    options.seed = seed;
    return resource_scheduling_solve(problem, options);
}
```

### 8.5 Job Shop

```cpp
#include "generic_resource_scheduler_v07.hpp"

// 共通入力: time_limit_msは初期構築後の改善時間(ms)、seedはuint64_tの乱数seed。
// 時刻・durationは同じ整数単位。要素数とIDはintで表せる範囲に収める。
// 入力: machine_countは正の機械数。jobs[j][op]は工程順の機械IDと時間。
// 各機械IDは0以上machine_count未満。工程間の待機を許す。
// 出力: start_time[j][op]、makespan、平坦化したタスクIDのraw_result。

struct job_shop_operation_input {
    int machine = 0; // 機械一覧の0始まりID。
    int duration = 1; // 処理時間。0以上。
};

struct job_shop_solution {
    std::vector<std::vector<long long>> start_time;
    long long makespan = 0;
    resource_scheduling_result raw_result;
};

job_shop_solution solve_job_shop(
    int machine_count,
    const std::vector<std::vector<job_shop_operation_input>>& jobs,
    double time_limit_ms,
    std::uint64_t seed) {
    if (machine_count <= 0) {
        throw std::invalid_argument("machine_countは正でなければならない");
    }

    resource_scheduling_problem problem;
    problem.resources.assign(static_cast<std::size_t>(machine_count), {1});
    problem.makespan_weight = 1;
    problem.mode_cost_weight = 0;

    std::vector<std::vector<int>> task_id(jobs.size());
    int task_count = 0;
    for (int job = 0; job < static_cast<int>(jobs.size()); ++job) {
        if (jobs[job].size() >
            static_cast<std::size_t>(
                std::numeric_limits<int>::max() - task_count)) {
            throw std::length_error("operation数がint範囲を超える");
        }
        task_id[job].resize(jobs[job].size());
        for (int operation = 0;
             operation < static_cast<int>(jobs[job].size()); ++operation) {
            task_id[job][operation] = task_count++;
        }
    }
    problem.tasks.resize(static_cast<std::size_t>(task_count));

    for (int job = 0; job < static_cast<int>(jobs.size()); ++job) {
        for (int operation = 0;
             operation < static_cast<int>(jobs[job].size()); ++operation) {
            const int task = task_id[job][operation];
            const auto& input = jobs[job][operation];
            problem.tasks[task].modes = {
                {input.duration, {{input.machine, 1}}, 0}
            };
            if (operation > 0) {
                problem.precedences.push_back(
                    {task_id[job][operation - 1], task});
            }
        }
    }

    resource_scheduling_options options;
    options.time_limit_ms = time_limit_ms;
    options.seed = seed;
    resource_scheduling_result result =
        resource_scheduling_solve(problem, options);

    job_shop_solution solution;
    solution.start_time.resize(jobs.size());
    for (int job = 0; job < static_cast<int>(jobs.size()); ++job) {
        solution.start_time[job].resize(jobs[job].size());
        for (int operation = 0;
             operation < static_cast<int>(jobs[job].size()); ++operation) {
            const int task = task_id[job][operation];
            solution.start_time[job][operation] = result.schedule.start_time[task];
            solution.makespan = std::max(
                solution.makespan,
                result.schedule.start_time[task] + jobs[job][operation].duration);
        }
    }
    solution.raw_result = std::move(result);
    return solution;
}
```

### 8.6 Flexible Job Shop

```cpp
#include "generic_resource_scheduler_v07.hpp"

// 共通入力: time_limit_msは初期構築後の改善時間(ms)、seedはuint64_tの乱数seed。
// 時刻・durationは同じ整数単位。要素数とIDはintで表せる範囲に収める。
// 入力: jobs[j][op]は1個以上の代替案。各案に機械ID・時間・費用を指定。
// machine_countは正、各機械IDは範囲内。mode_cost_weightは有限・非負。
// 出力: start_time[j][op]、選択machine、選択mode(代替案添字)、raw_result。

struct flexible_alternative_input {
    int machine = 0; // 機械一覧の0始まりID。
    int duration = 1; // 処理時間。0以上。
    long double fixed_cost = 0; // モード1回の有限費用。負値も可。
};

using flexible_operation_input = std::vector<flexible_alternative_input>;

struct flexible_job_shop_solution {
    std::vector<std::vector<long long>> start_time;
    std::vector<std::vector<int>> machine;
    std::vector<std::vector<int>> mode;
    resource_scheduling_result raw_result;
};

flexible_job_shop_solution solve_flexible_job_shop(
    int machine_count,
    const std::vector<std::vector<flexible_operation_input>>& jobs,
    long double mode_cost_weight,
    double time_limit_ms,
    std::uint64_t seed) {
    if (machine_count <= 0) {
        throw std::invalid_argument("machine_countは正でなければならない");
    }

    resource_scheduling_problem problem;
    problem.resources.assign(static_cast<std::size_t>(machine_count), {1});
    problem.makespan_weight = 1;
    problem.mode_cost_weight = mode_cost_weight;

    std::vector<std::vector<int>> task_id(jobs.size());
    int task_count = 0;
    for (int job = 0; job < static_cast<int>(jobs.size()); ++job) {
        if (jobs[job].size() >
            static_cast<std::size_t>(
                std::numeric_limits<int>::max() - task_count)) {
            throw std::length_error("operation数がint範囲を超える");
        }
        task_id[job].resize(jobs[job].size());
        for (int operation = 0;
             operation < static_cast<int>(jobs[job].size()); ++operation) {
            task_id[job][operation] = task_count++;
        }
    }
    problem.tasks.resize(static_cast<std::size_t>(task_count));

    for (int job = 0; job < static_cast<int>(jobs.size()); ++job) {
        for (int operation = 0;
             operation < static_cast<int>(jobs[job].size()); ++operation) {
            const int task = task_id[job][operation];
            for (const flexible_alternative_input& alternative :
                 jobs[job][operation]) {
                problem.tasks[task].modes.push_back({
                    alternative.duration,
                    {{alternative.machine, 1}},
                    alternative.fixed_cost
                });
            }
            if (operation > 0) {
                problem.precedences.push_back(
                    {task_id[job][operation - 1], task});
            }
        }
    }

    resource_scheduling_options options;
    options.time_limit_ms = time_limit_ms;
    options.seed = seed;
    resource_scheduling_result result =
        resource_scheduling_solve(problem, options);

    flexible_job_shop_solution solution;
    solution.start_time.resize(jobs.size());
    solution.machine.resize(jobs.size());
    solution.mode.resize(jobs.size());
    for (int job = 0; job < static_cast<int>(jobs.size()); ++job) {
        const std::size_t operation_count = jobs[job].size();
        solution.start_time[job].resize(operation_count);
        solution.machine[job].resize(operation_count);
        solution.mode[job].resize(operation_count);
        for (int operation = 0;
             operation < static_cast<int>(operation_count); ++operation) {
            const int task = task_id[job][operation];
            const int mode = result.schedule.mode_of_task[task];
            solution.start_time[job][operation] = result.schedule.start_time[task];
            solution.mode[job][operation] = mode;
            solution.machine[job][operation] =
                jobs[job][operation][mode].machine;
        }
    }
    solution.raw_result = std::move(result);
    return solution;
}
```

### 8.7 開始可能時刻・納期・重要度を考慮する計画

```cpp
#include "generic_resource_scheduler_v07.hpp"

// 共通入力: time_limit_msは初期構築後の改善時間(ms)、seedはuint64_tの乱数seed。
// 時刻・durationは同じ整数単位。要素数とIDはintで表せる範囲に収める。
// 入力: problemには資源・モード・先行辺を設定済みとする。
// priorities[i]でタスクiの時刻条件と2重みを上書きする。長さはタスク数。
// makespan_weightを上書きし、problem.mode_cost_weightはそのまま使う。
// 出力: 上書き後の問題でのresult。元のproblem自体は変更しない。

struct task_timing_priority {
    int release_time = 0; // 開始可能時刻。0以上。
    int due_time = -1; // 納期なしは-1。設定するなら0以上。
    long double completion_weight = 0; // 完了時刻の有限・非負重み。
    long double tardiness_weight = 0; // 納期超過の有限・非負重み。
};

resource_scheduling_result solve_with_timing_priorities(
    resource_scheduling_problem problem,
    const std::vector<task_timing_priority>& priorities,
    long double makespan_weight,
    double time_limit_ms,
    std::uint64_t seed) {
    if (priorities.size() != problem.tasks.size()) {
        throw std::invalid_argument("priority数がtask数と一致しない");
    }
    problem.makespan_weight = makespan_weight;
    for (int task = 0; task < static_cast<int>(problem.tasks.size()); ++task) {
        problem.tasks[task].release_time = priorities[task].release_time;
        problem.tasks[task].due_time = priorities[task].due_time;
        problem.tasks[task].completion_weight =
            priorities[task].completion_weight;
        problem.tasks[task].tardiness_weight =
            priorities[task].tardiness_weight;
    }

    resource_scheduling_options options;
    options.time_limit_ms = time_limit_ms;
    options.seed = seed;
    return resource_scheduling_solve(problem, options);
}
```

### 8.8 複数プロジェクトの共有資源計画

```cpp
#include "generic_resource_scheduler_v07.hpp"

// 共通入力: time_limit_msは初期構築後の改善時間(ms)、seedはuint64_tの乱数seed。
// 時刻・durationは同じ整数単位。要素数とIDはintで表せる範囲に収める。
// 入力: capacitiesは全project共通。mode内の資源IDもこの共通配列の添字。
// projects[p].precedencesはproject内タスクIDのDAG。project間の辺はない例。
// makespan_weightとmode_cost_weightは有限・非負の目的係数。
// 出力: start_time[p][i]、mode[p][i]、project順に統合したIDのraw_result。

struct shared_project_input {
    std::vector<scheduling_task> tasks; // project内IDの仕事一覧。各modesは非空。
    std::vector<std::pair<int, int>> precedences; // project内IDのbefore→after。
};

struct shared_projects_solution {
    std::vector<std::vector<long long>> start_time;
    std::vector<std::vector<int>> mode;
    resource_scheduling_result raw_result;
};

shared_projects_solution solve_shared_projects(
    const std::vector<int>& capacities,
    const std::vector<shared_project_input>& projects,
    long double makespan_weight,
    long double mode_cost_weight,
    double time_limit_ms,
    std::uint64_t seed) {
    resource_scheduling_problem problem;
    for (int capacity : capacities) problem.resources.push_back({capacity});
    problem.makespan_weight = makespan_weight;
    problem.mode_cost_weight = mode_cost_weight;

    std::vector<int> offset(projects.size() + 1, 0);
    for (int project = 0; project < static_cast<int>(projects.size()); ++project) {
        if (projects[project].tasks.size() >
            static_cast<std::size_t>(
                std::numeric_limits<int>::max() - offset[project])) {
            throw std::length_error("全projectのtask数がint範囲を超える");
        }
        offset[project + 1] =
            offset[project] + static_cast<int>(projects[project].tasks.size());
        problem.tasks.insert(
            problem.tasks.end(),
            projects[project].tasks.begin(),
            projects[project].tasks.end());
    }

    for (int project = 0; project < static_cast<int>(projects.size()); ++project) {
        const int task_count = static_cast<int>(projects[project].tasks.size());
        for (const auto& [before, after] : projects[project].precedences) {
            if (before < 0 || before >= task_count ||
                after < 0 || after >= task_count) {
                throw std::invalid_argument("project内task IDが範囲外");
            }
            problem.precedences.push_back(
                {offset[project] + before, offset[project] + after});
        }
    }

    resource_scheduling_options options;
    options.time_limit_ms = time_limit_ms;
    options.seed = seed;
    resource_scheduling_result result =
        resource_scheduling_solve(problem, options);

    shared_projects_solution solution;
    solution.start_time.resize(projects.size());
    solution.mode.resize(projects.size());
    for (int project = 0; project < static_cast<int>(projects.size()); ++project) {
        const int task_count = static_cast<int>(projects[project].tasks.size());
        solution.start_time[project].resize(static_cast<std::size_t>(task_count));
        solution.mode[project].resize(static_cast<std::size_t>(task_count));
        for (int local_task = 0; local_task < task_count; ++local_task) {
            const int task = offset[project] + local_task;
            solution.start_time[project][local_task] =
                result.schedule.start_time[task];
            solution.mode[project][local_task] =
                result.schedule.mode_of_task[task];
        }
    }
    solution.raw_result = std::move(result);
    return solution;
}
```

### 8.9 残り工程時間まで含めた部分問題

```cpp
#include "generic_resource_scheduler_v07.hpp"

// 共通入力: time_limit_msは初期構築後の改善時間(ms)、seedはuint64_tの乱数seed。
// 時刻・durationは同じ整数単位。要素数とIDはintで表せる範囲に収める。
// 入力: problemは局所工程の問題。tail_time[i]は完了後の固定後段時間(0以上)。
// tie_weight[i]は有限・非負、tie_scaleはその補助項の有限・非負係数。
// 両配列の長さはタスク数。後段で外部タスクと競合しないモデルを想定する。
// 出力: max(C_i+tail_i)+tie_scale*sum(tie_weight_i*C_i)+mode費用の最小化結果。
// 元のproblemは変更しない。Evaluatorは組み込みの納期項を加えない。

struct tail_aware_evaluator {
    std::vector<int> tail_time; // タスクIDごとの固定後段時間。
    std::vector<long double> tie_weight; // タスクIDごとの補助完了重み。
    long double tie_scale = 0; // 補助完了項の係数。有限・非負。

    long double operator()(
        const resource_scheduling_problem& problem,
        const resource_schedule& schedule) const {
        long double bottleneck = 0;
        long double weighted_completion = 0;
        long double mode_cost = 0;
        for (int task = 0; task < static_cast<int>(problem.tasks.size()); ++task) {
            const scheduling_mode& mode =
                problem.tasks[task].modes[schedule.mode_of_task[task]];
            const long long completion = schedule.start_time[task] + mode.duration;
            bottleneck = std::max(
                bottleneck,
                static_cast<long double>(completion) + tail_time[task]);
            weighted_completion += tie_weight[task] * completion;
            mode_cost += mode.fixed_cost;
        }
        return bottleneck + tie_scale * weighted_completion +
               problem.mode_cost_weight * mode_cost;
    }
};

resource_scheduling_result solve_tail_aware_subproblem(
    resource_scheduling_problem problem,
    const std::vector<int>& tail_time,
    const std::vector<long double>& tie_weight,
    long double tie_scale,
    double time_limit_ms,
    std::uint64_t seed) {
    if (tail_time.size() != problem.tasks.size() ||
        tie_weight.size() != problem.tasks.size()) {
        throw std::invalid_argument("Evaluator配列の長さがtask数と一致しない");
    }
    if (!std::isfinite(tie_scale) || tie_scale < 0) {
        throw std::invalid_argument("tie_scaleは有限かつ0以上でなければならない");
    }
    for (int task = 0; task < static_cast<int>(problem.tasks.size()); ++task) {
        if (tail_time[task] < 0 || !std::isfinite(tie_weight[task]) ||
            tie_weight[task] < 0) {
            throw std::invalid_argument("tail_timeとtie_weightが不正");
        }

        // custom objectiveの代理値を初期解heuristicへ与える
        problem.tasks[task].completion_weight = tie_scale * tie_weight[task];
        problem.tasks[task].tardiness_weight = 0;
        problem.tasks[task].due_time = -1;
    }
    problem.makespan_weight = 1;

    resource_scheduling_options options;
    options.time_limit_ms = time_limit_ms;
    options.seed = seed;
    return resource_scheduling_solve(
        problem,
        tail_aware_evaluator{tail_time, tie_weight, tie_scale},
        options);
}
```

### 8.10 外部で作った実行可能解の改善

```cpp
#include "generic_resource_scheduler_v07.hpp"

// 共通入力: time_limit_msは初期構築後の改善時間(ms)、seedはuint64_tの乱数seed。
// 時刻・durationは同じ整数単位。要素数とIDはintで表せる範囲に収める。
// 入力: problemは現在の問題。initialは全タスクを含む実行可能な3配列。
// task_orderもトポロジカル順列にする。開始時刻が正しいだけでは足りない。
// 出力: 現在の問題でのinitial_objectiveと、それ以下のimproved.objective。
// 元解の比較値も返すため、事前検査・evaluate・improve内検査の費用がかかる。

struct schedule_improvement {
    long double initial_objective = 0; // 呼出し時のproblemでの元解の目的値。
    resource_scheduling_result improved;
};

schedule_improvement improve_external_schedule(
    const resource_scheduling_problem& problem,
    const resource_schedule& initial,
    double time_limit_ms,
    std::uint64_t seed) {
    if (!resource_scheduling_is_feasible(problem, initial)) {
        throw std::invalid_argument("initial scheduleが実行可能でない");
    }

    schedule_improvement output;
    output.initial_objective =
        resource_scheduling_evaluate(problem, initial);

    resource_scheduling_options options;
    options.time_limit_ms = time_limit_ms;
    options.seed = seed;
    output.improved =
        resource_scheduling_improve(problem, initial, options);
    return output;
}
```

### 8.11 資源競合しない部分集合の切出し

```cpp
#include "generic_resource_scheduler_v07.hpp"

// 共通入力: time_limit_msは初期構築後の改善時間(ms)、seedはuint64_tの乱数seed。
// 時刻・durationは同じ整数単位。要素数とIDはintで表せる範囲に収める。
// 入力: whole_problemは有効な全体問題。selected_global_tasksは重複のない対象ID。
// fixed_completionは全体タスク数と同じ長さ。外部先行タスクの完了を指定する。
// 読まれないfixed_completion要素には-1可。外部先行の完了はintへ変換可能な非負値。
// 前提: 対象と外部の資源競合がなく、外部の固定開始時刻への終了上限制約もない。
// 出力: ID対応、対象だけのglobal_task_order、全体IDの時刻/mode(対象外は-1)、local_result。
// 全体の実行可能性と目的値は、元の全体解へ結果を戻した後に利用側で検査する。

struct selected_subproblem_solution {
    std::vector<int> local_to_global;
    std::vector<int> global_task_order;
    std::vector<long long> start_time_by_global_task;
    std::vector<int> mode_by_global_task;
    resource_scheduling_result local_result;
};

selected_subproblem_solution solve_independent_task_subset(
    const resource_scheduling_problem& whole_problem,
    const std::vector<int>& selected_global_tasks,
    const std::vector<long long>& fixed_completion,
    double time_limit_ms,
    std::uint64_t seed) {
    resource_scheduling_validate_problem(whole_problem);
    const int global_task_count =
        static_cast<int>(whole_problem.tasks.size());
    if (fixed_completion.size() != whole_problem.tasks.size()) {
        throw std::invalid_argument("fixed_completionの長さがtask数と一致しない");
    }

    std::vector<int> global_to_local(
        static_cast<std::size_t>(global_task_count), -1);
    for (int local = 0;
         local < static_cast<int>(selected_global_tasks.size()); ++local) {
        const int global = selected_global_tasks[local];
        if (global < 0 || global >= global_task_count ||
            global_to_local[global] != -1) {
            throw std::invalid_argument("selected task IDが不正または重複");
        }
        global_to_local[global] = local;
    }

    resource_scheduling_problem local_problem;
    local_problem.resources = whole_problem.resources;
    local_problem.makespan_weight = whole_problem.makespan_weight;
    local_problem.mode_cost_weight = whole_problem.mode_cost_weight;
    for (int global : selected_global_tasks) {
        local_problem.tasks.push_back(whole_problem.tasks[global]);
    }

    for (const scheduling_precedence& edge : whole_problem.precedences) {
        const int local_before = global_to_local[edge.before];
        const int local_after = global_to_local[edge.after];
        if (local_before != -1 && local_after != -1) {
            local_problem.precedences.push_back({local_before, local_after});
        } else if (local_before == -1 && local_after != -1) {
            const long long completion = fixed_completion[edge.before];
            if (completion < 0 ||
                completion > std::numeric_limits<int>::max()) {
                throw std::invalid_argument(
                    "外部先行taskの完了時刻をrelease_timeへ変換できない");
            }
            local_problem.tasks[local_after].release_time = std::max(
                local_problem.tasks[local_after].release_time,
                static_cast<int>(completion));
        }
    }

    resource_scheduling_options options;
    options.time_limit_ms = time_limit_ms;
    options.seed = seed;
    resource_scheduling_result result =
        resource_scheduling_solve(local_problem, options);

    selected_subproblem_solution solution;
    solution.local_to_global = selected_global_tasks;
    solution.start_time_by_global_task.assign(
        static_cast<std::size_t>(global_task_count), -1);
    solution.mode_by_global_task.assign(
        static_cast<std::size_t>(global_task_count), -1);
    for (int local = 0;
         local < static_cast<int>(selected_global_tasks.size()); ++local) {
        const int global = selected_global_tasks[local];
        solution.start_time_by_global_task[global] =
            result.schedule.start_time[local];
        solution.mode_by_global_task[global] =
            result.schedule.mode_of_task[local];
    }
    for (int local : result.schedule.task_order) {
        solution.global_task_order.push_back(selected_global_tasks[local]);
    }
    solution.local_result = std::move(result);
    return solution;
}
```

### 8.12 共通期限内の複数試行

```cpp
#include "generic_resource_scheduler_v07.hpp"

// 入力: problemは有効な問題。restart_countは正の最大試行数。
// total_time_msは有限・非負の総時間の目安。時計に収まる実用的な値を使う。
// first_seedから1ずつ増やして直列に試す。各回の初期構築費用も総時間へ含む。
// 出力: 最良の1試行のresult。カウンタは全試行の合計ではない。
// 総時間0でも初回の構築は行う。期限はsoftで、終了超過を一定msには保証しない。

resource_scheduling_result solve_with_multistart(
    const resource_scheduling_problem& problem,
    int restart_count,
    double total_time_ms,
    std::uint64_t first_seed) {
    if (restart_count <= 0) {
        throw std::invalid_argument("restart_countは正でなければならない");
    }
    if (!std::isfinite(total_time_ms) || total_time_ms < 0) {
        throw std::invalid_argument("total_time_msは有限かつ0以上でなければならない");
    }

    const auto begin = std::chrono::steady_clock::now();
    const auto deadline =
        begin + std::chrono::duration_cast<std::chrono::steady_clock::duration>(
                    std::chrono::duration<double, std::milli>(total_time_ms));
    std::optional<resource_scheduling_result> best;

    for (int restart = 0; restart < restart_count; ++restart) {
        const auto now = std::chrono::steady_clock::now();
        if (restart > 0 && now >= deadline) break;

        const int remaining_runs = restart_count - restart;
        const double remaining_ms = std::max(
            0.0,
            std::chrono::duration<double, std::milli>(deadline - now).count());
        resource_scheduling_options options;
        options.time_limit_ms = remaining_ms / remaining_runs;
        options.deadline = deadline;
        options.seed = first_seed + static_cast<std::uint64_t>(restart);

        resource_scheduling_result result =
            resource_scheduling_solve(problem, options);
        if (!best.has_value() || result.objective < best->objective) {
            best = std::move(result);
        }
    }
    return std::move(*best);
}
```

### 8.13 固定反復による比較・検査

```cpp
#include "generic_resource_scheduler_v07.hpp"

// 入力: problemは有効な問題。max_iterationsは0以上の反復上限、seedは乱数seed。
// 反復は追加順序評価とSA提案の合計。無効提案も数える。
// 出力: result。固定反復でも時間による冷却・停止の影響は完全には消えない。
// 開発用: 相対時間を1,000,000msにするので、そのまま競技本番へ使わない。

resource_scheduling_result solve_with_fixed_iterations(
    const resource_scheduling_problem& problem,
    long long max_iterations,
    std::uint64_t seed) {
    if (max_iterations < 0) {
        throw std::invalid_argument("max_iterationsは0以上でなければならない");
    }
    resource_scheduling_options options;
    options.time_limit_ms = 1'000'000;
    options.max_iterations = max_iterations;
    options.seed = seed;
    return resource_scheduling_solve(problem, options);
}
```

### 8.14 ターン間の再計画

```cpp
#include "generic_resource_scheduler_v07.hpp"

struct turn_replanning_result {
    bool reused_previous = false; // trueなら、前の実行可能解からimproveした。
    resource_scheduling_result result; // 現在の問題に対する解と目的値。
};

// 入力:
// current_problem: 現在の全タスク・資源・モード・先行関係・目的の重み。
// previous: 同じ意味のタスクID/モード添字を使う前の解。対応できなければnullptr。
// time_limit_ms: 初期構築後の改善ms。0でも初期構築・元解比較は行う。
// seed: uint64_tの乱数seed。0も可。
// deadline: 全呼出しで共有するsteady_clockの期限。省略時は共通期限なし。
// 出力: reused_previousと、現在の問題に対するresult。
// 前提: 実行済み/実行中タスクや外部の固定資源予約は利用側で処理済み。
// 前の解が実行可能でも、IDの「意味」の対応はこの関数では判定できない。
turn_replanning_result replan_turn(
    const resource_scheduling_problem& current_problem,
    const resource_schedule* previous,
    double time_limit_ms,
    std::uint64_t seed,
    std::optional<std::chrono::steady_clock::time_point> deadline = std::nullopt) {
    resource_scheduling_options options;
    options.time_limit_ms = time_limit_ms;
    options.seed = seed;
    options.deadline = deadline;

    turn_replanning_result output;
    if (previous != nullptr &&
        resource_scheduling_is_feasible(current_problem, *previous)) {
        output.reused_previous = true;
        output.result = resource_scheduling_improve(current_problem, *previous, options);
    } else {
        output.result = resource_scheduling_solve(current_problem, options);
    }
    return output;
}
```

### 8.15 資源使用が段階ごとに変わる工程

```cpp
#include "generic_resource_scheduler_v07.hpp"

struct phase_input {
    int duration = 1; // この段階の処理時間。0以上、全段階で同じ時間単位。
    std::vector<int> use; // capacitiesと同じ長さ・資源順序。未使用は0。
};

struct phased_project_input {
    int release_time = 0; // 最初の段階を始められる時刻。0以上。
    std::vector<phase_input> phases; // 実行順の段階列。空ならそのprojectの仕事はない。
};

struct phased_projects_solution {
    std::vector<std::vector<long long>> start_time; // [project][phase]の開始時刻。
    resource_scheduling_result raw_result; // project順・phase順の平坦化IDによる結果。
};

// 入力:
// capacities[r]: 全projectで共有する資源rの正の容量。
// projects[p]: project pの開始可能時刻と段階列。段階間の待機を許す。
// time_limit_ms: 初期構築後の改善ms。seed: uint64_tの乱数seed。
// 出力: 各project・各phaseの開始時刻。目的は全段階の最大完了時刻。
// 注意: 各段階の間では資源を解放する。無待機・待機中の占有は保証しない。
phased_projects_solution solve_phased_projects(
    const std::vector<int>& capacities,
    const std::vector<phased_project_input>& projects,
    double time_limit_ms,
    std::uint64_t seed) {
    resource_scheduling_problem problem;
    for (int capacity : capacities) problem.resources.push_back({capacity});
    problem.makespan_weight = 1;
    problem.mode_cost_weight = 0;

    std::vector<int> offset(projects.size() + 1, 0);
    for (std::size_t p = 0; p < projects.size(); ++p) {
        if (projects[p].release_time < 0) {
            throw std::invalid_argument("release_timeは0以上でなければならない");
        }
        if (projects[p].phases.size() >
            static_cast<std::size_t>(std::numeric_limits<int>::max() - offset[p])) {
            throw std::length_error("全段階数がint範囲を超える");
        }
        offset[p + 1] = offset[p] + static_cast<int>(projects[p].phases.size());
        for (std::size_t phase = 0; phase < projects[p].phases.size(); ++phase) {
            const auto& input = projects[p].phases[phase];
            if (input.use.size() != capacities.size()) {
                throw std::invalid_argument("useの長さが資源数と一致しない");
            }
            scheduling_mode mode;
            mode.duration = input.duration;
            for (int r = 0; r < static_cast<int>(capacities.size()); ++r) {
                if (input.use[r] < 0) {
                    throw std::invalid_argument("資源使用量は0以上でなければならない");
                }
                if (input.use[r] > 0) mode.renewable_uses.push_back({r, input.use[r]});
            }
            scheduling_task task;
            task.release_time = phase == 0 ? projects[p].release_time : 0;
            task.modes.push_back(std::move(mode));
            problem.tasks.push_back(std::move(task));
            if (phase > 0) {
                const int id = offset[p] + static_cast<int>(phase);
                problem.precedences.push_back({id - 1, id});
            }
        }
    }

    resource_scheduling_options options;
    options.time_limit_ms = time_limit_ms;
    options.seed = seed;
    auto result = resource_scheduling_solve(problem, options);
    phased_projects_solution output;
    output.start_time.resize(projects.size());
    for (std::size_t p = 0; p < projects.size(); ++p) {
        for (int id = offset[p]; id < offset[p + 1]; ++id) {
            output.start_time[p].push_back(result.schedule.start_time[id]);
        }
    }
    output.raw_result = std::move(result);
    return output;
}
```

## 9. 実装

### 9.1 全体の流れ

探索状態は「先行制約を満たすタスクの優先順列」と「各タスクのモード」である。開始時刻そのものを自由に動かす状態ではない。順列とモードを受け取るたびに、資源容量を守って各タスクをできるだけ早く配置し、その完成スケジュールの目的値を評価する。

1. 問題、オプション、必要なら`improve`の入力解を検証する。先行・後続リスト、トポロジカル順序、時刻範囲を構築する。
2. 問題の大きさと処理時間から資源calendarの表現を選ぶ。
3. `solve`では初期モードと優先順列を作る。`improve`では入力解のモードと順列を使う。
4. 優先順列を開始時刻へ変換し、初期解を評価する。`improve`では入力された元の開始時刻も評価し、良い方を最良結果として保持する。
5. 改善時間の計測を開始し、時間・反復上限が許せば、同じモードで最大3種類の追加優先順列を評価する。
6. 順序の移動、順序の交換、モード変更を焼きなましで試す。各候補の実行可能なスケジュールを生成して比較する。
7. 時間または反復上限に達したら、それまでに見つかった最良結果を返す。

空問題は空スケジュールを1回評価して終了するが、診断カウンタは全て0になる。全タスクが1モードで、トポロジカル順序も一意の場合は、初期配置だけで終了する。この場合の非空問題では`decoded_schedules=1`、残りのカウンタは0である。一般の場合には最適性の証明や最適解との差の保証を返さない。

実装用の型や処理は、`resource_scheduling_problem`のprivateな`implementation`へまとめられている。利用側がこの型を構築したり、その内部メソッドを呼んだりする必要はない。公開入口は第6章の関数群であり、探索器を永続保持するクラスはない。

### 9.2 入力検証とグラフの準備

容量、全モードの資源使用、時間、重み、IDを調べてから、先行辺を隣接リストへ変換する。入次数0のタスクを取り出すKahn法でトポロジカル順序を作り、全タスクを取り出せなければ閉路として拒否する。

Kahn法の各段階で選べるタスクが常に1個であり、さらに各タスクのモードが1個だけなら、探索可能な順列・モードの組合せは一意と判定できる。一方、単に資源が空、全重みが0、または解の目的値が同じという理由では、探索を一律に省略しない。

calendar用の時刻範囲を次で作る。$H$は時刻範囲の上端、$r_i$は開始可能時刻、$p_{i,m}$はモード$m$の処理時間、$M_i$はタスク$i$のモード集合である。空集合に対する最大値と和はここでは0とする。

$$H=\max\left(1,\max_i r_i+\sum_i\max_{m\in M_i}p_{i,m}+1\right)$$

全タスクを直列に置いても収まる範囲であり、最適完了時刻の予測値ではない。合計が`long long`を超えないことを検査する。この準備は各公開呼出しで行い、前の呼出しから差分更新する仕組みは持たない。

### 9.3 初期モードの選び方

`solve`は各タスクを単独に見て、処理時間、費用、資源の占有割合をまとめた代理値が小さいモードを選ぶ。これは初期値を作る判断であり、そのモードを固定する制約ではない。後の探索で他のモードへ変更できる。

$N$をタスク数、$R$を資源数、$a_{i,m,r}$をモードの資源使用量、$B_r$を容量とする。登録していない資源の使用量は0である。$w_M,w_K,w_i^C,w_i^T,k_{i,m}$は第1章の目的係数と費用である。資源占有割合$P_{i,m}$、平均的な時間重み$A$、タスク別の時間重み$B_i^{\mathrm{time}}$を次で定める。

$$P_{i,m}=\frac{1}{R}\sum_r\frac{a_{i,m,r}}{B_r}\quad(R>0),\qquad P_{i,m}=0\quad(R=0)$$

$$A=\frac{w_M+\sum_i(w_i^C+w_i^T)}{\max(1,N)},\qquad B_i^{\mathrm{time}}=\frac{w_M}{\max(1,N)}+w_i^C+w_i^T$$

モードごとに次の$Q_{i,m}$を計算し、最小のものを選ぶ。同値ならモード配列で先に現れるものを使う。

$$Q_{i,m}=B_i^{\mathrm{time}}p_{i,m}+w_K k_{i,m}+0.15A p_{i,m}P_{i,m}$$

処理時間が短く、費用が安く、共有資源を長く占有しにくいモードを初期値にしやすい式である。納期遅延を正確に予測する式ではない。`due_time=-1`でも`w_i^T`はこの代理値へ入る。独自Evaluatorを使っても、この初期化はproblem内の重みを参照するため、独自目的の傾向を重みに反映するときは第5章の注意に従う。

`improve`は、このモード選択を行わず入力解の`mode_of_task`を使う。

### 9.4 初期優先順列と追加の順列

先行タスクが全て取り出されたタスクだけを候補にし、優先値の大きいものを優先キューから取り出す。したがって、どの優先式でも先行制約を満たす順列になる。

まず、後続の重要さを表す$W_i$を逆トポロジカル順に計算する。$\operatorname{succ}(i)$は$i$の直接の後続集合で、後続がない場合の最大値は0とする。

$$W_i=w_i^C+w_i^T+\max_{j\in\operatorname{succ}(i)}W_j$$

これは1本の後続経路に沿った重みである。全子孫の重みを合計する値でも、処理時間によるクリティカルパス長でもない。

選択中のモードを$m_i$とする。納期の近さ$U_i$は、納期なしなら0、納期ありなら$1-\min(1,d_i/H)$である。基本順列の優先値$V_i$は次のとおり。

$$V_i=0.85\frac{W_i}{\max(1,\max_j W_j)}+0.30P_{i,m_i}+0.35U_i$$

候補を優先キューへ入れるたびに、$[0,0.03)$の乱数を優先値へ加える。完全に同じキーならタスクIDが大きい方を先に取り出す。seedを変えると、こうした同程度の優先度の順序にも変化が生じる。

基本初期解の構築後、改善枠内で次の3式を順番に試す。モードは共通のままで、各順列を配置・評価する。表の$p_i$は選択中モードの処理時間、$d_i$は納期である。

| 順列 | 優先値の作り方 | 狙い |
|---|---|---|
| 後続重み／時間 | $W_i/\max(1,p_i)$を計算し、その最大値と$10^{-18}$の大きい方で正規化 | 重要な後続を短い処理で解放しやすくする |
| 重み付き短時間 | $(1+w_i^C+w_i^T)/\max(1,p_i)$ | 短い仕事や完了の重要な仕事を前へ置く |
| 納期順 | 納期ありは$-d_i+p_i$、納期なしは$-H$ | 納期に対して余裕が少ない仕事を前へ置く |

これらも同じ乱数幅を加えて優先キューを使う。必ず3個を評価するわけではなく、各評価前に時間と反復上限を確認する。各評価は`iterations`と`decoded_schedules`を1増やすが、`accepted_moves`には含めない。`improve`でも追加順列を試し得る。

### 9.5 優先順列から開始時刻を作るSSGS

Serial Schedule Generation Scheme（SSGS）は、優先順列の先頭から1タスクずつ確定する配置手続きである。タスク$i$の最初の候補時刻を、開始可能時刻と全先行タスクの完了の最大値にする。

$$e_i=\max\left(r_i,\max_{j\in\operatorname{pred}(i)}C_j\right)$$

ここで$e_i$は資源を考える前の開始下限、$\operatorname{pred}(i)$は直接の先行集合である。先行がない場合の最大値は0とする。$e_i$以降で、必要な全資源を処理時間の全区間にわたり同時に確保できる最早時刻を探し、その場所へ予約する。

先に順列へ現れたタスクの予約は、そのdecode中には動かさない。ただし、後から処理した独立タスクを、既に配置したタスクより早い空き区間へ入れることはある。このため、`task_order`と開始時刻順は一致するとは限らない。

時間0または使用資源なしの場合は、資源待ちをせず開始下限へ置く。時間0の仕事は資源を予約しない。資源使用は半開区間なので、時刻$t$に終わる仕事と時刻$t$に始まる仕事は同じ資源を引き継げる。

配置を終えた後、Evaluatorをスケジュール全体に対して1回呼び、`long double`へ変換して有限性を確認する。資源配置の局所変更に対して、目的関数だけを利用側から差分更新するAPIはない。

### 9.6 資源calendarの2つの表現

calendarは、資源ごとに「どの時間帯へ何単位を予約済みか」を管理する内部データである。

**dense calendar**は、各資源・整数時刻の負荷を配列に持つ。1セルは64bitで、上位32bitを世代番号、下位32bitを使用量に使う。decodeをやり直すときは世代を進め、別世代のセルを未使用と解釈する。毎回全セルを0へ戻す書込みを避けるが、世代番号が周回した場合や必要な配列サイズが変わった場合は配列を初期化する。

配置候補の各時刻を調べ、容量超過の区間があれば、その超過が連続する区間の終わりまで開始候補を進める。いずれかの資源で開始候補を進めたら、必要資源の確認をやり直す。予約時は占有する整数時刻のセルを加算するので、処理時間が短い場合に適する。

**interval calendar**は、負荷が一定の区間の開始時刻と負荷を、資源別のソート済み`vector`に持つ。探索開始時は未使用の区間を用意し、予約の始点・終点が区間の途中なら境界を挿入して、重なる区間の負荷を加算する。

ある時刻を含む区間は二分探索する。容量超過なら超過が連続する区間の先へ進み、必要資源を確認し直す。時刻値が非常に大きくても、その全時刻を配列に展開しない。一方、平衡木ではないため、境界を挿入する際の`vector`要素移動や、複数区間の走査費用は発生する。

自動選択では、全タスクの**全モード**の使用一覧を集計する。$L$を資源使用要素の総個数、$u_{i,m}$をそのモードの使用一覧の長さとし、$L>0$のときの平均処理時間を次で求める。

$$\overline p=\frac{\sum_i\sum_{m\in M_i}p_{i,m}u_{i,m}}{L},\qquad L=\sum_i\sum_{m\in M_i}u_{i,m}$$

資源数と$L$がともに正で、次の2条件を両方満たす場合にdenseを選び、それ以外はintervalを選ぶ。

$$\overline p\leq14\log_2(N+1),\qquad HR\leq4{,}000{,}000$$

使用量`amount`の合計ではなく、使用一覧の要素数で重み付けする点に注意する。選択は呼出し時の問題から1回行い、探索中のモード変更ごとに方式を切り替えない。利用者が方式を強制する公開オプションはない。

### 9.7 同じ先頭部分の配置を再利用する仕組み

decoderは、直前にdecodeした順列・モード・開始時刻を保持する。次の候補と比べて、順列上の同じタスクが同じモードのまま続く先頭部分を探す。その部分では以前の開始時刻を使い、calendarへ予約し直す。以降のタスクだけ開始時刻を再探索する。

同じ先頭部分の配置は、後続タスクの順序やモード変更の影響を受けないため、この再利用ができる。ただし、calendarは再設定し、先頭部分の予約処理も必要である。「変更したタスク数だけの計算」になる保証はない。

比較相手は**直前にdecodeした候補**であり、直前に受理された探索状態とは限らない。不受理候補の配置もdecoderには残るが、次回はその候補との一致部分を改めて判定するので、誤った開始時刻を無条件に流用しない。

この再利用は1回のsolver実行内に閉じる。第3章のターン間の再実行ではdecoderを作り直すため、同じ仕組みが呼出しをまたいで働くわけではない。

### 9.8 焼きなましの近傍と受理

追加順列までに得た最良のSSGS状態から焼きなましを始める。`improve`で元の開始時刻がそれより良い場合も、元解は返却候補として別に保持する。探索の現在状態は、SSGSで表せる順列・モードの状態である。

| 提案 | 選ばれる確率 | 処理・実行可能性の確認 |
|---|---:|---|
| relocate | 55% | タスクを一様に選び、直接先行の位置より後、直接後続の位置より前に収まる別の位置へ移す。区間を回転し、影響した位置情報を更新する |
| swap | 25% | 異なる2位置を一様に選んで交換する。交換した2タスクに接続する先行辺を検査し、不正なら戻す |
| mode change | 20% | タスクを一様に選び、現在以外のモードを一様に選ぶ。モード選択自体に費用・時間による偏りは付けない |

移動先がない、交換で先行順序を壊す、他モードがない、といった提案はdecodeしないが、反復数には入る。全モードは入力時に容量に収まることを検証済みなので、モード変更後はSSGSの再配置によって資源制約を満たす。

現在の目的値を$f$、候補値を$f'$、差を$\Delta=f'-f$とする。$\Delta\leq0$なら受理し、悪化する場合は温度$T$を使った確率で受理する。ただし実装は指数が$-50$以下なら、確率計算を行わず不受理にする。

$$\Pr(\mathrm{accept})=\exp(-\Delta/T)\quad(\Delta>0,\ -\Delta/T>-50)$$

受理時は現在の目的値と`accepted_moves`を更新する。目的値が返却候補の最良値より厳密に小さければ、スケジュールを保存する。同値moveも受理されるが、同値だけでは最良結果を置換しない。不受理なら順序の逆回転、再交換、元モードの復元によって探索状態を戻す。

悪化moveを一時的に受理しても、最後に返すのは保存した最良解である。`improve`の元解もこの比較に含むため、決定的なEvaluatorで同じ問題を評価する限り、元解より悪い目的値は返さない。

### 9.9 温度・乱数・停止条件

焼きなまし開始時のSSGS目的値を$f_0$とし、温度は次の値から幾何的に下げる。ここで$T_0$は開始温度、$T_1$は最終温度、$q$は進捗である。

$$T_0=\max(10^{-12},\lvert f_0\rvert\,0.02/N),\qquad T_1=10^{-4}T_0,\qquad T=T_0(T_1/T_0)^q$$

進捗$q$は、改善開始からの経過時間の割合と、正の反復上限がある場合の反復進捗の大きい方を$[0,1]$に収める。時間進捗は追加順列の評価時間も含む。目的関数へ大きい定数を足すと解の優劣が同じでも$f_0$の絶対値が変わり、受理温度へ影響する。

SAでは8提案ごとに時刻を確認し、その確認点のうち反復数が64の倍数の箇所で温度を更新する。相対終了時刻は初期構築後の時刻に`max(0,time_limit_ms)`を足したものであり、絶対`deadline`があれば早い方を使う。反復上限は追加順列の評価数とSAの提案数の合計に適用する。

乱数器は64bit状態を加算し、shift・xor・乗算で混合する方式である。整数選択は乱数と選択肢数の128bit積の上位64bitを使い、実数乱数は上位53bitから$[0,1)$を作る。初期順列用には`seed`と固定定数のxorから作った別の乱数器を使い、SA用には`seed`から作った乱数器を使う。初期化で消費する乱数とSAの乱数列を分けている。

seedと反復上限を固定しても、経過時間を冷却に使うため、実行機や負荷の違いを含めた完全再現性は保証しない。

### 9.10 公開checkerと計算量の見方

公開checkerは、配列長、モードID、順列の重複・先行順序、開始可能時刻、先行タスクの完了条件を調べる。資源容量は、選択モードの使用ごとに開始時刻で使用量を加え、終了時刻で引くイベントを作り、時刻・資源順に走査する。

同じ時刻・同じ資源の増減をまとめてから負荷を判定するので、同時刻の終了と開始を半開区間どおりに扱える。時間0の使用はイベントを作らない。外部の不正解の検証中にも安全に集計できるよう、負荷集計は`long long`で行う。

実行時間を単に「タスク数の何乗」とするのは不正確である。$E$を先行辺数、$M$を全モード数、$u_{i,m}$を各モードの使用要素数、$U$を選択モードのうち処理時間が正の使用要素数とすると、主な費用は次のように分かれる。

| 処理 | 主な費用 |
|---|---|
| 問題検証とグラフ準備 | 概ね$O(N+E+R+M+\sum_{i,m}u_{i,m}\log(u_{i,m}+1))$。使用一覧の重複検出を含む |
| 優先順列1個の構築 | 概ね$O(E+N\log(N+1))$に、優先度用の選択モードの使用一覧走査を加える |
| 1候補のdecode | 順列・モードの処理、先行辺の確認、全資源の配置・予約、Evaluator。calendarの走査範囲と区間数に大きく依存する |
| 組み込み目的値の計算 | $O(N)$ |
| 公開checker | 問題検証に加え、概ね$O(N+E+R+U\log(U+1))$ |
| 探索全体 | 初期化と追加順列に、実際にdecodeした候補の費用を加える。無効提案もあるので反復数とdecode数は一致しない |

SSGSで先頭部分を再利用しても、順列・モード配列の扱いや再予約、目的値計算が残る。denseは整数時間の長さ、intervalは境界数や要素移動の費用を持つため、どちらも全ての入力で一定時間の配置にはならない。

メモリは問題のモード・使用一覧、先行グラフ、複数の順序・解配列、calendarからなる。denseのセル配列には$8HR$byteが必要で、intervalは資源ごとの予約境界数に応じて増える。大きな入力ではタスク数だけでなく、モード数、各モードが同時に使う資源数、先行辺数も測定条件へ含める。
