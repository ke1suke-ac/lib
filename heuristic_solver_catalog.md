# ヒューリスティックコンテスト向けソルバーライブラリ選定一覧

確認基準日：2026年9月8日  
対象：指定された10スレッド、11個のヘッダー。TSP・VRPのスレッドだけは、単一経路用と複数車両用に分けて掲載する。

本書は、File Libraryで最新として確認できたヘッダーの公開API、目的値の計算、主要な探索処理を照合した選定資料である。一般的な問題名から機能を推測せず、実装が扱う目的と制約を記載する。参照したファイルと対応スレッドは末尾の資料一覧に示す。今回、ソースの変更、再コンパイル、性能の再計測は行っていない。

## まず、この表で選ぶ

「何を決める問題か」と「他の選択との干渉が何か」を見ると選びやすい。

| 節 | いま決めたいこと | 選ぶライブラリ | 主な最適化対象 |
| --- | --- | --- | --- |
| 1 | 予算内で候補を選び、できるだけ多くの対象を覆う | Maximum Coverage v06 | 重複を除いた被覆価値の最大化。全被覆の費用最小化にも対応 |
| 2 | 必須の拠点を、安い辺のネットワークでつなぐ | Heuristic Steiner Tree v17 | 採用する辺の費用の合計を最小化 |
| 3 | 全仕事の担当先・実行方式を、担当先の容量内で決める | Generalized Assignment v12 | 割当費用の合計を最小化 |
| 4 | 物を場所へ一対一に配置し、物どうしの関係に伴う費用を減らす | QAP v07 | 関係の強さ×配置先間の費用＋単独配置費用を最小化 |
| 5 | ちょうどK個の施設を選び、各需要点に安くサービスする | Facility Location v11 | 各需要点の最安サービス費用＋施設選択費用を最小化 |
| 6 | 先行関係と同時使用できる資源量を守って、作業の開始時刻を決める | Generic Resource Scheduler v07 | 全体完了時刻・各作業の完了時刻・遅延・方式費用の加重和を最小化 |
| 7 | 複数の駒を、時刻ごとの衝突を避けて動かす | Multi-Agent Path Finding v04 | 全員の経路時間の合計、または全員が終わる時刻を最小化。搬送件数最大化も対応 |
| 8 | 寸法が決まった長方形を、重ねずに配置する | Rectangle Packing v09 | 固定箱内の利益最大化、高さ・箱数・外接矩形の大きさの最小化 |
| 9A | 1本の経路で指定頂点をすべて訪れる順番を決める | TSP Solver v07 | 巡回路または開いた経路の費用を最小化 |
| 9B | 仕事を複数車両へ分け、各車両の訪問順も決める | Routing Solver v05 | 配送費用最小化、または制約内での獲得報酬最大化 |
| 10 | 頂点を容量付きグループへ分け、頂点間の相性も考慮する | Capacitated Graph Labeling v22 | 単独の割当費用＋辺ごとのラベル組合せ費用を最小化 |

### 名前が似ているライブラリの分岐点

| 迷う組合せ | 選び分けるポイント |
| --- | --- |
| 被覆／施設配置 | 「半径内に入れば一律に得点」なら被覆。「最も近い、または最も安い施設までの費用」が得点に効くなら施設配置 |
| 一般化割当／グラフラベリング | 担当先ごとの容量と各仕事の費用だけなら一般化割当。仕事どうしが同じ担当先か、異なる担当先かでも費用が変わるならラベリング |
| QAP／グラフラベリング | 物と場所が一対一で、二項費用が「物間の関係×場所間の費用」に分かれるならQAP。複数頂点を同じグループへ入れる、個数・需要量の上下限を課すならラベリング |
| 一般化割当／スケジューリング | 担当先の総使用量だけを制限するなら一般化割当。終了した作業が資源を返し、その後に別の作業を実行できるならスケジューリング |
| TSP／VRP | 1本の訪問順だけならTSP。複数車両への分配、容量、仕事の選択、集荷と配達の組合せがあるならRouting |
| Routing／MAPF・MAPD | 移動費用だけを見て訪問順を決めるならRouting。同じ時刻の同じマス、正面衝突を禁止するならMAPF・MAPD |
| シュタイナー木／TSP | 辺を「設置する」費用を払うならシュタイナー木。辺を「通行する」たびに費用を払う訪問順の問題ならTSP |

### 数式の読み方

記号は各節で定義し直す。たとえば、別の節に登場するKは同じ量とは限らない。入力として与える量と、ソルバーが決める量を区別して説明する。

$\mathbf{1}[P]$ は、条件 $P$ が成り立つなら1、そうでなければ0を返す記号である。$\lvert S\rvert$ は集合 $S$ の要素数を表す。

$\operatorname{lexmin}(a,b,c)$ は**辞書順最小化**を表す。まず $a$ が小さい解を優先し、$a$ が同じ場合だけ $b$ を比較し、さらに同じ場合だけ $c$ を比較する。加重和ではない。負号を付けた量を最小化すると、その量を最大化することになる。

---

## 1. 最大被覆・予算付き被覆・最小費用集合被覆

**ヘッダー：`maximum_coverage_solver_v06.hpp`**  
**主要入口：`coverage_solver` の `solve_maximum_coverage`、`solve_budgeted_maximum_coverage`、`solve_minimum_set_cover`**  
参照資料：[S1]

### 何を解くか

候補を一つ選ぶと、その候補に対応する複数の対象がまとめて覆われる。選べる候補数や費用に制限がある中で、覆われる対象の価値を大きくする。

たとえば、監視カメラの設置候補ごとに「見えるマスの集合」が分かっているとする。重要なマスに大きな重みを付け、カメラを5台まで選んで監視できる価値を最大化する問題に使える。**同じマスを2台で監視しても、そのマスの価値は1回しか数えない。**

逆に、すべての対象を覆うことを必須として、選ぶ候補の費用を最小化する集合被覆も扱う。

### 入力と決定変数

| 記号 | 意味 |
| --- | --- |
| $U$、$u$ | 覆いたい対象全体の集合と、その一つ |
| $C$、$i$ | 選択候補全体の集合と、その一つ |
| $A_i\subseteq U$ | 候補 $i$ を選ぶと覆える対象の集合。入力で指定する |
| $w_u$ | 対象 $u$ を覆ったときの非負の価値。`target_weights` に対応 |
| $c_i$ | 候補 $i$ の非負の選択費用。`candidate_costs` に対応 |
| $S\subseteq C$ | 実際に選ぶ候補集合。ソルバーが決める |
| $U(S)$ | 選んだ候補全体で覆える対象集合。$U(S)=\bigcup_{i\in S}A_i$ |
| $W(S)$ | 重複を除いた被覆価値の合計。$W(S)=\sum_{u\in U(S)}w_u$ |
| $K$、$B$ | 候補数の上限、または選択費用の予算。入力で指定する |

### 目的関数

**候補数に上限がある最大被覆：**

$$\max_{S\subseteq C} W(S)\quad\text{subject to}\quad \lvert S\rvert\leq K$$

候補を少数に絞りながら、重要な対象を多く覆う。選択数の下限も指定する場合は、$K_{\min}$、$K_{\max}$ をそれぞれ許される最小・最大の選択数として、$K_{\min}\leq\lvert S\rvert\leq K_{\max}$ とする。両者を同じ値にすれば、その個数をちょうど選ぶ指定になる。

**予算付き最大被覆：**

$$\max_{S\subseteq C} W(S)\quad\text{subject to}\quad \sum_{i\in S}c_i\leq B$$

安い候補を多く選ぶか、高いが広く覆える候補を選ぶかを判断する。目的は被覆価値であり、費用は予算制約として働く。

**最小費用集合被覆：**
$$\min_{S\subseteq C}\sum_{i\in S}c_i\quad\text{subject to}\quad U(S)=U$$

対象をすべて覆った上で、選択費用を安くする。**重みが0の対象も、全被覆の対象から自動的に外れるわけではない。**

### 主要アルゴリズム

新しく覆える価値を差分管理する貪欲構築、複数の初期解、候補の交換、不要候補の除去、一部を外して組み直す探索を用いる。予算付きでは追加価値と費用効率を使い分け、集合被覆では覆える候補が少ない対象も考慮する。

### 選択時の注意

距離そのものを小さくする問題ではない。距離から「覆える／覆えない」を先に決めれば入力にできるが、遠近の違いが得点へ連続的に効くなら5節の施設配置が近い。選択必須・禁止の候補も扱えるが、必須候補の個数や費用は制限に含まれる。候補数制約だけの問題に、費用予算まで自動で付くわけではない。

---

## 2. シュタイナー木：必須頂点を安いネットワークでつなぐ

**ヘッダー：`heuristic_steiner_tree_v17.hpp`**  
**主要入口：`heuristic_steiner_tree` の `solve`、`improve`、`repair`、`augment`**  
参照資料：[S2]

### 何を解くか

無向グラフから辺を選び、指定された必須頂点どうしをすべてつなぐ。必須でない頂点を中継点として使ってよい。

たとえば、複数の住宅を配線でつなぐとき、住宅どうしを直接結ぶだけでなく、途中の交差点を分岐点に使うことで、設置する配線の合計を減らす。各住宅を訪問する順番は求めない。

### 入力と決定変数

| 記号 | 意味 |
| --- | --- |
| $G=(V,E)$ | 入力の無向グラフ。$V$ は頂点集合、$E$ は辺集合 |
| $T\subseteq V$ | 互いに接続する必要がある必須頂点、端点の集合 |
| $e$、$c_e$ | 辺と、その非負の採用費用 |
| $F\subseteq E$ | 採用する辺集合。ソルバーが決める |
| $B\subseteq E$ | `augment` に渡す、すでに利用できる既存辺の集合 |

### 目的関数

通常の求解では、$F$ が必須頂点をすべて接続するという条件の下で、次を最小化する。

$$\min_{F\subseteq E}\sum_{e\in F}c_e$$

目的値は**選んだ辺の費用を1回ずつ足したもの**である。同じ辺を複数の接続で共用しても、その分を重ねて加算しない。必須頂点が0個または1個なら、通常の接続費用は0にできる。

`augment` は、既存辺をそのまま使いながら接続を増やすための入口である。$B\subseteq F$ として、追加分だけを評価する。

$$\min_{F\supseteq B}\sum_{e\in F\setminus B}c_e$$

この場合、既存辺 $B$ の費用をもう一度支払う目的ではない。通常の総費用と、追加結果の `added_cost` を混同しない。

### 主要アルゴリズム

多始点Dijkstra法によるVoronoi分割と最小全域木を使う構築、端点を最短路で順次つなぐ構築を組み合わせる。不要な枝や閉路を整理し、端点からの枝・分岐点間の経路をつなぎ直す局所探索を行う。

### 選択時の注意

辺の向き、辺容量、通行回数に比例する費用、時刻ごとの衝突は直接のモデルに含まれない。通常の `solve` は指定した端点をすべてつなぐ問題であり、「接続する端点自体を報酬に応じて選ぶ」問題ではない。既存のネットワークを保持したいのか、全体を作り直してよいのかで `augment` と通常の改善を区別する。

---

## 3. 一般化割当：容量内で仕事の担当先・方式を決める

**ヘッダー：`generalized_assignment_solver_v12.hpp`**  
**主要入口：`generalized_assignment_solver` の `add_option`、`solve`、`improve`、`repair`**  
参照資料：[S3]

### 何を解くか

各仕事について、実行できる候補の中から一つを選ぶ。それぞれの候補には「担当先」「その担当先の容量をどれだけ使うか」「費用」がある。全仕事を実行し、どの担当先でも容量を超えないようにする。

たとえば、仕事Aは機械0なら容量を3消費して費用5、機械1なら容量を2消費して費用8、と指定できる。担当先ごとに性能が違い、**同じ仕事でも容量消費が変わる**場合に使える。同じ仕事・同じ担当先に、時間重視と費用重視など複数の方式を登録することもできる。

### 入力と決定変数

| 記号 | 意味 |
| --- | --- |
| $J$、$j$ | 仕事全体の集合と、その一つ |
| $A$、$a$ | 担当先全体の集合と、その一つ |
| $O_j$ | 仕事 $j$ に登録した選択肢の集合 |
| $o$ | 一つの選択肢。担当先・容量消費・費用を持つ |
| $a(o)$ | 選択肢 $o$ の担当先 |
| $d_o$ | 選択肢 $o$ が担当先の容量から消費する非負の量 |
| $c_o$ | 選択肢 $o$ の費用。負の費用も表現できる |
| $Q_a$ | 担当先 $a$ の容量上限 |
| $o_j\in O_j$ | 仕事 $j$ に採用する一つの選択肢。ソルバーが決める |

### 目的関数と制約

$$\min_{o_j\in O_j}\sum_{j\in J}c_{o_j}$$

各担当先 $a\in A$ について、次の容量制約を満たす。

$$\sum_{j\in J}\mathbf{1}[a(o_j)=a]d_{o_j}\leq Q_a$$

安い選択肢を選びたいが、一つの担当先に仕事を集めすぎると容量を超える。そのため、一部の仕事を別の担当先・方式へ移す組合せを探す。仕事の分割ではなく、仕事ごとに選択肢を一つだけ採用する。

### 主要アルゴリズム

選択の後悔値や資源価格を使った構築、単一仕事の移動による局所改善、複数仕事を外して再割当する適応的大近傍探索を用いる。再割当には後悔値ベースの修復を使い、探索中の候補受理には焼きなまし型の判定を使う。

### 選択時の注意

ここでの容量は、担当先に割り当てた仕事の**総量**である。仕事が終了すると容量が戻る時間軸はない。また、「仕事AとBが同じ担当先なら追加費用」のような仕事間相互作用も含まれない。前者は6節のスケジューリング、後者は10節のラベリングを検討する。全仕事への割当が必要なので、結果の `feasible()` を確認し、未割当を含む部分解の費用を完成解の得点として使わない。

---

## 4. QAP：相互関係を考慮した一対一配置

**ヘッダー：`qap_solver_v07.hpp`（内部バージョン v0.7.3）**  
**主要入口：`solve_qap`、`improve_qap`、`improve_qap_subset`**  
参照資料：[S4]

### 何を解くか

N個の物をN個の場所へ、一つずつ重複なく配置する。どの物をどこに置くかによる単独の費用に加えて、物どうしの関係と配置先どうしの距離・費用が得点を決める。

たとえば、通信量の多い機器どうしを近くに置きたい場合に使う。「機器Aを場所0に置く費用」だけでなく、**機器Bをどこに置いたかによって、Aの配置の良し悪しも変わる**ことが特徴である。

### 入力と決定変数

| 記号 | 意味 |
| --- | --- |
| $N$ | 物の個数。場所の個数も同じ |
| $i,j\in\{0,\ldots,N-1\}$ | 物の番号 |
| $a,b\in\{0,\ldots,N-1\}$ | 場所の番号 |
| $\pi(i)$ | 物 $i$ を置く場所。$\pi$ は全場所を一度ずつ使う順列で、ソルバーが決める |
| $F_{ij}$ | 物 $i$ から物 $j$ への関係の強さ。通信量、搬送量など。`flow` に対応 |
| $D_{ab}$ | 場所 $a$ から場所 $b$ への距離・相互作用費用。`distance` に対応 |
| $U_{ia}$ | 物 $i$ を場所 $a$ に置く単独の費用。`unary` を省略した場合は0 |

### 目的関数

$$\min_{\pi}\left(\sum_{i=0}^{N-1}\sum_{j=0}^{N-1}F_{ij}D_{\pi(i),\pi(j)}+\sum_{i=0}^{N-1}U_{i,\pi(i)}\right)$$

前半は、配置によって生じる物どうしの費用である。非負の通信量と距離を使う例なら、通信の多い組を近づけるほど小さくなる。後半は、場所との相性や、各物を特定の場所へ置く独立の費用である。

実装は $i=j$ の項も含め、**向きのある全組をそのまま加算する**。対称な入力でも2で割らない。そのため、無向の組を1回だけ数える別の得点式に合わせる場合は、入力の尺度と単独費用の尺度をそろえる必要がある。非対称行列や符号付きの費用も扱う。

### 主要アルゴリズム

2物の配置交換を基本近傍とし、しきい値以下の悪化を許すThreshold Acceptingとタブー探索を組み合わせる。密・疎、対称・非対称に応じて交換差分の計算方法を切り替える。

### 選択時の注意

基本形は一対一配置であり、一つの場所に何個も置く容量付きのグループ分けではない。任意の配置禁止や容量制約を標準で指定するモデルでもない。大きな単独費用を付ける方法は「不利にする」ものであって、禁止を保証する制約とは区別する。明示的な許可ラベル・個数制約が必要なら10節を検討する。

---

## 5. 施設配置：ちょうどK個の施設で需要を安く満たす

**ヘッダー：`facility_location_v11.hpp`**  
**主要入口：`solve_k_median`、`improve_k_median`、`facility_location_workspace`**  
参照資料：[S5]

### 何を解くか

候補地の中からちょうどK個の施設を選ぶ。各需要点は、選ばれた施設の中で最も安く利用できる施設を使う。必要なら、施設を使わず外部サービスへ頼る費用も設定できる。

たとえば、倉庫を3か所選び、各配送先から最寄りの倉庫までの距離の合計を小さくする。利用頻度の高い配送先には大きな重みを付ける。施設を開く費用を加えることもできるが、**施設数Kは入力として固定する**。

### 入力と決定変数

| 記号 | 意味 |
| --- | --- |
| $I$、$i$ | 需要点全体の集合と、その一つ |
| $C$、$f$ | 施設候補全体の集合と、その一つ |
| $K$ | 選ぶ施設数。1以上、候補数以下 |
| $S\subseteq C$ | 選択する施設集合。ソルバーが決め、$\lvert S\rvert=K$ を満たす |
| $w_i$ | 需要点 $i$ の非負の重み。未指定なら1 |
| $c_{fi}$ | 施設 $f$ から需要点 $i$ へサービスする非負の費用 |
| $b_i$ | 需要点 $i$ を外部サービスで処理する非負の費用。`fallback_cost` に対応 |
| $p_f$ | 施設 $f$ を選ぶ単独の費用。`facility_cost` に対応。未指定なら0で、負の値も許す |

### 目的関数

外部サービスを使わない基本形は、次の重み付き離散k-medianである。

$$\min_{S\subseteq C,\ \lvert S\rvert=K}\sum_{i\in I}w_i\min_{f\in S}c_{fi}$$

外部サービスと施設選択費用も指定した場合は、次を最小化する。

$$\min_{S\subseteq C,\ \lvert S\rvert=K}\left(\sum_{i\in I}w_i\min\left(b_i,\min_{f\in S}c_{fi}\right)+\sum_{f\in S}p_f\right)$$

内側の最小値が「需要点一つについて、今の施設集合で利用できる最安の方法」である。それを需要の重みで掛けて足し、さらに施設を選ぶ費用を足す。外部サービスを指定しない場合は、$b_i$ との比較を行わない。

たとえば、ある需要点について最寄り施設の費用が10、外部サービスが6なら、その需要点のサービス部分の費用は $6w_i$ となる。

### 主要アルゴリズム

重み付きD²-samplingによる複数の初期解、最近施設・第2近傍施設を使うFastPAM型の1施設交換、最良解から少数施設を交換して再探索する反復局所探索を用いる。

### 選択時の注意

**施設容量は扱わない。** 需要の重みが大きくても、施設を使える人数・物量の制約にはならない。施設間の接続、施設どうしの相性、最大距離だけを最小化するk-center、施設数そのものの最適化も別問題である。固定施設と候補地の制限は指定できるが、固定施設もK個の中に含まれ、施設選択費用も加算される。

---

## 6. 汎用資源スケジューリング：いつ、どの方式で実行するか

**ヘッダー：`generic_resource_scheduler_v07.hpp`**  
**主要入口：`resource_scheduling_solve`、`resource_scheduling_improve`、`resource_scheduling_evaluate`**  
参照資料：[S6]

### 何を解くか

作業ごとの実行方式と開始時刻を決める。「Aの後にBを行う」という先行関係と、機械・作業員などを同時に使える量を守る。作業は途中で中断せず、開始から終了まで必要資源を占有する。

たとえば、作業Aは通常方式なら4ターン・作業員1人、高速方式なら2ターン・作業員2人、と指定できる。資源は作業が終わると再び使えるので、「同じ作業員がAを終えてからBを行う」順序を表現できる。

資源制約付きプロジェクト計画、複数方式付きの計画、機械ごとの工程順を決めるJob Shop、代替機械を選ぶFlexible Job Shopを、この共通モデルへ落とし込める。

### 入力と決定変数

| 記号 | 意味 |
| --- | --- |
| $J$、$i$ | 作業全体の集合と、その一つ |
| $M_i$、$m_i\in M_i$ | 作業 $i$ の実行方式候補と、実際に採用する一つの方式 |
| $s_i$ | 作業 $i$ の開始時刻。ソルバーが決める |
| $p_{im}$ | 作業 $i$ を方式 $m$ で実行する所要時間 |
| $C_i=s_i+p_{i,m_i}$ | 作業 $i$ の完了時刻 |
| $C_{\max}=\max_{i\in J}C_i$ | 全作業が終わる時刻。makespan |
| $r_i$ | 作業 $i$ の実行を開始できる最も早い時刻。`release_time` |
| $D\subseteq J$、$d_i$ | 納期が指定されている作業集合と、その納期。`due_time` |
| $w_i$ | 完了時刻に掛ける重み。`completion_weight` |
| $v_i$ | 納期を超えた時間に掛ける重み。`tardiness_weight` |
| $f_{im}$ | 作業 $i$ の方式 $m$ を選ぶ固定費用。`fixed_cost` |
| $\alpha$、$\beta$ | makespanの重み `makespan_weight` と、方式費用の重み `mode_cost_weight` |

### 標準の目的関数

$$\min_{s,m}\left(\alpha C_{\max}+\sum_{i\in J}w_iC_i+\sum_{i\in D}v_i\max(0,C_i-d_i)+\beta\sum_{i\in J}f_{i,m_i}\right)$$

ここで $s$ は全作業の開始時刻を並べたもの、$m$ は全作業の選択方式を並べたものである。

第1項は「全体を早く終える」、第2項は「重要な作業を個別に早く終える」、第3項は「納期に遅れない」、第4項は「高価な実行方式を使いすぎない」という目的を表す。各重みを0にすれば、その項を評価から外せる。

たとえば、第1項だけなら全体完了時刻を最小化する。第2項も加えると、全体完了時刻が同じでも、重要な作業を先に終える解が有利になる。納期が未指定の作業は、第3項には入らない。

### 守る制約

開始可能時刻について $s_i\geq r_i$ を満たす。作業 $i$ が作業 $j$ の先行作業なら $C_i\leq s_j$ を満たす。

資源の種類を $h$、その利用可能量を $R_h$、作業 $i$ の方式 $m$ に必要な量を $a_{imh}$ とすると、各時刻 $t$、各資源 $h$ について次を満たす。

$$\sum_{i\in J}\mathbf{1}[s_i\leq t<C_i]a_{i,m_i,h}\leq R_h$$

つまり、**その時刻に実行中の作業だけ**の使用量を足す。$t$ はスケジュール上の時刻であり、ソルバーの計算時間ではない。

### 主要アルゴリズム

先行関係を守る作業順を、資源に空きがある最早時刻へ配置するSerial Schedule Generation Schemeで開始時刻へ変換する。作業順の移動・交換と方式変更を焼きなましで探索し、資源の空き管理には密な時間表または区間表を使う。

### 選択時の注意

納期は標準目的における**遅延ペナルティ**であり、必ず守られる最終期限ではない。作業の中断、直前に実行した作業次第で変わる段取り時間、全期間で使い切る予算などは直接の標準モデルに含まれない。独自Evaluatorで目的を置き換えられるが、それだけで新しい制約を扱えるようになるわけではなく、標準の4項も自動では足されない。

---

## 7. 複数エージェントの移動・搬送：MAPF／MAPD

**ヘッダー：`multi_agent_path_finding_v04.hpp`**  
**主要入口：`solve_mapf`、`improve_mapf`、`repair_mapf`、`solve_mapd`**  
参照資料：[S7]

### 何を解くか

同じ盤面やグラフ上の複数エージェントを、互いに衝突させずに動かす。1ターンで隣接頂点へ移動するか、その場に待つ。対象グラフは無向・単位移動時間である。

同時刻に同じ頂点へ入ることと、同じ辺を逆向きに通って位置を交換することを禁止する。たとえば倉庫の通路で、ロボットごとの最短路を独立に作ると衝突する問題に使う。

**MAPFは「各エージェントの行き先が決まっている移動」**、**MAPDは「集荷・配達タスクの担当も含めて決める搬送」**である。

### 7.1 MAPFの目的関数

| 記号 | 意味 |
| --- | --- |
| $A$、$a\in\{0,\ldots,A-1\}$ | エージェント数と番号 |
| $s_a$、$g_a$ | エージェント $a$ の始点と指定された終点 |
| $P_a$ | エージェント $a$ の位置列。時刻0の始点も含む。ソルバーが決める |
| $x_a(t)$ | エージェント $a$ の時刻 $t$ の位置 |
| $T_a=\lvert P_a\rvert-1$ | 出力経路の時間長。実装が費用として数える値 |
| $S=\sum_{a=0}^{A-1}T_a$ | 全エージェントの経路時間の合計。`sum_of_costs` |
| $L=\max_{0\leq a<A}T_a$ | 最長の経路時間。全員が完了する時刻。`makespan` |

`sum_of_costs` を選んだとき：

$$\operatorname{lexmin}(S,L)$$

`makespan` を選んだとき：

$$\operatorname{lexmin}(L,S)$$

前者は全員の拘束時間の合計を減らし、後者は最後の1体が終わる時刻を早める。主目的が同じ解どうしでは、もう一方の値で比較する。

経路に含まれる待機も1ターンとして数える。経路の終端後は終点に待機していると解釈するが、この**暗黙の待機は目的値へ足さない**。実装の集計は経路長に基づくため、自作経路の末尾へ明示的な待機を足すと、その分も集計される。途中で終点へ一度立ち寄っただけの時刻とも区別する。

### 7.2 MAPDの目的関数

集荷地点、配達地点、解禁時刻を持つタスクを入力し、有限ターン内で多く完了させる。1体が同時に担当するタスクは1件で、集荷・配達自体の作業時間は0である。

| 記号 | 意味 |
| --- | --- |
| $J$、$j$ | 入力された搬送タスク全体の集合と、その一つ |
| $p_j$、$q_j$ | タスク $j$ の集荷地点と配達地点 |
| $r_j$ | タスク $j$ の解禁時刻。これより前には割当・集荷しない |
| $H$ | 計画する最終時刻。`max_time` |
| $\mathcal C\subseteq J$ | 時刻 $H$ までに配達まで完了したタスク集合 |
| $c_j$ | 完了したタスク $j$ の配達完了時刻 |
| $F$ | 完了タスクについて、解禁から完了までの時間を足した値 |
| $M$ | 全エージェントの実移動回数。待機は含めない |
| $A$、$a$、$x_a(t)$、$t$ | エージェント数、番号、位置、時刻。MAPFと同じ意味 |

$$F=\sum_{j\in\mathcal C}(c_j-r_j)$$

$$M=\sum_{a=0}^{A-1}\sum_{t=0}^{H-1}\mathbf{1}[x_a(t+1)\ne x_a(t)]$$

自動方式が複数の候補を比較するときの目的は、次の辞書順である。

$$\operatorname{lexmin}(-\lvert\mathcal C\rvert,F,M)$$

最優先は完了件数、その件数が同じなら待ち時間を含むフロー時間、さらに同じなら実移動回数である。未完了タスクの待ち時間を $F$ へ加える式ではない。ただし、待ち時間を減らすために完了件数を落とす解は、辞書順で不利になる。

### 主要アルゴリズム

MAPFは優先順位付きの時空間A*、優先度継承とバックトラックを使うPIBT、選んだエージェントだけを再計画する大近傍探索を使う。MAPDは貪欲法・端点分散を考慮した貪欲法・最小費用流による担当割当とPIBTを組み合わせ、自動方式では複数方式・seedの結果を比較する。

### 選択時の注意

車両容量を持つ一般的な配送順序最適化とは別物であり、複数荷物を積み合わせる配送は9B節が近い。一方、Routingには時刻ごとの衝突回避がない。MAPDのタスクは呼出し時に登録し、将来解禁されるタスクもその配列で与える。搬送途中の荷物・担当状態をそのまま保存して次回呼出しへ再開するAPIではない。

MAPFの `success=false` は、探索時間不足や計画ターン上限不足でも起こる。MAPDの `valid=true` は有効な計画があるという意味で、全タスク完了を意味しない。

---

## 8. 矩形パッキング：長方形を重ねずに詰める

**ヘッダー：`rectangle_packing_v09.hpp`**  
**主要入口：`pack_rectangles_fixed_bin`、`pack_rectangles_strip`、`pack_rectangles_multiple_bins`、`pack_rectangles_bounding_box`**  
参照資料：[S8]

### 何を解くか

幅と高さが与えられた長方形について、座標、許可されていれば90度回転、複数箱なら箱番号を決める。長方形は軸に平行に置き、同じ箱内で重ねない。接することは許す。

固定サイズのキャンバスに価値の高い部品を詰める、幅が決まったロール紙の使用長を減らす、必要な箱の個数を減らす、全体を囲う長方形を小さくする、といった用途を選べる。

### 共通の記号

| 記号 | 意味 |
| --- | --- |
| $I$、$i$ | 入力矩形全体の集合と、その一つ |
| $w_i$、$h_i$ | 矩形 $i$ の元の幅・高さ。正の整数 |
| $v_i$ | 矩形 $i$ を配置したときの利益。`profit` |
| $R\subseteq I$ | 必ず配置すべき矩形集合。`required=true` のもの |
| $S\subseteq I$ | 実際に配置した矩形集合 |
| $x_i$、$y_i$ | 矩形 $i$ の配置座標。非負で、箱ごとの座標 |
| $\widetilde w_i$、$\widetilde h_i$ | 回転後の幅・高さ。回転しない場合は $w_i,h_i$、90度回転なら $h_i,w_i$ |
| $b_i$ | 配置先の箱番号。0始まり |
| $W$、$H$ | 配置で使う幅・高さ。$W=\max_{i\in S}(x_i+\widetilde w_i)$、$H=\max_{i\in S}(y_i+\widetilde h_i)$。空配置では0 |
| $V=\sum_{i\in S}v_i$ | 配置済み矩形の利益合計 |
| $A=\sum_{i\in S}w_ih_i$ | 配置済み矩形そのものの面積合計。隙間は含めない |
| $m=\lvert R\setminus S\rvert$ | 未配置の必須矩形数 |

$S$、座標、回転、箱番号がソルバーの決定対象である。以下の評価は、矩形の重なりや箱の境界を守ることを前提とする。$m>0$ の結果は、評価上の比較はできても必須配置を満たす完成解ではない。

### 8.1 固定サイズの箱：利益を多く入れる

入力の箱幅を $W_0$、高さを $H_0$ とする。配置は $W\leq W_0$、$H\leq H_0$ を満たす。

$$\operatorname{lexmin}(m,-V,-A,WH,H,W)$$

まず必須矩形の取りこぼしを減らし、次に利益、次に入った面積を大きくする。ここまで同じなら、配置が占める外接面積・高さ・幅の順に小さい方を選ぶ。

全矩形を必須にすれば「全て入る配置を探す」用途になる。任意矩形を混ぜれば「必須品を入れた残りへ、利益の大きい品を選んで詰める」用途になる。

### 8.2 幅固定の帯：使う高さを減らす

入力の帯幅を $W_0$ とし、$W\leq W_0$ の下で次を比較する。

$$\operatorname{lexmin}(m,H,W)$$

全矩形を必須として使う場合、主目的は使用高さ $H$ の最小化である。幅は入力で固定された許容幅であり、残りの横方向の隙間を減らすことが最優先ではない。

### 8.3 同じサイズの複数箱：箱数を減らす

各箱の幅・高さを $W_0,H_0$ とする。使用箱数を $B$、最後の箱に入った矩形面積の合計を $A_{\mathrm{last}}$ とする。箱番号が0始まりなら、最後の箱番号は $B-1$ である。

$$A_{\mathrm{last}}=\sum_{i\in S}\mathbf{1}[b_i=B-1]w_ih_i$$

$$\operatorname{lexmin}(m,B,A_{\mathrm{last}})$$

全矩形を必須にした上で、まず使用箱数を減らす。同じ箱数では最後の箱の使用面積を小さくする。ここでの面積は、最後の箱に置いた矩形の面積和であり、その箱の外接矩形面積ではない。何も置かない場合は $B=0$、$A_{\mathrm{last}}=0$ とする。

### 8.4 外接矩形：幅と高さの両方を決める

全矩形を必須として配置し、外接する幅 $W$ と高さ $H$ に対して、次の主目的を選ぶ。

| `rectangle_bounding_objective_type` | 最小化する主目的 | 意味 |
| --- | --- | --- |
| `area` | $WH$ | 外接矩形の面積を小さくする |
| `perimeter` | $W+H$ | 幅と高さの合計を小さくする |
| `weighted_sum` | $\alpha W+\beta H$ | 横方向と縦方向の費用の違いを反映する |

$\alpha$ は幅の重み `width_weight`、$\beta$ は高さの重み `height_weight` であり、入力で指定する。`perimeter` の実装上の評価値は**実際の周長の半分である $W+H$**。$2(W+H)$ と最適な配置は同じだが、返される評価値の尺度は異なる。

### 主要アルゴリズム

最大空き矩形を管理するMaxRects、空き寸法・面積・左下位置・接触辺長に基づく配置、置きにくい矩形を先に扱う構築を使う。複数の矩形順序・初期配置を試し、用途に応じた箱寸法の探索や隙間詰めを加える。

### 選択時の注意

矩形の寸法そのものを伸縮するモデル、任意角度の回転、不定形の物体の配置ではない。帯・複数箱・外接矩形で全品配置が目的なら、全品を必須にして取りこぼしを確認する。`feasible()` は必須矩形の未配置数を見るものであり、自作の配置が重ならないかなどの幾何検証は `validate_rectangle_packing` で別に行う。

---

## 9A. TSP：1本の訪問順を短くする

**ヘッダー：`tsp_solver_v07.hpp`**  
**主要入口：`tsp::solve_tsp`、`tsp::improve_tsp`。小規模用の厳密法として `tsp::held_karp_tsp`**  
参照資料：[S9]

### 何を解くか

指定された頂点をすべて一度ずつ訪れる順番を決める。最後に最初の頂点へ戻る巡回路と、戻らない開いた経路を扱う。開いた経路では始点・終点の固定も選べる。

たとえば、1体のロボットが点検地点をすべて訪れる順番だけを決めたい場合に使う。どの地点を訪れるかは入力で決まっており、得点に応じて訪問先を捨てる問題ではない。

### 入力と決定変数

| 記号 | 意味 |
| --- | --- |
| $N$ | 訪問対象の頂点数 |
| $p=(p_0,\ldots,p_{N-1})$ | 訪問順。入力された対象頂点を重複なく並べる。ソルバーが決める |
| $k$ | 訪問順の位置を表す添字 |
| $d(u,v)$ | 頂点 $u$ から $v$ へ移動する費用。入力で与える距離関数・距離表 |
| $q$ | 巡回路なら1、開いた経路なら0。`cycle` の設定に対応 |

### 目的関数

$N\geq2$ について、次を最小化する。

$$\min_p\left(\sum_{k=0}^{N-2}d(p_k,p_{k+1})+q\,d(p_{N-1},p_0)\right)$$

前半は順番に移動する費用、後半は巡回路の場合だけ必要な帰路である。始点や終点を固定する場合は、その条件を満たす順番の中で最小化する。対象が0個または1個なら、このライブラリの経路費用は0として扱う。

距離が非対称でも扱えるが、対称性の設定を実際の距離に合わせる。往路と復路が違う距離に対して、対称前提の評価を使わない。

### 主要アルゴリズム

最近傍構築を起点に、候補頂点を絞った2-opt、Or-optなどの区間移動、限定的な3-opt・並べ替えを使う。摂動による反復局所探索と、短い部分区間に対する動的計画法も組み合わせる。小規模全体を厳密に解く入口はHeld–Karp法である。

### 選択時の注意

頂点間の費用を受け取る訪問順ソルバーであり、盤面上の移動操作や複数体の衝突回避を直接出力するものではない。容量、時間帯、集荷・配達の先行関係、訪問先の選択が主題なら9B節または7節を検討する。`solve_tsp`・`improve_tsp` は順序配列を更新するAPIなので、費用の戻り値だけでなく更新後の配列を使う。

---

## 9B. Routing：複数車両への分配と訪問順を決める

**ヘッダー：`routing_solver_v05.hpp`**  
**主要入口：`routing::solve_cvrp`、`routing::solve_pdvrp`、`routing::solve_orienteering`、各 `solve_prize_collecting_*`・`solve_selective_*`**  
参照資料：[S9]

### 何を解くか

複数車両へ仕事を割り当てるだけでなく、各車両がどの順に訪問するかも決める。車両ごとの始点・終点、積載容量、経路費用の上限を持てる。

1回の訪問で終わる仕事と、「集荷地点へ先に行き、同じ車両で配達地点へ運ぶ」という対になった仕事を扱う。全仕事を実行する、ちょうど指定件数だけ選ぶ、制限内で報酬を大きくする、という用途を切り替える。

### 経路費用の定義

| 記号 | 意味 |
| --- | --- |
| $R$、$r\in\{0,\ldots,R-1\}$ | 車両数と車両番号 |
| $J$、$j$ | 入力された仕事全体の集合と、その一つ |
| $S\subseteq J$ | 実行する仕事の集合。全件型なら $S=J$ |
| $s_r$、$t_r$ | 車両 $r$ の始点と終点 |
| $P_r=(v_{r,1},\ldots,v_{r,n_r})$ | 車両 $r$ の訪問地点列。ソルバーが決める |
| $n_r$、$k$ | 車両 $r$ の訪問地点数と、その順番の添字 |
| $d(u,v)$ | 頂点 $u$ から $v$ への移動費用 |
| $h_{r,k}$ | 車両 $r$ がk番目の地点で行うサービスの費用。集荷・配達なら各地点の費用 |
| $C_r$ | 車両 $r$ の移動とサービスを合わせた経路費用 |
| $Q_r$、$L_r$ | 車両 $r$ の容量上限と経路費用上限。`capacity`、`max_cost` |
| $a_j$、$z_j$ | 仕事 $j$ の需要量・荷物量と、完了によって得る報酬 |
| $M\subseteq J$ | 選択型でも必ず実行する必要がある仕事集合 |
| $K$ | 選択件数固定型で、実行する仕事数 |

少なくとも1地点を訪れる車両について、費用は次のようになる。

$$C_r=d(s_r,v_{r,1})+\sum_{k=1}^{n_r-1}d(v_{r,k},v_{r,k+1})+d(v_{r,n_r},t_r)+\sum_{k=1}^{n_r}h_{r,k}$$

**空経路は $C_r=0$ と扱う。** 始点と終点が異なっていても、未使用車両にその移動費用を自動で請求するモデルではない。また、結果の `travel_cost` にはサービス費用も含まれる。

### 9B.1 全件配送：CVRP／PDVRP

すべての仕事を実行し、各車両について $C_r\leq L_r$ と容量制約を守って、合計費用を最小化する。

$$\min_{P_0,\ldots,P_{R-1}}\sum_{r=0}^{R-1}C_r\quad\text{subject to}\quad S=J$$

1訪問型のCVRPでは、各車両に割り当てた需要量の合計が $Q_r$ 以下であることを求める。集荷・配達型のPDVRPでは、集荷で $a_j$ を積み、配達で $a_j$ を降ろすので、**経路途中の積載量**が常に0以上 $Q_r$ 以下であることを求める。集荷は配達より先で、同じ仕事の両地点は同じ車両が訪問する。

全件型の入口は、仕事の `mandatory` フラグにかかわらず、入力した全仕事を対象にする。

### 9B.2 報酬優先：Prize Collecting／Orienteering

必須仕事 $M$ を実行し、各車両の制約を守りながら仕事集合 $S$ と経路を選ぶ。

$$\operatorname{lexmin}\left(-\sum_{j\in S}z_j,\sum_{r=0}^{R-1}C_r\right)\quad\text{subject to}\quad M\subseteq S$$

**まず報酬を最大化し、報酬が同じ解どうしだけで費用を最小化する。** 報酬から移動費用を引いた「純利益」の最大化ではない。たとえば、許される費用上限内なら、少し遠くても報酬が増える方を優先する。

集荷・配達型の報酬は、地点ごとではなく仕事の完了に対して1回数える。`solve_orienteering` は経路費用上限を使う選択型で、**積載容量を評価しない入口**である。容量も必要なら `solve_prize_collecting_vrp` などの容量を扱う入口を選ぶ。

### 9B.3 件数固定：Selective VRP／PDVRP

必須仕事を含めて、ちょうどK件を選んで実行する。

$$\min\sum_{r=0}^{R-1}C_r\quad\text{subject to}\quad M\subseteq S,\ \lvert S\rvert=K$$

これは「K件以内で報酬を最大化」ではない。K件を実行するという条件の下で費用を安くする。集荷・配達型でもKは仕事数であり、集荷地点と配達地点を別々に数えた訪問地点数ではない。

### 主要アルゴリズム

後悔値を考慮した挿入で仕事を配置し、仕事の移動・交換、経路内の順序改善などで費用を下げる。複数仕事を取り除いて再挿入する探索を組み合わせる。集荷・配達型では、集荷と配達の位置・順序・区間中の荷重を合わせて評価する。

### 選択時の注意

車両どうしの時刻付き衝突、訪問可能な時間帯、時刻で変わる移動費用は標準モデルでは扱わない。車両数を最小化する目的でもなく、用意した車両の中で経路を決める。`feasible` を確認し、全件型で仕事が残る結果をそのまま採用しない。衝突回避が本質なら7節、担当先だけを決めて訪問順が不要なら3節が近い。

---

## 10. 容量付きグラフラベリング：相性を考えたグループ分け

**ヘッダー：`capacitated_graph_labeling_v22.hpp`**  
**主要入口：`solve_capacitated_graph_labeling`、`improve_capacitated_graph_labeling`**  
参照資料：[S10]

### 何を解くか

各頂点へ一つのラベルを付ける。ラベルは担当者、チーム、区画、サーバーなどを表す。ラベルごとの人数・需要量を制限しながら、頂点単独の割当費用と、つながった頂点どうしの組合せ費用を減らす。

たとえば、通信する処理どうしを同じサーバーへ置きたいが、サーバーごとの負荷上限もある場合に使う。ある処理を移動すると、その処理の単独費用だけでなく、**隣接する処理との通信費用も変わる**。

### 入力と決定変数

| 記号 | 意味 |
| --- | --- |
| $V$、$v$ | 頂点集合と、その一つ |
| $E$、$e$ | 入力辺の集合と、その一つ。同じ端点の辺を複数登録した場合も各辺を区別する |
| $u_e$、$v_e$ | 入力辺 $e$ の第1・第2端点 |
| $K$、$g\in\{0,\ldots,K-1\}$ | ラベル数とラベル番号 |
| $\ell_v$ | 頂点 $v$ に付けるラベル。ソルバーが決める |
| $c_{vg}$ | 頂点 $v$ にラベル $g$ を付ける単独の費用。`unary_cost` |
| $\phi_e(a,b)$ | 辺 $e$ の第1端点がラベル $a$、第2端点がラベル $b$ のときの二項費用 |
| $d_v$ | 頂点 $v$ の非負の需要量。ラベルが変わっても同じ値 |
| $L_g$、$U_g$ | ラベル $g$ に属する頂点の需要量合計の下限・上限 |
| $l_g$、$u_g$ | ラベル $g$ に属する頂点数の下限・上限 |
| $A_v$ | 頂点 $v$ に許すラベルの集合。固定ラベルも指定できる |

### 目的関数と制約

$$\min_{\ell}\left(\sum_{v\in V}c_{v,\ell_v}+\sum_{e\in E}\phi_e(\ell_{u_e},\ell_{v_e})\right)$$

$\ell$ は全頂点のラベルを並べたものである。すべての頂点で $\ell_v\in A_v$ とし、各ラベル $g$ について、指定した需要量と個数の上下限を守る。

$$L_g\leq\sum_{v\in V}\mathbf{1}[\ell_v=g]d_v\leq U_g$$

$$l_g\leq\sum_{v\in V}\mathbf{1}[\ell_v=g]\leq u_g$$

需要量と個数は別の制約である。たとえば、人数を3人ちょうどにしながら、その3人の作業量合計にも上限を付けられる。需要量を全て1にした場合は、需要量の制約でも人数を表現できる。

### 二項費用の代表例

**異なるグループへ分けた辺に費用を払う場合：**

$$\phi_e(a,b)=w_e\mathbf{1}[a\ne b]$$

$w_e$ は辺 $e$ の重み、$a,b$ は両端のラベルである。非負の $w_e$ を使うと、強いつながりを持つ頂点どうしを同じグループへ寄せる目的になる。この形式をPotts型と呼ぶ。

**ラベルどうしの組合せに応じた費用を払う場合：**

$$\phi_e(a,b)=w_eD_{ab}$$

$D_{ab}$ はラベル $a$ と $b$ の組合せ費用である。たとえば、同じサーバーなら0、同じラックなら1、別ラックなら5という違いを表現できる。さらに、辺ごとに評価を変える独自の二項費用も指定できる。

二項費用は、入力辺を**1本につき1回**加算する。第1・第2端点の順序を保つので、非対称な費用もその順序に従う。無向の関係を表すつもりで同じ辺を両方向に登録すると、その分を二重に評価することになる。

### 主要アルゴリズム

許可ラベル、需要量、個数の上下限を考慮して初期解を構築し、単一頂点のラベル変更と2頂点のラベル交換を焼きなましで探索する。Potts型ではラベル別の隣接重みをキャッシュして差分評価を軽くする。

### 選択時の注意

グループの**連結性は自動では保証しない**。また、隣接頂点を必ず違う色にするグラフ彩色の制約を、標準の容量・許可ラベル制約とは別に持つわけではない。ペナルティを置くことと、必ず守る制約を追加することを区別する。担当先によって同じ仕事の需要量が変わるなら3節、一対一配置でQAPの費用形に合うなら4節も比較対象になる。

---

## 本番の選定で最後に確認すること

**目的関数と実行可能性を別々に確認する。** 目的値が良くても、必須の仕事・矩形・接続が欠けていれば採用できない。特に、部分解を返せるライブラリでは、費用が小さいだけでは完成解の品質比較にならない。

**同じ「改善」でも、前提をそろえる。** 前の解を再利用する場合、変更後の費用で評価し直す必要がある。変更後に制約違反になる解を、そのまま通常の改善入口へ渡せるとは限らない。全体改善、一部を固定した修復、作業配列の再利用はそれぞれ別の機能として扱う。

**名前だけで標準問題の全機能を期待しない。** この施設配置は固定K・容量なし、Routingの報酬型は辞書順、矩形の `perimeter` は幅と高さの合計、MAPFの費用は待機も含む経路時間である。本番の得点式との違いがある場合は、入力変換で一致するのか、別のソルバーが必要なのかを先に判断する。

本書で紹介したヒューリスティックの入口は、一般に最適性を保証するものではない。探索失敗を不可能性の証明と扱わず、利用する入口の実行可能性判定と、問題固有の出力検証を組み合わせる。

## 参照したソースと対応スレッド

各節の `[S1]` などは、以下の資料番号に対応する。リンクは指定された共有スレッド、ファイル名は実装確認に使ったヘッダーである。ヘッダー名のバージョンと、個別ガイド・実験報告のバージョンは一致するとは限らない。

| 資料 | 確認したヘッダー | 特に照合した実装上の事項 | 対応スレッド |
| --- | --- | --- | --- |
| [S1] | `maximum_coverage_solver_v06.hpp` | 被覆数と重みの集計、候補費用、個数型・予算型・集合被覆の分岐、構築と交換 | [Maximum Coverage](https://chatgpt.com/share/6a79d996-7058-83ee-bad3-40a143c07dc9) |
| [S2] | `heuristic_steiner_tree_v17.hpp` | 選択辺の費用、既存辺を無料とする追加接続、正規化と経路の再接続 | [Steiner Tree](https://chatgpt.com/share/6a79d9b4-1908-83e8-a2e4-73cedc67b38f) |
| [S3] | `generalized_assignment_solver_v12.hpp` | 選択肢単位の容量消費・費用、完全割当の判定、修復と探索 | [Generalized Assignment](https://chatgpt.com/share/6a79d9e3-ef94-83ee-bb3b-7102ab0fa0a6) |
| [S4] | `qap_solver_v07.hpp` | 全順序対と対角項を含む目的値、単独費用、交換差分、Threshold Acceptingとタブー探索 | [QAP](https://chatgpt.com/share/6a79d9ff-12a8-83ee-8fa2-c3b49d701ac7) |
| [S5] | `facility_location_v11.hpp` | 固定K、外部サービス、施設選択費用、`make_result`、1-swapと反復局所探索 | [Facility Location](https://chatgpt.com/share/6a79da18-0b5c-83e8-a087-0e00a7df0ce7) |
| [S6] | `generic_resource_scheduler_v07.hpp` | `builtin_evaluator`、開始時刻への変換、方式変更と焼きなまし | [Generic Resource Scheduler](https://chatgpt.com/share/6a79d8fe-da64-83ee-a1b2-566ee7ec531d) |
| [S7] | `multi_agent_path_finding_v04.hpp` | `compute_costs`、`better_result`、MAPDの候補比較と集計、衝突検証 | [Multi-Agent Path Finding](https://chatgpt.com/share/6a79d92f-fae4-83ee-99e8-b08bd3223c41) |
| [S8] | `rectangle_packing_v09.hpp` | `better_fixed`、`better_strip`、`better_multiple_bins`、外接矩形の `evaluate`、配置構築 | [Rectangle Packing](https://chatgpt.com/share/6a79d94b-3ea4-83ee-bbdd-b709297667bb) |
| [S9] | `tsp_solver_v07.hpp`、`routing_solver_v05.hpp` | 巡回／開経路、全件／報酬／件数固定の分岐、空経路とサービス費用、容量評価 | [TSP・VRP](https://chatgpt.com/share/6a79d965-6a58-83ee-b865-195ec808607c) |
| [S10] | `capacitated_graph_labeling_v22.hpp` | 単独・二項費用、需要量と個数の上下限、初期構築、ラベル変更と交換 | [Capacitated Graph Labeling](https://chatgpt.com/share/6a79d97d-e1d4-83ee-8c4a-2a080adc88aa) |

QAPの配布対象については、同時点の `qap_solver_benchmark_report_v14.md` に記載された正式な利用対象も照合した。各ライブラリの説明用の具体例は、本書のために作成したものであり、ベンチマーク結果や性能保証を意味しない。
